# VMware Global Monitoring Portal — runtime configuration (systemd EnvironmentFile)
# 수정 후:  systemctl restart vmware-portal

# --- core ---
PORT=4000
# 사용자 설정 저장 위치(앱 폴더 밖이라 업그레이드해도 보존됨): vcenters.json/users.json/upgrade.json
CONFIG_DIR=/etc/vmware-portal
# 데이터 소스: mock(데모) | live(실제 vCenter) | auto(실패 시 mock 대체)
DATA_SOURCE=mock
# vCenter 폴링 주기(ms)
POLL_INTERVAL_MS=30000
# 사설 vCenter 자체서명 인증서 검증 (true 면 검증, false 면 허용)
VC_TLS_REJECT_UNAUTHORIZED=false

# --- iDRAC 전력 수집 (Dell Redfish) ---
# 켜고 끄기(기본 on). 등록된 서버가 있을 때만 실제 수집합니다.
IDRAC_ENABLED=true
# 수집 주기(ms, 기본 60초)
IDRAC_POLL_INTERVAL_MS=60000
# 전력 이력 보관 일수(기본 90일, 0=무제한)
IDRAC_RETENTION_DAYS=90
# SQLite DB 경로(기본 $CONFIG_DIR/idrac-power.db — 업그레이드해도 보존)
# IDRAC_DB_PATH=/etc/vmware-portal/idrac-power.db
# [Active Directory(LDAP) 로그인] 설정 → 인증(AD) 메뉴에서도 구성 가능. env 예시:
#   AD_ENABLED=true
#   AD_URL=ldaps://dc.corp.local:636
#   AD_DOMAIN=corp.local                 # 로그인: user@corp.local 로 직접 바인드
#   AD_BASE_DN=DC=corp,DC=local          # memberOf(그룹) 조회용
#   AD_ADMIN_GROUP=VMware-Portal-Admins  # 이 그룹 멤버 → admin (operator/viewer 그룹도 가능)
#   AD_DEFAULT_ROLE=viewer               # 그룹 매칭 없을 때 기본 역할
#   AD_TLS_REJECT_UNAUTHORIZED=false     # 자체서명 인증서면 false
# AD 인증 실패 시 로컬 users.json 으로 폴백(기본 admin 유지).
#
# iDRAC 등록(서버명/IP/계정/매핑 호스트)은 포탈 관리자 → '전력 수집' 메뉴에서.
# 저장 위치: $CONFIG_DIR/idrac.json (0600)
# OME(OpenManage Enterprise) 전력 메트릭 튜닝(Power Manager): 전력값이 안 잡히면 조정.
#   OME_POWER_METRIC_TYPES=3,4,1   # 순서대로 시도(순간/평균/최대 시스템전력)
#   OME_POWER_DURATION=0           # 메트릭 조회 구간(0=최근)
#   OME_POWER_PLUGIN_ID=2F6D05BE-EE4B-4B0E-B873-C8D2F64A4625

# --- ★ 통합 엣지 모드 (권장: 엣지는 이 3줄이면 끝) ---
# 아래 3개만 설정하면 엣지의 전 기능이 켜집니다:
#   수집기 export + 위임 스캔/핑/캡처/로그 워커 + 사이트 인벤토리 push(live 수집)
#   + 중앙발 자동 업그레이드(/dl) + 부팅 시 중앙 자동 등록(수집 서버 수동 추가 불필요)
# EDGE_MODE=all
# CENTRAL_URL=http://중앙포탈:4000
# EDGE_TOKEN=강력한-공유-토큰        # 중앙의 CENTRAL_TOKEN과 같은 값
# (선택) COLLECTOR_DATACENTER=Poland-DC1   # 표시용 DC 이름(=AGENT_NAME 기본값)
# (선택) EDGE_ADVERTISE_URL=http://엣지실주소:4000  # NAT/프록시 뒤라면 명시
# 개별 env(DATA_SOURCE, AGENT_PUSH_INVENTORY, UPGRADE_* 등)를 쓰면 그 값이 우선합니다.

# --- RMA: 원격 명령 에이전트 (엣지 전용, v2.416 — 별도 프로세스 vmware-portal-rma@<인스턴스>) ---
# 중앙 '특수기능 › 원격 명령 실행(RMA)' 에서 보낸 프리셋 명령(서비스 상태/로그/재시작·df·ping 등)을
# 이 서버에서 실행해 결과만 회신합니다(아웃바운드 롱폴, 개별 엣지 토큰 필수).
# RMA_ENABLED=true                 # install.sh 가 이 값을 보고 인스턴스를 기동(기본 off)
# RMA_INSTANCES=default            # 공백 구분 — 한 서버에 여러 프로세스면 'a b c' (RMA_PRIORITY 는 rma-<이름>.env)
# RMA_PASSWORD=강력한-비밀번호      # 설정 시 이 값으로 서명된 명령만 실행(중앙 화면에 같은 값 등록). 권장.
# RMA_ALLOW_CUSTOM=false           # true 면 자유 명령(/bin/sh -c) 허용 — 프리셋 밖 명령이 필요할 때만 현장에서 켠다
# RMA_LONGPOLL_MS=20000            # 중앙 롱폴 대기(5000~55000)
# RMA_MAX_OUTPUT=262144            # 결과 출력 상한(바이트)
# --- RMA 점검·정책(v2.418, HostMonitor RMA 대응) ---
# RMA_COMMENT=Seoul-edge-01        # 중앙 화면 표시용 설명
# RMA_ENABLED_COMMANDS=            # 허용 명령 id(쉼표, 비우면 전부) / RMA_DISABLED_COMMANDS= 차단 목록
# RMA_ENABLED_TESTS=               # 허용 점검 id(쉼표, 비우면 전부) / RMA_DISABLED_TESTS=
# RMA_SERVICE_UNITS=               # service-start/stop/restart 허용 유닛(쉼표) — install.sh 가 sudoers 에 같은 목록 반영
# RMA_ALLOW_REBOOT=false           # true 면 reboot 프리셋 허용(sudoers 도 함께)
# RMA_FILE_ROOTS=/var/log          # 파일 점검(존재/크기/개수/텍스트 로그) 허용 루트(쉼표)
# RMA_ALLOW_SSH=false             # true 면 SSH 원격 실행/점검(ssh-exec·ssh) 허용 — 계정은 중앙 통합 계정 관리에서 브로커로 메모리 전달(v2.419)
# RMA_SSH_TARGETS=                 # SSH 대상 허용 목록(IP/CIDR/호스트명/*.suffix, 쉼표, 비우면 전부)
# SSH_LEGACY_FALLBACK=1            # SSH 알고리즘 협상 실패 시 구형(dh-group1-sha1/ssh-dss/aes-cbc) 포함 1회 재시도(0=끔, v2.421)
# SANSW_TEST_PICKUP_MS=600000      # 엣지 대행 연결 테스트: 엣지가 요청을 가져갈 때까지 대기 상한(기본 10분, v2.421)
# SANSW_TEST_RESULT_MS=300000      # 엣지 대행 연결 테스트: 가져간 뒤 결과 회신 대기 상한(기본 5분, v2.421)
# RMA_TEST_CONCURRENCY=4           # 동시 점검 수
# RMA_REMOTE_MANAGE=false          # true 면 중앙의 원격 관리 설정(롱폴·동시성·점검 축소)을 수용
# RMA_AUDIT_LOG=/var/log/vmware-portal-rma-audit.jsonl     # 성공 감사 로그(선택)
# RMA_FAILURE_LOG=/var/log/vmware-portal-rma-failure.jsonl # 거부·실패 로그(선택)
# RMA_OFFLINE_MINUTES=0            # 중앙 무연결 N분 이상이면 아래 명령 실행(0=사용 안 함)
# RMA_OFFLINE_PING_HOST=           # 무연결 시 ping 할 호스트(응답이면 CMD_OK, 아니면 CMD_FAIL)
# RMA_OFFLINE_CMD_OK= / RMA_OFFLINE_CMD_FAIL= / RMA_RESTORE_CMD=   # 현장 셸 명령(중앙이 바꿀 수 없음)

# --- 분산 수집 (대규모: 서버 1천대+ / DC 13개) ---
# 이 서버를 '수집 에이전트'로 만들려면 토큰과 데이터센터 이름을 설정하세요.
# 그러면 GET /api/collector/export 가 토큰으로 열리고, 중앙 포탈이 당겨갑니다.
# COLLECTOR_TOKEN=강력한-공유-토큰
# COLLECTOR_DATACENTER=Seoul-DC1
# 중앙 포탈에서 원격 수집기 당김 주기(ms, 기본 60초; 0이면 중앙 기능 비활성)
# COLLECTOR_PULL_INTERVAL_MS=60000
#
# [중앙 오케스트레이션] 에이전트에 IP를 할당해 원격 스캔시키기:
#  - 중앙 서버: CENTRAL_TOKEN 설정(에이전트 인증용). 관리자 → '에이전트 작업'에서 이름별 IP 할당.
#    CENTRAL_TOKEN=강력한-토큰
#  - 각 에이전트 서버: 자기 이름 + 중앙 주소 + 동일 토큰 설정. 할당분을 읽어 로컬 스캔 후 보고.
#    AGENT_NAME=Seoul-DC1
#    CENTRAL_URL=http://중앙포탈:4000
#    CENTRAL_TOKEN=강력한-토큰
#    AGENT_SCAN_INTERVAL_MS=3600000   # 스캔 주기(기본 1시간)
#    AGENT_AUTO_REGISTER=true         # 발견한 iDRAC 로컬 자동 등록(기본 on)
#
# [보안 권장] 엣지별 '개별' central 토큰으로 이관 — 공유 토큰 1개는 스코프가 너무 넓습니다.
#  공유 CENTRAL_TOKEN을 전 엣지가 함께 쓰면, 엣지 1대가 침해되거나 토큰이 유출될 때 ?agent= 이름만
#  바꿔 '다른 사이트'의 iDRAC 비밀번호·게스트 계정·사용자 해시까지 인출할 수 있습니다.
#  중앙 관리자 화면(설정 → 수집 서버 → '🔑 엣지별 개별 central 토큰')에서 엣지별 토큰을 발급하고,
#  그 값을 해당 엣지의 CENTRAL_TOKEN(통합 엣지 모드면 EDGE_TOKEN) 자리에 넣으면 됩니다.
#  개별 토큰은 그 엣지 데이터만 접근할 수 있고(남의 이름은 403), 엣지 코드 변경은 필요 없습니다.
#  미이관 엣지는 공유 토큰으로 계속 동작하므로 사이트별로 하나씩 옮길 수 있습니다(무중단).
#  전 엣지 이관이 끝나면 중앙에 CENTRAL_REQUIRE_AGENT_TOKEN=true 를 켜서 공유 토큰을 금지하세요.

# --- 보안 관련 예외 옵션(기본값이 안전 — 호환이 필요할 때만 켠다) ---
# AD 그룹→역할 매핑은 그룹명(CN) 또는 전체 DN '완전일치'가 기본입니다.
# 과거 부분문자열 매칭에 의존한 설정이었다면(예: AD_ADMIN_GROUP=IT 로 IT-Admins 매칭) 아래로 되돌릴 수
# 있으나 권한 상승 위험이 있어 비권장 — 설정값을 실제 그룹명으로 고치는 것이 정답입니다.
# (정확일치 실패 시 서버 로그에 '어떤 그룹 설정이 안 맞는지' 경고가 남습니다.)
# AD_GROUP_MATCH=substring
#
# 업그레이드 번들은 versions.json의 SHA-256 검증을 통과해야 설치·푸시됩니다.
# sha가 없는 사내 미러/수기 versions.json을 쓰는 경우에만 아래로 우회하세요(무결성 검증 포기).
# UPGRADE_ALLOW_UNVERIFIED=true
#
# SSRF 가드는 루프백(127.x/::1)을 차단합니다. 중앙과 엣지를 같은 서버에 올린 랩 구성이라면 허용하세요.
# SSRF_ALLOW_LOOPBACK=true

# --- 중앙↔엣지(WAN) TLS ---
# 중앙과 엣지 사이 HTTPS 인증서 검증은 기본 ON입니다(수집 토큰·배포 자격증명이 흐르는 구간).
# 엣지를 자체서명 HTTPS로 운영해 인증서 오류가 나면 그 노드에서만 아래를 켜세요(가능하면 사설 CA 신뢰가 우선).
# WAN_TLS_INSECURE=true

# --- auth ---
AUTH_ENABLED=true
# 토큰 서명 시크릿 — 설치 스크립트가 무작위로 채웁니다. 비우지 마세요.
AUTH_SECRET=
AUTH_TOKEN_TTL=8h
# users.json 미존재 시 시드되는 최초 admin 비밀번호.
#  · 비워두면(권장) 최초 기동에 임의 비밀번호가 생성되어
#    $CONFIG_DIR/initial-admin-password.txt (0600) 에 저장되고, 로그인 화면이 그 경로를 안내합니다.
#  · admin·operator 는 OTP 전용이므로 이 비밀번호는 '최초 1회 로그인 → OTP 등록'에만 쓰이며,
#    등록을 마치면 비밀번호와 위 파일이 자동 삭제됩니다.
DEFAULT_ADMIN_PASSWORD=

# --- 설정(Settings) 화면 접근 계정 ---
# '설정' 탭은 아래에 지정된 계정으로 로그인했을 때만 보이고 접근할 수 있습니다(서버측 강제).
# 지정 방법 3가지는 모두 합쳐서 적용됩니다:
#   ① 이 변수(콤마 구분)            : SETTINGS_OWNERS=noainred,junho
#   ② $CONFIG_DIR/settings-owners.txt : 한 줄에 계정 하나(# 주석 가능)
#   ③ 포탈 UI(설정 › 세션 보안)      : 화면에서 편집
# ①②는 서버 파일이라 UI 저장으로 지워지지 않습니다 — 모든 관리자가 설정 화면에 들어가지
# 못하는 잠금 상황에서 서버에서 직접 복구하는 안전장치입니다.
# (수퍼관리자 noainred 는 항상 자동 포함됩니다.)
# SETTINGS_OWNERS=noainred

# --- auto-upgrade (옵트인, 기본 비활성) ---
# UPGRADE_ENABLED=true
# UPGRADE_INSTALL_DIR=/opt/vmware-portal/app
#
# (A) GitHub versions.json 모니터링 후 자동 업그레이드:
#   UPGRADE_POLL_INTERVAL_MS=3600000   # 1시간마다 확인
#   UPGRADE_AUTO_APPLY=true            # 새 버전이면 자동 적용 + 재시작
#   감시 경로(remote base, 비우면 GitHub download/ 기본값 사용):
#   UPGRADE_REMOTE_BASE=https://raw.githubusercontent.com/noainred/The.DVC/main/download
#   (사설 레포면 PAT) UPGRADE_TOKEN=ghp_xxx
#
# (B) 로컬 감시 폴더 방식:
#   UPGRADE_WATCH_DIR=/opt/vmware-portal/incoming
#
# (선택) 자가 업그레이드 후 엣지에 푸시: UPGRADE_EDGES=[{"url":"https://edge1","token":"..."}]
