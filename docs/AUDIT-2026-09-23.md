# v2.579 — 아키텍처 점검·버그 수정·튜닝 (2026-09-23)

사용자 요청: "아키텍처 점검, 버그 수정, 튜닝". 방식은 앞선 세 릴리스와 같은 표준 설정(전수 · 전체 검증 · 모아서 릴리스).

## A. 방법

1. `server/src` **644 모듈 · 2,510 import edge** 를 그래프로 만들어(Tarjan SCC) 순환·팬인/팬아웃·계층 위반을 기계적으로 뽑았다.
2. 순환 멤버 15개를 **각각 단독 진입점으로 로드**해 TDZ(v2.566 유형) 여부를 실측했다 — 전부 OK.
3. 운영 규모(`MOCK_SCALE=3`: 33 vCenter · 558 호스트 · 6,004 VM)로 기동·RSS·폴링 라우트·루프 지연·gzip 을 쟀다.
4. 확정한 것만 고치고, 재수출로 호출부를 보존했으며, **규칙을 테스트로 고정**했다(`test/arch2579.test.js`).

## B. 확정 결함과 수정

| ID | 유형 | 내용 | 수정 |
|---|---|---|---|
| ARCH-01 | 사본 | 동시성 풀 스캐폴드 **24벌**(`eachLimited` 9 · `pool` 13 · 인라인 2). v2.575 "23→1" 은 `collectPool` 이름만 본 것이었다 | `util/pool.js` 하나로(`poolRun` 13 · `poolSettled` 11). 결과 모양은 호출부마다 예전 그대로. 남긴 2곳은 사유와 함께 `POOL_EXCLUDED` |
| ARCH-01b | 잠재 결함 | 사본들은 `limit<=0` 이면 **워커 0개 → 아무것도 실행하지 않고 조용히 종료**. 전 호출부가 하한 1 클램프라 오늘은 도달 불가(실행 확인) | `poolRun` 은 최소 1 워커 — 구조적으로 불가능. 테스트 `③-b` |
| ARCH-02 | 순환·역방향 | `util/ssrfLookup → collector/registry → util/resilientFetch`. registry 가 동적 import 로 순환을 미루고 있었다 | IP/URL 차단 판정 → `util/ssrfBlock.js`. registry 는 재수출(29곳 무변경) |
| ARCH-03 | 순환·역방향 | `collector/agent → routes/collector → collector/agent`. 거부 링버퍼가 라우트 파일에 | `collector/denyLog.js`. 라우트는 재수출(테스트 하니스 호환) |
| ARCH-04 | 역방향 | 도메인 4곳 → `routes/admin/shared.js` 의 도메인 헬퍼 10개 | `insights/analysisServers.js`. shared.js 는 재수출(라우트 5곳 무변경) |
| ARCH-05 | 역방향 | `proxy/sshGateway → routes/remote` 의 순수 함수 | `proxy/targetHostScope.js`. 라우트는 재수출(테스트 호환) |

결과: 순환 SCC **7 → 5**(남은 5개는 같은 도메인 안 2-cycle · 단독 로드 OK), 비-routes → routes 의존 **21 → 14**(전부 `index.js` 의 마운트).

## C. 튜닝 — 실측 결과, 고칠 것 없음

| 항목 | 값 |
|---|---|
| 기동 → `/api/health` | **1,100ms** |
| RSS(기동 직후 / 부하 후) | **183MB / 227MB** |
| 폴링 라우트 p50(9종) | 1.4 ~ **5.4ms**(`/api/hosts` 484KB) |
| 동시 20 × `/api/vms` 벽시계 | **89ms** |
| 루프 프로브 30초(100ms 간격) | p50 1.8 · p95 2.4 · p99 5.8 · **max 72.9ms(1회)** |
| gzip | `/api/hosts` 484KB → **27KB** · ETag 정상 |

하나의 72.9ms 스톨은 30초 안에 1회이고 폴링 주기와 겹친 것으로 보이나 **원인을 확정하지 않았다**(추측 기록). 이 수치들에서 고칠 결함을 만들지 않았다.

## D. 검증

| 항목 | 결과 |
|---|---|
| 서버 테스트 | **2,812건 전부 통과**(2,806 → +6 신규 `arch2579`) · 웹 **1,467건** · lint 0 error · 빌드 OK |
| 영향 모듈 31개 테스트 파일 | **432건 통과** |
| 편집 모듈 24개 단독 로드 | 전부 OK |
| 목 서버 스모크(`MOCK_SCALE=3`) | 수집 토큰 거부 2회 → `denyLog` 콘솔 기록 2줄 · `/api/admin/room-temp`·`/api/tools/esxi-temp`·`/api/admin/idrac/temps` 200(analysisServers 경로) · 폴러 예외 로그 0 |
| Chromium | **생략** — 화면이 바뀌지 않았다(서버 내부 재배치·테스트·문서만). 그 사실을 여기 적는다 |

## E. 하지 않은 것(정직 기록)

- 남은 순환 5개(`config ↔ secretVault` · `soapClient ↔ restClient` · `ipam/settings ↔ ledger` · `sanswitch/perfPush ↔ perfPoller` · `securitySettings ↔ auth`)는 각각 동적 import·지연 평가·함수 내 참조로 막혀 있고 단독 로드가 OK 라 이번에 풀지 않았다. 테스트가 개수 상한(5)을 고정해 **늘어나면 드러난다**.
- `util/` 의 예외 2건(`perf/monitor.js` · `vcenter/soapParse.js`)은 명시 allowlist 로 남겼다. `soapParse.js` 를 `util/` 로 옮기는 것은 별건.
- 웹 `console/` 셸(12파일)은 V4 가 순수 모듈 하나를 재수출해 쓰는 관계라 중복이 아니다 — 위치만 어색하고 손대지 않았다.
- `svcmon/pool.js`·`routes/admin/gpuGuest.js` 의 실행기 2곳은 범용 풀로 환원되지 않아 남겼다(사유를 테스트에 적었다).
