# 아키텍처 점검(특수 기능·신규 기능) — v2.613 — 2026-09-25

- 요청: "신규 기능, 특수기능 아키텍처 점검" → 선택 "둘 다(점검 먼저 → 그 결과를 화면으로)" · 전체 검증.
  이 문서는 1단계(점검 + 수정)다. 2단계(자동화 가능한 판정을 운영 포탈이 스스로 돌리는 특수 기능 › 포탈 점검 › **아키텍처 점검** 화면)는 v2.614.
- 기준 커밋: `8ece9d9` (v2.612.0)
- 대상: 특수 기능 카탈로그(91항목)·V4 트리·toolcats·`auth/toolAccess.js` 집행·가시성/검색·해시 라우트·`web/src/views/tools` 194파일 + v2.547 이후 신규 기능(파트 장애·베어메탈 사용률·엣지 로그·통신 점검·설정 점검·토큰/인벤토리 점검·통신/데이터 흐름/3단 지도·CVP·iLO).
- 방법: 8축 병렬 발견(A 카탈로그 · B 의존 · C 계약 · D 웹 · E 저장소 · F 엣지 · G 런타임 · H 테스트/문서) → 축별 적대적 검증(의도·재현 두 렌즈) → 파일이 겹치지 않는 8그룹 병렬 수정(그룹마다 회귀 테스트 + 워크트리 변이 검증) → 리드 통합(그룹 범위 밖 잔여) → 전량 테스트·빌드·Chromium.
- 발견 96 = 확정 57 · 부분 확정(SPLIT) 25 · 가능성 6 · 반증 8. 반증 제외 심각도: medium 12 · low 52 · info 24(high 0 — 이번 축은 보안이 아니라 구조·규약이다).
- 회귀: `server/test/audit2613{a..h}.test.js` + 웹 `views/audit2613a.test.js`·`views/audit2613b.test.js`·`hooks/hashTab.test.js`·`tools/cvpText.test.js`·`tools/partFaultText.test.js`·`idrac/sensorText.test.js`·`util/fmt.test.js`. 그룹별 변이 검증(고친 파일을 기준 커밋으로 되돌리면 해당 테스트가 실패) 전부 확인 — 각 그룹 보고의 변이 매트릭스 참조.
- 그룹 보고 원문: 아래 '수정 그룹 보고' 절(a~h).

## 확정 결함 요약(축별 대표)

- **A 카탈로그**: `adminOnly` 플래그와 실제 API 게이트가 양방향으로 어긋났다(15개) → 카탈로그 플래그를 docs/API.md 게이트 열과 대조하는 테스트로 고정. 도구 키 엔드포인트를 소유 화면 밖(IPMS·NSX)에서 부르면서 `toolAllowed` 를 보지 않던 것 · `toolAccess.js` 매핑 선언이 사실과 다르던 것(current-users/horizon-sessions·storage-track) · 포탈 점검/통신 점검/파트 장애 서브탭이 URL 에 없던 것 · 잠금 판정이 화면 4곳에 흩어졌던 것 · `#/explore`·`#/nsx` 옛 주소 · 주석의 낡은 도구 개수.
- **B 의존**: 순수 util 이 `agent/` 에 살아 `config.js` 가 도메인을 import 하던 것(envTimeout·central404 → util/) · `idrac/redfish.js` 의 정적+동적 이중 import · '순환 회피' 라 적혔지만 순환이 없는 동적 import 3곳 · 설정 pull 4종의 404 미분류 · `findCollector` 사본 · 풀 스캐폴드 스윕 정규식.
- **C 계약**: 중앙 pull 사다리 3벌(엣지 로그·bm-usage·토큰 점검)이 서로 드리프트 → `central/edgePull.js` 하나 · CVP·통신 점검의 엣지 최소 버전 게이트 부재 · `/guest-disk` 응답의 거절 요약 부재.
- **D 웹**: 화면 10곳이 `/auth/me` 를 다시 부르던 것 · `fmtBytes` 결측이 `0 B` · `ago/fmtAgo/toneVar` 사본 23벌 · 직접 `fetch(` 7곳(그중 1곳은 403·409 를 성공처럼 보이던 무음 실패) · 수제 setInterval 폴링 · 온도 임계 숫자 복제.
- **E 저장소**: CVP 상태 파일 2개가 백업 '설정 변경' 감시에 설정으로 잡힘(v2.590 P1 재발) → 상태 파일을 만드는 헬퍼가 스스로 등록(`util/stateFiles.js`) · SQLite open 코어 사본 3벌 · 등록부 5종의 손상/필터 사본 → `util/registryCore.js` · 파트 장애 보존일 설정 부재 · clamp 사본.
- **F 엣지**: sanswitch push 단일비행이 형제와 다른 모양(2초×90 루프) · SAN·PDU 의 상태 전용 push 부재(v2.583 설계 잔여) · `readDropSummary` 사본 3벌 · 중앙 라우트 51개의 인라인 게이트 쌍 → `requireCentral()`.
- **G 런타임**: 서비스 점검이 폴러 13개만 보고 26개를 놓침 → `edgelog/spec.js` 에서 파생 · 손 풀 2곳 · bmusage 예산이 장비 시한과 결합되지 않음 · 기동 스태거 110초 · gpu-guest·fleet push 의 BIG_JSON/gzip/413 누락.
- **H 테스트/문서**: 설정 pull 진입 함수 4종을 아무 테스트도 부르지 않음 · `*Status` 스윕 정규식이 async 를 놓침 · 2줄 stripComments 사본 24파일 · 절대 시간 단언 24곳 · 느린 테스트(실제 SSH 60초) · ARCHITECTURE.md 손 목록(41 디렉터리 미기재) → `scripts/arch-doc.mjs` 생성기.

## 리드 통합에서 한 것(그룹 범위 밖)

- `config.js` import 경로(util/envTimeout) · `idracTelemetry.js` 주석 · `secOutbound2583`·`audit2597` 테스트 파일 목록/정규식 · `linkcheck/run.js` 태그 2줄 · `scripts/api-doc.mjs GUARD_NOTE` 에 `requireCentral`.
- 웹: `SpecialTools.jsx` 가 `/auth/me` 대신 현재 사용자 객체를 읽고(`App.jsx` 가 인증 꺼짐 때도 `serviceHubUrl` 을 실어 준다) `IpamStandalone` 재수출 삭제 · `SanSwitchTool`·`PortalCheck` 의 `ago` 사본 정리(⚠ PortalCheck 의 것은 그룹 보고와 달리 사용 중이었다 — relTime import 로 복원) · `CvpTool` 상태 셀이 old-version 조치 문구를 본문에 그린다 · `PartFaults` 에 이력 보존일 칸 + `retentionText`(0 일 = env 전부 보관을 빈 조각으로 두지 않는다).
- 문서 생성기 4종 재실행(API·ENV·CONFIG-FILES·ARCHITECTURE/INDEX).

## 남긴 것(다음 점검 후보)

- B: 정적+동적 이중 import 잔여 7곳(`KNOWN_STATIC_DYNAMIC_DUP`) · idrac→bmusage 방향 역전(파서 이동 별건).
- D: 수제 setInterval 폴링 후속 12곳(허용 목록에 사유) · WEB2613-11(a) 조기 return 뒤 훅 스윕.
- E: 큰 설정 파일 카탈로그(`configFiles/catalog.js`) · clamp 사본(vmseries·relaycheck·perfSettings·publicapi·ipam/scanStore) · `config.js retentionEnv` 와 partfault 자체 파싱 통합.
- F: SAN·PDU 상태 전용 보고를 싣는 라우트/화면(`edgeSanSwitchReports`·`edgePduStatus().status` — 서버 계약만 갖춤) · bmUsage TIMEOUT_MS 두 곳 읽기(동적 import 라 통합 보류).
- G: storage cliSsh 예산(150s) vs poller 시한(180s) 결합 · 서비스 점검 `coverage`·`specKey` 의 웹 표시.
- 정직 기록: 전부 목 중앙·실 라우터·합성 입력 재현이다. 실엣지(28곳)·실장비로는 확인하지 못했다.



## A 특수 기능 카탈로그·셸·집행 — 검증 원문

# 적대적 검증 — 축 `catalog`(CATALOG·ROUTE·PERM — 특수 기능 카탈로그 정합)

- 기준: HEAD `8ece9d9`(v2.612.0). 읽기 전용 · 전량 테스트/빌드 없음.
- 방법: 발견 스크립트 `catalog/xcheck{,2,3,4}.mjs` 4개를 **재실행**(결과 재현) + 소스 직접 열람 + `toolAccessIssue` 를
  스크래치 `CONFIG_DIR`(거부 목록 심은 permissions.json)로 **실제 호출**(`verify-catalog/repro02_03.mjs`).
- 결과: 확정 6 · SPLIT 4 · PLAUSIBLE 1 · 반증 0(단 04 는 '결함' 부분을 반증하고 설계 전제만 남김). 심각도는 전반적으로 **낮춘다** —
  기본 역할의 거부 목록이 비어 있고(`permissions.js DEFAULT_TOOLS_DENIED = {operator:[], viewer:[]}`) `insights`·`inv.nsx`·`dashboard` 가
  두 역할 기본 보유라, 발견 대부분은 **관리자가 거부·허용 목록을 실제로 건드린 계정**에서만 드러난다.

| ID | 판정 | 심각도(검증 후) | 근거 한 줄 | 권장 최소 수정 | 자동 점검 |
|---|---|---|---|---|---|
| CATALOG2613-01 | **SPLIT** | low | ①`adminOnly=true·API tools`(9개)는 **설계**(CLAUDE.md v2.555/v2.573 "표시 관례이고 접근제어가 아니다" + `userToolText.js:151` 모달 경고 + 명시 허용 우회) ②`adminOnly=false·주 API admin`(real-os·portaldb·serveranalysis·nic-speed·nic-models·shutdown 6개 — 소스로 재확인)와 `perm:'insights'` 누락 3개는 실재하나, 6개는 403→`ErrorBox`→AccessDenied 로 **무음이 아니고** shutdown 은 v2.590 W2 가 그 모양을 **의도적으로 확정**했으며, insights 는 두 역할 기본 보유라 회수한 계정에서만 뜻이 생긴다 | 5개(real-os·portaldb·serveranalysis·nic-speed·nic-models)에 `adminOnly:true`, fleet·powermap·topo3d 에 `perm:'insights'` — shutdown 은 v2.590 결정대로 둔다. 회귀: 웹 테스트가 `docs/API.md` 게이트 열과 플래그를 대조(`gate` 필드 신설은 별건) | **아니오(런타임)** — `requireRole()` 클로저에 이름 표식이 없어 서버가 자기 라우터 스택에서 게이트 종류를 읽을 수 없고, 카탈로그는 번들에만 있다. 테스트 스윕만 |
| CATALOG2613-02 | **SPLIT** | low | 4곳 모두 `toolAllowed` 0건·재현(`repro02_03.mjs`: vm-clone 거부 시 `/vm-clone/badges` 403, ipam 거부 시 `/ipam`·`/ipam/sheet` 403). 그러나 ①`VCenterDetail` 배지·`VCenters` TrendKpi 는 **그 도구의 데이터**이고 주석이 실패 시 숨김을 명시(설계) ②`IpmsMatches`(vms·hosts·datastores·networks 탭 'IPMS 포함' 체크가 ipam 접근과 무관하게 뜬다)와 `Nsx.jsx` 시트는 403 을 **"자료가 없습니다"/"수집된 IP가 없습니다"** 로 그린다 — v2.590 W2·W3 규약("못 읽음을 기본값으로 칠하지 말 것") 위반. 기본 거부 목록이 비어 있어 **역할 거부·허용 목록 모드에서만** 드러난다 | `IpmsMatches`·`Nsx.jsx` 두 곳: `toolAllowed('ipam')` 이 거짓이면 체크박스/절을 숨기거나 `catch` 에서 403 을 AccessDenied 문구로 분기. 배지·TrendKpi 는 그대로(설계). 회귀: 웹 스윕 '/tools/<seg> 를 부르는 비소유 파일은 toolAllowed( 를 가진다' 는 **허용 목록(사유)** 을 둬야 한다(배지·TrendKpi·taskLabel 등 정당한 예외가 많다) | 아니오(web 소스) |
| CATALOG2613-03 | **CONFIRMED** | medium | `toolKeyForPath` 실행: `/vm-track/ds-{top,changes}` → `vm-track`, `ds-change-log` 만 `storage-track`; `/current-users/combined`·`/horizon-sessions*` → **null(통과)** 인데 `curuser` 는 `toolCoverage` 에서 `enforced`(관리 화면 '서버 집행' 열). 두 파일이 CurrentUsers 화면 전용임은 grep 으로 확정(HorizonSessionsPanel/Settings 는 CurrentUsers.jsx 에서만 import). `toolAccess.js` 머리말·v2.506 은 **거짓 선언**을 이 모듈이 없애려는 사고 그 자체로 적는다 | `TOOL_PATH_KEYS` 에 `'current-users':'curuser'`·`'horizon-sessions':'curuser'`; `TOOL_PATH2_KEYS` 에는 **StorageTrackTool 전용인 `ds-list`·`ds-pivot`·`ds-series-all` 만** 추가 — ⚠ `ds-series` 는 `Datastores.jsx` 의 `DsTrendModal`(가드 없음), `ds-top` 은 V4 Storage(`toolAllowed('vm-track')` 로 가드), `ds-changes` 는 VmTrackTool 이 함께 쓰므로 옮기면 **공유 엔드포인트 함정**이다. 노트 문장은 실제 목록으로. nsx 노트는 `/api/nsx(inv.nsx perm)` 로 정정 | **예(런타임·부분)** — 서버가 `api` 라우터 스택의 `/tools/*` 첫 세그먼트를 열거해 `toolKeyForPath` null 인 것을 허용 목록(`ip-ping`·`vclogs`·`groups`)과 대조하면 `current-users`·`horizon-sessions` 가 즉시 잡힌다. '선언 문장이 사실인가' 는 web 호출부가 필요(테스트) |
| CATALOG2613-04 | **SPLIT** | info | ①정규식 취약: 세 정규식 모두 지금 **91/91**(`audit2506` 엄격형·`toolCategories2455` 유연형·`v4Portal2508` 앵커형)이고 하한(`>=79`·`>=50`)이 있어 12개 이상 빠지면 깨진다 — 실제 사고 0건, 계약이 아니라 우연인 점만 참 ②'배포 서버에 카탈로그 없음' 은 **명시된 설계**(`toolcats/catalog.js:16` "존재 여부는 웹이 판단 … 목록을 서버로 복사하면 둘이 어긋나는 날이 온다") — 결함이 아니라 task #32(아키텍처 점검 화면)의 **설계 전제**다 | 결함으로 고치지 않는다. #32 를 만들 때 '카탈로그 JSON 을 `web/public` → `web/dist` 로 두고 서버가 그 사본을 읽는다' 를 **설계 결정**으로 다룰 것(`catalog.js:16` 의 경고와 충돌하므로 그 주석도 함께 고쳐야 한다). 정규식은 세 테스트가 한 헬퍼(`_toolKeys.js`)를 쓰게 통합하면 충분 | 해당 없음(전제 조건 자체) |
| CATALOG2613-05 | **CONFIRMED** | low | `tool === '` 를 보는 테스트는 `tokenCheck2560.test.js:470` 1건(portal-check)뿐 — 재확인. 카탈로그 91 vs 렌더 분기 86(+external/topTab/comingSoon 5 = 정합)이나 고정하는 계약 0. CLAUDE.md v2.573 "나머지 등록처는 이미 자동이거나 테스트가 막고 있다 … 남은 수동 작업은 toolAccess.js 하나" 가 **렌더 분기·lazy import·`scoped` 30키 목록**을 빠뜨렸다. 지금 어긋난 것은 없다(잠재) | 웹 vitest 하나: `SpecialTools.jsx` 를 문자열로 읽어 `{tool === 'k'` 집합 == `TOOLS.filter(비 external/topTab/comingSoon)` 키 집합. `TOOL_VIEWS` 맵 리팩터는 선택(테스트만으로 계약이 선다). CLAUDE.md v2.573 문장 정정 | 브라우저 쪽은 번들이 맵을 export 할 때만 가능 — 지금은 테스트만 |
| CATALOG2613-06 | **CONFIRMED** | low | `useHashTab` 미사용 + 로컬 `useState('tokens')`/`useState('settings')` 재확인. 규약 근거는 `hooks/hashTab.js` 머리말(사용자 요구 '하위 메뉴 상태에서 새로고침해도 유지', v2.438). ⚠ 범위가 더 넓다 — 도구 루트에 서브탭을 둔 **`PartFaults`(`open`/`events`)·`SanSwitchTool`(`ports`/사용량)** 도 같은 상태(CvpTool 의 `tab` 은 DeviceModal 안, RoomTemp 는 '보기 전환' 이라 제외). ⚠ 발견의 'EmptyInvModal 링크' 는 사실이 아니다 — `#/tools/portal-check` 링크는 `Collectors.jsx:605`(DiagModal '토큰 점검 열기') 하나이고 그 링크는 **의도적으로** 토큰 탭이다 | 4개 도구(PortalCheck·LinkCheck·PartFaults·SanSwitchTool)에 `useHashTab({ base:['tools','<k>'], valid, fallback })`. 회귀 스윕은 '도구 루트에서 탭 버튼 배열을 `useState` 로 드는 파일' 기준으로 쓰되 모달 내부 탭은 제외 목록에 | 아니오(web 소스) |
| CATALOG2613-07 | **CONFIRMED** | low | `toolVisibility.js` 에 `perm`/`can` 참조 0 · `V4App.jsx:152` 와 `Palette.jsx:24` 는 `toolHidden` 만 · `version_4/pages/Tools.jsx:104` 의 잠금 판정 `!(canTools && toolAllowed(k))` 도 perm 을 안 본다 — 재확인. 단 tree.js 주석의 정책은 "잠긴 도구는 숨기지 않고 회색" 이므로 문제는 '보인다' 가 아니라 **perm 잠금이 회색 표시에 반영되지 않는다** 이고, `insights`·`inv.nsx`·`dashboard` 는 두 역할 기본 보유·`svcmon` 없는 viewer 는 `tools` 도 없어 이미 전부 잠김 → 실효 범위는 권한을 **회수한 operator** 뿐 | `lockReasonOf` 를 `toolVisibility.js` 로 옮겨 `{isAdmin, toolsAllowed, can}` 을 받게 하고 V4App·Tools.jsx·Palette 의 잠금 판정이 그것을 쓴다(숨김 축 `toolHidden` 은 그대로) | 아니오 |
| CATALOG2613-08 | **CONFIRMED** | low | 인라인 가드 7개 재확인(xcheck (a)). `lockReasonOf` 가 `adminOnly && !isAdmin && !toolExplicitlyAllowed` 로 먼저 막으므로 도달 경로는 **허용 목록 명시 허용 비관리자** 뿐이고, 그때 21개는 화면→403→AccessDenied, 7개는 '관리자 전용 기능입니다' — 같은 조건에 두 문구. `vmprovision` 은 조회가 `requirePerm('vm.provision')`(admin 아님)이라 인라인 가드가 서버보다 **더 좁다** | 7개 인라인 가드 제거(서버 403 + AccessDenied 로 통일) + 웹 테스트 `isAdmin ?` 분기 0 | 아니오 |
| CATALOG2613-09 | **CONFIRMED** | info | `TABS` 에 `explore`·`nsx` 없음 → `tabFromHash` null → `getLandingTab()` + `replaceState` 로 조용히 랜딩(코드 경로 확인). 앱 안에 `#/nsx`·`#/explore` 참조는 **0건**이라 영향은 v2.274 이전 외부 북마크뿐 | `MOVED_TABS` 에 `explore:['tools','explore']`, `nsx:['tools','nsx']` + `hashTab.test.js` 케이스(2줄) | 아니오 |
| CATALOG2613-10 | **PLAUSIBLE** | info | 수치는 재현(점검 7·지도 5·인사이트 2·스토리지 5·아이콘 5종 공유·desc>200 8개). 그러나 규약 위반이 아니다 — desc 클리핑은 v2.576 `.vc-card .muted { overflow-wrap:anywhere }`(styles.css:613)가 이미 막았고('v2.507 클리핑의 재료' 는 낡은 근거), 묶음·라벨은 **제품 판단** | 판단 사항으로 사용자에게 제시만. `insights` 라벨 구분(예: '운영 인사이트(라이트사이징·N+1)')은 저비용 | 예(브라우저 — 번들 TOOLS 로 라벨 접두·아이콘·desc 길이 계산) · 묶음은 사람 판단 |
| CATALOG2613-11 | **CONFIRMED** | info | grep 재현 7곳(`tree.js:4` · `toolcats/catalog.js:8,32,33` · `toolSections.js:5` · `SpecialTools.jsx:141,252`) vs `TOOLS.length` 91 | 숫자 삭제, "specialToolsList.js 의 전부" 로 | 테스트 스윕만(주석은 번들·런타임에 없다) |
| CATALOG2613-12 | **SPLIT** | info | 세그먼트≠키는 `TOOL_PATH_KEYS` 기준 **16종**(발견의 21 에는 확장자 변형 `gpu.csv`·`gpu.json`·`ipam.csv`·`ipam.xlsx`·`vm-export.csv` 5개가 섞여 있다 — `stripExt` 가 처리하는 같은 키). "v2.547 이후 신규는 전부 세그먼트=키" 도 틀렸다(`edge-log-local→edge-log`, v2.549). 규약 부재는 사실이나 매핑표가 그 대가로 존재하고 테스트(`audit2506`)가 고정한다 | 규약 한 줄을 `toolAccess.js` 머리말에 적고, **새로 추가되는** 항목만 세그먼트=키 를 테스트로 고정(기존 16종은 `k` 불변 규약 때문에 못 바꾼다) | 예(런타임) — 서버가 라우터 스택으로 `/tools/*` 세그먼트를 열거해 두 표와 대조 |

## 검증 기록(발견별)

### 01 — 방향을 나눠야 한다
- 재실행(`xcheck2.mjs`): `adminOnly=true` 인데 GET 이 `tools` 뿐인 도구 9개 그대로. 그러나 `web/src/api.js:57-70` 과 `userToolText.js:151`
  ("조회 API 가 admin 을 요구하는 도구는 열 때 403 이 됩니다(권한을 넓히지는 않습니다)") 이 그 관계를 **이미 선언**하고 있다 →
  v2.555 가 명시 허용으로 해소한 설계. 이 방향을 '결함' 으로 다시 세면 v2.555 결정을 되돌리는 셈이다.
- `real-os`: 발견이 '주 API' 라 했는데 `GuestOsTools.jsx:16 GuestOs` 는 `/tools/guest-os`, **`:180 RealOs`** 는 `/admin/os-scan*` 만 쓴다 → real-os 는 맞다.
  `portaldb`(`/admin/portal-db` usePolling) · `serveranalysis`(`/admin/idrac`·`/admin/datacenters`) · `nic-speed`/`nic-models`(`/admin/idrac/nic-*`) ·
  `shutdown`(`/admin/emergency-stop`) 전부 소스로 확인. 셋 다 `.catch(e => setErr(e.message))` → `ErrorBox` → AccessDenied 라 **무음 실패는 아니다**.
- shutdown 은 CLAUDE.md v2.590 W2 가 "operator 는 … '❔ 확인하지 못했습니다' + AccessDenied" 로 **화면을 고쳐 두었다** — 카드를 막는 대신 그 모양을 택한 기록.
- `insights` 는 `DEFAULT_MATRIX` operator·viewer 모두 기본 보유 → fleet·powermap·topo3d 의 perm 누락은 회수한 계정에서만 뜻이 있다.
- 자동 점검: `auth.js:970 requireRole(...roles)` 가 돌려주는 클로저에 이름 표식이 없다(`gateName` grep 0) → 서버가 라우터 스택에서 게이트 종류를 못 읽는다.

### 02 — 재현했지만 2곳은 설계
- `repro02_03.mjs`(operator 거부 목록 `vm-clone, ipam, storage-track, curuser, vm-track`): `/vm-clone/badges`·`/ipam`·`/ipam/sheet`·`/vm-track` 전부 403.
- `vmClone.js:105-113` 라우트 주석은 "로그인 사용자 전체 허용" 인데 `/tools` 접두 `toolGate`(`routes/api.js:72`) 가 먼저 걸린다 — 라우트 저자의 의도와
  게이트가 어긋난 것은 사실. 다만 배지는 vm-clone 잡 정보이고 vm-clone 은 `adminOnly` 라 operator 카드는 잠기고 V4 내비는 숨긴다 — 그 사용자에게 배지가
  안 보이는 것을 '무관한 화면이 빈다' 고 보긴 어렵다. `VCenters.jsx:31` 주석 "tools 권한이 없거나 … 카드 자체를 숨긴다" 도 의도된 강등.
- `IpmsMatches.jsx:47` 는 403 을 "해당 검색어와 일치하는 IPMS 자료가 없습니다", `Nsx.jsx:315` 는 "이 서브넷에서 수집된 IP가 없습니다" 로 그린다 —
  v2.590 W2·W3 유형. `App.jsx:481` 의 'IPMS 포함' 체크박스는 `toolAllowed('ipam')` 과 무관하게 뜬다.

### 03 — 재현 + 수정안 경고
- `/current-users/combined`·`/horizon-sessions*` 통과, `ds-top`·`ds-changes` 는 `vm-track` 키(위 표). `CurrentUsers.jsx:427-429` 는 '전체'(combined)·
  'Windows'(curuser)·'VDI'(horizon-sessions) 세 탭이라 curuser 거부 시 **세 탭 중 둘이 그대로 동작**한다.
- ds-* 공유 관계(grep): `ds-series` ← `DsTrendModal.jsx` ← `Datastores.jsx:6,53`(가드 없음) · `ds-top` ← `version_4/pages/Storage.jsx:29`(`toolAllowed('vm-track')`) ·
  `ds-changes` ← `VmTrackTool.jsx`. 발견의 "ds-* 6개 추가" 를 그대로 하면 `toolAccess.js:56-57` 이 경고한 공유 엔드포인트 함정을 만든다.

### 04 — 설계 전제이지 결함이 아니다
- 세 정규식 카운트 91/91/91(직접 실행). `packaging/offline/build-package.sh:120` 은 `web/dist` 만 복사(확인). `toolcats/catalog.js:16` 이 서버 사본을 명시적으로 금한다.
  '서버에 카탈로그가 없어 자가 점검을 못 한다' 는 #32 의 요구이고, 그 결정은 이 주석의 근거(둘이 어긋나는 날)와 맞바꿔야 한다.

### 06 — 범위 정정
- `views/tools` 에서 `useHashTab` 없이 탭 상태를 드는 파일: PortalCheck·LinkCheck·**PartFaults(`open`/`events`)·SanSwitchTool(`ports`/사용량)**·CvpTool(DeviceModal 내부 — 제외)·
  RoomTemp(보기 전환 — 제외)·SanZoningPanel(패널 내부 — 제외).

### 12 — 수치 정정
- `Object.entries(TOOL_PATH_KEYS).filter(([s,k]) => s !== k)` = 16: capacity-forecast→forecast · deep-search→deepsearch · duplicate-ips→dupip · esxi-temp→esxitemp ·
  guest-os→guestos · network-check→net-check · orphan-vmdk→orphanvmdk · edge-log-local→edge-log · service-check→davinci-svc · vmware-config→vmware-backup ·
  rightsize→rightsizing · sanswitch→san-switch · storage→storage-mon · thin-vms→thinvms · vm-finder→vmfinder · vmseries→waste.

## 중복
- 01 ↔ 07 ↔ 08 은 같은 뿌리(카드 잠금 판정 `lockReasonOf` 가 서버 게이트와 다른 축을 본다)의 세 단면이다 — 수정은 `lockReasonOf` 를 `toolVisibility.js` 로
  옮기는 07 의 리팩터 하나에 01(플래그 보정)·08(인라인 가드 제거)을 얹으면 된다.
- 03 ↔ 12 는 같은 표(`TOOL_PATH_KEYS`)의 두 관점(누락 / 이름 규약).

## 정직 기록
- 브라우저(Chromium)로 화면을 열어 보지는 않았다 — 판정은 소스·스크립트·함수 호출 재현 기준이다. 02 의 "없습니다" 문구가 실제 403 에서 뜨는지는 코드 경로
  (`fetchJson` reject → `catch → setRows([])` → `rows.length === 0` 분기)로 확인했고 렌더로는 보지 않았다.
- 01 의 '무음이 아니다' 는 `ErrorBox` 가 403 을 AccessDenied 로 바꾼다는 v2.398 기록에 의존한다(각 화면에서 실제 렌더 확인은 안 했다).


## B 서버 의존 방향·모듈 배치 — 검증 원문

# arch2613 — 축 B `deps` 적대적 검증(DEP-LAYERING)

- 기준: `/home/user/The.DVC` HEAD `8ece9d9`(v2.612.0). 읽기 전용 — 저장소 수정 0 · 커밋 0 · 전량 테스트/빌드 0 · 목 서버 0(`pgrep` 0).
- 방법: 발견 스크립트(`deps/{dirscc,sim,webdup}.mjs`)를 다시 돌리고, 독립 스크립트
  `verify-deps/reach2.mjs`(동적 import 124곳 전수 BFS 되돌이 경로)·`verify-deps/webago.mjs`(웹 헬퍼 본문 정규화 비교)로 재유도했다.
  export 집합은 실제 `import()` 로 읽었고, 함수 본문 동일성은 `md5sum` 으로 확인했다. 렌즈 ① 의도(CLAUDE.md 규약 인용) ② 재현.
- 소요: 약 7분(벽시계). 시각 18:17 KST.

## 요약표

| ID | 판정 | 심각도(검증 후) | 근거 한 줄 | 권장 최소 수정 | 자동 점검 |
|---|---|---|---|---|---|
| DEPS2613-01 | CONFIRMED | low(↓medium) | `config.js:5 → agent/envTimeout.js` + agent/ 밖 18파일 import 재현. 단 v2.579 규칙 ②는 util/ 만 제한하고 config.js 주석이 "무의존 leaf 모듈" 로 **의도된 선택**(v2.605 TIM2605-04)임을 밝힌다 — 규약 위반이 아니라 잠재 순환 위험. `collector/denyLog.js` 는 collector 도메인 상태(importer 2곳 전부 collector 계열)라 '자리 잘못 잡은 util' 이 아니고, `central/pullStats.js` 는 Map 을 든 저장소라 util/ 이동 대상이 아니다 | `agent/envTimeout.js`·`agent/central404.js`·`agent/centralReply.js` 를 `util/` 로 옮기고 원위치는 `import…; export {…}` 재수출(v2.575 규약); arch2579 ②에 "config.js 는 util/·security/·node 내장만" 추가 | 예 — 배포 서버의 `server/src` 를 arch2579 와 같은 정규식으로 그래프화(707파일 <1초) |
| DEPS2613-02 | SPLIT | low(↓medium) | (a) `redfish.js:21` 정적 + `:1204` 동적 **같은 모듈 이중 import**·`idrac→bmusage` 방향 역전은 재현(둘 다 v2.598 커밋 3798103b). (b) **"반대 방향 import 를 해도 순환이 아니다" 는 틀렸다** — redfish.js 가 :21 에서 정적 import 하므로 idracTelemetry→redfish 를 넣는 순간 2-cycle 이다(BFS 확인). 주석의 결론(반대 방향 금지)은 맞고 표현("동적 import 하므로")만 불완전 — '자기모순' 아님 | `:1204` 동적 import 를 :21 정적 import 로 합친다(1줄). 파서를 `idrac/telemetryParse.js` 로 옮기는 것은 선택 | 예 — 같은 파일이 같은 모듈을 정적+동적으로 import(소스 정규식) · 디렉터리 2-cycle(그래프) |
| DEPS2613-03 | SPLIT | low(↓medium) | 재유도: 동적 import 124곳 중 정적 되돌이 경로 있는 곳 **3**(config.js:386·perfPush.js:92·restClient.js:197) — 일치. '순환 근거' 8곳 중 **3곳만 확정**: `collector/registry.js:120`(주석 "순환 import 를 피하려고 지연 로드" — v2.579 ARCH-02 뒤 되돌이 경로 없음)·`partfault/hooks.js:31·34`(머리말 "순환 방지" — `partfault/{push,poller}.js → idrac/poller.js`·`→ hooks.js` 정적 경로 없음). **4곳 반증**: `routes/collector.js:81·110·138` 의 '순환' 은 :38 머리말(ARCH-03 기록)이고, `powerstore.js:271` 의 '순환' 은 :12(powerstoreCore 분리 사유) — 발견 스크립트가 **파일 단위 grep** 으로 근접 주석이 아닌 텍스트를 귀속시켰다. `redfish.js:1204` 는 순환 주석 없음(DEPS-02 중복). 참고: arch2579 는 동적 import 도 edge 로 세므로 동적화가 테스트상 순환을 숨기지 못한다 | 3곳(registry.js:120·hooks.js:31·34)을 정적 import 로 바꾸고 주석 삭제. 테스트: 동적 import 근처(±4줄)에 '순환' 이 있으면 되돌이 경로가 있어야 통과 + "되돌이 경로 있는 동적 import 는 이 3곳" 고정 | 예 — 그래프+BFS 런타임 실행 · 근접 주석 판정은 ±N줄로 |
| DEPS2613-04 | SPLIT | low(↓medium) | 중복은 재현: `corruptOnlyReason` 4벌 정규화 md5 **동일**, `devicesForThisNode/ForAgent` 동일 시그니처 3벌. **'형제 비대칭' 결과 반증**: v2.612 fix-d 의 503(`registryUnreadable`)은 **중앙→엣지 설정 배포 라우트**(storage/pdu/sanswitch/cvp-config + agentUsers)의 보호이고, `routes/central.js` 는 idrac/bmstor/ping/horizon 등록부를 import 하지 않는다(grep 0 — bmstor/ping 은 잡 큐, iDRAC 은 대역 push). 엣지 목록 소거 경로가 없으므로 `registryLoadError` 부재는 같은 결함이 아니다. 넷 다 `preserveCorrupt` 는 한다. 웹에 '손상 보존본' 문구 소비처 0 — 판정 갈라짐 실증 없음 | `corruptOnlyReason`·`registryLoadError` 상태·`devicesForThisNode/ForAgent` 만 `util/deviceRegistryCore.js` 로 뽑아 4벌이 import(수십 줄). 전체 팩토리(load/persist/secretCarry)는 별건 — 비밀 승계 회귀 위험 | 부분 — 배포 소스 함수 본문 정규화 해시로 '동일 본문 N벌' 보고 가능 |
| DEPS2613-05 | SPLIT | low(↓medium) | export 집합 차이는 `import()` 로 재현(cvp: `requestCvpCollect/takeCvpRequests/…`, TAKE_MAX 10, `requestState/lastDroppedRequest` 없음). **'시한 출처 결함' 반증**: storage/pdu/sanswitch 폴러 자체가 같은 env(`storage/poller.js:29`·`pdu/poller.js:26`·`sanswitch/poller.js:27`)로 장비 시한을 굳히므로 래퍼와 폴러가 **같은 출처**(중앙 설정 배포 없음); cvp 는 폴러가 `settings.deviceTimeoutMs`(`cvp/poller.js:85`)를 읽고 래퍼도 같다 — 도구마다 일관. TAKE_MAX 10 은 `cvpConfigPull.js:68 slice(0,10)` 과 일치(v2.591 '엣지 처리량만큼'). 남는 것은 이름 체계 비대칭 | 이름 별칭(`requestCollect` 등 표준 이름을 cvp 에도 export) + `requestState`·`lastDroppedRequest` 추가. 팩토리는 선택 | 예 — 4모듈 import 후 **기능 집합**(drops/state/take/ack) 차집합(이름이 아니라 능력으로) |
| DEPS2613-06 | CONFIRMED | low(↓medium) | `grep -L classifyCentral404` → gpuGuest·pdu·storage·users 4벌 재현; 넷 다 `if (!res.ok) throw new Error('x-config <- 404')` 라 사유를 가르지 않는다(거짓 '구버전' 은 아님 — 미분류). v2.600 EDGE2600-06 테스트(`audit2600c:225`)는 vmSeries·svcmon 만 고정 → 형제 누락(CLAUDE.md v2.582 "형제 규약 비대칭은 결함") | 4파일에 `if (res.status === 404) { const c = await classifyCentral404(res); last = {…, kind: c.kind, reason: c.reason}; return {ok:false, kind:c.kind, reason:c.reason}; }` 4~5줄씩. 팩토리는 선택 | 예 — `agent/*ConfigPull.js` 소스에 `classifyCentral404` 유무 스캔 |
| DEPS2613-07 | SPLIT | low(↓medium) | grep 표 재현(수치 일치 — `-i` 차이만). **`_again` 6/15 은 결함 아님**: `_again` 은 진행 중에 외부 트리거(설정 push·collectNow·hooks)가 오는 모듈에만 필요하고 inventory/gpuGuest/fleet/capacity 는 주기 전용. gzip/413: `capacityPush` 본문은 rows ≤64(중앙 :531 slice) 라 소형 — 필요 없음; `fleetPush` 는 머리말이 "메타만" 이라 하지만 베어메탈 목록은 서버 수 비례(현장 1,135대) — 1MB 도달 여부 **미확인**. 위치 분리(agent/ 9 vs 기능 디렉터리 6)는 사실이고 DEPS-10 2-cycle 의 원인 | 팩토리보다 먼저: fleet·gpu-guest-data 등 **본문이 함대 규모에 비례하는 POST 경로**만 BIG_JSON 등록 + 413 로그(v2.503 체크리스트). 위치 통일은 별건 | 예 — 런타임 `app._router` 의 `/api/central/*` POST 와 BIG_JSON 마운트 대조 + 소스 `413`/`gzip` 스캔 |
| DEPS2613-08 | PLAUSIBLE | low | 재현: gzip 0·413 문자열 0·`index.js` BIG_JSON 에 `gpu-guest-data` 없음(전역 1mb). 본문 = hosts + vms(GPU VM 당 ~120B) + diag(vCenter 당 ≤200건 × ~350B ≈ 70KB — `gpu/poller.js:224·225` 상한). 1MB 는 vCenter 15곳 × GPU VM 200대 급이라 이 현장에서 가능성 낮음. 413 은 `catch` 로 콘솔에 남아 무음은 아니나 재시도 없이 그 주기 소실 | `index.js` 에 `app.use('/api/central/gpu-guest-data', BIG_JSON)` 1줄 + `audit2590:179` 식 테스트 1줄 | 예 — 라우터 스택에서 BIG_JSON 미마운트 POST central 경로 목록 |
| DEPS2613-09 | CONFIRMED | low(↓medium) | 수치 전부 재현(2,085줄 · 라우트 51 · 404 게이트 51 · 403 게이트 51 · 정적 60 · 동적 34). 4곳(:1231·:1414·:1467·:1703)의 agent 대조는 :294 미들웨어가 `requestedAgent`(:253 — query.agent·X-Agent-Name·body.agent)로 이미 하는 것과 **같은 소스**를 다시 본다 → 중복(무해한 2중 방어). 오늘 게이트 누락 0(51=51). 파일 크기를 금지하는 규약은 없고 문서화된 고통은 줄 번호 테스트(v2.569) | `requireCentral()` 미들웨어 1벌로 2줄 게이트 51회를 대체(동작 동일) → 이후 `central/routes/<feature>.js` 로 점진 분리(선언 직후 `wrapAsyncRouter`) | 예 — 라우트 수·줄 수·`await import(` 수 임계 + 게이트 반복 소스 스캔 |
| DEPS2613-10 | CONFIRMED(관찰) | info(↓low) | `dirscc.mjs` 재실행: 38 디렉터리 1 SCC · 2-cycle 표본 일치. 그러나 v2.579 규칙은 파일 단위 3개뿐이고 CLAUDE.md 는 `central/` 을 인메모리 저장소로 **의도**해 적었다(v2.548·v2.560) — 규약 위반이 아니라 규약 부재. 파일 SCC 4는 문서와 일치 | 계층 규칙을 채택할 때만: arch2579 에 디렉터리 SCC 크기 상한(현재 38)을 두고 줄여 간다 | 예 — 파일 SCC 와 같은 코드 |
| DEPS2613-11 | SPLIT | low | 사본은 실재하나 **"13파일 동일 본문" 반증**: `ago` 14정의/**9종** 본문(바이트 동일은 5개뿐 — 나머지는 floor/round·''/'—'·ms/ts 가 다르고 `collectDropText.ago` 는 경과 ms 라 `elapsedText` 대응), `fmtAgo` 7정의/5종(`CapacityAdvisor` 는 90초·5400초 임계), `toneVar` 3정의/2종. 갈라진 사본이 곧 v2.574 `relTime.js` 머리말이 경고한 드리프트라 결함은 맞다 | 화면별 `dash`/`subMinute` 옵션을 지켜 `relTime.agoText`/`elapsedText`·`util/fmt.fmtAgo` 로 치환; vitest 스윕 "`views/**` 로컬 `ago`/`fmtAgo`/`toneVar` 정의 0" | 아니오 — 번들 minify. CI vitest 스윕 |
| DEPS2613-12 | CONFIRMED(기지 잔여) | low | 재현: 정의 정규식 34건(발견 30 — 범위 차이), `clampSetting` importer 5(그중 `ipam/scanStore.js:63` 은 import 하면서 로컬 clamp 도 유지). CLAUDE.md v2.595 "clamp 사본 16벌 — 다음 점검 후보" 로 **이미 등재된 잔여**이고 v2.596 CLAMP 는 빈 칸 판정만 numOrNull 로 고쳤다. 시그니처 4종 사실 | settings 모듈부터 `clampSetting(v,{min,max,def})` 로 치환 + 스윕 테스트 | 예 — 배포 소스 로컬 clamp 정의 수 |
| DEPS2613-13 | CONFIRMED | info | 줄 수 재현(central 2,085 · StorageMonTool 1,804 · soapClient 1,732 · support 1,658 · toolsCapacity 1,636 · redfish 1,478 · SanSwitchTool 1,441). redfish export 는 26(발견 19 — 상수 포함 여부) | 선택 — DEPS-02 와 함께 `redfish.js` 관심사 분리 | 예 — 줄 수·export 수 임계 |

중복 표시: DEPS-02 ⊂ DEPS-03(redfish.js:1204)·DEPS-10(bmusage↔idrac) · DEPS-08 ⊂ DEPS-07 · DEPS-01 ⊂ DEPS-10(agent↔기능 2-cycle) 및 DEPS-07(push 위치).

## 판정 상세

### DEPS2613-01 — CONFIRMED · low
- 재현: `grep -rl "agent/envTimeout" server/src | grep -v ^./agent/` = **18**(config.js 포함). `config.js:5` 주석 "v2.605 TIM2605-04: 요청 시한 env 정규화(무의존 leaf 모듈)".
- 의도 렌즈: `arch2579.test.js` ②는 **util/ 의 바깥 import** 만 막는다. 도메인→agent/·central/ 를 막는 문서 규칙은 없다. 발견이 말하는 위험(누군가 envTimeout 에 config 를 import 하면 파일 순환)은 실재하지만 **현재 결함이 아니다**.
- 반증 일부: `collector/denyLog.js` 의 importer 는 `collector/agent.js`·`routes/collector.js` — collector 도메인의 상태 모듈이 제자리에 있는 것(v2.579 ARCH-03 결과). `central/pullStats.js` 는 Map 저장소(계측)라 '순수 util' 이 아니다 — 옮긴다면 `perf/` 가 맞고 util/ 은 아니다.
- 자동 점검: 예. 데이터 원천 = 배포 서버 `server/src`(그대로 배포된다). "import 가 node:/util/config 뿐 + util/ 밖 + fan-in 디렉터리 ≥3" 판정.

### DEPS2613-02 — SPLIT · low
- 재현: `redfish.js:21`(정적)·`:1204`(동적) 같은 모듈. 두 줄과 `idracTelemetry.js:42` 주석이 **같은 커밋**(3798103b, v2.598)에서 생겼다.
- 반증: 발견 보고 72행 "반대 방향 import 를 해도 순환이 아니다" — `reach2.mjs`: `idrac/redfish.js → bmusage/parse/idracTelemetry.js` 정적 edge 존재 → 반대 방향을 넣으면 즉시 2-cycle. 주석의 결론은 옳다. "자기모순" 은 성립하지 않고 "동적 import 하므로" 가 불완전한 표현일 뿐.
- 남는 사실: 범용 클라이언트가 기능 파서에 의존(방향 역전)하고 `:1204` 는 캐시 조회일 뿐인 중복 — 정리 가치는 있으나 런타임 영향 0.

### DEPS2613-03 — SPLIT · low
- 재현(`reach2.mjs`, 독립 구현): 동적 import 124곳(발견 116 — 해석 범위 차이), 정적 되돌이 경로 있는 곳 **3** — 발견과 일치.
- 확정 3곳: `collector/registry.js:120`(주석이 순환 회피라 적었으나 되돌이 경로 NONE — v2.579 ARCH-02 이후 낡음; 주석 자신이 "leaf util" 이라 인정) · `partfault/hooks.js:31·34`(머리말 "순환 import 방지 — idrac/poller.js 가 이 파일을 import" — 앞 절은 참(`idrac/poller.js → partfault/hooks.js`)이지만 `partfault/push.js`·`partfault/poller.js` 에서 `idrac/poller.js` 나 `hooks.js` 로 가는 정적 경로가 **없어** 순환이 성립하지 않는다).
- 반증 4곳: `routes/collector.js` 의 '순환' 은 :38 한 곳(ARCH-03 재수출 설명)이고 :81·:110·:138 근처 주석은 크기 상한·무음 실패 금지뿐. `powerstore.js` 의 '순환' 은 :12(powerstoreCore 분리 사유 — `powerstore.js → (동적) powerstoreSsh.js → powerstore.js` 를 설명하는 **맞는** 주석)이고 :271 은 수집 방식 분기. 발견 `reach.mjs` 의 `if(/순환/.test(raw))` 는 **파일 전체** 검사라 귀속이 틀렸다. `redfish.js:1204` 는 DEPS-02 와 중복이고 :147 주석은 옳다.
- 심각도: 주석 정확성·가독성 문제. `arch2579` 는 동적 import 도 edge 로 세므로 동적화가 순환 검출을 우회하지 못한다(테스트 상 안전).

### DEPS2613-04 — SPLIT · low
- 재현: 4파일 `corruptOnlyReason` 본문 md5 `8b5ff5fc…` 동일. `devicesForThisNode/devicesForAgent` 시그니처 3벌 동일.
- 반증: v2.612 fix-d(릴리스 노트 5번째 항목)는 "중앙의 스토리지·SAN·PDU·CVP 등록부나 엣지 배포 계정 파일이 손상되면 빈 목록을 내려보내 엣지가 지우던 것 → 503". `routes/central.js` 의 등록부 import 는 storage/pdu/sanswitch/cvp/agentUsers 뿐(grep). iDRAC·bmstor·ping·Horizon 등록부는 엣지에 배포되지 않는다(bmstor/ping 은 `*-jobs` 큐, iDRAC 은 대역 push). 따라서 그 넷의 `registryLoadError` 부재는 v2.612 결함의 형제가 **아니다**(preserveCorrupt 는 넷 다 한다). 웹에서 `손상 보존본` 문구 소비처 0 — 화면 판정도 없다.
- 남는 사실: 코어 중복(v2.513 '코어는 하나다' 정신) — 갈라짐 실증은 없으므로 low.

### DEPS2613-05 — SPLIT · low
- 재현(`import()`): storage/pdu/sanswitch 는 `requestCollect/takeRequestsForAgent/ackCollect/hasPendingRequest/requestState/lastDroppedRequest/recentCollectDrops/setCollectBaseResolver/TAKE_MAX=20`; cvp 는 `requestCvpCollect/takeCvpRequests/ackCvpCollect/hasPendingCvpRequest/recentCvpCollectDrops/setCvpCollectBaseResolver/perItemMs/TAKE_MAX=10`.
- 반증(시한 출처): 세 도구의 폴러 장비 시한도 env 전용(`storage/poller.js:29`·`pdu/poller.js:26`·`sanswitch/poller.js:27`) — 중앙 배포 설정이 아니므로 래퍼가 env 를 읽는 것이 옳다(cvp 는 폴러가 설정을 읽으므로 래퍼도 설정). v2.611 TIM2611-02 는 cvp 에만 해당했다. TAKE_MAX 10 은 `cvpConfigPull.js:68 slice(0,10)` 과 짝.
- 남는 사실: 이름 체계·`requestState`/`lastDroppedRequest` 부재 → 화면 `collectDropText` 계열이 cvp 에서 같은 정보를 못 받는지는 별도 확인 필요(이번 범위 밖).

### DEPS2613-06 — CONFIRMED · low
- 재현: `grep -L classifyCentral404 agent/*ConfigPull.js` = gpuGuest·pdu·storage·users. 넷 다 `if (!res.ok) throw new Error('<x>-config <- ${res.status}')`.
- 의도: `agent/central404.js` 머리말 "예전 pull 모듈들은 … 404 를 전부 '구버전 중앙' 으로 읽었다" — 이 넷은 그렇게 **읽지는 않고**(사유 미분류) 상태에 `<- 404` 만 남긴다. v2.600 테스트는 vmSeries·svcmon 2벌만 고정. 규약(EDGE2600-06 · v2.549 '404 두 뜻')이 6벌에만 적용된 형제 누락.

### DEPS2613-07 — SPLIT · low
- 재현: grep 표 수치 일치(`-ci` 로 gzip 이 1~2 더 잡힘).
- 반증(`_again`): v2.596 의 `_again` 은 **설정 push**(이벤트 트리거)용이고 storage/pdu/partfault/cvp/perfPush 는 collectNow·hooks 트리거가 있어 채택했다. inventory/gpuGuest/fleet/capacity/guestDisk 는 타이머 전용 — 겹침 시 다음 틱이 온다. '15벌 제각각' 은 필요가 다른 것을 같은 잣대로 센 것.
- 확인: `capacityPush` 본문 rows 는 중앙이 64개로 자를 정도의 소형(gzip 불필요). `fleetPush` 는 서버 수 비례인데 gzip·BIG_JSON 없음 — 1MB 도달 여부 **미측정**(가능성). `guestDiskPush`·`inventoryPush` 는 gzip + BIG_JSON 등록 + `!res.ok` throw 라 413 이 콘솔에 남는다(문자열 '413' 만 없음).
- 위치 분리(agent/ 9 vs 기능 6)는 사실이며 DEPS-10 의 agent↔기능 2-cycle 원인.

### DEPS2613-08 — PLAUSIBLE · low
- 재현: `index.js:306~355` BIG_JSON 목록에 `gpu-guest-data` 없음(전역 `express.json({limit:'1mb'})` 적용), `gpuGuestPush.js` gzip 0. diag 상한 vCenter 당 200건(`gpu/poller.js:224·225`), vms 는 성공 GPU VM 만. 1MB 초과는 GPU VM 수천 대 급.
- 413 은 `catch` 에서 `[gpu-guest-push] 실패: gpu-guest -> 413` 로 남는다(무음 아님) — 다만 재시도 없이 그 주기 소실(v2.503). 1줄 등록으로 닫힌다.

### DEPS2613-09 — CONFIRMED · low
- 재현: 전 수치 일치. `requestedAgent`(:253) = `query.agent || X-Agent-Name || body.agent`; 4개 config 라우트의 인라인 대조(`req.query.agent || X-Agent-Name`)는 부분집합이라 미들웨어(:294~:310)가 먼저 403 을 낸다 → 도달 불가 중복.
- 의도: 파일 크기·인라인 핸들러를 금지하는 규약 없음. 게이트 누락 0. 결함이 아니라 유지비 문제 → low.

### DEPS2613-10 — CONFIRMED(관찰) · info
- 재현: `node dirscc.mjs` → 38 디렉터리 SCC 1, 2-cycle 표본 일치.
- 의도: 디렉터리 계층 규칙은 존재하지 않는다(v2.579 ①②③ 는 파일 단위). `central/` 이 저장소 역할을 겸하는 것은 CLAUDE.md 가 여러 곳(v2.548 partFaultEdge 인메모리·v2.560·v2.584 pullStats)에서 **설계로** 적었다. 규칙을 만들 것인지의 판단 사항.

### DEPS2613-11 — SPLIT · low
- 재현(`webago.mjs`): `ago` 14정의 9종 본문(바이트 동일 5: BulkDeploy·RelayCheck·RelayTopo·SanSwitch·StorageMon), `fmtAgo` 7정의 5종, `toneVar` 3정의 2종(partFaultText 는 red/amber/green 별칭 포함 — 다른 계약).
- "13파일 동일" 은 webdup 의 '앞 6줄' 기준이 다른 본문을 같다고 센 것 — 반증. 그러나 갈라진 사본(floor/round·''/'—')이 바로 `relTime.js` 머리말이 v2.574 에 실측한 드리프트 유형이므로 정리 대상은 맞다. 치환 시 `collectDropText.ago(ms)` 는 `elapsedText` 로, 결측 표기('' vs '—')는 옵션으로 보존해야 화면이 조용히 바뀌지 않는다.

### DEPS2613-12 — CONFIRMED(기지 잔여) · low
- 재현: 정의 34건(발견 30 — `clampPage`·`clampIntervalMs` 등 포함 여부 차이). `util/clampSetting` importer 5(`ipam/scanStore.js` 는 import 하면서 :63 로컬 clamp 도 남아 있음). `cvp/settings.js clampNum` NaN 경로 도달 불가는 발견의 정직 기록대로.
- 의도: CLAUDE.md v2.595 "clamp 사본 16벌 — 다음 점검 후보" 로 등재된 잔여 — 새 결함이 아니라 **미이행 후속**. v2.596 CLAMP2596 은 빈 칸 판정만 고쳤다.

### DEPS2613-13 — CONFIRMED · info
- 재현: 7파일 줄 수 일치(±1). `idrac/redfish.js` export 26(상수 포함). 규약 없음 — 정보.

## 확인했고 문제 없던 것 / 발견 보고의 방법 오류
- 발견 `reach.mjs` 의 '순환 근거' 판정은 **파일 전체 텍스트** grep 이라 4곳을 오귀속했다(DEPS-03). 근접(±4줄) 판정으로 바꿔야 자동 점검에 쓸 수 있다.
- 발견 보고 DEPS-02 의 "반대 방향 import 를 해도 순환이 아니다" 는 정적 import(:21)를 놓친 오류.
- `webdup.mjs` 의 '앞 6줄 동일' 은 본문이 다른 함수를 같다고 센다(DEPS-11) — 자동 점검이라면 전체 본문 정규화 해시로.
- 파일 단위 SCC 4·util 밖 import 0·도메인→routes 0·`export … from` 0 은 발견과 같이 재확인.
- 저장소 수정 0(`git status` 0), 목 서버 0.


## C 중앙↔엣지 계약 — 검증 원문

# CONTRACT 축 — 적대적 반증 검증 (v2.613)

- 기준: `8ece9d9`(v2.612.0 merge) · 읽기 전용 · 재현 명령은 `verify-contract/repro.sh` · 발견 스크립트 `contract/parity.mjs` 를 다시 돌렸다(11번째 대조에서 EISDIR 로 중단 — 그 앞 10개 대조는 출력 확인).
- 두 렌즈: **의도**(CLAUDE.md / server/CLAUDE.md 규약 위반인가 · 이미 수용/고침/'남긴 것' 인가) · **재현**(코드를 직접 열고 node 로 실행). 확신이 없으면 REFUTED.
- 결과: CONFIRMED 4(01·04·05·06) · SPLIT 1(03) · PLAUSIBLE 1(10) · REFUTED 4(02·07·08·09). 발견의 **medium 3건 중 1건(03)이 low 로 내려갔고**, low 5건 중 3건(07·08·09)은 결함이 아니었다.

| ID | 판정 | 심각도(검증 후) | 근거 한 줄 | 권장 최소 수정 | 자동 점검 |
|---|---|---|---|---|---|
| CONTRACT2613-01 | **CONFIRMED** | medium | 세 파일의 kind 리터럴 집합이 **10종 완전 동일**(내 추출)이고 순서·문자열까지 같다 — 그리고 **이미 갈라져 있다**: 404 본문 `reason` 이 객체일 때 edgeLogPull(:40 `String()`) → `disabled/'[object Object]'`, tokenCheckPull(capStr) → `disabled/'(형식 오류)'`, bmUsageEdgePull(strOf, v2.604 CEN2604-03 만 적용) → `old-version`. 같은 입력에 **세 답**. 테스트는 `audit2601f` 가 outbound 태그만 본다(kind 대조 0건). 규약: '코어는 하나다'(v2.513·v2.533·v2.575 IMP-08·v2.579 ARCH-01) + v2.553 code↔text 1:1 | `central/edgePull.js pullFromEdge(agent, path, {timeoutMs, retries, fetchImpl, bodyOk})` + `EDGE_PULL_KINDS` export. 세 모듈은 저장(put*)만 남긴다. `retries`(edgeLog·bmUsage 1 / tokenCheck 0 — "재시도가 판정을 흐린다" 의도)와 `TIMEOUT_MS` 는 옵션으로. 웹은 `edgeLogText.FETCH_KIND_TEXT`(이미 10키 전부 보유)를 공용 맵으로 승격. 테스트: `EDGE_PULL_KINDS` ↔ 웹 맵 키 1:1 | **부분 가능** — 배포 서버는 `server/src` 를 갖고 있으므로 런타임에 세 파일의 `kind:` 리터럴 집합을 추출해 다르면 경고(중복 코어의 대리 지표). 리팩터 뒤에는 `EDGE_PULL_KINDS` 를 `/api/…/contract` 로 내려 브라우저 번들 `Object.keys(FETCH_KIND_TEXT)` 와 대조 가능 |
| CONTRACT2613-02 | **REFUTED** | info | 서버 kind 3종(`not-registered`·`disabled-central`·`no-url`)은 `pullTokenCheck` 의 **조기 return** 이라 `putEdgeTokenReport` 를 거치지 않는다 → `edge.lastAttempt.kind`·`edge.kind` 에 **도달 불가**. 게다가 인출 라우트(`portalCheck.js:256`)가 행을 `registered && url` 로 걸러 앞 둘은 애초에 생기지 않고, `disabled-central` 은 run 응답 `results[].kind` 에만 실리는데 `PortalCheck.jsx:101` 은 `runSummary(r)`(개수만) 로 그린다 — `results[]` 를 렌더하는 코드 0건. 웹 맵 7키 = **도달 가능한 kind 집합과 정확히 일치** | 수정 불필요. (01 리팩터 시 공용 맵을 쓰면 자연히 10키가 된다 — 그때도 화면 변화 없음) | 아니오(도달 불가 경로) |
| CONTRACT2613-03 | **SPLIT** | low (발견 medium → low) | **사실인 부분**: `/guest-disk` 응답(`central.js:808`)은 `skippedVms/skippedParts` 를 싣지 않고 `guestDiskPush.js:72`·`vmSeriesPush.js:77` 은 `res.ok` 만 본다(`readCentralReply` 미사용 — 11개 push 중 이 둘만). **틀린 부분**: '엣지는 보냈다(N대) 인데 중앙에는 없다' 는 실제로 생기지 않는다 — 엣지가 push 전에 **같은 함수** `sanitizeGuestDiskVms` 를 이미 거친다(`guestdisk/service.js:76`, v2.601 RECENT2601-01). 그 출력은 `nn()` 으로 allocGB/usedGB/capGB/usedGB 가 전부 유한수라 중앙의 `finite()` 검사(`db.js:145-148`)는 v2.601+ 엣지에서 **0건을 건너뛴다**(방어선 2중일 뿐). 파티션 결측(`partsUnknown`)은 엣지가 이미 세고, 그 중앙 합산은 CLAUDE.md v2.600 '남긴 것' 에 **이미 적혀 있다**. `centralReply.js` 머리말이 대상을 '거절 요약을 내는 5개 push' 로 명시했으므로 guest-disk 누락은 규약 위반이 아니라 **응답에 요약이 없어 읽을 것이 없는** 상태. vmseries 는 `sanitizeVmSeriesBody` 가 레이아웃 불일치 행을 조용히 버리지만 응답 `spikes` 수를 엣지가 보낸 수와 대조하면 알 수 있는데 읽지 않는다(잠재) | `/guest-disk` 응답에 `...(skippedVms||skippedParts ? { dropped:{vms,parts} } : {})` + `guestDiskPush` 가 `dropSummaryOf(await readCentralReply(res))` 로 `last`·콘솔에. `vmSeriesPush` 는 응답 `spikes` < 보낸 행 수면 `last.dropped` 에 기록. **구버전 엣지 호환 문제 없음**(필드 추가만) | **가능(서버 소스 스윕)** — `src/agent/*Push.js`·`*/push.js` 에서 `/api/central/` POST 와 `readCentralReply` 동반 여부(허용 목록 + 사유). 런타임 대안은 응답에 `dropped` 가 생긴 뒤에만 뜻이 있다 |
| CONTRACT2613-04 | **CONFIRMED** | medium | `central/cvpEdge.js` 에 `version|legacy|MIN_|classify` **0건**(재확인), `cvp.js:44` 는 `{pending:true, note:'…보고가 아직 없습니다'}`. 웹 `cvpText.serverState`(:117-118)는 그 상태를 `수집 기록 없음 · 첫 수집을 **기다리는 중**이거나 엣지가 아직 보고하지 않았습니다` 로 그린다 — v2.608 미만 엣지는 `agent/cvpConfigPull.js` 자체가 없어 **영원히** 보고하지 않는데 '기다리는 중' 이다. 형제 5종(partFaults 2.548·edgeLog 2.549·bmUsage 2.554·tokenCheck 2.560·iLO 2.610)은 전부 `MIN_*_EDGE_VERSION` + 분류를 갖는다. `docs/CVP.md §6 정직 기록` 에도 이 항목 없음(=인지된 '남긴 것' 아님). 규약: v2.554 "`edge-waiting` 이 아니라 `edge-silent` … 구버전/버전 미상/첫 보고 대기를 각각 다르게" · v2.548 `classifyEdges` | `central/cvpEdge.js` 에 `MIN_CVP_EDGE_VERSION='2.608.0'` export · `statusOf(srv)` 가 `allCollectorStatus()[agent].version` 을 `cmpVersion` 으로 대조해 `pending` 대신 `{kind:'old-version'|'unknown-version'|'silent'|'waiting', edgeVersion}` · `cvpText.serverState` 가 kind 별 문구(구버전=업그레이드 · 미상=엣지 로그 · silent=중앙 점검 3주기 뒤). `partFaults.classifyEdges` 를 `central/edgeClassify.js` 로 승격하면 01 계열 중복도 준다(선택) | **가능** — 런타임에 `central/*Edge.js`·`*Pull.js` 의 export 에 `MIN_*_EDGE_VERSION` 유무를 열거하고, `agent` 필드를 가진 등록부(cvp·storage·sanswitch·pdu·partfault)와 대조해 게이트 없는 기능을 표시 |
| CONTRACT2613-05 | **CONFIRMED** | low | `routes/api/linkCheck.js`·`linkcheck/*.js` 에 `minEdgeVersion|MIN_EDGE` **0건**(재확인). 서버는 행에 `edgeVersion`(:93)만 싣고 웹 `rowState` 는 기본 인자 `'2.552.0'` 으로 판정(`data?.minEdgeVersion` 은 undefined → 기본값). 형제는 서버 상수를 응답에 싣는다(`edgeLog.js:166 minVersion`·`bmUsage.js:410 minEdgeVersion`). 현재 값은 **옳다**(엔드포인트는 v2.552 부터 있고 그 뒤 계약 변경 없음)라 화면 오류는 없다 — 소유자 비대칭만. 규약: v2.493 '주기·상한 같은 숫자는 화면에 하드코딩하지 말 것 — API 가 주는 값을 쓴다' | `linkcheck/links.js` 에 `export const MIN_EDGE_VERSION = '2.552.0'` + `/tools/link-check` 응답에 `minEdgeVersion` 포함 · `linkCheck2552.test.js` 에 응답 키 존재 1줄 | **가능(서버 측)** — 행에 `edgeVersion` 을 싣는 라우트 응답에 `minEdgeVersion|minVersion` 이 있는지 응답 스키마 셀프체크 |
| CONTRACT2613-06 | **CONFIRMED** | low | `edgeSweep2574.test.js:59-61` 의 `/export function (\w*[Ss]tatus\w*)\s*\(/` 는 **`async`** 를 못 본다 — `logs/poller.js:177 export async function logStatus()`(파일명 패턴은 맞는다). `capacity/sampler.js:95`·`security/certMonitor.js:111` 은 파일명 패턴(`Worker|poller|scheduler`) 밖. 셋 다 `index.js:493-495` stagger 목록에서 역할 무관하게 시작(capacity 는 `config.capacity.enabled` 만 본다)하고, spec 머리말 규칙 "index.js 가 부팅에 import 하는 모듈만" 을 만족하므로 등재 대상. `edgelog/collect.js:62` 가 `await fn()` 이라 async 도 그대로 받는다. 내 전수 스윕(`export (?:async )?function \w*[Ss]tatus\w*`)에서 index.js 시작 모듈 중 미등재가 이 셋 외에도 있다(`security/loginMonitor.js`·`reports/dailyReport.js`·`vmclone/runner.js`·`ping/service.js` 등 — 중앙 전용/엣지 무관 여부는 이번에 가르지 않았다). 규약: v2.574 IMP-06 "`length >= 20` 검사는 추가를 잊은 것을 절대 잡지 못한다" | 스윕 정규식을 `export (?:async )?function` 으로 + 대상 선정을 파일명이 아니라 **`index.js` 의 `start*` import 원천 모듈**로(시작되는 모듈이 곧 대상, `EXCLUDED` 는 사유와 함께). spec 에 `collect.vcLogs`(logStatus)·`collect.capacity`(capacitySamplerStatus)·`collect.certs`(certStatus) 등재 — `logStatus()` 의 `dbPath` 는 `redactDeep`/`scopeFilePaths` 대상인지 확인 | **가능** — 런타임에 `index.js` 가 시작한 폴러 모듈 목록과 `edgelog/spec.js mod` 집합을 대조 |
| CONTRACT2613-07 | **REFUTED** | info | 발견 자체가 "실제 결함은 아니다" 라고 적었다. 재확인: 세 지도 라우트는 `memoJson` TTL 12초(`dataFlow.js:63`·`commMap.js:93`·`deviceFlow.js:30`) · 폴링 15초 — v2.577 `/top` 규약 그대로 준수. `/tools/rma`(등록부 인메모리)·`/tools/service-check`(캐시 반환)·`/ping/status`(latest) 는 무거운 집계가 아니다. 'TTL ≤ 폴링' 은 CLAUDE.md 에 **없는 규약**(v2.508 규약은 '무거운 API 15초 금지' 이고 `v4Portal2508` 이 그것만 고정한다 — 의도된 범위). 부수: 발견의 파일 경로 `views/tools/PingMonitor.jsx`·`DavinciChecks.jsx` 는 **존재하지 않는다**(실제는 `web/src/views/`) | 수정 불필요. 원하면 `v4Portal2508` 의 폴링 스윕을 `views/**/*.jsx` 로 넓혀 '`/tools/capacity-forecast`·`orphan-vmdk` 스캔' 금지 목록만 재사용(새 불변조건을 만들지 않는다) | 아니오(웹 폴링 주기는 번들 밖) |
| CONTRACT2613-08 | **REFUTED** | info | **"넘으면 무음" 이 틀렸다**: `gpuGuestPush.js:90` `if (!res.ok) throw new Error('gpu-guest -> 413')` → `:98 console.warn('[gpu-guest-push] 실패: …')` + `last.error` — 로그·상태에 남는다. 크기: 레코드 `{vmId,utilPct,utilNA,memUsedPct,host,vcenterId}` ≈ 150B → 1MB 는 **GPU 게스트 VM 약 7,000대/엣지**(현장 전체 VM 5,850). `/result`(`scanner.js:65-74`)는 `unreachable`·`notIdrac` 이 **개수**(`idrac/scan.js:38`)라 본문은 `found[]` 뿐이고, 413 은 `postError` 로 상태·콘솔에 남는다(v2.593 EDGE-2). 등록 18경로 전부 근거 주석 있음(발견도 인정). '허용 목록 + 스윕 테스트' 는 규약이 아니라 제안 | 수정 불필요. (선택) `index.js` BIG_JSON 목록 옆에 '작아서 등록하지 않음' 주석 1줄 — 다음 감사가 같은 후보를 다시 세지 않게 | 가능하나 불필요(런타임 `app._router.stack` 의 bigJsonGate 경로 ↔ central POST 대조) |
| CONTRACT2613-09 | **REFUTED** | info | 서버가 `FAIL_KINDS` **라벨 맵 자체를 같은 응답에 싣고**(`linkCheck.js:124 failKinds: FAIL_KINDS`) 웹이 그것으로 그린다(`LinkCheck.jsx:370 data?.failKinds?.[e.fail_kind]?.label || e.fail_kind`) — 코드와 문구가 **한 응답에 같이 오므로 드리프트가 구조적으로 불가능**하다(1:1 테스트가 필요한 것은 두 곳이 따로 들 때다). '원시 failKind 노출' 은 단계 추적 `trailFromLatest` 의 `error` 가 **`title` 툴팁**(`LinkCheck.jsx:57` `— ${s.error}`)에만 들어가는 것 — 본문 셀이 아니다. v2.553 '문장은 웹' 과의 소유자 비대칭은 패턴 기록이지 결함이 아니다(v2.552 가 먼저였고 v2.553 이 자기 모듈 규약으로 적었다) | (선택) `trailFromLatest(latest, phases, failKinds)` 로 `data.failKinds[failKind].label` 을 툴팁에 쓰기 — 1줄 | 아니오 |
| CONTRACT2613-10 | **PLAUSIBLE** | low | 재추출: 웹 `bmUsageText.js` 의 kebab 리터럴 26종 중 서버 사유 코드로 쓰는 12종(`no-idrac`·`no-os-cred`·`no-idrac-cred`·`no-telemetry`·`empty-report`·`ids-unmatched`·`no-registry-match`·`idrac-ent`·`no-db`·`no-value`·`assign-ghost`·`registry-no-vc`…)은 **전부 서버 소스에 존재**한다. 서버에만 있는 것(`telemetry-ok`·`no-signal`·`no-threshold`·`stale-drop`·`recover`)은 알림 엔진 내부 상태라 화면 코드가 아니다. 웹에만 있는 것(`no-corp`·`no-target`·`off-central`·`off-edge`·`stale-failed`·`both-off`·`not-eligible`)은 **웹이 만드는 상태**다(`:93`·`:105`). 즉 **현재 드리프트 0** — 남는 것은 'parity 테스트가 없다' 뿐(`grep -rl bmUsageText server/test` 0건 재확인). 폴백 `:532 || kind` 는 대체 경로 실패 원문이 없을 때만 | `bmusage/reasons.js` 에 서버 사유 코드를 모으고(5개 모듈의 리터럴을 import 로 바꾼다) `bmUsage2550` 계열 테스트에 웹 문구 키 대조 1건. 화면 변화 0 | 01 과 같은 방식(서버 enum 노출 뒤 브라우저 측 대조) — 지금은 소스 스윕만 가능 |

## 중복·연관
- 01 ↔ 02: 02 는 01 의 '웹 맵 누락' 으로 제시됐지만 도달 불가라 **독립 결함이 아니다**(01 리팩터 시 공용 맵으로 자연 해소). 01 ↔ 10 ↔ 09: 같은 'code↔text 계약' 주제이나 10 은 드리프트 없음, 09 는 구조상 드리프트 불가.
- 04 ↔ 05: 둘 다 '엣지 최소 버전' 계약의 소유자 문제. 04 는 게이트 자체가 없고(medium), 05 는 웹만 든다(low). 같은 수정 회차에 묶으면 좋다.
- 03 ↔ CLAUDE.md v2.600 '남긴 것(엣지 push 게스트 디스크의 partsUnknown 중앙 합산)' — 03 의 파티션 축은 그 항목과 겹친다.

## 정직 기록
- 목 서버·Chromium 은 띄우지 않았다 — 02·04·09 의 '화면에 무엇이 보이는가' 는 JSX·문구 모듈 코드 판독이다(02 는 렌더 코드 부재로 판정 — `results[]` 를 그리는 코드가 정말 0건인지 `PortalCheck.jsx` 전수 grep 으로 확인했다).
- 01 의 '세 답' 은 `capStr`·`strOf`·`String` 을 node 로 실행해 확인했고, `kindFor404` 자체는 비export 라 코드 판독으로 조합했다.
- 06 의 '추가 미등재 후보'(loginMonitor·dailyReport 등)는 중앙 전용/엣지 무관 여부를 가르지 않았다 — 수정 회차에서 `EXCLUDED` 사유와 함께 결정할 것.
- 08 의 `/result` 는 `found[]` 레코드 크기를 재지 않았다(iDRAC 1대 레코드 ≈ 수백 B, 엣지당 수천 대는 현장 규모(1,135대/16법인) 밖이라 판정).
- 발견 스크립트 `parity.mjs` 는 11번째 항목(`web/src/views` 디렉터리 readFileSync)에서 EISDIR 로 죽는다 — 앞 10개 대조는 정상 출력. 그 스크립트의 `lits()` 는 `kind: '…'` 리터럴만 잡아 `kind = cond ? 'timeout' : 'unreachable'` 을 놓친다(그래서 발견의 'web-only timeout·unreachable' 은 추출 결함이고 실제 웹 맵은 맞다 — 02 판정과 일관).


## D 웹 공용 모듈·화면 규약 — 검증 원문

# WEB 축 적대적 검증 — arch2613 · `web.md` 16건

- 기준 `8ece9d9`(working tree clean) · 읽기 전용 · 전량 테스트·빌드·Chromium 없음 · 목 서버 미기동
- 방법: 발견 보고의 근거를 **전부 다시 그렸다** — grep 재실행, 발견 축의 `fmt.mjs`·`scan.mjs` 재실행, 서버 라우트 게이트 대조,
  `web/dist` 청크 문자열 판독, 콘솔/V4 페이지 공통 줄 재측정. 판정 기본값은 REFUTED(불확실하면 기각).
- 요약: CONFIRMED 12 · SPLIT 3(02·09·15) · PLAUSIBLE 1(05) · REFUTED 0(단 02 의 핵심 시나리오는 반증). 심각도는 검증 뒤
  **medium 8건 → medium 0 · low 12 · info 4** 로 낮췄다 — 이 축의 발견은 전부 '규약·구조' 결함이고 **틀린 값을 사용자에게
  보여주는 도달 가능한 경로는 한 건도 확인되지 않았다**(03 의 `0 B` 는 현재 호출부가 전부 서버 계산값·null 가드라 잠재).

## 판정표

| ID | 판정 | 심각도(검증 후) | 근거 한 줄 | 권장 최소 수정 | 자동 점검 |
|---|---|---|---|---|---|
| WEB2613-01 | CONFIRMED | low(↓medium) | `grep "fetchJson('/auth/me')" views/` = **10건** 재현. `api.js:29-32 setCurrentUser/getCurrentUser/can` 실재, `App.jsx:151` 이 모든 뷰 렌더 **전에** 채우고 `/auth/me`·로그인 응답(`routes/auth.js:124,152`)이 `role`·`serviceHubUrl`·`isSettingsOwner` 를 전부 담으므로 재조회로만 얻는 값은 없다. 그러나 결과는 **버튼 노출**뿐이고 서버가 집행하므로 틀린 데이터·권한 우회 없음 → low. 방향 불일치(`CvpTool.jsx:49 !u \|\| admin`)도 실재. | 10곳을 `getCurrentUser()?.role` 로 바꾸고 `views/**` 에 `fetchJson('/auth/me')` 0건 스윕(허용 `App.jsx`·`api.js`). SpecialTools 의 `serviceHubUrl` 도 같은 객체에서 읽는다. | 아니오 — 웹 소스 필요(런타임 `/api/auth/me` 호출 빈도는 셀 수 있으나 판정이 아니다) |
| WEB2613-02 | **SPLIT** | low(↓medium) | **핵심 시나리오(operator 가 전 서버 '미지정') 반증**: `/admin/datacenters` 를 삼키는 세 컴포넌트가 전부 **같은 adminOnly 데이터를 catch 없이** 먼저 받는다 — `ServerInfoByVcenter` 는 `Promise.all([fetchJson('/admin/idrac') …])`(`idracCore.js:84 adminOnly`), `HardwareSummary` 는 `/admin/idrac/hardware-summary`(`idracCore.js:148 adminOnly`), `ServerAnalysis` 탭도 `/admin/idrac/unsupported`. operator 는 403 이 `setErr` → `ErrorBox`(AccessDenied)로 떨어져 `dcOf()`·`'__unmapped__'` 그룹핑에 **도달하지 못한다**(`toolAccess.js:287` 도 이 도구를 `/api/admin/idrac/* · /api/admin/datacenters(adminOnly)` 로 선언). `Hardware/Esxi/Hba/VcVersion` 은 `/tools/*` 라 `dcs` 를 쓰지 않는다. **남는 것**: admin 세션에서 `/admin/datacenters` 만 일시 실패하면 `.catch(() => ({assign:{}}))` 이 '미지정' 으로 칠한다(v2.590 W3 문구 규약 위반이지만 권한 시나리오가 아니라 일시 오류 시나리오). | `HardwareTools.jsx:205,516` 의 catch 를 `DatastoreUsage.jsx:85-158` 방식(`dcErr` 상태 + '법인 정보를 읽지 못함' 라벨)으로. 우선순위 낮음. | 아니오(권한 시나리오가 도달 불가라 거부 기록 휴리스틱은 무의미) |
| WEB2613-03 | CONFIRMED | low(↓medium) | `fmt.mjs` 재실행으로 **실행 재현**: `util/fmt.fmtBytes(null)="0 B"`(`'abc'` throw) · `vmSeriesText.fmtBytes(null)="0 B"` · `sanSwitchPerfText.bytesText(null)="0 B"` · `fmtAgo(ISO)="NaN시간 전"`. 정정 2건: ① `MetricsSettings.jsx:240` 은 `util/fmt` 가 아니라 **로컬 사본**(`:212 if (!b) return '0'`) — 사본은 9벌이 아니라 **10벌**이고 `util/fmt.fmtBytes` 의 실제 import 는 `PortalBackup`·`DavinciChecks` 뿐 ② **null 도달 경로를 확인하지 못했다** — `VmSeriesSettings:191` 의 `totalBytes` 는 `routes/api/vmSeries.js:98 usage.reduce(…)`(항상 숫자), `freeBytes`·`db.fileBytes`·`gzBytes` 는 `!= null`/오류 분기로 가드, `est.*` 는 순수 계산. 즉 **잠재 결함**(규약 위반은 확정, 화면 거짓은 현재 미도달). | `util/fmt.fmtBytes` 를 `numOrNull` 기반(null→'—')으로 고치고 `vmSeriesText`·`sanSwitchPerfText`·`MetricsSettings` 로컬을 그 함수로(재수출은 `import…; export{…}`). vitest 하나가 남는 벌을 같은 입력으로 대조. `fmtAgo` 는 `relTime.js` 로. | 아니오 — 웹 소스 필요 |
| WEB2613-04 | CONFIRMED | low(↓medium) | `PortalCheck.jsx:548 useState('tokens')`·`:562` 스위치, `LinkCheck.jsx:71 useState('settings')` 재현. `useHashTab`(v2.438, "useState 를 이 훅으로 바꾸기만 하면 된다") 사용 화면 **23곳**, 같은 파일군의 `ServerAnalysis` 도 `base:['tools','serveranalysis']` 로 쓴다. `toolFromHash` 는 `parts[1]` 만 보므로 `#/tools/portal-check/inventory` 가 도구 매칭을 깨지 않는다(적용 안전). 다만 딥링크 부재는 UX 결함이지 데이터 정직성 결함이 아니고, `Collectors.jsx:605` 의 유일한 외부 진입은 기본 탭(tokens)이라 지금은 동작한다 → low. v2.614 세 번째 서브메뉴 전에 고치면 좋다. | `useHashTab({ base:['tools','portal-check'], valid: VIEWS.map(([k])=>k), fallback:'tokens' })`; LinkCheck 동일. | 아니오 |
| WEB2613-05 | PLAUSIBLE | low(↓medium) | awk 스윕 재현: `views/tools` **15곳** 일치 + `views/` 4곳 더(`BulkDeploy` ×2·`CollectorSync`·`GpuGuestSettings`). `styles.css:276 table{width:100%}` 이고 BmUsage·`.v3-table` 셀에 `white-space:nowrap` 없음 → 짜부라짐이 **구조적으로 가능**. 단 ① `HardwareTools.jsx:482` 는 `whiteSpace:'nowrap'` 이라 해당 없음 ② PortalCheck 2곳(4열·3열)·LinkCheck(4열)은 좁은 표라 실익 낮음(PortalCheck 의 주 표 5곳은 이미 `minWidth`) ③ Chromium 400px 로 재지 않았다. 실질 대상은 **BmUsage ×5(지표 열 8+)**·PartFaults·EdgeLog ×2·SettingsCheckPanel ×2·RelayTopoTool. | 열 6개 이상인 표만 `<STable minWidth={N}>` 로(래퍼 제거). 스윕 테스트는 '래퍼 다음 3줄 안 `<STable` 에 minWidth 없음' + 열 수 조건. | 아니오 |
| WEB2613-06 | CONFIRMED | low(↓medium) · **결정 사항** | 공통 고유 줄 재측정(공백 정규화·기호 줄 제외): Overview 42 · Compute 39/88 · Storage 36 · Network 44/107 · Facility 28 · Alarms 33/94 — 보고값(48·44·42·50·34·39)과 같은 차수. `audit2506.test.js:64` 가 두 셸을 하드코딩. **INTENT**: CLAUDE.md v2.579 정직 기록이 "console/ 은 consoleData.js 재수출 관계라 **중복이 아니고 위치만 어색하다 — 손대지 않았다**" 고 적어 **알려지고 수용된 상태**다(규약 '셸을 더 만들지 말 것' 은 신규 셸 금지이지 기존 콘솔 제거 지시가 아니다). 그 기록은 데이터 코어에 대해서는 맞고 **페이지 JSX 에 대해서는 30~45% 중복이 실재**하므로 문구 자체는 정정 대상. | 결정 항목으로 올린다(① `#/console` → V4 리다이렉트 + `console/pages` 삭제 ② 콘솔 페이지를 V4 래퍼로). 당장은 CLAUDE.md v2.579 문장을 '데이터 코어는 공유, 페이지 JSX 는 두 벌' 로 정정. | 부분 — `web/dist/assets/DvcConsole-*.js`·`V4App-*.js` 공존은 런타임 확인 가능, 중복 판정은 웹 소스 |
| WEB2613-07 | CONFIRMED | info(↓medium) | 재측정 일치: StorageMonTool **1,804/37** · SanSwitchTool **1,441/45** · HardwareTools 896/38(export 5) · RemoteCommand 629/36 · IpamSettings 506/37. `web/eslint.config.js` 에 `max-lines`·`react-hooks`·`complexity` 0건. **INTENT**: CLAUDE.md 에 파일 길이·훅 수 규칙이 **없다** — 규약 위반이 아니라 유지보수 관찰. `useState>8` 파일은 보고 26 이 아니라 `views/**` 기준 **70**(집계 단위 차이). | 규칙을 만들지 여부부터 결정(`max-lines` 800 경고 · 신규 파일부터 오류). StorageMonTool 분리는 별건. | 아니오 |
| WEB2613-08 | CONFIRMED | low(↓medium) | `SpecialTools.jsx:560 export { IpamStandalone } from './tools/IpamCore.jsx'`(정적) · `App.jsx:21` 이 허브 모듈 경유 lazy · `IpamCore.jsx:9-10` 이 IpamNet·IpamSettings 정적 import 재현. dist: `SpecialTools-*.js` **145,655B / gz 40,553B**, 별도 IPAM 청크 **없음**, 청크 안에 IPAM 고유 문자열(`IP 스캔` 6 · `vc-ranges` 11 · `ipam/policies` 5) — 정황이 아니라 **확정**. `Settings-*.js` 295,883B/gz 78,827B(v2.580 기록 250KB/74.7KB gz → gz 기준 **+5.5%**, 정적 import 30·lazy 13). `App.jsx:19-20` 주석 "Ipam 본체가 그 파일의 헬퍼들과 얽힘" 은 v2.282 분리 이후 **낡은 근거**. 영향은 gz 약 40KB 의 이중 로드(성능)이지 정확성이 아니다. | `App.jsx:21` 을 `import('./views/tools/IpamCore.jsx')` 로, `SpecialTools.jsx:560` 삭제(`Summary.jsx` 의 `GuestOsVmsModal` 재수출도 같은 검토). Settings 재성장은 5% 라 별건. | **가능** — 서버가 `web/dist`(`index.js:412 express.static(config.webDist)`) 를 서빙하므로 자체 점검이 `assets/*.js` 의 청크명·크기(gz)를 읽어 상한·증가율을 보고할 수 있다 |
| WEB2613-09 | **SPLIT** | low | 정정: `views/tools` 의 `setInterval(` 폴러는 4곳이 아니라 **14곳**(VmClone·IpamSettings ×2·BmStorage·StorageTrack·HardwareTools:687·IpamNet·SanSwitch·Shutdown·RemoteCommand·Pdu·StorageMon·DirUsage·RelayCheck) — 즉 '예외' 가 아니라 **통용 패턴**이다. `IpamSettings` 2곳은 이미 v2.611 `deniedRef` 로 403 정지가 있어 대상이 아니다. **확정 부분**: `RemoteCommand.jsx:39` 는 `/tools/rma/history` 가 `rma.js:155 adminOnly, fullScopeOnly` 이고 SpecialTools 인라인 isAdmin 게이트(7키)에 `rma` 가 없어 operator 가 주소로 열면 **15초마다 403 + `.catch(()=>{})`** — 실제 감사·거부 기록 오염. `usePolling` 의 403 정지(`api.js:~506`)·ETag 는 실재. | RemoteCommand·PduTool 을 `usePolling` 으로. 14곳 전체는 별건(허용 목록 스윕은 사유와 함께). | 가능(휴리스틱) — 거부 기록에서 같은 토큰·같은 경로 403 의 고정 간격 반복 |
| WEB2613-10 | CONFIRMED | low | `grep -E "\bfetch\("` views = **7건** 재현(GpuTool:93,372 · IpamSettings:92 · IpamNet:48,358 · PortalBackup:44 · DavinciChecks:111), 전부 수동 `Authorization`. `delJson(292)`·`downloadFile(302)`·`postDownload(328)` 실재. 추가 발견: `IpamNet.jsx:48` DELETE 는 `res.ok` 를 보지 않아 **403·409 가 조용히 성공처럼**(바로 `load()`) — 무음 실패 1건. GpuTool 은 `saveResponseAsFile` 이 ok 를 본다. | `delJson`/`downloadFile` 로 교체, `views/**` `\bfetch(` 0건 스윕. | 가능(휴리스틱, 계측 추가 필요) — `perf/requestId.js` 는 클라이언트 id(`w…`)와 서버 생성(`s…`)을 접두로 구분하므로 경로별 '클라이언트 id 없음' 을 세면 우회 호출이 드러난다(지금은 그 통계를 남기지 않는다) |
| WEB2613-11 | CONFIRMED | low | (a) 훅-조기return 스윕: `web/src`·`server/test` 에 0건 확인(#310 을 언급하는 서버 테스트 3개는 전부 다른 뜻의 '조기 return') · 발견 축 휴리스틱은 진양성 0. (b) `audit2575.test.js:116-138` 은 `styles.css`·`v4.css`·`console.css` 선택자만 검사; JSX 인라인 `textTransform:'uppercase'` **6곳** 재현 — 그러나 `MonoLabel` 호출 3곳(`ServerTempBoard:289,316`·`parts.jsx:95`)은 전부 **고정 한글 문구**이고 `ServerTempBoard:268 'Sampling'`·`StorageGrowthTool:117` 은 장식 라벨이라 **현재 새는 값 없음**(스윕 공백만 실재). (c) `uiText.test.js:64-67` 은 `web/src` 만 순회; 서버 문구 백틱 검사는 기능별 5파일 확인. (d) = 05 중복. | (b) `audit2575` 에 JSX 인라인 스윕(허용 About·장식 라벨 사유) (c) 서버 '웹 문구 모듈' 허용 목록 한 테스트 (a) 는 진양성 0 이라 후순위. | (c) 가능(서버가 자기 소스를 갖는다) · (a)(b) 아니오 · (d) 05 참조 |
| WEB2613-12 | CONFIRMED | low | `sensorText.js:90 c >= 40 … c >= 32` 리터럴, `board.js:17-18` 상수 소유, `shared.jsx:56-58` 은 import, `IdracDetailModal.jsx:273` 이 사본 사용, `sensorText.test.js:73-77` 기대값도 리터럴 — 전부 재현. `web/src` 의 `>= 40 ?`·`>= 32 ?` 리터럴은 이 한 곳뿐(`HardwareTools:884` 의 32 는 Gbps). `board.js` 는 import 0 인 순수 모듈이라 `sensorText.js` 가 import 해도 v2.556 '판정 모듈 의존 0' 규약과 충돌하지 않는다. | `sensorText.js` 가 `TEMP_WARN_C/TEMP_HOT_C` import, 테스트 기대값도 상수. | 아니오 |
| WEB2613-13 | CONFIRMED | low | `tool === '` 분기 **86**(보고 87 — 1 차이는 `:466-469` 집계 방식) · `React.lazy(` 86 · 카탈로그 91 · `adminOnly:true` 28 · 인라인 `isAdmin` 게이트 **7키**(capacity-advisor·dir-usage·mail-diag·vmprovision·agent-scans·login-fails·net-issues) 재현. 짝 고정 테스트 없음 확인 — `v4Portal2508.test.js:36` 은 카탈로그만, `tokenCheck2560.test.js:470` 은 `portal-check` 한 줄. 7키만 다르게 다루는 사유가 코드에 없는 것도 사실(v2.555 '`adminOnly` 는 표시 관례' 와 정합). | `TOOL_VIEWS` 맵 + `every(k in TOOL_VIEWS)` 테스트. 리팩터 없이도 카탈로그 키 ↔ `tool === '<k>'` 문자열 대조 테스트는 바로 쓸 수 있다. | 리팩터 뒤 가능(번들 내 데이터 대조) · 지금은 아니오 |
| WEB2613-14 | CONFIRMED | info | `scan.mjs` 재실행 = **19/70**. 독립 교차검사(어떤 `*.test.*` 도 그 JSX 이름을 언급하지 않음)는 30/68 — 차이는 같은 이름의 순수 모듈 테스트 유무 판정 기준. 방향은 같다(하한 19). | 새 화면 규약(`*Text.js` 분리) 소급은 별건·후순위. | 아니오 |
| WEB2613-15 | **SPLIT** | info | 확정: `Kpi` 정의 3곳(`primitives.jsx:41`·`version_4/ui.jsx:22`·`Insights.jsx:15`). 정정 2건: ① `blankOr/blankKeep` 사용처는 22파일이고 그중 **`views/tools` 4파일**(HorizonSessionSettings·CurrentUsersSettings·MailDiag·RelayCheckTool) — "도구 파일 21개 중 0" 은 **틀렸다**(17/21) ② `fmtAgo` 류 **독립 구현은 4벌이 아니라 9벌**(`util/fmt`·MetricsSettings:18·CapacityAdvisor:51·gpu-guest/shared:6·EdgeUserDeploy:10·DavinciChecks:10·GpuGuestDiag:9·cvpText:20·consoleData:50). 서버 clamp 미확인 2경로는 검증하지 않았다(info). | 정보 — 03 과 함께 `relTime.js`/`numOrNull` 통합 시 같이. | 아니오 |
| WEB2613-16 | CONFIRMED | info | `location.hash =` 직접 쓰기는 9곳이 아니라 **13곳**(VCenters:54·V4Portal:16·DvcConsole:48 누락). 그러나 `App.jsx:200` 이 `movedTabHash` 를 **hashchange 마다** 적용하므로 옛 주소로 직접 써도 리다이렉트된다 — "호출부가 각자 알아야 한다" 는 과장. 헬퍼 부재는 사실. | `goTool(k, sub)` 헬퍼는 있으면 좋고 없어도 동작한다 — 후순위. | 아니오 |

## 중복·연관

- 11(d) ≡ 05. 15 의 시각 헬퍼 ≡ 03 의 `fmtAgo`. 01·02 는 같은 '`.catch(()=>{})` 기본값 칠하기' 계열이지만 02 는 권한 시나리오가 도달 불가라 별개로 남겼다.
- 04 는 v2.614(포탈 점검 세 번째 서브메뉴) 착수 전에 처리하는 것이 순서상 맞다 — 새 서브메뉴가 같은 useState 셸 위에 올라간다.

## 정직 기록 — 하지 않은 것

- Chromium 을 띄우지 않았다 — 05 의 400px 짜부라짐·02 의 admin 일시 실패 화면은 소스 판독이다.
- 03 의 `null` 도달 경로는 **현재 호출부**만 봤다(6곳). 서버가 앞으로 null 을 내는 순간 잠재가 실제가 된다.
- 15 의 서버 clamp 미확인 2경로(`/admin/ipam/scan/settings`·PDU 임계)는 검증 범위에서 뺐다(info).
- `web/dist` 는 HEAD 커밋보다 18분 앞선 빌드(07:56Z vs 커밋 17:14 KST=08:14Z) — 08 의 판정은 소스 + 청크 문자열이라 빌드 시차와 무관하다.


## E 저장소·등록부·설정 — 검증 원문

# v2.613 아키텍처 점검 — 축 E(PERSISTENCE) 적대적 검증

기준: `8ece9d9`(v2.612.0 머지). 읽기 전용 · 발견 스크립트는 없어(persist/ 에 `start` 만) **증거를 전부 직접 다시 냈다**
(node one-liner · grep · 스크래치 CONFIG_DIR 재현). 작성 2026-09-25 (KST 18:1x).

## 판정표

| ID | 판정 | 심각도(검증 후) | 근거 한 줄 | 권장 최소 수정 | 자동 점검 |
|---|---|---|---|---|---|
| PERSIST2613-01 | **CONFIRMED** | high → **medium** | 실행: `isRuntimeStateFile('central-agent-cvp.json')===false`·`('cvp-push.json')===false`(형제 `central-agent-storage`·`sanswitch-perf-push`·`central-agent-sanswitch-perf` 는 true). `cvp/push.js:169` 는 표본이 있는 push 마다 `saveCursor`(기본 `pushMs` 5분, `{lastRowid, at}`), `central/cvpEdge.js:178-179` 는 push 마다 `{at: now}` 로 `writer.save()`(atomicWrite → rename → fs.watch 발화). 둘 다 `settingsFingerprint` 에 포함되므로 `unchanged` 생략(service.js:162)에 걸리지 않는다 → CVP 를 켠 현장에서 중앙은 push 마다 `change` 백업. **추가(보고서에 없음)**: 엣지 `agent/configPush.js configWatchRelevant` 가 같은 판정을 쓰고 `_pushConfigNow({onlyIfChanged})` 지문도 `cvp-push.json` 을 포함해 **엣지 설정 push 가 30분 → 5분마다**(v2.602 EDGE2602-01 재발). 심각도를 낮춘 이유: v2.590 이후 `AUTO_REASON_KEEP`(10) 이 정기·수동을 보호하므로 실해는 '변경 백업의 무용화(≈50분 안에 밀림)' + 엣지 설정 push 트래픽이고, CVP 는 기본 꺼짐(v2.608). | `RUNTIME_STATE_NAMES` 에 `'central-agent-cvp.json'`·`'cvp-push.json'` 추가 + `audit2590`·`audit2602f` 표본에 포함. 구조는 02. | **가능(런타임)** — ① `listBackups()` 의 reason==='change' 생성 간격 분포(최근 24h) — 중앙값이 push 주기(5분·60초)와 같으면 경보 ② CONFIG_DIR `.json` mtime 을 두 시점(1h) stat 해 바뀐 파일 ∧ `isRuntimeStateFile()===false` → '상태 파일 오분류 후보'. 소스: `createDebouncedWriter(`·`CURSOR_FILE` 의 파일명 리터럴 ∖ RUNTIME_STATE_NAMES. |
| PERSIST2613-02 | **CONFIRMED** | medium(구조 — 단독 런타임 영향 없음) | 재실행: CONFIG-FILES.md 이름 **180**(보고서 168) 중 `portalDb PURPOSES` 44키가 덮는 것 **42**(보고서 44) — 결론 동일. MIGRATABLE 19 중 **13** 이 PURPOSES 에 없어 `purposeOf()` 폴백 'SQLite 데이터베이스' 로 표시(용도 없음). `config-doc.mjs:128 kind()` 는 `.json` 전부 '설정' — CONFIG-FILES.md:46 은 `central-agent-cvp.json` 을 **'설정'** 이라 적으면서 설명은 "수집 **상태** 보관" 이라 한 줄 안에서 모순. 이름 목록을 손으로 더한 이력이 v2.590·595·596·601·602 다섯 릴리스 → 01 은 여섯 번째. 01 의 근본 원인이지 중복은 아님. | 보고서 제안(카탈로그 + 헬퍼 자동 등록) 타당. 최소: `config-doc.mjs kind()` 가 `isRuntimeStateFile`/MIXED 를 import 해 '상태'·'혼합' 을 적고, portalDb PURPOSES 에 MIGRATABLE 13개 추가. | **부분(런타임)** — readdir(CONFIG_DIR) ∖ (PURPOSES ∪ MIGRATABLE ∪ RUNTIME_STATE ∪ MIXED ∪ SECRET_FILES) 개수는 낼 수 있으나 정상 설정 파일도 섞여 **카탈로그 도입 전에는 결함 판정 불가**(KPI 정보용). |
| PERSIST2613-03 | **SPLIT** | low → **info(알려진 잔여)** | 사본 사실은 확인: `storage/db.js:85` 가 `isSqliteLockError` 를 **자체 export**(sqliteOpen 과 같은 정규식), `metrics/db.js:272-282` 자체 `LOCK_RE`+`initSqliteRetrying`(=`retryOnLock` 사본), `horizon/sessionDb.js:103-105` `lockError/retryAt/LOCK_RETRY_MS` 사본(isSqliteLockError 만 import). `openSqlite(`·`createLockRetry(` 호출 0. **INTENT**: CLAUDE.md v2.599 DB2599-02 가 "폴링 경로 15종 적용, **미적용 7종(점검 시에만·ipam 외부 공유·v2.597 기적용)** 은 그룹 e 보고에 근거" 로 이미 기록 → 새 발견이 아니라 알려진 잔여(그룹 e 원문은 저장소에 없어 7종 명단은 확인 못 함). 'metrics 규칙 2 위반' 은 실효 미미: journal_mode 의 잠금이 삼켜져도 바로 뒤 CREATE 가 같은 잠금으로 던져 `initSqliteRetrying` 이 받는다(잔여: 두 exec 사이에 잠금이 풀리면 그 프로세스만 비-WAL — 성능 저하뿐). `audit2611c:403` 이 이 셋을 이름으로 박은 것은 사실. | 세 모듈을 `openSqlite` + `createLockRetry`/`retryOnLock` 으로 교체하고 `audit2611c` 이름 목록 제거(리팩터, 동작 변경 0). | **가능(소스)** — 배포 `server/src` 정규식 `new DatabaseSync(` ∧ ¬`openSqlite(`. 런타임 PRAGMA 대조는 핸들 미노출 → 불가. |
| PERSIST2613-04 | **CONFIRMED**(PLAUSIBLE 부분 → 실행으로 확정) | low | 5개 `load()` 본문 대조: cvp 만 최상위 형식 + 원소 `s && typeof s==='object'` 필터, sanswitch/storage 는 `Array.isArray` 만 + `preserveCorrupt(FILE)` 에 사유 없음, pdu 는 `openSecretsDeep(배열)`, bmstor 는 원소 변형. **스크래치 CONFIG_DIR 재현**(원소 하나 `null`): bmstor → `bm-storage.json.corrupt.<ts>` 로 **파일 전체 보존 + 목록 0**(정상 서버 'a' 소실) / sanswitch·storage → `listDevices()` 가 **매 호출 TypeError**(`Cannot destructure property 'password' of 'object null'`) / cvp 는 필터. 도달 경로는 손편집·손상 파일뿐(save 경로는 null 을 쓰지 않는다) → low 유지. CLAUDE.md '코어는 하나다'(v2.513) 의 등록부 판이라는 해석은 타당. | 최소: sanswitch·storage·pdu·bmstor `load()` 에 cvp 와 같은 원소 필터(`filter(s => s && typeof s==='object')`) + `preserveCorrupt(FILE, e.message)`. 구조(공용 `createSecretRegistry`)는 별건. | **부분(런타임)** — 등록부 파일을 readdir+JSON.parse(봉인은 열지 않음)해 목록 원소 중 비객체 개수 → '손상 원소 N'. 사본 수 자체는 소스 정규식으로 표시만(판정 불가). |
| PERSIST2613-05 | **CONFIRMED**(사실) | low → **info** | grep `MAX_AGENTS\s*=`: 값 64(svcmonEdge, env)·128(edgeRecord env, svcmonAssign)·200(edgeLogStore env, edgeLogJobs, sanSwitchPerfEdge, **storage/intervals.js:143** — 보고서 누락)·256(gpuGuestDiag, idracScanJobs)·500(fleet env, ingestStats, pullStats, ingestReject env)·없음(linkCheckEdge·tokenCheckPull·bmUsageEdgePull — `new Map()` 무상한, 개별 토큰·등록부 이름으로만 유계). env 이름 5종 확인(+`CENTRAL_FLEET_MAX_UNVERIFIED_AGENTS`). **INTENT**: '상한 상수 하나' 규약은 CLAUDE.md 에 없고(v2.605 CEN2605-02 는 admitAgent **사용**을 요구), 최저 64 도 현장 28(30+)보다 크다 → 운영 결함 아님, 통일 제안. | 제안대로 `EDGE_MAX_AGENTS` 기준 파생은 선택 사항; 최소는 상한 없는 3곳에 '등록부/개별 토큰 한정' 주석 + 테스트 선언. | **가능** — 소스 정규식으로 값·env 종류 수; 런타임은 `*Status().agentCap` 이 있어야(현재 edgeLogStore 만). |
| PERSIST2613-06 | **SPLIT** | low | (a) `partfault/db.js:60` `Math.max(30, Number(env)||730)` — env 전용·설정 없음(`partfault/settings.js`·`routes/api/partFaults.js` retention 0건, 화면은 `db.retentionDays` 표시만) **확인**. 단 "partfault **만**" 은 과장 — `vmtrack/db.js:551` 도 `Number(env)||1095` 같은 꼴(축 범위 v2.547 이후 밖이라 보고서가 못 봤을 뿐). 형제 bmusage/linkcheck/cvp/curuser/horizon 은 설정 파일 + clampInt **확인**. (b) `PARTFAULT_RETENTION_DAYS=0` → 730 은 `config.js:35-38 retentionEnv` 의 '0 = 전부 보관' 계약(v2.583) 및 CLAUDE.md v2.602 'VMPERF_RETENTION_DAYS=0 = 무제한' 과 불일치 **확인**. 다만 ENV.md:142 는 이 키에 keep-all 을 약속하지 않고 `Math.max(30,…)` 하한은 의도로 읽힌다 → 사용자 약속 위반이 아니라 **계약 불일치**(low). | (b) 최소: `retentionEnv` 경유 + 0 은 무제한(prune 생략), 음수·NaN 은 기본값 — vmtrack 도 같이. (a) 설정 파일 노출은 기능 결정(사용자 판단). | **부분** — 런타임 `process.env.*_RETENTION_DAYS` 값 × 모듈 status 의 `retentionDays`(partfault 는 이미 실림) 대조 → '0 인데 730 으로 보고' 를 잡을 수 있다; 출처(settings/env/default)는 status 에 실어야 보인다. |
| PERSIST2613-07 | **PLAUSIBLE**(유지) | low → **info** | `createActivityLog(` 사용 6곳(storage·sanswitch 2·bmusage·horizon·curuser) **확인**, `cvp/`·`routes/api/cvp.js`·`CvpTool.jsx` 에 0건 **확인**. `cvp/db.js` 표는 device_latest·port_latest·port_sample·port_daily 뿐(실패 사유·이력 열 없음) → 실패 이력은 `cvpPollerStatus._last`(인메모리) + `central-agent-cvp.json` 의 마지막 상태(캐시)뿐 **확인**. **INTENT**: v2.516 작업 로그는 사용자 요청("스토리지 모니터링처럼 하단에 진행상태와 로그")으로 생긴 화면 기능이고 `docs/CVP.md`·CLAUDE.md v2.608 에 CVP 요구 0건 → 규약 위반이 아니라 형제 비대칭(정보). CLAUDE.md v2.582 '형제 비대칭은 결함' 은 수집 **프로토콜**(collect-all) 건이라 그대로 적용하기 어렵다. | 기능 결정(사용자). 하면 `cvp/activityLog.js` + 폴러·`cvpEdge` 기록 + `CollectActivity` 패널. | **부분** — `edgelog/spec.js collect.*` 키 ↔ CONFIG_DIR 의 `<도구>-activity.json` 존재 대조 → '작업 로그 없는 수집기' 목록(정보). |
| PERSIST2613-08 | **CONFIRMED** | info | `audit2590.test.js:90-96` 고정 표본 8+7(CVP 두 파일 없음 → 01 통과) **확인**; `audit2602f:48-51` configWatchRelevant 도 고정 표본 **확인**; `audit2611c:403` chmod 검사 3파일 이름 **확인**; `arch2582` ARCH-1 은 `JSON.parse(fs.readFileSync` ∧ `atomicWriteFileSync(` 리터럴이 있는 모듈만(`createDebouncedWriter` 사용 `central/cvpEdge.js`·`agentConfig.js` 는 사각 — 오늘은 캐시라 무해) **확인**; ARCH-4 는 타이머 변수 이름 6종 **확인**. `createDebouncedWriter` 를 보는 테스트는 `audit2599b` 하나. | 스윕 대상을 헬퍼 호출(`createDebouncedWriter(`·`createActivityLog(`·`createAuthGuard(`·`CURSOR_FILE`)로 도출 — 그 파일명 리터럴이 `isRuntimeStateFile()===true` 인지 테스트 1개면 01 류를 CI 가 잡는다. | **가능(런타임)** — `*.db`·`-wal`·`-shm` mode&0o077, dbDir 0o700, SECRET_FILES mode 를 stat. |

## 반증 시도 기록(REFUTED 된 것 없음 · 정정 3건)
- 01 을 뒤집으려 `settingsFingerprint`/`unchanged` 생략과 `AUTO_REASON_KEEP` 을 봤다 — 생략은 지문 대상에 두 파일이 들어 있어 무효, 보관 상한은 정기·수동만 보호 → 결함 성립, 심각도만 medium.
- 02 수치 정정: 168→**180**, 44→**42**(결론 불변).
- 03: 새 결함이 아니라 v2.599 문서의 '미적용 7종' 잔여 → SPLIT/info.
- 06: "partfault 만" → vmtrack 도 같은 꼴(축 범위 밖).
- 05·07: 규약 위반 근거가 CLAUDE.md 에 없어 info.

## 검증하지 못한 것(정직 기록)
- v2.599 '그룹 e 보고'(7종 명단·사유) 원문이 저장소에 없다 — 03 의 '알려진 잔여' 판정은 CLAUDE.md 한 줄에 근거.
- 01 의 실제 백업 슬롯 소진은 시간 경과가 필요해 파일 시스템으로 재현하지 않았다(지문·감시 경로 코드로 판정).
- 04 의 pdu 등록부는 실행하지 않았다(코드상 sanswitch 와 같은 꼴).


## F 엣지 push/pull — 검증 원문

# 축 F(EDGE-SYMMETRY) 적대적 검증 — v2.613 아키텍처 점검

- 기준: 작업 트리 HEAD `8ece9d9`(v2.612.0 머지) · 읽기 전용 · 실행한 것: 발견 스크립트 `arch2613/edge/selfcheck.mjs` 재실행 + 주석 제거판 재계수 `arch2613/verify-edge/recheck.mjs`(결과 `recheck.json`) · 정규화 diff(bmstor/jobs ↔ captureJobs) · 소스 통독(발견이 지목한 줄 전부) · CLAUDE.md/docs/AUDIT 의 '남긴 것' 대조.
- 렌즈: **INTENT**(문서화된 규약 위반인가 / 의도·수용·기수정·기록된 잔여인가) · **REPRO**(근거를 직접 재도출, 반증 시도). 불확실하면 REFUTED.
- 결과: CONFIRMED 10 · SPLIT 3 · REFUTED 2. 심각도는 검증 후 재판정(발견의 medium 4 → 2, low 8 → 6, info 3 → 7).
- ⚠ 발견 스크립트는 주석을 제거하지 않았다 — `storageConfigPull`(setInterval 오탐)·`upgrade/upgrade.js`(주석의 `/api/collector/` 오탐) 두 곳이 그 때문에 잘못 세였다. 재계수는 `test/_stripComments.js` 를 거쳤다.

| ID | 판정 | 심각도 | 근거 한 줄 | 권장 최소 수정 | 자동 점검 |
|---|---|---|---|---|---|
| EDGE2613-01 | **CONFIRMED** | medium | `export function reap*Claims(` 6모듈(bmstor·capture·edgeLog·idracScan·ping·rma) + logQueries·testRuns 자체 만료, 공용 `createCollectRequestQueue` 사용자 4 — 재계수 일치. 의미론 편차 실증: `'expired'` 상태는 pingJobs·logQueries 에만 있고 `captureJobs.getCaptureResult` 는 UNDONE_TTL(10분) 뒤 `{state:'unknown'}`(사유 없음 — v2.590 P11 이 ping 에서 고친 바로 그 '무한 대기 뒤 미상') 이 capture·bmstor 에 그대로다. INTENT: CLAUDE.md v2.290 은 '패턴을 따를 것', v2.590 P16 은 '**새** 위임 큐는 이 팩토리' — 기존 8개에 소급 규칙은 없으나 '코어는 하나다'(v2.513) 계열. 정규화 diff 는 발견의 35/72 보다 큰 82줄/99줄(필드명·reap 골격 동일, 숫자는 판정에 비중 없음). | 발견안대로 `util/jobQueue.js` 코어 + capture/bmstor 부터 이식하되, **최소 수정은 `expired`+사유를 capture/bmstor/edgeLog 에 먼저**(P11 의 형제 누락 — 화면이 '결과 없이 폐기' 를 말하게). | **가능(소스)** — 배포 `server/src` 에서 `export function reap\w*Claims\(` 수 vs `createCollectRequestQueue\(` 사용자 수(재계수 6/4). 런타임: `centralRouter.stack` 의 `*-jobs`·`*-result` 쌍 열거. |
| EDGE2613-02 | **CONFIRMED** | medium | 세 모듈 본문 통독 — `findCollector → enabled/url → withOutboundTag(resilientFetch) → catch timeout/unreachable → readJsonCapped → 401/403 → 404 kindFor404 → !ok http → bad-body → put*` 사다리 3벌 그대로. **드리프트 실증**: v2.604 CEN2604-03(reason 객체 → '[object Object]') 수정이 bmUsage(`strOf`)·tokenCheck(`capStr`)에만 적용되고 `edgeLogPull.js:39` 는 여전히 `String(body.reason)`; 저장 키도 edge-log/bm-usage 는 `col.name` · token-check 는 `col.id`(v2.583 #29 가 한 벌만 바꿈). | `central/edgePull.js pullFromEdge()` + `createEdgePullStore()` 로 통합(발견안). 최소 수정은 `edgeLogPull.kindFor404` 의 `String(body.reason)` → `capStr`(CEN2604-03 형제). | **가능(소스)** — `function kindFor404\(` 개수(3). 런타임: `collectorRouter.stack` GET(/export·/ping 제외) ↔ 중앙 pull 모듈 `MIN_EDGE_VERSION` 대조. |
| EDGE2613-03 | **CONFIRMED** | low(↓) | `sanswitch/push.js:66,127` `let _busy=false` → `{ok:false, reason:'이전 push 진행 중'}` 는 `*/push.js`·`perfPush.js` 중 유일(나머지 5개는 `_busy=null`+`_again`). `agent/sanSwitchConfigPull.js:35-58` 이 `BUSY_REASON` 문자열 대조 + `sleep(2s)×90` 루프 — `test/audit2601c.test.js:294` 가 그 동작을 고정한다(동작은 정상). INTENT: 명시 규약 위반은 아니나 v2.582 '형제 도구의 규약 비대칭은 결함' 계열. 심각도는 '동작함·문구 결합만 취약' 이라 low. | `sanswitch/push.js` 를 `_busy(프라미스)+_again` 으로, `pushAfterCollect` 는 `pushSanSwitchNow()` 결과 기록만 남기고 루프 제거(audit2601c 의 `pending` 단언은 `_again` 합류 결과로 바꾼다). | **가능(소스)** — `*/push.js` 에서 `let _busy = false`(재계수 1). |
| EDGE2613-04 | **CONFIRMED**(기록된 잔여) | low(↓) | `sanswitch/push.js:141-146` 위임 N대·스냅샷 0 → `return {ok:true, sent:0}`(중앙 무보고) · `pdu/push.js:91-93` 보류 중 `_last` 만 · `statusOnly === true` 수신은 `routes/central.js:1336`(/storage-data) 1곳 · `centralStatusOnlySupport` 정의는 `storage/push.js:150` 1곳 — 전부 재현. **INTENT: 이미 두 번 기록된 잔여** — CLAUDE.md v2.593 '남긴 것 ③ PDU·SAN push 0건 조기 반환(EDGE-3 — v2.583 설계)' · v2.594 '남은 판단 EDGE-3' · `storage/push.js:60` 주석 "SAN·PDU 는 첫 수집 대기 중 아무것도 보내지 않는다 — 같은 쪽으로 실패한다". v2.603 규약('반증 기록은 그 근거로는 결함이 아니다' 이지 '결함이 없다' 가 아님)대로 새 근거(프로브가 storage 지역 함수라 재사용 불가 · PDU 인메모리 재기동 창)는 유효. 실제 창은 PDU 보류 상한 15분이지만 첫 poll(기본 5분) 뒤 해제되므로 보통 ≤5분·SAN 은 파일 캐시라 첫 기동 1회. | 발견안 ①(`centralStatusOnlySupport` 를 `agent/centralCaps.js` 로 승격, storage 재수출)만 먼저 — ②~④(sanswitch/pdu statusOnly 송수신)는 EDGE-3 판단이 필요한 별건으로 두되 문서의 '남긴 것' 에 ①이 선행조건임을 적는다. | **가능(런타임)** — `ingestStats` 로 '위임 장비 있는 엣지인데 `-data` 수신도 statusOnly 도 없음' 표시. 소스: `statusOnly === true` 가 없는 `*-data` POST 경로 목록(재계수: 수신 1곳). |
| EDGE2613-05 | **CONFIRMED** | low | `function readDropSummary(` 3벌(storage:110·pdu:26·sanswitch:115) · `agent/centralReply.js` 사용자 8파일(발견의 7 과 근사) — 재계수 일치. sanswitch 판본만 `coerced`·`zoningTrimmed` 를 읽는다(공용 `dropSummaryOf` 에는 없음). INTENT: `centralReply.js` 머리말이 "storage·SAN·PDU 는 readDropSummary 로 이미 읽는다 — 이것은 그 형제 누락" 이라 **알고 남긴** 상태(규약 위반이 아니라 통합 미완). pdu 의 '도메인 간 import 회피' 사유는 cvp/partfault 가 이미 `../agent/centralReply.js` 를 import 하므로 낡았다(확인). | `dropSummaryOf` 에 `coerced`·`zoningTrimmed`·`evicted` 추가 후 세 push 가 `readCentralReply+dropSummaryOf+warnDrop` 사용(콘솔 문구 유지 — 로그 분석 카탈로그 probe 확인). | **가능(소스)** — `function readDropSummary\(` > 0. |
| EDGE2613-06 | **CONFIRMED** | low | `storageConfigPull:43`·`pduConfigPull:45`·`gpuGuestConfigPull:45`·`usersConfigPull:43` 전부 `if (!res.ok) throw new Error('<x>-config <- ${res.status}')` — 본문 미판독(재계수 4). `classifyCentral404` 도입 커밋 `78231de` 는 curUser·partFault·sanSwitch·svcmon·vmSeries 5개만 고쳤다(의도적 제외 근거 없음). 정정: 이 4곳은 '구버전' 이라 **단정하지도 않는다**(`<- 404` 만 남김) — '틀린 조치' 가 아니라 '미분류'. | 4곳에 `if (res.status===404) { const c = await classifyCentral404(res); … }` 6줄(sanSwitchConfigPull 과 동형). | **가능(소스)** — `agent/*ConfigPull.js` 중 `classifyCentral404` 없는 파일. |
| EDGE2613-07 | **CONFIRMED** | info(↓) | `X-Central-Token` 있고 `X-Agent-Name` 없는 엣지 모듈 16 — 재계수 일치. INTENT: `routes/central.js:6-15` 는 "**신규 라우트** 규약(라우트 작성자 책임)" 이고 미들웨어 `requestedAgent`(:253) 는 `?agent → X-Agent-Name → body.agent` 순으로 읽어 기능·보안 공백 없음(발견도 인정). 소급 규칙이 없으므로 순수 편차 → info. | `agent/centralHeaders.js` 하나로 치환 + 스윕(발견안). 우선순위 낮음. | **가능(소스)** — 위 조건 파일 목록(16). |
| EDGE2613-08 | **CONFIRMED** | low | `edgeLogPull.js:30 findCollector`(async, 동적 import) · `idracScanPush.js:68 findCollectorForAgent`(sync) 2벌, 둘 다 name/id 소문자 비교 — 재계수 일치. `collector/registry.js` export 에 이름 조회 없음(확인). `bmstor/poller.js:18 → central/idracScanPush.js` 는 도메인→기능 import(arch2579 는 routes/·util/ 방향만 고정하므로 **위반은 아님**, 회색지대). 부수: `registry.js` 는 central/ 을 import 하지 않아 edgeLogPull 의 동적 import 는 순환 사유가 없다. | `collector/registry.js findCollectorByName(agent)` 신설 + 두 모듈 `import { x } from …; export { x };` 재수출(v2.575 규약). | **가능(소스)** — registry 밖 `loadCollectors().find(` 이름 비교 패턴(현재 idracScanPush 1 + edgeLogPull 의 `list.find`). |
| EDGE2613-09 | **CONFIRMED**(편차) | info(↓) | 주석 제거 재계수: adaptive 4(cvp·pdu·sanSwitch·storage) · setInterval 6(curUser·gpuGuest·partFault·svcmon·users·vmSeries) — 발견의 storage setInterval 은 주석 오탐이었고 발견도 그렇게 적었다. 첫 지연 40/45/50초 등 제각각(확인). INTENT: v2.409 규약은 **중앙이 배포하는 주기**에 대한 것이고 그 둘(storage·pdu)은 adaptive 라 위반 없음. 발견 스스로 '결함 아님·패턴 편차'. | 통합 러너는 선택 사항 — 착수한다면 EDGE-06·07 을 같은 러너에 흡수하는 편이 3건을 한 번에 닫는다. | **가능(소스, 주석 제거 필수)** — 판단은 사람. |
| EDGE2613-10 | **SPLIT** | low | ✔ `relaycheck/checks.js:67,76` resilientFetch 무태그(`target.expectAgent` 가 있어 태그 가능) · ✔ `linkcheck/checks.js:161,170` `recordOutbound(url,{…})` 태그 없음(`linkcheck/run.js:40` 이 `link.to` 로 수집 서버를 안다) — 둘 다 CLAUDE.md v2.601 '남긴 것: 태그 없는 중앙 → 엣지 호출 4종' 에 **이미 기록된 잔여**. ✘ `upgrade/upgrade.js:498` 은 **반증** — 주석(:467)에만 `/api/collector/` 가 있고 실제 호출은 `/api/upgrade/bundle` 로 `outboundStats` 가 **설계상 기록하지 않는 경로**(포탈 사이 `/api/collector·/api/central` 만); v2.604 의 '다음 후보' 는 readJsonCapped 스윕 얘기였다. `collector/upgradePush.js` 는 v2.601 에 이미 `tag:` 를 싣는다. | relaycheck 는 `withOutboundTag(target.expectAgent, …)`, linkcheck 는 `recordOutbound(url, {…, tag: link.to})` 두 줄. | **부분** — 소스 스캔은 linkcheck(경로 변수 조립)를 못 본다. 런타임이 정확: `outboundStats` 행 중 `/api/collector/` 경로인데 `tag` 가 빈 것 목록. |
| EDGE2613-11 | **REFUTED** | info | 산수 재확인: 추적 600줄×2,000자=1.2MB 상한 + `cliRaw` 24명령×`RAW_LIMIT` 4,000자≈96KB 는 맞다. 그러나 추적 줄의 실체는 `makeTracer` 에 들어가는 **진행 문구와 `verbose` 시 ssh2 `debug` 콜백 한 줄(`ssh2: DEBUG: …`, 수십~수백 자)** 이고(`proxy/sshExec.js:91`), 2,000자 줄을 600번 채우는 경로가 없다 → 현실 상한 ≈100~200KB, 기본 1MB 미만. 413 이 성립하려면 줄 평균 1,700자 이상이어야 한다. `BIG_JSON` 미등록(`index.js`)은 사실이나 결함이 아니다. | 없음(예방 차원 `BIG_JSON` 등록은 무해하지만 근거 없는 수정은 하지 않는다 — v2.550.3 규약). | 아니오 — '어느 POST 가 큰가' 는 사람이 정한다. |
| EDGE2613-12 | **SPLIT** | info | ✔ `routes/central.js` 인라인 게이트 51/51(라우트 51) · collector.js `/ping` 404 `{ok:false}`·`/export` `{error}`·나머지 `{ok:false,reason}` — 재계수 일치. ✘ 그러나 **의도된 형태** — `central.js:297` "각 라우트가 404/403을 구분해 응답(하위호환 유지)" 로 미들웨어가 `next()` 하고 라우트가 답하는 설계. ✘ '같은 404 를 세 판정이 다르게 읽는다' 는 부정확 — `kindFor404` 는 /edge-log·/bm-usage·/token-check 응답만 받고(그 셋은 reason 을 싣는다) /ping 은 보지 않으며, `relaycheck:69` 의 'auth' 판정은 /ping 404 를 "COLLECTOR_TOKEN 미설정(또는 포탈 아님)" 이라 명시해 읽는다(오독 아님). 남는 사실은 응답 모양 비대칭뿐. | 하려면 `/ping`·`/export` 404 에 `reason` 추가(2줄) 정도 — 라우터 미들웨어 승격은 설계 변경이라 별건. | **가능(런타임)** — 라우터 스택 열거로 게이트 유무 확인은 되나 의도된 형태라 '점검 항목' 가치는 낮다. |
| EDGE2613-13 | **CONFIRMED** | info | `admitAgent` 사용 8 파일(재계수 일치: agentConfig·cvpEdge·edgeRecord·partFaultEdge·pduEdge·sanSwitchEdge·sanSwitchPerfEdge·storageEdge). `edgeLogStore.js:75` 거절만(퇴출 없음) · `svcmonEdge.js:103` 거절만 · `fleet.js:154`·`gpuGuestDiag.js:78` 자체 퇴출(gpuGuestDiag 는 재삽입으로 LRU 유지 — 정상) · `sanSwitchPerfEdge.js:104` admitAgent 뒤 `slice` 퇴출은 도달 불가(중복). 실운영 28곳 / 상한 200·64 라 영향 없음. | edgeLogStore·svcmonEdge 를 `admitAgent` 로(선택). | **가능(소스)** — central/ 에서 agent 키 Map 을 두며 `admitAgent` 를 import 하지 않는 파일. |
| EDGE2613-14 | **REFUTED** | info | "방향 하나만 검사한다는 사실이 코드 어디에도 없다" 는 틀렸다 — `test/audit2604a.test.js:243` 주석이 "**agent/ 는 엣지 → 중앙 방향(중앙 응답)이라 대상이 아니다**" 라고 범위를 명시하고 `if (rel.startsWith('agent/')) continue;` 로 집행한다. 엣지가 중앙 응답을 `res.json()` 으로 읽는 모듈 23개(재계수)·`readJsonCapped` 는 cvp·perfPush 뿐인 사실은 맞으나 중앙은 신뢰 경계 안이고 그 판단이 테스트에 적혀 있다. | 없음. | 아니오. |
| EDGE2613-15 | **SPLIT** | info | ✔ 사실: `pdu/intervals.js` 에 `intervalsForAgent` 없음·`storage/intervals.js:190` 에 있음(재계수) · `routes/central.js:1240/1420` 이 각각을 쓴다. ✘ "'같은 계약' 주석과 다르다" 는 오독 — `pdu/intervals.js` 머리말의 '계약' 은 **배포 규칙**(지정 키만·하한·빈 값 미지정·`PDU_INTERVALS_LOCAL`)이고 PDU 는 그것을 지키며, 머리말은 "왜 파일을 나눴나" 까지 적어 두었다. 엣지별 값은 애초에 약속된 적이 없다 → 기능 비대칭이지 결함 아님. | 필요할 때 기능 요구로 다룬다(공통 코어 추출은 그때). | 아니오(제품 판단). |

## 재현 로그(요약)
- `selfcheck.mjs` 재실행 → 발견 본문 수치와 동일(readDropSummary 3 · kindFor404 3 · reap 6 · collectRequestQueue 4 · no404classify 4 · XAgentName 16 · busy_boolean 1). 단 `findCollector_copies` 는 정규식이 `async` 를 못 잡아 1 로 나왔고, 실제는 2(`recheck.mjs` 로 확인).
- `recheck.mjs`(주석 제거판): `configPull_setInterval` 6(storage 제외) · `central_to_edge_untagged` = `collector/upgradePush.js`(이미 `tag:` 있음 — 스캔 정규식 한계) + `relaycheck/checks.js` 만 · `kindFor404_reason_coercion` = edgeLog `String(body.reason)` / bmUsage `strOf(` / tokenCheck `capStr(`.
- `captureJobs.getCaptureResult`: `jobs.get` 실패 → `{state:'unknown'}`; `UNDONE_TTL` 10분 → v2.590 P11 의 '만료 사유' 가 capture·bmstor 에 없음(EDGE-01 의미론 편차 실증).
- 정규화 diff(bmstor/jobs ↔ captureJobs, 주석 제거·모듈명 치환·공백 정리): 79/99줄 중 diff 82줄 — 골격(reqId·pendingByAgent·claim/reap/TTL)은 같고 세부는 발견이 적은 것보다 더 갈라져 있다(통합 시 '결과 동일성' 테스트가 더 중요).
- EDGE-11: `makeTracer` 상한 600×2,000 · `RAW_LIMIT` 4,000 · `verbose` = `auth.debug` 콜백 한 줄 — 1MB 도달 경로 없음.
- 실행하지 않은 것(정직 기록): 목 서버·라우터 스택 런타임 열거·전량 테스트(지시대로 생략). EDGE-04 의 PDU '재기동 뒤 구분 불가 창' 길이는 코드(`PDU_PUSH_WITHHOLD_MAX_MS` 15분 · `firstPollDone` 해제 · 기본 poll 5분)로만 산정했다.

## 총평
- 수정 가치가 있는 확정분: **01·02**(medium — 사본 사이에 이미 의미론·보정 드리프트가 실증됨), **03·05·06·08**(low — 작은 형제 누락), **04**(low — 두 번 기록된 잔여의 선행조건인 프로브 승격만). 나머지는 info 또는 반증.
- 다음 회차 자동 점검('아키텍처 점검' 화면) 후보로 남길 항목은 소스 스캔이 정확한 01·02·03·05·06·07·08·13 과 런타임이 정확한 04·10 이다. 09·11·12·14·15 는 사람 판단 영역이라 화면 항목으로 부적합.


## G 런타임·폴러·예산 — 검증 원문

# arch2613 · 축 RUNTIME-OPS 적대적 검증 결과

- 기준 커밋 `8ece9d9`(v2.612.0) · 읽기 전용 · 재현 스크립트: `verify-runtime/sweep02.mjs`(상태 export 스윕) · `verify-runtime/stagger06.mjs`(스태거 집계) · DB import 부작용은 인라인 `node --input-type=module`(빈 `CONFIG_DIR` 에 모듈만 import 후 파일 생성 여부) · `node --test test/edgeSweep2574.test.js`(8/8 통과 확인)
- 판정 규칙: INTENT(문서 규약 위반인가) + REPRO(직접 재현) · 불확실하면 REFUTED 쪽

| ID | 판정 | 심각도(검증 후) | 근거 한 줄 | 권장 최소 수정 | 자동 점검 |
|---|---|---|---|---|---|
| RUNTIME2613-01 | CONFIRMED | medium | `health/services.js` 는 고정 13항목이고 `STATUS_SPEC` 59모듈 중 55개가 미import — 그중 폴러·push·pull **26개**(storage v2.302·sanswitch v2.410·pdu v2.424 까지 포함 — '2.520 이후' 보다 넓다). 모듈 머리말·화면 문구("포탈 내부 서비스/수집기 상태를 한눈에")가 완전성을 약속하는데 CVP·bmusage·linkcheck·partfault·curuser·horizon 폴러가 멈춰도 `overall:'ok'`. v2.590 W1('샘플러가 멈춰도 정상')과 같은 계열이 26개에 남아 있음. 소비처 `DavinciChecks.jsx:16`(15초 폴링). 문서에 '전 폴러를 services.js 에' 라는 명시 규약은 **없음**(구조 결함이지 규약 위반은 아님) | `getServiceCheck()` 끝에서 `STATUS_SPEC` 을 순회해 `await import(mod)[fn]()`(부팅 로드 모듈만 — spec 규칙 그대로)로 공통 판정 행 생성(`enabled===false→off` · `atOf(last?.at)` 경과 > `intervalMs×2`(최소 30분) → warn · `running`). ⚠ `logStatus` 처럼 **async 상태 함수**가 있으므로 함수는 `await` 필요 → `getServiceCheck` 를 async 로 바꾸거나 별도 `getPollerChecks()` 추가. 엣지 노드에서는 중앙 전용 항목 제외. 회귀 테스트: spec 의 `mod` 집합 ⊆ services 가 부르는 모듈 집합 | 가능(서버 런타임) — ⚠ 발견 보고의 '키 차집합' 은 **이름공간이 달라 무의미**(`push.cvp` vs `cvp`). `STATUS_SPEC[].mod` ↔ `services.js` 가 import/호출하는 모듈 경로로 대조(수정 후에는 `checks[].specKey` 로 1:1) |
| RUNTIME2613-02 | SPLIT | low | (a) **확정** — `edgeSweep2574` 정규식 `/export function (\w*[Ss]tatus\w*)/` 은 `export async function logStatus()`(`logs/poller.js:177`)를 못 본다. 파일명은 스윕 패턴(`poller.js`)에 맞고 spec·EXCLUDED 어디에도 없는데 테스트 8/8 통과(재현). `startLogPoller` 는 무조건 기동·설정 기본 `enabled:true`(`logs/settings.js:14`)라 엣지에서도 돈다 → CLAUDE.md v2.574 "*Status() 를 export 하는 워커·폴러가 전부 표에 있는지" 의 의도 위반. (b) **반증/범위** — `capacity/sampler.js`·`security/{login,cert}Monitor.js` 는 테스트 주석이 대상을 "agent 의 *Worker.js 와 poller.js·scheduler.js" 로 **선언**하고 있어 사각이 아니라 선언된 범위(발견 보고의 확장 패턴 `monitor\.js` 도 대소문자 때문에 그 둘을 못 잡는다). `perf/monitor.js requestStatus` 는 폴러가 아님 | ① 정규식을 `export (?:async )?(?:function|const) (\w*[Ss]tatus\w*)` 로 ② `logs/poller.js` 를 `collect.vcLogs` 로 등재(`collect.js:62` 가 `await fn()` 이라 async 무해) ③ 파일 패턴 확장은 별건 — 넓히면 `EXCLUDED` 사유 필요 | 가능(서버 런타임) — `server/src` 를 확장 정규식으로 훑어 spec 과 대조(웹 번들 불필요) |
| RUNTIME2613-03 | SPLIT | low | (a) **확정** — 같은 관심사 세 벌: `tokenProbe.probeAll`(poolSettled+예산 90s+초과 개수) / `portalCheck.js:266` 인라인 손 풀(총 예산 없음) / `bmUsage.js:427` 순차 `for`(총 예산 없음). (b) **확정** — `arch2579` ③ 정규식이 `Math\.min\(limit` 리터럴이라 `Math.max(1, Math.min(PROBE_CONCURRENCY…))` 를 못 보고, `portalCheck.js`(v2.560)는 `POOL_EXCLUDED` 에도 v2.594 '결함 아님' 7곳 목록에도 없다 → CLAUDE.md v2.579 "풀 스캐폴드는 util/pool.js 하나" 위반이 심사 없이 남아 있음. (c) **반증** — "최악 1,600s > `server.requestTimeout` 600s": ① Node 의 `requestTimeout` 은 **요청 수신** 시한이지 핸들러 소요와 무관 ② 웹은 `BmUsage.jsx:105` 가 `agents:[agent]` 로 **한 번에 한 엣지**만 보내므로 요청당 최악 2×20s=40s(웹 `MUT_TIMEOUT_MS` 180s 이내). 40대 순차는 직접 API 호출에서만 성립(잠재). edge-pull 은 28곳/4×20s=140s < 180s 이고 **36곳부터** 웹 시한(180s)에 걸린다(30+ 확장 시 경계) | ① `portalCheck.js:266` 손 풀 → `poolSettled(rows, PROBE_CONCURRENCY, …)` + 총 예산(`PROBE_BUDGET_MS` 재사용, 초과 개수 응답) ② `bmUsage.js` 인출 → `poolSettled(agents, 4, …)` + 총 예산 ③ `arch2579` 정규식을 `Array\.from\(\{\s*length:\s*Math\.(?:min|max)\(` 로 넓히고 v2.594 7곳을 `POOL_EXCLUDED` 에 사유와 함께 명시. 공용 `edgeFanout` 헬퍼는 셋의 결과 모양이 달라 별건 | 가능(서버 소스 스캔) — 손 풀 idiom 정규식 + 상수 산수. ⚠ 기준값은 `server.requestTimeout` 이 아니라 웹 `MUT_TIMEOUT_MS`(180s): `ceil(N/동시성) × 시한 ≤ 180s` |
| RUNTIME2613-04 | CONFIRMED | low | (a) `sanswitch/push.js:37 pushMs`·`perfPush.js:33 perfPushMs` 는 `Math.max(60_000, Number(env) …)` 로 상한 없음 — v2.599 T2599-02 "새 주기 env 는 `clampIntervalMs`" 규약 위반. 같은 기능의 `sanSwitchConfigPull.js:21` 은 v2.600 EDGE2600-05 에 고쳐졌는데 push 둘은 빠짐(형제 비대칭 — 재현: 소스 확인). 영향은 작다 — `adaptiveTimer.arm` 이 2^31 로 자르므로 1ms 루프는 없고 `*Status().intervalMs` 만 무장값과 다르게 보고. (b) 'SAN 수집 주기 env 전용(중앙 배포 없음)' 은 사실이지만 CLAUDE.md v2.410 의 '같은 규약' 목록(동시성·시한·가드·adaptiveTimer)에 **중앙 배포는 없다** — 규약 위반이 아니라 기능 부재(정보) | `pushMs = () => clampIntervalMs(Number(env) \|\| 5*60_000, 5*60_000, 60_000)` · `perfPushMs` 도 동일(`loadPerfSettings().intervalMs` 는 이미 LIMITS 6h 클램프). 중앙 배포는 사용자 결정 사항 | 가능(서버 소스 스캔) — `Math\.max\(\d[\d_]*,\s*Number\(process\.env\.\w+_MS\)` |
| RUNTIME2613-05 | CONFIRMED | low(잠재 · env 필요) | `DEVICE_TIMEOUT_MS = Math.max(20_000, env \|\| 60_000)`(poller:38) → `withDeadline` 이 `deadlineMs` [1s,2h] 로만 자름 · OS 예산 `reqTimeoutMs(env, 50s, {min:10s, max:600s})` · ENT 예산 `Math.max(15s, env \|\| 45s)` — 세 env 가 독립이라 `BMUSAGE_DEVICE_TIMEOUT_MS=30000` 하나로 예산 50s 안의 두 번째 시도가 시한에 먼저 끊긴다(v2.550.3 이 고친 결함의 env 재발 경로, 재현: 상수 경로 추적). 테스트 둘은 소스 기본값만 정규식 대조(확인). ⚠ 형제 storage(`cliSsh.js:42` 150s vs `poller.js:29` 180s)도 **같은 형태**(테스트만) — 저장소 관행이라 bmusage 만의 누락은 아님 | 폴러 기동 시 `DEVICE_TIMEOUT_MS = Math.max(env값, max(OS_BUDGET, ENT_BUDGET) + 10_000)` 로 보정 + 보정 시 콘솔 1줄 + `bmUsageStatus()` 에 세 값. storage 형제도 같은 보정(비대칭 방지) | 가능(서버 런타임) — 세 모듈 import 후 상수 관계 비교(부팅 로드 모듈이라 부작용 없음) |
| RUNTIME2613-06 | CONFIRMED | low | 스태거 배열은 **74개**(발견 보고 77 은 과다 — `_stripComments` 기준·sed 기준 둘 다 74) × 1.5s → 마지막 `startLinkCheckWorker` 109.5s. 첫 실행 = 스태거 + `firstDelayMs`: bmusage 106.5+45=**151.5s** · partfault poller 100.5+90=**190.5s** · linkCheckWorker 109.5+60=**169.5s** · edgeLogWorker 105+35=140s(재현: 각 모듈 firstDelay 확인). 항목이 늘수록 꼬리 기능 첫 실행이 선형으로 밀리는 구조는 사실이나 문서 규약 위반은 아님(스태거 자체는 v2.4xx CPU 평탄화 의도). 01 의 '손 목록 세 벌' 과 구조 원인 공유(부분 중복) | 최소: 간격을 `Math.min(1500, Math.floor(60_000 / stagger.length))`(74개면 ~810ms → 꼬리 60s) 또는 기능군별 묶음 기동. 레지스트리 통합은 01 과 함께 별건 | 가능(부분) — 런타임에서 `index.js` 소스의 배열 길이 파싱(74) + 각 `*Status().last==null` 경과로 첫 실행 지연 관측 |
| RUNTIME2613-07 | SPLIT | info | **DB 부분 반증** — 빈 `CONFIG_DIR` 에서 `ipam/db.js`·`storage/db.js`·`idrac/db.js` 를 **import 만** 하면(300ms 대기) 파일이 **하나도 생기지 않는다**(재현 · 신규 6모듈과 동일). 세 모듈 다 첫 호출에 연다(`getDb()`/`await import('node:sqlite')`). 계측 훅의 'import 단계' 는 `index.js` **최상위 문장**(`store.start()`·`applyGrowthSettings()`·`runZeroCapacityPurge()` 등)의 실행을 포함해 귀속이 틀렸다 → '형제 비대칭(eager vs lazy)' 주장 성립 안 함. **타이머 부분 확정** — 모듈 스코프 `setInterval` 은 `gpu/store.js:98`·`central/idracScanJobs.js:615`·`rma/agent.js:179` 로 실재(전부 `unref`) — 정보 | 없음(정보). 계측 훅을 쓰려면 스택에서 `index.js` 본문 프레임을 분리해 귀속할 것 | 가능(서버 런타임 `--import` 훅) — 단 'import 시 부작용' 판정은 `index.js` 최상위 호출을 제외해야 한다(이번 오판의 원인) |
| RUNTIME2613-08 | CONFIRMED(기록된 후속 후보 중복) | info | `^(export )?(function\|const) clamp` 정의 **32개**(발견 보고 28 — 정규식 차이), `clampSetting` 사용 5파일(alerts·gpu/settings·osScanner·ipam/scanStore·mail/settings) 확인. ⚠ 32 에는 차트·퍼센트용 숫자 clamp 도 섞여 '설정 clamp 사본' 수는 그보다 적다(의미 분류 필요). CLAUDE.md v2.595 가 이미 "clamp 사본 16벌 — 다음 점검 후보" 로 적어 둔 항목의 재계수 | 신규 기능부터 `clampSetting({min,max,def})` 로; 스윕 테스트는 '설정 저장 경로에서 쓰이는 clamp' 로 좁혀 감소 방향 고정 | 가능(서버 소스 스캔) — 단 정의 수만 세면 과대계수 |
| RUNTIME2613-09 | CONFIRMED | info | `agent/*ConfigPull.js` 10개 = `setInterval` 6(curUser·gpuGuest·partFault·svcmon·users·vmSeries) / `startAdaptiveTimer` 4(cvp·pdu·sanSwitch·storage) 확인. INTENT: adaptive 인 넷 중 storage·pdu·sanSwitch 는 주기가 **중앙 배포 대상**(`intervals.js`)이라 v2.409 규약상 adaptive 가 필수이고, env 전용 6개는 setInterval 이어도 규약 위반이 아니다 — '두 계열' 은 절반은 원칙적 분기. 코어 하나 '형태' 관점의 정보 | 팩토리 통합은 별건(기능 차이 0) — 새 pull 을 만들 때 주기가 중앙 배포면 adaptive, env 면 어느 쪽이든 | 가능(서버 소스 스캔) |

## 정직 기록
- 실행하지 않은 것: 전량 `npm test`·`build`·목 서버 기동(발견 축이 이미 72초 실측). 단일 테스트 파일 `edgeSweep2574` 만 돌렸다(8/8).
- 07 의 반증은 **모듈 단독 import** 기준이다 — 운영 부팅에서 세 DB 가 `app.listen` 전에 열리는 것 자체는 사실(원인이 index.js 최상위 호출).
- 03(c) 의 웹 시한 180s 는 `web/src/api.js:178 MUT_TIMEOUT_MS` 기준 — 브라우저·프록시 시한은 별도.


## H 테스트 구조·문서 — 검증 원문

# TEST·DOC 축 적대적 검증 (v2.613, 기준 HEAD 8ece9d9) — 읽기 전용

실행한 것: `server/test` grep 집계 재계산, 12개 성능 테스트 파일을 **CPU 부하 3개(load avg 3.5/4코어) 아래에서 각 2회** 실행, `_stripComments` 상태기계 vs 2줄 정규식을 `server/src` 711파일에 적용해 코드 토큰(function/return/catch/await/import/export/if) 개수 차이 집계, 웹 import 그래프 재계산(두 규칙), `docs/`·CLAUDE.md 인용 줄 직접 열기, `fulltest.log`(v2.611 전량) 재집계. 전량 테스트·빌드는 돌리지 않았다. 스크립트: `verify-testdoc/{stripdiff2,webgraph,webgraph2}.mjs`.

| ID | 판정 | 심각도 | 근거 한 줄 | 권장 최소 수정 | 자동 점검 |
|---|---|---|---|---|---|
| TESTDOC2613-01 | **REFUTED**(의도된 설계) | info | `[file:line]` 앵커 + `--check` 전문 비교는 v2.563 이 **일부러** 택한 계약이다(CLAUDE.md L1391~1430 "행이 가리키는 소스 줄을 실제로 열어 라우터 호출인지 확인" · L1430 "라우트를 추가하면 생성기를 돌릴 것"). 기록된 파손 3건(v2.569·2.590·2.607)은 전부 '생성기를 안 돌렸다/게이트를 등록 안 했다' 를 테스트가 **잡은** 사례이지 테스트의 결함이 아니다. `docs/API.md` 41/214 커밋 변경은 사실이나 그것이 규약의 비용이다. 부수 관찰: CI 의 `api-doc --check` 는 `continue-on-error` 인데 `npm test`(apiDoc2563 첫 테스트)가 같은 검사를 **차단형**으로 돌린다 — 모순이지만 결함은 아니다. | 없음(설계 변경 제안으로만: `--check` 를 줄 번호 정규화 후 비교 + 줄 번호 차이는 경고). | no — `docs/` 미배포. 런타임 (method,path,gate) 대조는 이미 `dataFlow2587`(unmapped 0)이 하는 별개 검사. |
| TESTDOC2613-02 | **SPLIT** | low | 사실 부분: ≤200ms 절대 단언 **26곳**(발견 23 + `audit2606g` 자신의 `tNew < 60` 5곳 포함 — 즉 v2.611 이 고친 파일에도 남아 있다). 반증 부분: v2.611 규약(L3814)은 **`tOld > X`(옛 구현이 느려야 한다) 쪽**을 비율로 바꾸라는 것이고 그 형태는 전부 바뀌었다. `tNew < X` 상한은 v2.603 이 "회귀와 확실히 갈리는 값이면" 허용한 형태다. 실측: 12파일 × 2회를 CPU 부하 3개 아래에서 돌려 **24/24 통과**(재현 실패). 여유가 얇은 곳은 `audit2591:339`(`<40` vs 예전 84ms, 2.1배)와 `audit2602a:180·188`(`<50`, 옛 수치 기록 없음) 정도. 플래키 커밋 이력에도 ms 임계 사례 없음(v2.590 재게시는 ssh2 키). | 여유 3배 미만인 2~3곳만 비율형(`tOld > 40 && tOld > tNew*5`) 또는 ≥1초 상한으로. 23곳 일괄 교체는 근거 부족. | no |
| TESTDOC2613-03 | **REFUTED** | info | `audit2600b:197-203` 은 `Date.now()` 를 쓰지만 표본은 **과거만** 심고(`if (at > now) continue`) 단언은 `pts.length >= 10`·`devices === 2`·`carryMaxMs >= 2h` 뿐이다. `sumCapacityBuckets`(storage/db.js:470-)는 마지막 버킷 `floor(now/b)*b` 까지 격자를 만들고 장비별 마지막 값을 staleMs(주기×2 = 2h) 안에서 이월하므로, 마지막 시간대 표본이 빠져도(직전 표본은 ≤1h26m 전) 모든 버킷이 devices=2 다. v2.517 결함(버킷 조회가 run 시작 시각에 걸림)의 변형이 아니다. 남는 것은 머리말(L16 "고정값")과 본문의 **주석 불일치**뿐. | 주석 정정 또는 `now` 를 정시−30분 고정값으로 한 줄. | no |
| TESTDOC2613-04 | **SPLIT** | low | IMP-06 `called >= 10` 은 실제 일치 13개라 최대 3개가 조용히 빠져도 통과 — 확정(이 부분은 05 와 **중복**). 나머지 셋은 반증: `edgeLog2549:63` 은 같은 테스트에 `STATUS_KEYS.size === STATUS_SPEC.length` 가 있고 열거는 `edgeSweep2574` 가 담당(중복이지만 무해), `logAnalysis2583:242` 는 바로 아래서 전 규칙을 순회, `audit2506:347-348` 은 `cov.undeclared deepEqual []` 옆의 '매핑 삭제 감지' 바닥값(주석이 용도를 적음). 발견 자신이 인정한 "바닥값 + 열거" 정당 형태다. | IMP-06 에만 기대 목록 deepEqual 추가(05 의 수정에 포함). | no |
| TESTDOC2613-05 | **CONFIRMED** | medium(high→) | 사실: `pullCurUserConfigNow`·`pullPartFaultConfigNow`·`pullStorageConfigNow`·`pullUsersConfigNow` 는 `server/test` 참조 0(재확인), IMP-06 describe 는 "push/**pull** 진입 함수를 실제로 호출한다" 인데 정규식은 `push\w*Now|run\w*Once` 뿐(13개, pull 0개)이고 대상은 `agent/` 뿐(v2.566 사고 모듈은 `sanswitch/perfPush.js`). 심각도 하향 근거: ① v2.566 의 결함 종류(import 이름 const 가림 TDZ)는 `sanPerfPush2566:139-171` 이 **`src` 전체를 walk** 하는 정적 스윕으로 고정 ② 4개 모듈은 `index.js:71·123·126·134` 가 import 하므로 앱 부팅 테스트 31개가 링크 시점 오류(SyntaxError·없는 export)를 잡는다 ③ IMP-06 식 '중앙 미설정 호출' 은 함수 첫 줄(설정 검사)만 지나므로 얻는 깊이도 얕다. 남는 공백은 '함수 본문 런타임 오류' 이고 그것은 실제 호출 테스트가 없다. | IMP-06: 정규식 `(push\|pull\|run\|poll)\w*(Now\|Once)`, 대상 `agent/*.js` + `*/push.js` + `*/poller.js`, `fn.length===0` 또는 `...args` 만 호출, 기대 목록(파일→함수명) deepEqual. | 부분 — 배포 `server/src` 에서 진입 모듈 import + export 가 function 인지는 자기 점검 가능. **호출은 불가**(중앙이 설정된 운영 서버에서는 실제 pull/push 가 나간다). |
| TESTDOC2613-06 | **PLAUSIBLE** | low | 수치가 규칙에 따라 달라진다: '`views/tools` 로컬 js 중 테스트된 것' 규칙으로는 **24/70**(발견은 12), '`web/src` 어디든 테스트된 모듈을 import' 규칙으로는 **2/70**(`DeviceFacetBar.jsx`·`ipamShared.jsx`). 발견의 12 는 어느 규칙으로도 재현되지 않았다. 로직 모듈 미테스트 2개(`relTime.js`·`serverTemp/useTempSparklines.js`)는 재현. `RelayCheckTool.jsx` 는 `api/ui/STable/blankOr` 만 import 하고 kind/reason 문구 11곳이 인라인 — 일반 논지는 성립. 규약(v2.398 '판정·문구는 순수 모듈에') 위반이라기보다 적용 정도의 문제. | 스윕을 넣으려면 '테스트됨' 의 규칙을 먼저 고정하고 허용 목록으로 시작. RelayCheckTool 문구 분리는 선택. | no |
| TESTDOC2613-07 | **CONFIRMED** | info | `fulltest.log`(v2.611): tests 3,941 · wall 220.4s · 합산 503.7s · 상위 61.2s(audit2599c 실제 라우터 01~06)·61.1s(audit2600b)·60.1s(sanSwitch 재진입 — `pollSanSwitchOnce` 2회가 등록 host 로 실제 SSH 시도 후 시한)·20.0s×2(register-collector 가 `registry.js:135` 8초 ping)·17.5s(svcmon). CLAUDE.md L4040·L4055 "115초(1,910건)" 은 'v2.513 실측' 이라 명시된 낡은 수치이고 "이 수치를 기준으로 추정할 것" 이라 적혀 있다. | 문서 수치 갱신(220초·3,941건) + 네트워크 대기형 2곳(sanSwitch·register-collector)에 목 lookup/짧은 시한 env. | no |
| TESTDOC2613-08 | **CONFIRMED** | medium | 규약 L1063(v2.574 "2줄 판본은 틀렸다 — `_stripComments.js` 를 쓸 것") 위반 **25파일**(재확인, 그중 v2.574 이후 작성 4: audit2604e·2611b·2611e·2612d). 실측: 2줄 정규식은 `server/src` 711파일 중 **39파일에서 코드 토큰을 먹는다**(configPush.js 외에 `index.js`·`routes/api.js`·`routes/svcmon.js`·`security/credentialStore.js`·`storage/collectors/vplex.js` 등), 그리고 25개 테스트 중 **12개가 그 파일을 실제로 읽는다**(audit2611b·authzGates2536·commMap2584·tokenCheck2560 → `routes/api.js` / bmUsage2550·bmUsagePerf2550 → `osSsh.js` / audit2611e → `ipam/settings.js` / audit2612d → `storage/collectRequests.js` / ssrfAgents2537 → `ssrfLookup.js`·`nsx/client.js` / atRest2538·audit2604e·hardening2577·audit2575 → `index.js`). 특정 단언이 실제로 오판 중인지는 확인하지 않았다(정직 기록). 스캐폴드 복제 수치(runLive 6·부팅 31·req.user 21·express() 67·createServer 58·mkdtemp 266)는 재현. | 25파일의 인라인 제거기를 `_stripComments` import 로 교체 + 테스트-of-테스트 스윕(`replace(/\/\*[\s\S]*?\*\//g` 0건). 헬퍼 4종 공용화는 별건(리팩터). | no |
| TESTDOC2613-09 | **CONFIRMED**(건수 정정) | medium | 규약 L3046(v2.582/2586 "고친 뒤에는 그 줄을 반드시 고칠 것") 위반. 코드로 재확인한 낡은 줄 **7줄 + 부분 1줄**: L3370(①→`store.js usageReadable` 4곳, ④⑤→v2.594 L3406 이 '목록에서 뺀다' 고 적고도 안 뺌), L3453(→`agent/pduConfigPull.js:57`), L3461(3건 중 2건 — `sqliteOpen` 적용·`util/agentKey` 9곳), L3607(→`trimSlashes`), L3672(EDGE2605-05 → v2.612 L3834), L3697(3건 → v2.607), L3720(`sftpReadFile` → v2.612 L3844 SFTP_READ_MAX). **반증 2**: L3597 은 linkcheck·relaycheck 가 여전히 `withOutboundTag` 0건이라 4종 중 2종만 낡음(부분), L3844 는 '남긴 것' 이 아니라 v2.612 **'고친 것' 목록**이다(발견의 오독). | 해당 7줄에 '✅ vX 에 고침' 표기(v2.586 방식) 또는 BACKLOG.md 이관 + 스윕. | no |
| TESTDOC2613-10 | **CONFIRMED** | low | 53개 중 36개를 열어 대조: **24개 무관**(App.jsx:409 탭 onClick · config.js:236 CENTRAL_TOKEN · redfish.js:653 PowerLimit · store.js:402 lastError · unitySsh.js:609 은 EOF 328 초과 · publicApi.js:87 빈 줄 · isilonSsh.js:189·unity.js:42·xtremio.js:38·rightsize.js:176·remoteInventory.js:56·checksLogs.js:113·checker.js:113·api.js:135 등), 12개 유효. "약 35/53" 은 신뢰할 만하다. 다만 CLAUDE.md 는 릴리스별 **기록**이라 인용 당시 사실이었고, 정직성 규칙은 '근거를 붙여라' 이지 '줄 번호를 유지하라' 가 아니다 → low. | 새 인용은 심볼명·함수명으로; 기존은 점진 정정(스윕은 허용 목록으로 시작). | no |
| TESTDOC2613-11 | **CONFIRMED** | medium | 규약 L1391(v2.563 "문서는 손으로 적는 목록을 두지 않는다") 대비: `ARCHITECTURE.md` 마지막 변경 6e4a924(v2.583, 09-23) · `server/src` 63디렉터리 중 **41개** 미언급 · commmap/dataflow/devflow/cvp 키워드 **0회**; `INDEX.md` 누락 **18개**(AUDIT-09-24* 15 + 09-25* 2 + CVP.md — 발견의 '19' 는 산수 오류); README L348 특수 기능 절에 comm-map·data-flow·device-flow·cvp **0회**. `CVP.md` 일치는 확인하지 않았다(발견 주장 그대로). 문서만의 문제라 배포 영향 0. | 생성 절(모듈 지도·INDEX·README 특수 기능)을 `scripts/arch-doc.mjs` 로 + CI `--check` 스텝 분리. | no |
| TESTDOC2613-12 | **SPLIT** | low | 사실: 서버 테스트가 `web/src` 문구 파일을 읽어 대조하는 것은 13파일(tokenCheck·commMap·dataFlow·devFlow·audit2600f(invCheck) 등)이고 규약 L2979(v2.584 "키 집합 1:1 을 서버 테스트가 대조")는 기능별로만 적용됐다 — 구조적 비일관성은 맞다. 반증: 표본 대조 결과 **지금 드리프트 0** — `sanPerfDiag` 서버 12종 = 웹 12종, `edgeLog` 웹 11키 ⊇ 서버 8종(+ `t(res.reason)` 폴백). `settingsCheckText` 는 **목록에 잘못 들어갔다** — 웹은 서버가 보내는 `f.fix` 문구를 그대로 그리므로(`settingsCheckText.js:99`) code→text 맵 자체가 없다. 웹 테스트의 '서버 목록 사본' 은 `sanPerfDiagText.test:18-26` 이 확인, `partFaultText.test:398`·`cvpText.test` 배열은 웹 로컬 키라 사본이 아니다. | 서버 `KINDS` export + 대조 표 하나(선택). 우선순위 낮음 — 드리프트가 없다. | 가능(서버 KINDS ↔ 번들 `*_TEXT` 키) — 단 웹에 키 맵이 있는 기능에 한함. |
| TESTDOC2613-13 | **CONFIRMED** | low | BIG_JSON 등록 **21**(central 18 + `admin/log-analysis/paste` + svcmon 2), `centralRouter.post` **31**, 게이트 밖 **13**(발견 목록과 동일) · `MIXED_STATE_FILES` 테스트 참조 **0** · `onIntervalsChange` 참조 **1** — 전부 재현. 규약 근거 L2396(v2.503 "새 push 경로는 BIG_JSON 등록 확인")·v2.574 열거 스윕 원칙. 13경로는 발견도 결함으로 주장하지 않았고 `fleetPush.js:7` 이 "본문은 작아(메타만)" 를 명시 — '작다' 의 기준이 코드에 없다는 지적은 타당하나 결함 아님. | `test/bigJson.test.js`(POST 전수 − 등록 = 허용 목록 deepEqual) + MIXED_STATE 스윕. | 가능(BIG_JSON: `app._router.stack` 대조 + ingestStats 관측 최대 본문). MIXED_STATE 는 실행 필드 목록이 코드에 있어야 함. |

## 중복·병합
- 04 의 IMP-06 부분은 **05 에 포함**(같은 수정). 09·10 은 둘 다 CLAUDE.md 낡음(원인 같음 — 산문 백로그) · 07 의 문서 수치 낡음도 같은 계열. 02·08 은 '테스트 공용 헬퍼' 제안을 공유.

## 검증에서 새로 본 것(발견 밖)
- `audit2606g`(v2.611 이 비율로 고친 파일) 안에도 `tNew < 60` 절대 상한 5곳이 남아 있다 — 규약 문장이 '비율' 을 `tOld` 쪽에만 적용했으므로 위반은 아니나 02 의 대상 집계에 넣어야 한다.
- CI 의 `api-doc --check` 는 `continue-on-error` 지만 `npm test` 의 apiDoc2563 첫 테스트가 같은 검사를 차단형으로 돌린다(01 부수 관찰).

## 확인하지 못한 것(정직 기록)
- 08: 12개 테스트의 개별 단언이 2줄 제거기 때문에 지금 오판 중인지(false pass)는 검사하지 않았다 — 파일 단위 교집합까지만.
- 06: 발견의 '12개' 산출 규칙을 재현하지 못했다(두 규칙으로 24·2).
- 11: `docs/CVP.md` ↔ 코드 일치는 발견 주장을 재확인하지 않았다.


## 수정 그룹 보고 (a)

# fix-a — G1 특수 기능 카탈로그·도구 셸·집행 매핑 (v2.613)

기준 커밋 8ece9d9. 변이 검증은 `wt-a`(detached worktree) 에서 ID 별로 되돌려 실행했고 끝나면 worktree 를 지웠다.
회귀: `web/src/views/audit2613a.test.js`(30건) · `server/test/audit2613a.test.js`(4건) · `web/src/hooks/hashTab.test.js`(+2건, CATALOG2613-09).
기존 테스트: 서버 13파일 185건(arch2582·audit2447·audit2506·audit2602e·audit2605e·audit2611b·linkCheck2552·logAnalysis2583·
tokenCheck2560·toolCategories2455·v4Portal2508·userTools2555·audit2613a) 전부 통과 · 웹 13파일 165건 통과 · eslint 변경 파일 0 error
(PartFaults.jsx 의 exhaustive-deps 경고 3건은 기존 것 — 내 변경과 무관한 줄).

## ID 별

| ID | 변경 | 테스트 | 변이(되돌리면) |
|---|---|---|---|
| CATALOG2613-01 | `web/src/views/specialToolsList.js:58-61,90-95` — real-os·portaldb·serveranalysis·nic-speed·nic-models `adminOnly:true` · fleet·powermap·topo3d `perm:'insights'`. shutdown 은 그대로(v2.590 W2). | 웹 `CATALOG2613-01` 10건 — docs/API.md 게이트 열을 파싱해 주 API 가 `역할 admin` 이면 adminOnly, `/api/insights` 그룹(마운트 requirePerm('insights'))이면 perm 을 대조 | specialToolsList.js 되돌림 → **8건 실패** |
| CATALOG2613-02 | `web/src/components/IpmsMatches.jsx`(toolAllowed('ipam') 이 거짓이면 조회하지 않고 `IPMS_DENIED_TEXT` · 조회 실패는 `ErrorBox`) · `web/src/views/Nsx.jsx:293-320` SegmentVms(같은 규칙, `IPAM_DENIED_TEXT`). 배지·TrendKpi 는 설계 — 그대로 | 웹 `CATALOG2613-02` 3건(두 파일 소스 + **스윕**: 소유 화면(SpecialTools lazy import 파일·views/tools/**) 밖에서 매핑된 `/tools/<seg>` 를 부르는 파일은 `toolAllowed(` 를 갖거나 허용 목록(11개, 사유)에 있다) | 두 파일 되돌림 → **3건 실패** |
| CATALOG2613-03 | `server/src/auth/toolAccess.js:48-53`(`current-users`·`horizon-sessions`→`curuser`) · `:130-137`(`vm-track/ds-list`·`ds-pivot`·`ds-series-all`→`storage-track` — ds-series/ds-top/ds-changes 는 공유라 제외) · `:152-160` `UNMAPPED_TOOL_SEGMENTS`(ip-ping·vclogs·groups + 사유) · `:325-327` nsx 노트 → `/api/nsx(requirePerm('inv.nsx'))` · `:356-357` storage-track 노트 실제 목록 | 서버 `CATALOG2613-03` 3건 — 매핑 실행 + 공유 경로의 웹 호출부 근거 + **실제 `api` 라우터 스택을 열거해** 미매핑 세그먼트 == UNMAPPED 선언 + 노트 문장·`/nsx` 게이트 별칭 대조 | toolAccess.js 되돌림 → **3건 실패**(12 도 1건 실패) |
| CATALOG2613-05 / WEB2613-13 | 코드 변경 없음(TOOL_VIEWS 맵 리팩터는 하지 않았다 — `tokenCheck2560.test.js:470` 이 `tool === 'portal-check' && <PortalCheck />` 문자열을 고정하고 있어 맵으로 바꾸면 그 테스트가 깨진다). 계약만 테스트로 세웠다. | 웹 `CATALOG2613-05/WEB2613-13` — ToolPanel 의 `{tool === 'k' && <` 집합 == 카탈로그 키 − external/topTab/comingSoon, 중복 분기 0 | 합성 변이(cvp 분기 1줄 삭제) → **1건 실패** |
| CATALOG2613-06 / WEB2613-04 | `PortalCheck.jsx:24,549-551` · `LinkCheck.jsx:21,72-73` · `PartFaults.jsx:21,43-44` — `useHashTab({ base:['tools',<k>], valid, fallback })`. **SanSwitchTool 은 손대지 않았다**(아래 '검증표와 다른 판단') | 웹 `CATALOG2613-06/WEB2613-04` 4건(3파일 base/fallback + 스윕: views/tools 에서 최상위 `const VIEWS = [` 를 가진 파일은 useHashTab) | 3파일 되돌림 → **4건 실패** |
| CATALOG2613-07 | `web/src/views/toolVisibility.js:39-71` `lockReasonOf(t,{isAdmin,toolsAllowed,can,toolAllowed})`(순수 — api.js 를 import 하지 않고 판정 함수를 인자로) · `SpecialTools.jsx:10,219-221`(로컬 구현 제거, `toolExplicitlyAllowed` import 제거) · `version_4/pages/Tools.jsx:18,35-41`(`locked`·`openable` 이 lockReasonOf, 🔒 title 에 사유) · `version_4/V4App.jsx:26,155-160,198-206`(내비 도구 항목에 🔒·회색·title 사유 — **예전엔 잠금 표시가 전혀 없었다**) · `version_4/Palette.jsx:8-10,30-36,58-73`(제안 행에 🔒 접두 + title) | 웹 `CATALOG2613-07` 4건(순서·문구 3종·admin/명시 허용·백틱 0·네 화면이 같은 import 를 쓰고 로컬 판정 0) | 5파일 되돌림 → **4건 실패** |
| CATALOG2613-08 | `SpecialTools.jsx:533-543` 7개 인라인 `isAdmin ? … : '관리자 전용'` 제거(capacity-advisor·dir-usage·mail-diag·vmprovision·agent-scans·login-fails·net-issues) — 관리자 판정은 서버 403 + AccessDenied 하나 | 웹 `CATALOG2613-08` — `&& (isAdmin ? <` 0 · 인라인 문구 0 · 7키가 다른 21개와 같은 모양 | SpecialTools.jsx 되돌림 → **1건 실패** |
| CATALOG2613-09 | `web/src/hooks/hashTab.js:41-55` `MOVED_TABS` 에 `explore:['tools','explore']`·`nsx:['tools','nsx']` | `hashTab.test.js` `CATALOG2613-09` 2건 | hashTab.js 되돌림 → **2건 실패** |
| CATALOG2613-11 | 주석의 도구 수 삭제: `version_4/tree.js:4` · `toolSections.js:5` · `server/src/toolcats/catalog.js:8,32-33` · `SpecialTools.jsx:16,141,252` · 추가로 같은 결함이 있던 `version_4/pages/Tools.jsx:4,9` · `V4App.jsx:6` | 웹 `CATALOG2613-11` 6파일 — `(도구|기능|카드) N개/장` 패턴 0 | 6파일 되돌림 → **6건 실패** |
| CATALOG2613-12 | `toolAccess.js:18-23` 머리말 규약 한 줄 + `:139-149` `LEGACY_SEGMENT_KEYS`(frozen Set 18개 = 기존 16 + 이번에 매핑한 current-users·horizon-sessions — 라우트는 v2.525 부터 있던 옛 세그먼트) | 서버 `CATALOG2613-12` — 세그먼트≠키 집합 == LEGACY · frozen · ≤18 · 두 세그먼트 표는 report/·vm-track/ 만 · 매핑 키 전부 카탈로그 실재 | toolAccess.js 되돌림 → **1건 실패** |

## 검증표와 다른 판단
- **CATALOG2613-06 의 SanSwitchTool 은 대상이 아니다.** 검증표는 "SanSwitchTool(`ports`/사용량) 도 같은 상태" 라 했지만 `SanSwitchTool.jsx:79` 의
  `tab` 은 **포트 상세 모달(PortDetail)의 탭**이다 — `:192` 에서 `setDetail(...)` 로 장비를 열 때 `setTab('ports')` 로 초기화되고 `:1316-1327` 은
  `d.deviceId` 가 있는 모달 안에서 그려진다. 검증표 자신이 '모달 내부 탭은 제외' 라 했으므로 `#/tools/san-switch/perf` 같은 주소는 뜻이 없다(장비 id 가 없다).
  스윕 테스트도 그래서 '최상위 `const VIEWS`' 기준이고 SanSwitchTool 은 걸리지 않는다.
- **CATALOG2613-05 의 TOOL_VIEWS 리팩터는 하지 않았다**(선택 사항) — 위 표의 이유(tokenCheck2560 의 문자열 고정).
- CATALOG2613-07 검증표는 "Palette 의 잠금 판정" 을 말했지만 팔레트에는 원래 잠금 개념이 없었다(숨김만). 제안 행에 🔒 와 사유 title 을 붙이는
  최소 형태로 넣었다 — 숨김 축(`visibleTools`)은 그대로다.

## 행동 변화(사용자에게 보이는 것 — 리드가 Chromium 으로 볼 것)
- 01: 비관리자에게 real-os·portaldb·serveranalysis·nic-speed·nic-models 카드가 **회색 잠금**(그리드) · V4 내비·팔레트에서 **숨김**(hideAdminOnly).
  insights 권한을 회수한 계정은 fleet·powermap·topo3d 가 잠긴다(기본 매트릭스는 두 역할이 insights 를 보유 — 기본 설치 동작 불변).
- 07: V4 좌측 내비의 잠긴 도구가 🔒·회색으로 보인다(예전엔 표시 없음) · 팔레트 행에 🔒 접두. 1440/400 에서 내비·팔레트 스크린샷 판독 필요.
- 02: ipam 을 거부한 계정이 VM/호스트 검색에서 'IPMS 포함' 을 켜면 '자료가 없습니다' 대신 접근 권한 안내가 뜬다. NSX 세그먼트 상세도 같다.
- 08: 허용 목록으로 명시 허용된 비관리자가 그 7개 도구를 열면 '관리자 전용 기능입니다' 카드 대신 화면 → 서버 403 → AccessDenied(다른 21개와 동일).

## 리드가 할 것
- CLAUDE.md v2.573 항목의 "나머지 등록처는 이미 자동이거나 테스트가 막고 있다 … 남은 수동 작업은 toolAccess.js 하나" 문장 정정(렌더 분기는 이제
  `audit2613a` 웹 테스트가 고정 · `scoped` 30키 인라인 목록은 여전히 미고정 — 카탈로그 `scope:'vcenter'` 플래그는 별건).
- 라우트 변경 없음 → `docs/API.md` 재생성 불필요. `scripts/api-doc.mjs GUARD_NOTE` 변경 없음(새 게이트 헬퍼 없음).
- `pgrep -af "node src/index.js"` 에 1개가 떠 있다 — **내가 띄운 것이 아니다**(이 그룹은 서버를 기동하지 않았다). 다른 그룹 것으로 보인다.

## 남긴 것(정직 기록)
- 브라우저로 보지 않았다(규칙 3 — 빌드 금지). 07 의 V4 내비·팔레트 표시는 렌더 확인이 필요하다.
- CATALOG2613-02 스윕의 허용 목록 11개 사유는 소스 grep 으로 확인했다(VCenterDetail 배지·VCenters TrendKpi 는 검증표의 '설계', 설정 화면 5개는
  admin 설정 화면, 문구 모듈 3개는 호출 없음, EmptyInvModal 은 서버 adminOnly). 실제 403 렌더는 보지 않았다.
- LEGACY_SEGMENT_KEYS 에 current-users·horizon-sessions 를 넣었다 — '새 항목은 세그먼트=키' 규약과 충돌하지 않도록 옛 라우트임을 주석에 적었다.


## 수정 그룹 보고 (b)

# fix-b.md — v2.613 수정 그룹 G2(웹 공용 모듈·화면 규약) 보고

기준 커밋 8ece9d9. 테스트: 웹 `web/src/views/audit2613b.test.js`(vitest 20건) · 서버 `server/test/audit2613b.test.js`(node:test 2건).
변이 검증: 워크트리 `wt-b`(8ece9d9 + 이 그룹의 파일만 복사) — 아래 표. 워크트리는 제거했다. 목 서버는 띄우지 않았다(0).
`cd web && npx eslint <변경 파일 전부>` → **오류 0**(경고 24건은 전부 기존의 `react-hooks/exhaustive-deps`·unused-directive).
관련 기존 테스트 통과: 웹 `fmt·vmSeriesText·sanSwitchPerfText·sensorText·perfMonitorText·remoteCommand·collectDropText·partFaultText·bmUsageText·edgeLogText·relTime·uiText·cssVars`(266건) ·
서버 `arch2582·audit2506·audit2604e·audit2605b·audit2605e·v4Portal2508·tokenCheck2560·userTools2555·commMap2584·audit2575`(163건).

## ID 별

| ID | 바꾼 것(file:line) | 테스트 | 변이(기준판으로 되돌림 → 결과) |
|---|---|---|---|
| WEB2613-01 | `api.js:31-44` `hasRole(...roles)` 추가(현재 사용자 객체를 읽는다 — 부팅 전이면 false). 화면 9곳의 `useState+useEffect(fetchJson('/auth/me'))` 를 `const isAdmin = hasRole('admin')` / `canManage = hasRole('admin','operator')` 로: `NetworkCheck.jsx:57` `VcenterPorts.jsx:14` `Alarms.jsx:18` `tools/VmInfoTools.jsx:37` `tools/GuestDiskReport.jsx:62` `tools/HorizonSessionSettings.jsx:51`(async 블록의 조회도 제거) `tools/CurrentUsersSettings.jsx:61` `tools/IpamCore.jsx:77` `PingMonitor.jsx:68`. **SpecialTools.jsx:193 은 G1 파일이라 손대지 않았다**(허용 목록에 사유). | vitest `WEB2613-01 …` 3건(views/** 스윕 0건 + 허용 목록 실재 + 9곳 hasRole·setIsAdmin 0) | api.js + 9파일 → **2 failed** |
| WEB2613-02 | `tools/HardwareTools.jsx`: `DcErrNote` 컴포넌트 추가(:~19); `/admin/datacenters` 호출 3곳(HardwareSummary :137·ServerInfoByVcenter :205-208·ServerAnalysis :518) 이 `dcErr` 를 든다(`.catch(() => {})`·`{assign:{}}` 폴백 0). '미지정(법인 없음)' 라벨·select 항목·배지가 dcErr 이면 '법인 정보를 읽지 못함'. 안내 카드 3화면(403 이면 권한 문구, 아니면 사유). | vitest `WEB2613-02 …` 2건 | HardwareTools.jsx → **2 failed** |
| WEB2613-03 | `util/fmt.js`: `fmtBytes(n,{dash})` 를 `numOrNull` 기반으로(결측 → `—`, 0 → `0 B`, 'abc' 는 throw 대신 `—`); `fmtAgo(ts,opt)` 는 `relTime.agoText` 껍데기. `vmSeriesText.js:1,16` · `tools/sanSwitchPerfText.js:1,71` 이 import + 재수출(`import…; export {…}` / `export const bytesText = fmtBytes`). `MetricsSettings.jsx` 로컬 `fmtBytes`·`fmtAgo` 삭제 → util/fmt import. `util/fmt.test.js` 기대값 갱신(null → `—`, 미래 → `방금`). ⚠ 표기 차이(정직 기록): vmSeries GB 소수 2자리→1자리, KB 정수→1자리; MetricsSettings 1KB 미만 `0 KB`→`N B`. 테스트가 옛 사본 세 벌을 그대로 넣어 **같은 단위·값(±0.5)** 을 대조하고 결측 갈림(`0 B`/`0`)을 문서화한다. | vitest `WEB2613-03 …` 3건 | fmt.js+vmSeriesText+sanSwitchPerfText+MetricsSettings → **2 failed** |
| WEB2613-08 | `App.jsx:19-22` — `lazy(() => import('./views/tools/IpamCore.jsx').then(m => ({default: m.IpamStandalone})))`. **`SpecialTools.jsx:560` 의 재수출 삭제는 G1 영역 — 하지 않았다**(남겨도 동작하지만 허브 청크에 IPAM 이 계속 동봉될 수 있다 — 리드가 G1 과 함께 삭제해야 번들 분리가 완성된다. 빌드 크기 검증은 이 그룹에서 하지 않았다). | vitest `WEB2613-08` 1건 | App.jsx → **1 failed** |
| WEB2613-09(SPLIT) | `tools/RemoteCommand.jsx:~35` 실행 이력 → `usePolling('/tools/rma/history', {limit:100, _r: refreshTick}, 15_000)`(수제 setInterval·`.catch(()=>{})` 제거 — 403 정지·ETag 획득). `tools/PduTool.jsx:~52` 목록 → `usePolling('/tools/pdu', {_r: rev}, 30_000)`; `load()` 는 `_r` 을 올려 즉시 재조회, 삭제·저장 실패 사유는 `actErr` 로 분리. 후속 12곳(허용 목록에 사유): VmCloneTool·IpamSettings×2(403 정지 있음)·BmStorageTool·StorageTrackTool·HardwareTools:687·IpamNet(403 정지 있음)·CapacityTools(경과초 표시)·SanSwitchTool(G1)·ShutdownTool·StorageMonTool·DirUsageReport·RelayCheckTool. | vitest `WEB2613-09 …` 2건(views/tools setInterval 이 후속 목록 밖 0 + 두 화면 usePolling) | RemoteCommand+PduTool → **2 failed** |
| WEB2613-10 | 직접 `fetch(` 7곳 → `api.js`: `tools/GpuTool.jsx:93,372` `downloadFile`(Content-Disposition 이름 우선 — zip 전환 유지; `getToken`·`saveResponseAsFile`·`dayStamp` import 제거) · `tools/IpamSettings.jsx:92` `delJson` · `tools/IpamNet.jsx:48,358` `delJson`(**:48 은 예전에 res.ok 를 보지 않아 403·409 가 성공처럼 보이던 무음 실패 — 이제 throw·`ok:false` 를 사유로 말한다**; `authHdr` 삭제) · `PortalBackup.jsx:44` `downloadFile(path, name)` · `DavinciChecks.jsx:111` `downloadFile`(서버가 항상 CD 를 준다). | vitest `WEB2613-10 …` 2건(views/** `\bfetch(` 0 + 옮긴 5파일) | 5파일 → **2 failed** |
| WEB2613-11 | 제품 변경 없음(스윕 공백). `server/test/audit2613b.test.js`: (b) `web/src` JSX 인라인 `textTransform: uppercase` 스윕 — 허용 목록 파일·개수·사유(About ×3 · StorageGrowthTool ×1 · ServerTempBoard ×1 · parts.jsx MonoLabel ×1), 목록 항목 실재 검사 포함. (c) 서버 문구 모듈 열거(`*Text.js`·`*Advice.js`·`remedy.js`·`*Remedy.js`·`*Findings.js` — 현재 7개)에서 템플릿 리터럴 이스케이프 백틱 + `'`\w` 형태 0건. 허용: `relaycheck/remedy.js`(steps 를 web `RelayCheckTool.jsx:142` 가 `<code>` 로 그린다 — BoldText 아님, 셸 명령 인용). **uiText.test.js 는 손대지 않았다**(web/src 만 훑는 그 스윕은 그대로 유효하고, 추가한 것은 서버 쪽뿐). | node:test 2건 | 합성 변이 — `Overview.jsx` 에 uppercase 4곳 주입 → (b) **fail**; `util/bulkAdvice.js` 에 `\`host\`` 템플릿 추가 → (c) **fail**; 되돌리면 2 pass |
| WEB2613-12 | `views/idrac/sensorText.js:14` `import { TEMP_WARN_C, TEMP_HOT_C } from '../tools/serverTemp/board.js'`; `tempColorOf` 가 상수를 쓴다(:~92). `sensorText.test.js` 기대값도 상수(+`TEMP_HOT_C-1` 경계 추가). | vitest `WEB2613-12 …` 2건(import + web/src 전체 `>= 40 ?`·`>= 32 ?` 0, HardwareTools:884 의 32 는 Gbps 라 제외) | sensorText.js → **2 failed** |
| DEPS2613-11(SPLIT) | 신규 `views/tools/toneVar.js`(순수 코어 — partFaultText 의 규칙·테마 토큰 `--green/--amber/--red/--text-faint`). `bmUsageText.js:62`·`edgeLogText.js:49`·`partFaultText.js:328` 은 import + 재수출. ⚠ BmUsage·EdgeLog 의 색이 hex 폴백(`#4ade80` 등, 정의되지 않은 `--ok/--warn/--bad`)에서 테마 토큰으로 바뀐다(두 테스트 기대값 갱신). `relTime.js elapsedText` 의 `subMinute` 에 숫자(ms) 임계 허용(remoteCommand 의 '5초 미만 = 방금' 계약 보존). `ago` 사본 → `agoText`: `BulkDeploy` `tools/RelayTopoTool` `tools/RelayCheckTool` `tools/StorageMonTool` `tools/PduTool` `collectors/EmptyInvModal` `PingMonitor`(floor→round · 하루 이상은 `N일 전`); `perfMonitorText.js` 는 `export { agoText as ago }`; `tools/remoteCommand.js` 는 `agoText(ts, now, {subMinute: 5000})` 껍데기; `tools/collectDropText.js` 는 `elapsedText(ms, {dash:'', subMinute:'방금', future:''})`(1시간 이상 `N시간 M분 전` → `N시간 전`). `fmtAgo` 사본 → `util/fmt.fmtAgo`: `MetricsSettings`(dash '없음') `CapacityAdvisor`(90초/5400초 임계 → 코어 60/3600 — 그 임계의 근거가 없었다) `gpu-guest/shared.jsx`(삭제 · `VmCredManager`·`GpuGuestSettings` 가 `fmtAgo(ts,{dash:'없음'})` 직접) `GpuGuestDiag`(dash '없음') `EdgeUserDeploy`(호출부가 at 있을 때만) `DavinciChecks`(동일). **손대지 않은 사본(G1 파일)**: `tools/SanSwitchTool.jsx:39 ago`(5회 사용) · `tools/PortalCheck.jsx:49 ago`(사용 0 — 죽은 코드). 허용 목록에 사유. | vitest `DEPS2613-11 …` 5건(정의 스윕: 코어 위임 한 줄 껍데기만 허용 · toneVar 정의 위치=코어 하나 · 세 모듈 동일 함수 · fmtAgo/agoText/elapsedText 규칙) | 전량 되돌림(toneVar.js 삭제) → **모듈 로드 실패(Test Files 1 failed)**; toneVar.js 만 남기고 사본 7파일 되돌림 → **3 failed** |

## 손댄 파일(49)
web/src/api.js · App.jsx · util/fmt.js · util/fmt.test.js · views/tools/relTime.js · views/tools/toneVar.js(신규) · views/{NetworkCheck,VcenterPorts,Alarms,PingMonitor,BulkDeploy,CapacityAdvisor,DavinciChecks,EdgeUserDeploy,GpuGuestDiag,GpuGuestSettings,MetricsSettings,PortalBackup}.jsx · views/collectors/EmptyInvModal.jsx · views/gpu-guest/{VmCredManager,shared}.jsx · views/idrac/sensorText.js(+test) · views/perfMonitorText.js · views/tools/{CurrentUsersSettings,GpuTool,GuestDiskReport,HardwareTools,HorizonSessionSettings,IpamCore,IpamNet,IpamSettings,PduTool,RelayCheckTool,RelayTopoTool,RemoteCommand,StorageMonTool,VmInfoTools}.jsx · views/tools/{bmUsageText,collectDropText,edgeLogText,partFaultText,remoteCommand,sanSwitchPerfText}.js · views/vmSeriesText.js · views/tools/{bmUsageText,edgeLogText}.test.js · views/audit2613b.test.js(신규) · server/test/audit2613b.test.js(신규).

## 남긴 것(정직 기록)
- **needs file `web/src/views/SpecialTools.jsx`(G1)**: (a) `:193` `fetchJson('/auth/me')` → `hasRole('admin')` + `getCurrentUser()?.serviceHubUrl`(⚠ AUTH_ENABLED=false 이면 App 이 `{name:'Anonymous', role:'admin'}` 만 채워 serviceHubUrl 이 없다 — 그 경우만 `/auth/me` 폴백을 두거나 App 이 `/auth/me` 응답으로 채우게 해야 한다) (b) `:560` `export { IpamStandalone } from './tools/IpamCore.jsx'` 삭제(WEB2613-08 의 번들 분리 완성). 두 허용 목록(`WEB2613-01`·`DEPS2613-11`)이 G1 파일을 사유와 함께 들고 있다 — 리드가 고치면 목록에서 빼면 된다(항목 실재 검사가 있어 빼지 않으면 실패한다).
- **needs file `web/src/views/tools/SanSwitchTool.jsx`·`PortalCheck.jsx`(G1)**: `ago` 사본(SanSwitch 5회 사용 · PortalCheck 사용 0).
- WEB2613-09 후속 12곳(위 표) — 그중 IpamSettings·IpamNet 은 이미 403 정지가 있어 대상 아님, CapacityTools 는 네트워크가 아니다.
- WEB2613-11(a) 조기 return 뒤 훅 스윕은 만들지 않았다(검증에서 진양성 0·후순위). (b) 의 MonoLabel 은 `{label}` prop 을 받는 구조라 '값이 들어가지 않는다' 를 소스로 고정하지 못했다 — 허용 목록 사유에 주의를 적었다.
- WEB2613-03: `fmtAgo(ISO 문자열)` 은 이제 `NaN시간 전` 대신 `—`(결측 표기)다 — ISO 를 파싱하지는 않는다(호출부 전부 epoch ms — 정직 기록).
- 화면(Chromium) 검증은 하지 않았다(그룹 규칙 — 리드의 통합 검증). 표시 변경 요약: DEPS/WEB-03 의 표기 차이(위 표), BmUsage·EdgeLog 톤 색(hex → 테마 토큰), HardwareTools 안내 카드 3곳.
- 릴리스 노트·CLAUDE.md·docs 는 건드리지 않았다.
- 수행 시간 약 17분(변이 검증 포함).


## 수정 그룹 보고 (c)

# fix-c — G3 서버 의존 방향·동적 import·설정 pull 404 (v2.613)

기준 커밋 8ece9d9 · 회귀 테스트 `server/test/audit2613c.test.js`(6건) + `server/test/arch2579.test.js` 개정(②-c 신설 · ③ 정규식/POOL_EXCLUDED).
수행 시간 약 16분(변이 검증·워크트리 포함). 목 서버 기동 0.

## ID 별

### DEPS2613-01 — envTimeout/central404 를 util/ 로 (CONFIRMED)
- `server/src/util/envTimeout.js`(신규, 본체 이동) · `server/src/util/central404.js`(신규, 본체 이동). 머리말에 이동 사실 기재.
- `server/src/agent/envTimeout.js` · `server/src/agent/central404.js` → `import {…} from '../util/…'; export {…};` 재수출 셸(v2.575 규약).
  agent/ 밖 18개 importer 는 옛 경로 그대로 동작(같은 함수 정체성 — 테스트가 `===` 로 고정).
- `agent/centralReply.js` 는 지시대로 손대지 않았다.
- `test/arch2579.test.js` ②-c 신설: `config.js` 의 정적·동적 import 대상은 `util/`·`security/`·node 내장만.
- ⚠ **needs file `server/src/config.js:5`** (내 파일 집합 밖): `'./agent/envTimeout.js'` → `'./util/envTimeout.js'` 한 줄. 이것을 적용하기 전까지
  arch2579 ②-c 가 메인 트리에서 실패한다(현재 그 상태 — 의도된 실패). 워크트리 검증은 그 한 줄을 적용한 상태로 했다.
- 테스트: `DEPS2613-01`(audit2613c) — 본체 위치·재수출 형태·util 본체가 leaf 인지 · arch2579 ②-c.
- 변이: 셸 2 + util 2 삭제 + config.js 되돌림 → audit2613c #1 FAIL(+#4 도 import 실패로 FAIL) · arch2579 ②-c FAIL.
- 문서: `docs/ENV.md:365` 가 정의 위치를 `agent/envTimeout.js` 로 적고 있다 — **lead 가 생성기(`node scripts/env-doc…`, ENV.md 생성기)를 다시 돌릴 것**.

### DEPS2613-02 — redfish.js 같은 모듈 정적+동적 이중 import (SPLIT → (a) 만)
- `server/src/idrac/redfish.js:21` 정적 import 에 `pickReports, buildIdracUsage` 를 더하고 `:1204` 의 동적 import 한 줄 삭제(2줄 변경).
  파서 이동은 지시대로 생략.
- 테스트: `DEPS2613-02` — src 전수에서 '정적으로 가져온 모듈을 동적으로 또 import' 하는 곳을 센다. **기지 잔여 7곳**(bmusage/poller→config ·
  routes/api/{cvp,edgeLog,horizonSessions,partFaults} · routes/central→collector/state · svcmon/formats→util/csv)은 내 파일이 아니라 고치지
  않고 `KNOWN_STATIC_DYNAMIC_DUP` 로 고정(늘면 실패·줄면 통과). redfish 항목이 그 안에 없음을 별도 단언.
- 변이: redfish.js 되돌림 → #2 FAIL.
- ⚠ **needs file(문구만) `server/src/bmusage/parse/idracTelemetry.js:42`**: 주석 "redfish.js 가 이 파서를 **동적** import 하므로" 는 이제
  정적이다 — "정적 import 하므로 반대 방향 import 는 2-cycle" 로 고칠 것(결론은 그대로 옳다 — verify 의 판정과 같다). `redfish.js:147` 주석은 그대로 옳다.

### DEPS2613-03 — 거짓 '순환 회피' 주석의 동적 import 3곳 (SPLIT → 확정 3곳)
- `server/src/collector/registry.js` `tokenFetch`: `resilientFetch` 정적 import(:16 추가) + 동적 import·낡은 주석 삭제, 사유 주석으로 교체.
- `server/src/partfault/hooks.js`: `./push.js`·`./poller.js` 정적 import(:12-13), 콜백의 동적 import 2곳 제거, 머리말 '순환 방지' 문장 교체.
  arch2579 ②-b(SCC ≤4) 그대로 통과 — 되돌이 경로 없음 재확인.
- 테스트: `DEPS2613-03` — src 전수 동적 import 마다 ±4줄(원문, 주석 포함) 에 '순환' 이 있으면 **정적 되돌이 경로(BFS)** 가 있어야 통과 +
  되돌이 경로 있는 동적 import 는 정확히 3곳(`config.js→security/secretVault.js`, `sanswitch/perfPush.js→sanswitch/perfPoller.js`,
  `vcenter/restClient.js→vcenter/soapClient.js`)으로 고정 + 고친 3곳이 정적 import 인지.
- 변이: hooks.js 되돌림 → #3 FAIL · registry.js 되돌림 → #3·#5 FAIL.

### DEPS2613-06 + EDGE2613-06 — 설정 pull 4벌의 404 사유 판정 (CONFIRMED)
- `server/src/agent/{gpuGuestConfigPull,pduConfigPull,storageConfigPull,usersConfigPull}.js`: `classifyCentral404` import(`../util/central404.js`) +
  `if (res.status === 404) { … }` 분기(각 9줄). 상태(`last`/`_last`)에 `ok:false, kind, error|reason`, 반환 `{ok:false, kind, reason}`
  (gpuGuest·users 는 기존 `error` 필드도 유지), `_logChange('404', …)` 로 콘솔(10분 창). 응답 모양 변경 없음(추가 필드만).
- 테스트: `DEPS2613-06·EDGE2613-06` — ① `agent/*ConfigPull.js` 전수(9개)에 `classifyCentral404(` 있는지 ② in-process HTTP 목 중앙이
  JSON `{ok:false,reason:'central 비활성화'}` / JSON 다른 사유 / express 식 HTML 404 를 돌려줄 때 4모듈 각각 반환 kind
  (disabled/refused/no-endpoint)·상태 kind·ok=false·사유·콘솔 줄을 단언.
- 변이: 4파일 각각 되돌림 → #4 FAIL(4/4).

### EDGE2613-08 — findCollector 사본 2벌 → registry 하나 (CONFIRMED)
- `server/src/collector/registry.js` `findCollectorByName(agent)` 신설(name/id 대소문자·공백 무시, 원본 토큰 포함, 빈 키 → null).
- `server/src/central/idracScanPush.js`: 사본 삭제, `import { findCollectorByName }` + `export { findCollectorByName as findCollectorForAgent }`
  (bmstor/poller.js·idracScanPush.test.js 호환), 내부 호출 2곳 교체. `loadCollectors` import 제거.
- `server/src/central/edgeLogPull.js`: **내 편집은 메인 트리에 남아 있지 않다** — CONTRACT2613-01·EDGE2613-02 그룹이 이 파일을
  `central/edgePull.js` 사다리로 재작성했고, 그 `edgePull.js:31` 이 이미 `findCollectorByName` 을 registry 에서 import 해 `findCollector`
  래퍼(try→null)를 제공한다(내 설계와 동형). 워크트리 변이 검증은 내 판본(정적 import + 래퍼)으로 했다.
- 테스트: `EDGE2613-08` — 실제 등록부에 항목을 넣고 4가지 표기로 조회·원본 토큰·`findCollectorForAgent === findCollectorByName`·
  edgeLogPull `findCollector` 래퍼 동치 + 소스 스윕(registry 밖에서 `.find(` + `.name` + `toLowerCase()` 한 줄 사본 0 · edgeLogPull 의
  registry 동적 import 0).
- 변이: idracScanPush.js 되돌림 → #5 FAIL · edgeLogPull.js(기준판) → #5 FAIL.

### RUNTIME2613-03(b) — arch2579 ③ 정규식 (SPLIT (b))
- `test/arch2579.test.js`: `POOL_SCAFFOLD_RE` = `Array.from({ length: Math.min|max(…중첩 1단…) }, async …|worker)` — 콜백이 인덱스를 받는
  매핑(`(_, k) =>`, nsx/mock.js·idrac/partsInventory.js)은 제외. v2.594 '결함 아님' 7곳(6파일)을 `POOL_EXCLUDED` 에 사유와 함께 명시.
  기준 트리에서 유일하게 잡히는 것이 `routes/api/portalCheck.js:266` 이고(의도), 메인 트리에서는 다른 그룹이 (a)를 `poolSettled` 로 바꿔
  ③ 이 **현재 통과**한다.
- 테스트: `RUNTIME2613-03(b)`(audit2613c) — arch2579 소스(주석 제거)에서 옛 리터럴 부재·정규식 추출 후 5개 표본 판정·POOL_EXCLUDED 사유 6파일.
- 변이: arch2579 되돌림 → #6 FAIL.

## 변이 매트릭스(워크트리 wt-c, 기준 8ece9d9 + 내 파일 + config.js 1줄 + edgeLogPull 내 판본)
| 되돌린 것 | audit2613c | 비고 |
|---|---|---|
| 셸 2 + util 2 삭제 + config.js | #1 FAIL, #4 FAIL | arch2579 ②-c FAIL |
| idrac/redfish.js | #2 FAIL | |
| partfault/hooks.js | #3 FAIL | |
| collector/registry.js | #3 FAIL, #5 FAIL | |
| gpuGuest / pdu / storage / users ConfigPull | #4 FAIL ×4 | |
| central/idracScanPush.js | #5 FAIL | |
| central/edgeLogPull.js | #5 FAIL | |
| test/arch2579.test.js | #6 FAIL | |
전부 복원 뒤 6/6 통과. 워크트리 제거 완료.

## 기존 테스트
메인 트리에서 통과: idracScanPush · pullCollectorByAgent · collectorSelfRegister · collectorIdentity2424 · idracPollHook2583 · audit2600c ·
audit2602f · audit2606b · redfishCollections2548 · bmUsage2551 · audit2611e · audit2603a · audit2612d · audit2607e · tokenCheck2560 · audit2601f.
⚠ 메인 트리에서 실패하는 3개(`secOutbound2583` #18 · `audit2600b` · `audit2606a`)는 **기준 트리 + 내 파일만** 둔 워크트리에서 전부 통과했다 —
다른 그룹의 진행 중 편집이 원인이다(secOutbound2583 #18 은 `central/edgeLogPull.js` 가 `readJsonCapped(res,` 를 더 이상 직접 부르지 않는
CONTRACT/EDGE 그룹 재작성 때문 — 그 그룹 또는 lead 가 그 테스트의 파일 목록을 `central/edgePull.js` 로 옮겨야 한다).

## 손댄 파일
server/src/util/envTimeout.js(신규) · server/src/util/central404.js(신규) · server/src/agent/envTimeout.js · server/src/agent/central404.js ·
server/src/agent/gpuGuestConfigPull.js · server/src/agent/pduConfigPull.js · server/src/agent/storageConfigPull.js · server/src/agent/usersConfigPull.js ·
server/src/idrac/redfish.js · server/src/collector/registry.js · server/src/partfault/hooks.js · server/src/central/idracScanPush.js ·
server/test/arch2579.test.js · server/test/audit2613c.test.js(신규). (central/edgeLogPull.js — 편집했으나 다른 그룹 재작성에 덮임, 위 참조.)

## 남긴 것 / lead 가 할 일
1. `server/src/config.js:5` import 경로 `./util/envTimeout.js` 로(1줄) — 없으면 arch2579 ②-c 실패.
2. `docs/ENV.md` 재생성(정의 위치 `agent/envTimeout.js` → `util/envTimeout.js`).
3. `bmusage/parse/idracTelemetry.js:42` 주석 문구("동적 import" → "정적 import").
4. DEPS2613-02 사이드: 정적+동적 이중 import 잔여 7곳은 고정 목록으로만(다음 점검 후보) — 이 그룹 범위 밖.
5. DEPS2613-12(clamp) 는 G5.
6. `secOutbound2583.test.js:139` 파일 목록 — edgeLogPull 재작성 그룹과 맞출 것.


## 수정 그룹 보고 (d)

# fix-d — G4a 중앙 pull 사다리 통합 · 버전 게이트 · 호출 태그 (v2.613)

기준 커밋 8ece9d9 · 회귀 `server/test/audit2613d.test.js` 5건 + 웹 `cvpText.test.js` +3건. 변이 검증은 worktree `wt-d`(제거함)에서 수행.
소요 약 15분(도구 호출 기준). 서버 프로세스 기동 없음(`pgrep` 0).

## ID 별

### CONTRACT2613-01 + EDGE2613-02 — 중앙 pull 사다리 하나(`central/edgePull.js`)
- **신규** `server/src/central/edgePull.js` — `pullFromEdge(agent, path, {timeoutMs, retries, fetchImpl, bodyOk, minVersion, what, label, authReason})` →
  `{ok, kind?, reason?, ms, body?, col, fetched}` · `EDGE_PULL_KINDS`(10종) · `kindFor404(body,{path,minVersion,what})` · `findCollector`(→ `collector/registry.js findCollectorByName` 래퍼).
  사다리: findCollector → enabled/url → withOutboundTag(col.id)(fetchImpl=resilientFetch) → catch timeout/unreachable → readJsonCapped → 401/403 → 404 kindFor404 → !ok http → bad-body.
  `fetched:false` = 등록부 단계 실패(호출부가 보관소에 남기지 않는다 — 예전 동작 그대로).
- `central/edgeLogPull.js`(전면 축소, :1-46): 쿼리 조립 + `putEdgeLog` 만. `findCollector` 재수출(`import … ; export { findCollector }`), `MIN_EDGE_VERSION`·`EDGELOG_PULL_TIMEOUT_MS` 유지. auth 사유 문구는 `authReason` 옵션으로 예전 문장 그대로.
- `central/bmUsageEdgePull.js`(:140-156): `pullBmUsage` 가 코어 호출 + `putEdgeBmUsage`. 정제(`sanitizeBmUsageSnap`)·env·STALE_MS 그대로.
- `central/tokenCheckPull.js`(:109-129): `pullTokenCheck(agent,{selfProbe,fetchImpl,timeoutMs})` 시그니처 유지, `retries: 0` 유지, `const name = col.id || col.name || agent;` 줄 유지(audit2583 소스 고정).
- **드리프트 해소**: 404 본문 `reason` 은 셋 다 `strOf`(v2.604 CEN2604-03) — reason 이 객체면 셋 다 `old-version`(예전: edgeLog `disabled/'[object Object]'` · tokenCheck `disabled/'(형식 오류)'` · bmUsage `old-version`). http 사유의 `(reason)` 도 strOf(edgeLog 의 `[object Object]` 제거). bad-body 판정은 `isObj(body) && ok!==false && isObj(node)`(bmUsage 의 엄격한 쪽으로 통일 — edgeLog·tokenCheck 는 `!!body.node` 였다).
- 테스트: `CONTRACT2613-01:`(EDGE_PULL_KINDS ↔ 웹 `edgeLogText.FETCH_KIND_TEXT` 키 1:1 + 세 모듈 사다리 조각 0 소스 스윕) · `EDGE2613-02:`(목 엣지로 세 모듈을 같은 입력 6종으로 돌려 kind·reason 동일 + 응답 모양·보관소 lastAttempt + 코어의 readJsonCapped/전역 fetch 0 + `kindFor404(` 1개).
- 변이: 세 모듈 되돌림 → 01·02 실패 / `edgePull.js` 삭제 → 01·02·04·05 실패(import).

### CONTRACT2613-04 — CVP 엣지 버전 게이트
- `central/cvpEdge.js`(:24-71): `MIN_CVP_EDGE_VERSION='2.608.0'` · `CVP_EDGE_KINDS=['old-version','unknown-version','silent','waiting']` · `SILENT_AFTER_INTERVALS=3` · `edgeVersionOf(agent, allCollectorStatus())`(id·자기보고 agent 둘 다 대조, 대소문자 무시 — linkCheck.js 와 같은 판단) · `classifyCvpEdge({edgeVersion, minVersion, sinceMs, intervalMs, now})`(순수 · cmpVersion null 은 낮은 버전으로 보지 않음 · sinceMs 없으면 escalate 안 함).
- `routes/api/cvp.js`(:36-55, 응답 필드만): `statusOf` 가 보고 없는 위임 CVP 에 `{pending:true, ok:null, source:'edge', kind, edgeVersion, minEdgeVersion, note}` — `pending`·`note` 는 그대로 남겨 구버전 웹 호환. silent 기준 시각은 `CENTRAL_STARTED_AT`(이번 기동), 주기는 `loadSettings().intervalMs`(캐시).
- `web/src/views/tools/cvpText.js`(:110-135·:144-152): `EDGE_NO_REPORT_TEXT`(kind → {tone,label,detail}, 백틱 0, 홑화살괄호) + `export CVP_EDGE_KINDS`; `serverState` 가 `st.kind` 있으면 그것을, 없으면(구버전 중앙·중앙 직접) 예전 문구. old-version=bad(현재/필요 버전 + 업그레이드) · unknown-version/silent=warn · waiting=muted.
- 테스트: `CONTRACT2613-04:`(상수·분류표·edgeVersionOf·웹 키 1:1 + 실제 `api` 라우터로 위임 CVP 2대 → kind old-version/unknown-version·edgeVersion·minEdgeVersion) · 웹 vitest 3건(키 1:1 · kind 별 tone/문구 · kind 없음/꺼짐 예전 문구).
- 변이: cvpEdge.js+cvp.js 되돌림 → 04 실패 / cvp.js 만 되돌림 → 04 실패 / 웹 cvpText.js 되돌림 → vitest 2 실패.

### CONTRACT2613-05 — linkcheck minEdgeVersion 을 서버가 준다
- `linkcheck/links.js`(:31-38): `export const MIN_EDGE_VERSION = '2.552.0'`.
- `routes/api/linkCheck.js`(:21·:113): import + `/tools/link-check` 응답 `minEdgeVersion`. 웹 `LinkCheck.jsx:122` 는 이미 `data?.minEdgeVersion` 을 읽고 `linkCheckText.rowState` 의 old-version 분기가 있다(확인 — 웹 무변경).
- 테스트: `CONTRACT2613-05:`(상수 == 웹 기본 인자 '2.552.0' + 실제 라우터 응답 키). 변이: links.js+linkCheck.js 되돌림 → 실패.

### EDGE2613-10 — 중앙→엣지 호출 태그
- `relaycheck/checks.js`(:11·:63-80·:112): `portalPing(…, {tag})` 가 `withOutboundTag(tag, …)` 로 감싼다(ping·health 둘 다). `runCheck` 는 `target.collectorId || target.expectAgent`(poller.js 가 둘 다 col.id 로 채운다). 태그 없으면 예전 그대로.
- `linkcheck/checks.js`(:131·:163·:171): `stepHttp({…, tag})` 옵션 → `recordOutbound(url,{…,tag})` 두 곳.
- ⚠ **linkcheck 쪽은 배선이 반쪽이다** — `stepHttp` 를 부르는 `linkcheck/run.js runLink`(내 편집 범위 밖)가 `tag` 를 넘기지 않으면 실제 폴러 경로에서는 여전히 태그가 없다. 아래 '리드가 할 것' 참조. 테스트는 `stepHttp` 에 tag 를 직접 주는 경로(메커니즘)만 고정한다.
- 테스트: `EDGE2613-10:`(목 엣지로 `runCheck(edge-portal)` → outboundStats `/api/collector/ping` tag='tag-r' · collectorId 없는 대상은 tag '' · `stepHttp({tag})` → tag='tag-l'). 변이: relaycheck/checks.js 되돌림 → 실패 / linkcheck/checks.js 되돌림 → 실패.

## 변이 매트릭스(wt-d, 기준 8ece9d9 + 내 파일 + 의존 `collector/registry.js`)
| 되돌린 것 | 01 | 02 | 04 | 05 | 10 | 웹 |
|---|---|---|---|---|---|---|
| 없음(기준) | ok | ok | ok | ok | ok | 38/38 |
| edgeLogPull+bmUsageEdgePull+tokenCheckPull | **FAIL** | **FAIL** | ok | ok | ok | — |
| edgePull.js 삭제 | **FAIL** | **FAIL** | **FAIL** | **FAIL** | ok | — |
| cvpEdge.js + cvp.js | ok | ok | **FAIL** | ok | ok | — |
| cvp.js 만 | ok | ok | **FAIL** | ok | ok | — |
| links.js + linkCheck.js | ok | ok | ok | **FAIL** | ok | — |
| relaycheck/checks.js | ok | ok | ok | ok | **FAIL** | — |
| linkcheck/checks.js | ok | ok | ok | ok | **FAIL** | — |
| web cvpText.js | — | — | — | — | — | **2 FAIL** |

## 기존 테스트 실행(변경 모듈 관련)
통과: edgeLog2549(16) · tokenCheck2560(41) · audit2601f(7) · audit2583(18) · audit2604a(12) · audit2606a(12) · audit2605f(13) · bmUsage2550(35) · bmUsage2551(21) · bmUsageEnterprise2554(34) · linkCheck2552(19) · relayCheck2430(5) · relayProbe2439(7) · cvp2608a(15) · cvp2608b(7) · cvpChoices2609(7) · audit2589 · 2604d · 2605d · 2607c · 2611c(20 — 한 번 1건 실패 뒤 재실행 20/20: 다른 그룹의 동시 편집 중 실행이었을 가능성, 내 파일과 무관해 보이나 확정하지 못했다) · 2612d · 2613c · settingsCheck2553 · 웹 uiText(4) · cvpText(38).
**예상된 실패 2건(리드 조치 필요)**: 아래.

## 리드가 할 것 (내 편집 범위 밖)
1. **`server/test/secOutbound2583.test.js:138`** — 파일 목록 `['collector/puller.js','central/edgeLogPull.js','central/bmUsageEdgePull.js','central/tokenCheckPull.js']` 에서 뒤 셋을 **`central/edgePull.js`** 로 바꿀 것(응답 읽기가 코어로 옮겨져 세 모듈에 `readJsonCapped(res,` 가 없다 — 현재 그 테스트 1건 실패). 같은 검사가 `audit2613d` EDGE2613-02 안에 이미 있다.
2. **`node scripts/api-doc.mjs`** — `routes/api/cvp.js`(+5줄)·`linkCheck.js`(+1줄) 줄 번호가 밀려 `apiDoc2563` 2건 실패(docs/API.md 재생성).
3. **`server/src/linkcheck/run.js`**(EDGE2613-10 잔여): `specFor` 의 `central->edge` 분기에 `tag: t(c?.id) || t(link.to)` 를 spec 에 넣고, `runLink` 의 `stepHttp({...})` 호출에 `tag: spec.tag || ''` 를 넘길 것(2줄). 이것 없이는 통신 점검 폴러 경로의 태그가 여전히 비어 있다.
4. `web/src/views/tools/CvpTool.jsx:151` — 상태 셀의 detail 을 `sv.label === '실패'` 일 때만 본문에 그린다. old-version 의 조치 문구(업그레이드)가 툴팁(title)에만 남는다(v2.516 '실패 사유를 툴팁에만 두지 말 것'). 조건을 `sv.tone === 'bad' || sv.tone === 'warn'` 로 넓히길 권한다(선택).
5. 의존: `edgePull.findCollector` 는 다른 그룹(EDGE2613-08)이 `collector/registry.js` 에 넣은 `findCollectorByName` 을 쓴다 — 그 변경이 함께 들어가야 한다(기준 커밋에는 없다. 변이 검증 시 registry.js 를 함께 복사했다). 그 그룹이 `edgeLogPull.js` 에 넣은 재수출은 내 재작성에 흡수했다(같은 의미).

## 정직 기록 · 남긴 것
- 동작 변경(의도): 404 본문 reason 이 **객체**이면 tokenCheck 도 이제 `old-version`(예전 `disabled/'(형식 오류)'`). edgeLog·tokenCheck 의 bad-body 판정이 `node` 객체 여부까지 본다(문자열 node 는 예전엔 통과). 어느 테스트도 그 값을 고정하고 있지 않았다.
- CVP `silent` 는 중앙 **이번 기동** 기준 `주기×3` 이다 — 보관분(central-agent-cvp.json)은 재시작을 넘어 남으므로 재시작 직후에는 `waiting` 으로 시작한다(없는 문제를 만들지 않는 쪽).
- `edgeCvpSummary`(edges 배너)는 보고한 엣지만 다루므로 손대지 않았다 — 구버전 엣지는 서버 행(`servers[].status.kind`)에서 보인다.
- 실장비 엣지로는 확인하지 못했다(목 http 서버 + 실제 라우터).


## 수정 그룹 보고 (e)

# fix-e — G4b 엣지 push 규약 통일·중앙 게이트 미들웨어 (v2.613)

기준 커밋 8ece9d9 · 회귀 `server/test/audit2613e.test.js` 8건(전부 통과) · 변이 검증 8/8(아래 표) · 소요 약 14분(측정 마커 이후 — 소스 읽기 약 10분은 마커 전이라 미측정).

## ID 별

### DEPS2613-09 — `routes/central.js` 게이트 2줄 × 51 → `requireCentral()` 1벌
- `server/src/routes/central.js:330` `export function requireCentral({ notFound })` — 404(central 비활성) → 403(인증 실패) 순서·본문 그대로. 51개 라우트 선언 전부 `centralRouter.X('/p', requireCentral(…), handler)`. 404 본문 변형은 `notFound` 옵션으로 보존: `/assignment`·`/register-collector` → `'central 비활성화 (CENTRAL_TOKEN 미설정)'`, `/result`·`/rma-result`·`/ping-result`·`/log-query-result`·`/capture-result`·`/bmstor-result`·`/ip-scan-result` → `{ok:false}`, 나머지 42 → `'central 비활성화'`.
- 4곳(storage-config·pdu-config·sanswitch-config·cvp-config)의 `'토큰의 agent 와 요청 agent 불일치'` 재대조는 **지웠다** — `:294` 미들웨어가 `requestedAgent`(query.agent → X-Agent-Name → body.agent, 소문자 비교)로 같은 판정을 먼저 끝내므로 도달 불가 중복(부분집합). 유일한 차이는 미들웨어가 이름을 64자로 자른다는 것(`strAgent/capTrim`)뿐이며 엣지 이름은 그보다 짧다.
- `/rma-poll` 은 선언 다음 줄 `res.locals.perfExpectSlow = true` 가 있어 게이트를 그 줄 뒤에서 뺐다(순서 동일).
- 테스트: `DEPS2613-09: central 라우트 51개 전부 requireCentral()…`(소스 열거 — 선언마다 둘째 인자 검사, 인라인 게이트 1건 = 미들웨어 본체, 중복 문자열 0) · `DEPS2613-09: 런타임 — …`(실제 centralRouter: 비활성 404 본문 3변형 deepEqual · health-probe 도 404(비활성이 토큰보다 먼저) · 틀린 토큰 403 `{ok:false, reason:'토큰 불일치'}` · 맞으면 200).
- 변이: central.js 를 base 로 → 소스 테스트 FAIL(런타임 테스트는 PASS — 동작 보존 리팩터이므로 기대대로).
- ⚠ centralRouter 를 띄우는 테스트 28파일 전부 통과(`rg -l centralRouter server/test`). **apiDoc2563 2건 실패** — docs/API.md 가 낡음(줄 번호) + `requireCentral` 이 GUARD_NOTE 에 없음 → 리드 작업(아래).

### EDGE2613-03 — sanswitch push 단일비행 `_busy=null + _again`
- `server/src/sanswitch/push.js` `let _busy = null; let _again = false;` · `pushSanSwitchNow()` 가 storage/pdu 와 같은 합류 래퍼, 본체는 `pushSanSwitchOnce()`(`finally { _busy=false }` 제거).
- `server/src/agent/sanSwitchConfigPull.js` `pushAfterCollect()` = `await pushSanSwitchNow()` + `recordCollectPush(r)` — `BUSY_REASON` 문자열 대조·`sleep(2s)×90` 루프·`{pending, done}` 반환 삭제(storageConfigPull 의 `await pushStorageNow()` 와 같은 표준 경로. `_pull` 은 원래 `await pushAfterCollect()` 였으므로 호출부 변경 없음).
- 기존 테스트 수정: `server/test/audit2601c.test.js` 'EDGE2601-06 — SAN' — `r.pending===true`·`r.done` 단언을 **합류 결과**(끝나기 전엔 settle 안 됨 → release 후 `ok:true`, POST 2회, `collectPush.ok`)로 바꿨다.
- 테스트: `EDGE2613-03: …` — 소스(6개 push 모듈에 `let _busy = false` 0·`_again = true` 존재 · configPull 에 BUSY_REASON/maxTries/sleep 0 · `await pushSanSwitchNow()`) + 런타임(목 중앙에서 첫 push 를 막고 두 번째 호출이 settle 되지 않다가 끝난 뒤 POST 2회·둘 다 ok).
- 변이: sanswitch/push.js + sanSwitchConfigPull.js base → FAIL. audit2601c 수정분도 base 에서 FAIL(M7).

### EDGE2613-04 — SAN·PDU 상태 전용 push + 중앙 수신 + 공용 프로브
- 신규 `server/src/agent/centralStatusOnly.js`: `centralStatusOnlySupport({ minVersion, now, fetchImpl })`(health-probe 버전, **필요 버전별 캐시** 1h/실패 5m) · `STATUS_ONLY_MIN_CENTRAL='2.581.0'`(storage) · **`DEVICE_STATUS_ONLY_MIN_CENTRAL='2.613.0'`**(sanswitch/pdu — 수신이 이 릴리스에 생겼으므로 ≤2.612 중앙에 빈 목록을 보내면 목록이 비워진다) · `sendStatusOnly(route, status, extra)` · `_resetStatusOnlyCapForTest`.
- `server/src/storage/push.js:13-16` 공용 import + 옛 이름 재수출(import+export 형태 — audit2602a 가 `push.centralStatusOnlySupport`·`_resetStatusOnlyCapForTest` 를 쓴다) · `:134` `sendStatusOnly` 는 공용 호출 1줄.
- `server/src/sanswitch/push.js` `assigned !== 0` 분기: 프로브 → 미지원이면 `statusSent:false, statusSkipped`(상태·반환), 지원이면 `/api/central/sanswitch-data` 에 `{devices:[], chunk:0, chunks:1, statusOnly:true, status:{reason:'no-snapshots'|'registry-unreadable', registered}}`.
- `server/src/pdu/push.js` `wh.withhold` 분기: 같은 규칙, `/api/central/pdu-data` 에 `{snapshots:[], statusOnly:true, status:{reason, registered, missing}}` · 상태에 `statusSent/statusSkipped/statusError`.
- 중앙: `server/src/routes/central.js:1622`(/sanswitch-data)·`:1400`(/pdu-data) `statusOnly === true` → `saveEdge*Status` 만, 응답 `{ok:true, saved:0, statusOnly:true, recorded}`(/storage-data :1306 과 같은 모양). `server/src/central/sanSwitchEdge.js` `saveEdgeSanSwitchStatus`·`edgeSanSwitchReports()`(storageEdge 와 같은 모양 `{agent, at, deviceCount, status}`) + 장비 보고 시 `status` 보존 · `server/src/central/pduEdge.js` `saveEdgePduStatus` + `edgePduStatus()` 에 `status` 필드 + 보존. 아는 키만·64자.
- 테스트 2건: 엣지(목 중앙 버전 2.612 → *-data POST 0·`statusSkipped:'central-too-old'` / 2.613 → statusOnly 본문 도착·X-Agent-Name / 버전별 캐시 분리) · 중앙(실 라우터: 장비 1대 저장 → statusOnly → 목록 그대로 + status 기록 + 재보고 뒤 보존 + 아는 키만).
- 변이: 엣지 3 push + centralStatusOnly.js 삭제 → 엣지 테스트 FAIL · central.js+두 Edge 를 base → 중앙 테스트 FAIL.
- 기존 테스트 수정: `server/test/audit2583.test.js` '#13 SAN·PDU push' — 목 중앙이 version 을 주지 않으므로 이제 health-probe GET 1회가 나간다(데이터 POST 는 여전히 0). `seen` 에서 health-probe 를 뺀 것으로 0건 단언 + `statusSkipped:'central-version-unknown'` 단언 추가.
- ⚠ verify 표는 ①(프로브 승격)만 권했으나 태스크 지시대로 ②~④ 중 ②③(송·수신)까지 했다. ④ 화면은 내 파일 밖(아래 '리드').

### EDGE2613-05 — readDropSummary 사본 3벌 → `util/centralReply.js`
- 신규 `server/src/util/centralReply.js`(agent/centralReply.js 본체 이동): `dropSummaryOf` 가 `dropped`(객체)·`coerced`·`zoningTrimmed`·`evicted` 를 더 읽고 text 에 싣는다(`거부 N(notOwned 1)` 처럼 `dropped` 사유도) · `dropText()` export · `mergeDrop` 이 `dropped` 를 키별 합산. `server/src/agent/centralReply.js` 는 `import {…} from '../util/centralReply.js'; export {…};` 재수출(같은 함수 — 테스트 고정).
- storage/pdu/sanswitch push 의 `function readDropSummary` 삭제, 셋 다 `dropSummaryOf(await readCentralReply(res))` + `dropText`. 콘솔 문구(`[x-push] 중앙이 … 받지 않았습니다(…)`)는 그대로(로그 분석 probe 는 `[storage-push] 실패:`·413 문구뿐 — logAnalysis2583 통과).
- 테스트: src 전수에서 `function readDropSummary(` 0 · 세 push 가 공용 import+호출 · 재수출 형태(`export … from` 0) · 필드·text·merge · 런타임(sanswitch push 가 목 응답 `rejected/dropped/zoningTrimmed` 를 상태에 남긴다).
- 변이: agent/centralReply.js base + util 삭제 + 세 push base → FAIL.

### CONTRACT2613-03 (SPLIT) — /guest-disk `dropped:{vms,parts}` · guestDiskPush/vmSeriesPush 가 응답을 읽는다
- `server/src/routes/central.js:809-811` `commit.skippedVms/skippedParts` 가 있을 때만 `dropped:{vms,parts}`(없으면 필드 없음 — 구버전 엣지 호환).
- `server/src/agent/guestDiskPush.js` `pushOne` 이 `dropSummaryOf(await readCentralReply(res))` → `mergeDrop` 누적 + `warnDrop('gd-push', …)` · `last.drop`·반환 `drop`.
- `server/src/agent/vmSeriesPush.js` `post()` 가 응답 `spikes`(받아들인 행 수)를 보낸 행 수와 대조 → `last.dropped:{rows, sentRows, text}` + `replyUnread`(응답 못 읽은 청크 수) + 콘솔(10분 1줄).
- 테스트: 정상 push 응답에 `dropped` 없음(실 라우터) · 응답 조립 소스 고정 · guestDiskPush 소스(import·판독·warnDrop·last) · `dropSummaryOf({dropped:{vms:1,parts:2}})` → rejected 3 · vmSeries 런타임(목 중앙이 1행 덜 받아들이면 `dropped.rows===1`, 전부 받으면 필드 없음).
- ⚠ 정직 기록: verify 표대로 v2.601+ 엣지는 push 전에 같은 `sanitizeGuestDiskVms` 를 거쳐 HTTP 로는 skipped 를 만들 수 없다(2중 방어) — 중앙 응답 조립은 소스로만 고정했다.
- 변이: central.js + 두 push base → FAIL.

### RUNTIME2613-04 — `pushMs`·`perfPushMs` → `clampIntervalMs`
- `server/src/sanswitch/push.js` `pushMs = () => clampIntervalMs(Number(env)||5m, 5m, 60_000)` · `server/src/sanswitch/perfPush.js` `perfPushMs = () => clampIntervalMs(Number(env)||loadPerfSettings().intervalMs, loadPerfSettings().intervalMs, 60_000)`.
- 테스트: 소스(`Math.max(N, Number(process.env.*_MS)` 0 + clampIntervalMs 사용) + 런타임(`1e12`→MAX_TIMER_MS · `10`→60s · `abc`→기본).
- 변이: 두 파일 base → FAIL.

### EDGE2613-12 — 지시대로 손대지 않음.

## 변이 검증 매트릭스(worktree wt-e, base 8ece9d9 + 변경 파일 복사 → 8/8 통과 확인 후)
| 변이 | 되돌린 파일 | 결과 |
|---|---|---|
| M1 DEPS2613-09 | routes/central.js | 소스 테스트 FAIL · 런타임 PASS(동작 보존) |
| M2 EDGE2613-03 | sanswitch/push.js · agent/sanSwitchConfigPull.js | FAIL |
| M3a EDGE2613-04 엣지 | storage/pdu/sanswitch push.js · agent/centralStatusOnly.js(삭제) | FAIL |
| M3b EDGE2613-04 중앙 | routes/central.js · central/sanSwitchEdge.js · central/pduEdge.js | FAIL |
| M4 EDGE2613-05 | agent/centralReply.js · util/centralReply.js(삭제) · 세 push.js | FAIL |
| M5 CONTRACT2613-03 | routes/central.js · agent/guestDiskPush.js · agent/vmSeriesPush.js | FAIL |
| M6 RUNTIME2613-04 | sanswitch/push.js · sanswitch/perfPush.js | FAIL |
| M7 audit2601c 수정분 | sanswitch/push.js · agent/sanSwitchConfigPull.js | FAIL |
worktree 제거 완료. 띄운 서버 없음(`pgrep -af "node src/index.js"` 0).

## 실행한 기존 테스트
- 배치 1(모듈 커버 21파일): 259/259 통과.
- 배치 2(centralRouter 마운트 28파일 + apiDoc2563·logAnalysis2583·audit2583·2590·2591·2595·2597·2599c·2599d): 476/480 — 실패 4건:
  · apiDoc2563 ×2 — docs/API.md 낡음(줄 번호) + `requireCentral` 미등록(리드).
  · audit2583 #13 — 위에서 수정(재실행 통과).
  · **audit2597 L2597-02 — 내 변경과 무관**(`storage/db.js` 의 `isSqliteLockError` 블록을 검사 — 다른 그룹의 PERSIST2613-03 이 그 파일을 고치는 중).
- 별건 관찰: `arch2579 ②-c` 가 `config.js → agent/envTimeout.js` 로 실패 — 다른 그룹(DEPS2613-01) 작업 중. 내 파일 아님.

## 손댄 파일
server/src/routes/central.js · server/src/util/centralReply.js(신규) · server/src/agent/centralReply.js(재수출) · server/src/agent/centralStatusOnly.js(신규) · server/src/storage/push.js · server/src/pdu/push.js · server/src/sanswitch/push.js · server/src/sanswitch/perfPush.js · server/src/agent/sanSwitchConfigPull.js · server/src/agent/guestDiskPush.js · server/src/agent/vmSeriesPush.js · server/src/central/sanSwitchEdge.js · server/src/central/pduEdge.js · server/test/audit2613e.test.js(신규) · server/test/audit2601c.test.js · server/test/audit2583.test.js

## 남긴 것(정직 기록)
- EDGE2613-04 ④ **화면**: `edgeSanSwitchReports()`·`edgePduStatus()[].status` 를 아직 어떤 라우트·화면도 싣지 않는다(내 파일 밖 — `routes/api/sanSwitch.js`·`routes/api/pdu.js`(`edges` 는 status 필드가 자동으로 실린다)·`commmap/build.js`·웹 `storageListText.edgeReportNotes` 재사용). 서버 계약(모양 = storage 와 동일)은 갖춰 두었다.
- CONTRACT2613-03 의 중앙 `dropped` 응답은 HTTP 로 도달시킬 수 없어 소스 고정뿐.
- 실엣지로 확인하지 못했다 — 전부 목 중앙·실 라우터 재현이다.

## 리드가 할 일
1. `scripts/api-doc.mjs` `GUARD_NOTE` 에 `requireCentral` 추가(예: '**central 게이트**(v2.613 DEPS2613-09) — CENTRAL_TOKEN/개별 토큰 미설정이면 404, 토큰 불일치면 403. 51개 central 라우트 공통.') 후 `node scripts/api-doc.mjs` 로 docs/API.md 재생성(apiDoc2563 2건이 지금 실패).
2. 문서: CLAUDE.md v2.593 '남긴 것 ③ PDU·SAN push 0건 조기 반환(EDGE-3)'·v2.594 '남은 판단 EDGE-3' 를 고침(v2.613 EDGE2613-04)으로 정정. ENV.md·CONFIG-FILES.md 영향 없음(새 env·파일 없음).
3. (선택) 위 '남긴 것' ④ 화면 노출.


## 수정 그룹 보고 (f)

# fix-f — G5 저장소·등록부·설정 규약 (v2.613)

기준 커밋 8ece9d9. 회귀 테스트 `server/test/audit2613f.test.js` 8건(전부 통과). 변이 검증 25/25(아래 표).
검증한 기존 테스트: 첫 배치 36파일(`run-f-existing.log`) + 둘째 배치 58파일(`run-f-existing2.log`) — 실패 1건은 **의도된 것**(아래 '리드가 할 일' 1번).

## ID 별

### PERSIST2613-01 — CVP 상태 파일 2개가 백업 '설정 변경' 감시에 설정으로 잡히던 것
- `server/src/backup/service.js:46` — `RUNTIME_STATE_NAMES` 에 `'central-agent-cvp.json'`·`'cvp-push.json'` 명시 추가(이중 안전망). `:63` — `isRuntimeStateFile` 이 `isRegisteredStateFile()` 도 본다.
- `server/src/cvp/push.js:21,34` — `registerStateFile('cvp-push.json')` 로 커서 파일을 등록하고 `CURSOR_FILE` 이 그 이름을 쓴다.
- `server/test/audit2590.test.js:90-92` — P1 표본에 두 이름 추가.
- 테스트 `PERSIST2613-01 — …`: 명시 목록·`isRuntimeStateFile`·`configWatchRelevant`(agent/configPush.js)·지문 동일성.
- 변이: backup/service.js 되돌림 → FAIL.

### PERSIST2613-08 — 상태 파일을 만드는 헬퍼가 스스로 등록(구조)
- **새 파일** `server/src/util/stateFiles.js` — `registerStateFile(name)`·`isRegisteredStateFile(name)`·`registeredStateFiles()`(import 0 인 leaf, basename 비교).
- `server/src/central/edgeRecord.js:29,191` — `createDebouncedWriter(file…)` 첫 줄에서 등록.
- `server/src/util/activityLog.js:27,41` — `createActivityLog({fileName})` 등록.
- `server/src/util/authGuard.js:26,73` — `createAuthGuard({file})` 등록.
- `util/collectRequestQueue.js` 는 **파일을 쓰지 않는다**(grep `fs.`·`atomicWrite` 0) → 등록 없음.
- 테스트 `PERSIST2613-08 — …`: ① 이름 규약 밖의 합성 이름 3개로 헬퍼를 만들면 그 순간 상태가 되는지(헬퍼별 변이 민감) ② cvp/push.js 로드 후 커서 등록 ③ 열거 스윕 — 저장소의 `createAuthGuard({file:'…'})`·`createActivityLog({fileName:'…'})`·`createDebouncedWriter(` 호출 모듈의 configDir 리터럴·`registerStateFile('…')` 에서 뽑은 파일명 전부(≥20)가 `isRuntimeStateFile()===true`. `createDebouncedWriter` 를 쓰는데 파일명 리터럴이 없는 모듈은 실패(스윕이 못 보는 형태를 조용히 넘기지 않는다).
- 변이: backup/service.js·edgeRecord.js·activityLog.js·authGuard.js·cvp/push.js 각각 되돌림 → 5/5 FAIL.

### PERSIST2613-02 — 용도 카탈로그(부분)
- `server/src/insights/portalDb.js:69-116` — `PURPOSES` 에 MIGRATABLE 13개 DB + 신규 기능 설정·등록부 JSON 12개 + `RUNTIME_STATE_NAMES` 전부(13개) 추가, `export { PURPOSES }`.
- 테스트 `PERSIST2613-02 — …`: `MIGRATABLE ⊆ PURPOSES` · `RUNTIME_STATE_NAMES ⊆ PURPOSES` · 신규 기능 파일 12개(소스 리터럴 실재 확인 후) ⊆ PURPOSES · 설명에 백틱 0.
- 큰 카탈로그(`configFiles/catalog.js`)·`config-doc.mjs kind()` 의 '상태/혼합' 표기는 **하지 않았다**(과제 범위 '부분' — 리드 판단).
- 변이: portalDb.js 되돌림 → FAIL.

### PERSIST2613-03 — SQLite open 코어 사본 3벌
- `server/src/storage/db.js:86,102` — `createLockRetry(30_000)` + `openSqlite(new DatabaseSync(FILE()))`. 자체 `isSqliteLockError`·`_retryAt`·손 PRAGMA(busy_timeout·WAL) 제거. 명시 `fs.chmodSync(…, 0o600)` 은 openSqlite 뒤에 **남긴다**(secAudit2535 '모든 SQLite 모듈은 chmodSync 0600' 스윕 규약 — 처음 지웠다가 그 테스트가 잡아 되살렸다. 세 모듈 공통). `export { isSqliteLockError }` 재수출(import+export 형태 — 기존 호출부·audit2597 호환). `_resetForTest` 가 `lockRetry.ok()`.
- `server/src/horizon/sessionDb.js:104,117` — `lockError/retryAt/LOCK_RETRY_MS` 사본 → `createLockRetry`; 상태 문구는 `lockRetry.note()`, 적재 거부 사유는 `lockRetry.lastLock()`; `_expireLockRetryForTest` 는 `lockRetry._expire()`.
- `server/src/metrics/db.js:33,271` — `openSqlite(new DatabaseSync(DB_PATH))`(예전: busy_timeout 두 번 + journal_mode 잠금 삼킴); `initSqliteRetrying` 은 `retryOnLock(initSqlite, {tag:'metrics'})` 얇은 별칭(이름 유지 — audit2597:80 `initSqliteRetrying().catch` 가 그 이름을 고정). `LOCK_RE` 사본 제거.
- `server/test/audit2611c.test.js:403` — 3파일 이름 기반 chmod→PRAGMA 검사 삭제(일반 스윕이 대신한다).
- 테스트 `PERSIST2613-03 — …`: 일반 스윕(`new DatabaseSync(` ∧ ¬`openSqlite(` = ∅, 예외 `ipam/writeWorker.js`·`upgrade/dbCheckpoint.js` 사유 명시) + 세 모듈에 사본 흔적 0 + `storage.isSqliteLockError === sqliteOpen.isSqliteLockError` + 세 DB 실제 open.
- 변이: storage/db.js·metrics/db.js·horizon/sessionDb.js 각각 되돌림 → 3/3 FAIL.
- ipam.db(wal:false)는 건드리지 않았다.

### PERSIST2613-04 + DEPS2613-04 — 등록부 코어
- **새 파일** `server/src/util/registryCore.js` — `corruptOnlyReason(file)` · `makeLoadError()`(`get/set/clear/checkCorruptOnly(file)`) · `filterObjectElements(list)→{list,dropped}` · `devicesForThisNode(list,{agentName,isEdge})` · `devicesForAgent(list,agentName)`. fs·path 만 import(arch2579 util 규약).
- `storage/registry.js:43,54` · `sanswitch/registry.js:41,51` · `pdu/registry.js:34,42` · `cvp/registry.js:41,51` · `bmstor/registry.js:29,37` — 4벌 동일 본문 `corruptOnlyReason`·`let _loadError` 제거, 원소 필터, `preserveCorrupt(FILE, e?.message)`(sanswitch·storage·bmstor 에 사유 추가), `devicesForThisNode/ForAgent`(cvp 는 `serversFor*`) 가 코어로 위임(등록부의 export 시그니처는 그대로). bmstor 는 `registryLoadError()` 를 새로 export(형제와 같은 판정 — 소비처는 없다). storage·sanswitch 는 JSON null·배열을 cvp 처럼 '형식 오류' 로 손상 처리(v2.601 규약 유지).
- 비밀 승계(`accessMoved`)는 옮기지 않았다.
- ⚠ 동작 차이(정직 기록): ① storage·sanswitch `devicesForAgent('')` 는 예전에 **agent 가 빈 장비(중앙 직접)** 를 돌려줬는데 이제 `[]`(pdu 판정으로 통일 — 빈 이름이 중앙 직접 장비와 짝지어지지 않게). ② 엣지 판정에서 agent 앞뒤 공백을 무시한다(pdu 와 같게). 기존 storageMon·sanSwitch·pdu·cvp2608 테스트 전부 통과.
- 테스트 `PERSIST2613-04/DEPS2613-04 — …`: 소스(정의 1벌·5벌 import·사유 전달) + 코어 단위 + **실행**: 원소 `null`/문자열이 든 등록부 파일 5종에서 목록이 살아 있고 `.corrupt.` 보존이 생기지 않는다(예전: storage·sanswitch TypeError, bmstor 파일 전체 보존+목록 0) + 손상 보존본만 남은 상태에서 5벌 모두 `registryLoadError()`.
- 변이: 등록부 5파일 각각 되돌림 → 5/5 FAIL.

### PERSIST2613-06 — 파트 장애 보존일
- `server/src/partfault/settings.js:36` `RETENTION_LIMITS={min:30,max:3650,def:730}` · DEFAULTS 에 `retentionDays` · `loadPartFaultSettings` 가 범위로 정규화 · `savePartFaultSettings` 가 `retentionDays` 패치(빈 칸·비숫자 = 미지정, `:72`) · **`partFaultRetention()`**(`:117`) → `{days, source:'env'|'settings'|'default'}`. env `PARTFAULT_RETENTION_DAYS`: 빈 값·비숫자·음수 = 미지정(설정으로), `0` = 전부 보관, 양수 = 하한 30.
- `server/src/partfault/db.js:65,249,322` — 모듈 상수 `RETENTION_DAYS` 제거, prune 때마다 `partFaultRetention()` 을 읽는다(중앙 설정 변경이 재시작 없이 적용). `partFaultDbStatus()` 에 `retentionSource` 추가(응답 확장 — 기존 키 유지). `days===0` 이면 prune 생략.
- `routes/api/partFaults.js` — **무변경**: GET(`settings: loadPartFaultSettings()`)과 PUT(`savePartFaultSettings(req.body)`)이 같은 함수라 `retentionDays` 가 그대로 노출·저장된다(테스트가 소스로 고정).
- 웹: `PartFaults.jsx` 에는 '기능 켜기' 체크박스 한 줄뿐 **설정 패널이 없다** + 웹은 내 편집 범위 밖 → UI 는 리드에게(아래).
- `config.js retentionEnv` 는 export 되지 않아 import 하지 못했다 — 같은 의미(0 = keep all, 빈 값 = 미지정)를 `partFaultRetention()` 안에 구현하고 주석에 그 계약을 적었다(코어 통합은 아래 '리드가 할 일' 3번).
- vmtrack/db.js 의 같은 꼴(`Number(env)||1095`)은 verify 가 지적했지만 내 파일 범위 밖이라 손대지 않았다.
- 테스트 `PERSIST2613-06 — …`: 기본·저장·빈 칸 유지·하한·상한·다른 칸 저장 무영향·손편집 비숫자·env 7경우·`partFaultDbStatus` 값·prune 생략 소스·라우트 함수 공유.
- 변이: partfault/settings.js·partfault/db.js 각각 되돌림 → 2/2 FAIL.

### DEPS2613-12 + RUNTIME2613-08 — 설정 모듈 clamp 사본
- `bmusage/settings.js`(6곳) · `linkcheck/settings.js`(11곳) · `cvp/settings.js`(2곳, `clampNum` 삭제) · `curuser/settings.js`(9곳) · `horizon/sessionSettings.js`(7곳) · `partfault/settings.js`(신규 필드) — 로컬 `clamp*` 정의 0, 전부 `clampSetting(v, {min,max,def})`.
- ⚠ 의미 보존: curuser·horizon 의 계약은 '빈 값·비숫자·**0 이하** = 기본값'(기존 테스트 `horizonSessions2525:209` 가 `normalize({intervalMs:0})===def` 를 고정 — 첫 판에서 실제로 깨졌다). `clampSetting` 은 0 을 값으로 보고 하한으로 올리므로 그 두 모듈은 `positive(v)`(0 이하 → null) 를 먼저 거친다(`curuser/settings.js:49`·`horizon/sessionSettings.js:38`). bmusage 는 `Number(null)===0 → 하한` 이던 것이 `→ 기본값` 으로 바뀐다(v2.596 규약 방향, 저장 경로는 `dropUnspecifiedNumbers` 가 이미 걸러 실효 차이 없음).
- 테스트 `DEPS2613-12/RUNTIME2613-08 — …`: 6모듈 스윕(`const|let|var|function clamp\w*` 0, import·사용) + 동작(빈 값·null·비숫자·배열 → 기본값, 숫자 문자열은 값, 패치 빈 칸은 이전 값).
- 변이: 6파일 각각 되돌림 → 6/6 FAIL.
- 범위 밖 잔여(verify 가 센 34건 중): vmseries/relaycheck/perfSettings/publicapi 등 — 손대지 않았다.

### TESTDOC2613-13 — MIXED_STATE_FILES 스윕(테스트 전용)
- 테스트 `TESTDOC2613-13 — …`: ① 항목마다 그 파일명 리터럴 + `atomicWriteFileSync` + `last*/useCount` 를 가진 저장 모듈이 실재(죽은 항목 0) ② MIXED ∩ 상태파일 = ∅ ③ 반대 방향 — 저장 레코드에 실행 필드를 **점 대입**(`.lastX =`)하는 모듈의 configDir JSON 중 하나는 MIXED 거나 상태 파일 ④ 지문이 last*/useCount(중첩·배열 포함)만 빼고 설정 변화는 잡는다.
- ⚠ 한계(정직 기록): 모듈이 파일 여럿을 쓰면(alerts.js: alerts.json + alerts-state.json) 실행 필드가 어느 파일로 가는지 소스로 가를 수 없어 '그중 하나라도' 로 판정한다. 파일명을 변수로 조립하는 모듈은 못 본다.
- 변이: `MIXED_STATE_FILES` 에서 `'api-keys.json'` 제거(sed) → FAIL(반대 방향 스윕이 `publicapi/keys.js` 를 잡는다).

## 변이 매트릭스(워크트리 wt-f, 기준 8ece9d9 — 제거 완료)
| ID | 되돌린 파일 | 결과 |
|---|---|---|
| PERSIST2613-01 | backup/service.js | FAIL ✓ |
| PERSIST2613-08 | backup/service.js · central/edgeRecord.js · util/activityLog.js · util/authGuard.js · cvp/push.js (각각) | 5/5 FAIL ✓ |
| PERSIST2613-02 | insights/portalDb.js | FAIL ✓ |
| PERSIST2613-03 | storage/db.js · metrics/db.js · horizon/sessionDb.js (각각) | 3/3 FAIL ✓ |
| PERSIST2613-04/DEPS-04 | storage·sanswitch·pdu·cvp·bmstor registry.js (각각) | 5/5 FAIL ✓ |
| PERSIST2613-06 | partfault/settings.js · partfault/db.js (각각) | 2/2 FAIL ✓ |
| DEPS2613-12/RUNTIME-08 | 설정 모듈 6개 (각각) | 6/6 FAIL ✓ |
| TESTDOC2613-13 | MIXED 항목 제거(sed) | FAIL ✓ |
| baseline / restored | — | 8 pass ✓ |

## 손댄 파일
신규: `server/src/util/stateFiles.js` · `server/src/util/registryCore.js` · `server/test/audit2613f.test.js`
수정: `server/src/backup/service.js` · `central/edgeRecord.js` · `util/activityLog.js` · `util/authGuard.js` · `cvp/push.js`(등록만) · `insights/portalDb.js` · `storage/db.js` · `metrics/db.js` · `horizon/sessionDb.js` · `storage/registry.js` · `sanswitch/registry.js` · `pdu/registry.js` · `cvp/registry.js` · `bmstor/registry.js` · `partfault/db.js` · `partfault/settings.js` · `bmusage/settings.js` · `linkcheck/settings.js` · `cvp/settings.js` · `curuser/settings.js` · `horizon/sessionSettings.js` · `server/test/audit2590.test.js` · `server/test/audit2611c.test.js`
무변경: `util/collectRequestQueue.js`(파일을 쓰지 않는다) · `util/clampSetting.js`(확장 불필요) · `routes/api/partFaults.js`(GET/PUT 이 이미 같은 저장 함수를 쓴다) · `test/arch2582.test.js`(registryCore 에 `JSON.parse(fs.readFileSync` 가 없어 스윕 무영향 — 14건 통과 확인).

## 리드가 할 일(내 범위 밖)
1. **needs file `server/test/audit2597.test.js:79`** — `L2597-02` 가 storage/db.js 의 **손 사본 모양**(`if (isSqliteLockError(e)) {…_retryAt = Date.now() + 30_000;…return null;`)을 정규식으로 박아 두어 PERSIST2613-03 리팩터로 **그 한 줄이 실패한다**(`:74-77` 의 `isSqliteLockError` import 와 `:80` 의 `initSqliteRetrying().catch` 는 통과). 제안: 79행을 `assert.match(s, /if \(lockRetry\.onFail\(e\)\) \{[\s\S]{0,200}return null;/)` 로. (되돌리기 대안: 리팩터를 버리면 03 을 '남긴 것' 으로 기록.)
2. **웹 UI(PERSIST2613-06)**: `web/src/views/tools/PartFaults.jsx` 에 설정 패널이 없고(체크박스 1개) 웹은 내 편집 범위 밖. `settings.retentionDays`(GET) / `PUT /tools/part-faults/settings {retentionDays}` 가 준비돼 있으니 숫자 칸(30~3650일, 빈 칸 = 유지)을 붙이면 된다. `db.retentionDays===0`(env 전부 보관)이면 현재 문구 `이력 보존 N일` 이 비어 보인다 — `retentionSource` 로 '전부 보관(env)' 을 적을 것.
3. **`config.js retentionEnv` export** 여부 판단: 지금은 `partfault/settings.js partFaultRetention()` 이 같은 계약을 자체 구현한다. 리드가 export 하면 그 함수의 env 파싱 3줄을 `retentionEnv` 로 바꿀 수 있다(코어는 하나). `vmtrack/db.js:551` 의 같은 꼴도 verify 가 지적했다.
4. `docs/CONFIG-FILES.md`·`docs/API.md` 재생성 여부: 새 파일 2개(`stateFiles.js`·`registryCore.js`)는 라우트·설정 파일이 아니라 API 문서 무영향으로 보이나, `partfault-settings.json` 의 새 필드 설명은 `scripts/config-doc.mjs DESC` 쪽(손대지 않음).
5. `apiDoc2563` 2건 실패는 다른 그룹이 편집 중인 `routes/central.js` 줄 번호 이동(문서 재생성 `node scripts/api-doc.mjs` 필요) — 내 파일이 아니다.
6. 기존 테스트 배치에서 `arch2579 ②-c`(config.js import) 가 한 번 실패로 찍혔으나 재실행에서 통과 — 다른 그룹이 config.js 를 동시 편집하던 순간의 일시 상태다(내 변경과 무관, config.js 는 내 파일이 아니다).

## 남긴 것(정직 기록)
- PERSIST2613-02 의 큰 카탈로그(`configFiles/catalog.js` 로 backup/secretVault/portalDb/config-doc/dbLocation 파생)는 하지 않았다 — 과제가 '부분' 이다.
- verify-deps 가 센 clamp 사본 중 이 6모듈 밖(vmseries·relaycheck·perfSettings·publicapi·ipam/scanStore …)은 그대로다.
- `bmstor/registry.js` 에는 `_resetForTest` 가 없어 테스트가 손상 보존본 판정을 `?fresh=1` 별도 모듈 인스턴스로 확인했다.
- 실장비·실제 운영 CONFIG_DIR 로는 확인하지 않았다(합성 파일·목 설정).


## 수정 그룹 보고 (g)

# fix-g — v2.613 그룹 G6(런타임 — 서비스 점검 커버리지·풀·예산·기동·push 크기 규약)

기준 커밋 8ece9d9 · 회귀 `server/test/audit2613g.test.js` 6건(ID 별 1건 + RUNTIME2613-01(b) 순수 판정) · 변이 검증 5/5(아래 표).
검증 실행: `node --test server/test/audit2613g.test.js` → 6/6 · 변경 모듈을 덮는 기존 테스트 29파일 실행(아래 '기존 테스트' 절).

## ID 별

### RUNTIME2613-01 — `health/services.js` 가 `edgelog/spec.js STATUS_SPEC` 의 `collect.*` 전부 + 중앙 전용 2개를 본다
- `src/health/services.js:25-105` — spec `mod` 경로 → 모듈 대응표 `MODS`(정적 import 30개. **`getServiceCheck()` 는 동기 유지** — 소비처
  `routes/api/checksLogs.js:66` 이 `res.json(getServiceCheck())` 그대로이고 그 파일은 내 편집 범위 밖. 동적 import 를 쓰면 async 가 되어 소비처를
  고쳐야 하므로 정적 import 를 택했다 — spec 규칙상 전부 index.js 가 부팅에 import 하는 모듈이라 새 타이머가 켜지지 않는다).
- `:107-111` `CENTRAL_ONLY_SPEC`(relaycheck `relayCheckStatus` · partfault `partFaultStatus`) — spec 이 일부러 뺀 중앙 전용 둘. 엣지 노드
  (`config.agent.centralUrl` 있음)에서는 행을 만들지 않고 `coverage.skippedOnEdge` 로 밝힌다.
- `:120-166` 순수 판정 `pollerLastAt`·`pollerIntervalMs`·`pollerEnabled`·`pollerCheck(st,{now,uptimeMs})` — 모듈마다 다른 시각 이름 9종·주기 이름 6종
  을 읽고, 꺼짐 → off · 마지막 실행이 `max(30분, 주기×2)` 초과 → warn · 실행 기록 없이 기동 후 그 경계를 넘김 → warn(v2.590 W1).
- `:168-181` `readStatus` — 비동기 상태 함수(`partFaultStatus`·새로 spec 에 들어온 `logs/poller.js logStatus`)는 캐시 + 갱신을 띄워 두고 첫 조회는
  '상태 조회 중' 이라 말한다(동기 경로에서 지어내지 않는다).
- `:288-310` 고정 13항목 중 spec 과 겹치는 4개(vcenter/metrics/gpu-guest/ipscan)에 `specKey` 를 달아 두 줄로 그리지 않고, 나머지 spec 행을 추가.
  응답에 `coverage{fixed,pollers,centralOnly,skippedOnEdge,notCovered,note}` 추가(기존 `overall/summary/checks/generatedAt` 모양 그대로, 행 모양
  `{key,label,status,detail,at}` + `specKey` 추가).
- ⚠ 작업 중 다른 그룹이 spec.js 에 `collect.vcLogs`·`collect.capacity`·`collect.certs` 3개를 더했다(CONTRACT2613-06·RUNTIME2613-02) — MODS 에
  같이 넣었다(`logs/poller.js`·`capacity/sampler.js`·`security/certMonitor.js`). 테스트가 spec ⊆ MODS 를 고정하므로 앞으로 spec 에 항목이 늘면
  이 테스트가 먼저 깨진다.
- 테스트: `RUNTIME2613-01`(spec collect.* 키 ⊆ 점검 행 `specKey` · 중복 0 · 점검 미구현 0 · 중앙 전용 2행 · coverage.note · 소스 MODS 대조 · 동기 유지)
  + `RUNTIME2613-01(b)`(순수 판정 경계·시각 이름 9종·주기 이름). 변이: services.js 를 기준판으로 → **둘 다 FAIL**.

### RUNTIME2613-03(a) — 엣지 팬아웃 두 곳을 `util/pool.js poolSettled` + 총 예산으로
- `src/routes/api/portalCheck.js:43,265-283` — 손 풀(`Array.from({length}) + for(;;)`) → `poolSettled(rows, PROBE_CONCURRENCY, …)` + 예산
  `PROBE_BUDGET_MS`(형제 `tokenProbe.probeAll` 과 같은 값). 남은 예산 < `PROBE_TIMEOUT_MS` 면 시작하지 않고 `kind:'budget'`, 응답에 `budgetExceeded`
  추가(기존 필드 그대로 + 1개 추가).
- `src/routes/api/bmUsage.js:33-34,135-140,434-467` — 순차 `for` → `poolSettled(agents, EDGE_PULL_CONCURRENCY(4), …)` + 예산 `EDGE_PULL_BUDGET_MS`
  (기본·상한 150초 — 웹 `api.js MUT_TIMEOUT_MS` 180초보다 작게) · 엣지당 최악 `EDGE_PULL_WORST_MS`(시한 20초 × 재시도 포함 2) 로 판정 · 결과는 입력
  순서 보존 · 응답에 `budgetExceeded`. 상수는 모듈 최상위(등록 함수 안 export 는 SyntaxError — v2.593 교훈을 첫 판에서 밟고 고쳤다).
  시한 env 는 `reqTimeoutMs`(v2.605 TIM2605-04 스윕 통과).
- 테스트: 소스(손 풀 0·poolSettled import·순차 for 0) + 산수(예산 + 최악 ≤ 웹 시한) + **실제 api 라우터**로 `/tools/bm-usage/edges/pull`
  (env 로 예산을 하한 20초에 두면 최악 40초 > 예산 → 세 곳 전부 시도 없이 `budget`, `budgetExceeded:3`). 변이: 두 라우트 파일 기준판 → FAIL.
- (b)(`arch2579` 정규식 확장·POOL_EXCLUDED 정비)는 내 파일 범위 밖(`test/arch2579.test.js`) — 손대지 않았다.

### RUNTIME2613-05 — bmusage 세션 예산을 장비 시한과 묶는다(기동 시 잘라내고 콘솔 1줄)
- `src/bmusage/poller.js:38-56` — `DEVICE_TIMEOUT_MS` 줄은 **그대로**(`bmUsagePerf2550`·`bmUsageEnterprise2554` 가 그 줄을 정규식으로 고정) ·
  순수 `coupleBudget(wanted, device, floor)` → `min(wanted, max(floor, device − 5초))` · OS 예산 하한 10초·ENT 15초 · 잘랐으면 `console.warn` 1줄 ·
  `bmUsageStatus()` 에 `osBudgetMs/osBudgetClamped/entBudgetEffectiveMs/entBudgetClamped/budgetMarginMs`(:95-96) · 수집 호출에 `budgetMs` 전달(:285,:351).
- `src/bmusage/collectors/osSsh.js:156,171-172` — `collectOsUsage(host, { signal, budgetMs = SESSION_BUDGET_MS })` 옵션 추가(기본은 예전 상수 — 다른
  호출부 동작 불변). `idracEnterprise.js` 는 이미 `budgetMs` 옵션이 있어 손대지 않았다.
- 검증 표의 권고(장비 시한을 올리는 쪽)가 아니라 과제 지시(예산을 시한 − 5초로 자르는 쪽)를 따랐다 — 시한은 관리자가 명시한 값이고 예산은 그 안에서
  스스로 조절하는 값이라 자르는 쪽이 지시와도 맞다.
- 테스트: 순수 판정 4조합 + 소스(옵션 전달) + **자식 프로세스 env 조합 3개**(기본 → 50/45 안 잘림·경고 없음 / `BMUSAGE_DEVICE_TIMEOUT_MS=30000` →
  25/25 잘림 + 경고 2줄 / 예산 env 만 90·70초 → 55/55). 변이: poller.js + osSsh.js 기준판 → FAIL.
- ⚠ 정직 기록: 형제 storage(`cliSsh.js` 150초 vs `poller.js` 180초)는 검증 표가 '같은 형태' 라 적었지만 내 파일 범위 밖 — 손대지 않았다.

### RUNTIME2613-06 — 기동 스태거 간격을 총량으로
- `src/index.js:535-542` — `STAGGER_STEP_MS = min(1500, floor(60000 / N))` · `i * STAGGER_STEP_MS`. 현재 N=74 → 810ms → 마지막 기동 59.1초
  (전 110초). `firstDelayMs` 는 각 모듈 것이라 그대로.
- 테스트: index.js 소스에서 배열 항목 수를 세고 공식을 읽어 마지막 기동 ≤ 60초를 계산(항목 60~200 범위 검사 + 간격 ≥300ms 경고). 변이: index.js
  기준판 → FAIL(같은 파일이라 DEPS 테스트도 함께 FAIL).

### DEPS2613-07 + DEPS2613-08 — fleet·gpu-guest-data push 의 v2.503 체크리스트(gzip · 중앙 BIG_JSON · 413 로그)
- `src/index.js:351-354` — `app.use('/api/central/gpu-guest-data', BIG_JSON)` · `app.use('/api/central/fleet', BIG_JSON)`.
- `src/agent/gpuGuestPush.js:14-15,22-26,58-60,93-105` · `src/agent/fleetPush.js:8-9,13-14,101-102,140-153` — 본문 gzip(`AGENT_PUSH_GZIP=false` 로 끔,
  inventoryPush 와 같은 env) · `Content-Encoding: gzip` · `res.status === 413` 이면 원본/압축 KB 와 함께 콘솔 경고(중앙 버전 안내 포함). 응답 읽기·
  centralDropped 처리는 그대로(기존 audit2604a/2605d/2606c/2606d/2607e 통과 — 두 목 중앙 모두 gzip 을 해제한다).
- 테스트: index.js 두 줄 + 두 모듈 소스 + **목 중앙 실제 호출**(Content-Encoding gzip · 해제 본문 온전 · 413 응답 시 경고 문자열). 변이: 두 push 모듈
  기준판 → FAIL.

### CONTRACT2613-08 — 반증 · 주석 1줄
- `src/index.js:355-356` — `capacity-report`(rows ≤64)·`/result`(found[] 뿐)는 '작아서 등록하지 않음' 근거 주석. 테스트가 문구 존재를 고정한다.

## 변이 검증 표(wt-g @8ece9d9 + 내 파일 8개 + 테스트, node_modules 심링크)
| 되돌린 파일 | 결과 |
|---|---|
| (전부 적용) | 6/6 ok |
| health/services.js | RUNTIME2613-01 · 01(b) FAIL, 나머지 ok |
| routes/api/portalCheck.js + bmUsage.js | RUNTIME2613-03(a) FAIL |
| bmusage/poller.js + collectors/osSsh.js | RUNTIME2613-05 FAIL |
| index.js | RUNTIME2613-06 FAIL + DEPS2613-07·08 FAIL |
| agent/gpuGuestPush.js + fleetPush.js | DEPS2613-07·08 FAIL |
| (복원) | 6/6 ok |
worktree 는 `git worktree remove --force` 로 지웠다.

## 기존 테스트(변경 모듈을 덮는 29파일)
전부 통과: audit2590 bmUsagePerf2550 bmUsageEnterprise2554 bmUsage2550 bmUsage2551 audit2604a audit2605d audit2606c audit2606d audit2607e
tokenCheck2560 scopeLeak2574 audit2598c audit2605b audit2607b scopeStatus2583 audit2575 securityFix2026-09-01 edgeSweep2574 audit2603a audit2599a
audit2598b audit2605c audit2591.
실패 4 파일 — 판정:
- `audit2605f`(TIM2605-04 env 스윕) — **내 것이었다**(첫 판의 `Number(env)||` 형태) → `reqTimeoutMs` 로 고쳐 13/13 통과.
- `arch2579` ②-c `config.js → agent/envTimeout.js` — 다른 그룹의 config.js 변경(내 파일 아님). 기준판에는 없다.
- `audit2583` #13(SAN·PDU push) — 메인 트리에서만 실패, **기준판 + 내 파일 8개(wt-g)에서는 18/18 통과** → 다른 그룹의 `pdu/push.js`·`sanswitch/push.js`·
  `routes/central.js` WIP 와의 상호작용(내 변경과 무관).
- `apiDoc2563` — `docs/API.md` 가 낡음(내 bmUsage.js 상수 삽입으로 라우트 줄 번호 이동 + 다른 그룹의 central.js 변경). **리드가 `node scripts/api-doc.mjs`
  실행 필요**(docs/ 는 편집 금지).

## 파일
src/health/services.js · src/routes/api/portalCheck.js · src/routes/api/bmUsage.js · src/bmusage/poller.js · src/bmusage/collectors/osSsh.js ·
src/index.js · src/agent/gpuGuestPush.js · src/agent/fleetPush.js · test/audit2613g.test.js (모두 server/ 아래)

## 남긴 것(정직 기록)
- RUNTIME2613-03(b): `test/arch2579.test.js` 정규식 확장·POOL_EXCLUDED 사유 명시 — 내 범위 밖.
- RUNTIME2613-05 형제 storage(`cliSsh.js` 예산 150초 vs `poller.js` 시한 180초) 결합 — 내 범위 밖(검증 표가 '같은 형태' 라 적음).
- `EDGE_PULL_WORST_MS` 가 `BMUSAGE_PULL_TIMEOUT_MS` env 를 `central/bmUsageEdgePull.js:25` 와 **두 곳에서** 읽는다(그쪽이 export 하지 않음, 내 범위 밖).
  리드가 그 파일에서 `TIMEOUT_MS` 를 export 하면 라우트가 import 하도록 바꾸는 것이 맞다.
- 서비스 점검 `coverage` 필드·`specKey` 는 웹 `DavinciChecks.jsx` 가 아직 그리지 않는다(행 자체는 기존 카드 그리드에 그대로 나온다). 화면에 '대상 밖 push/pull 워커
  N개' 문구를 보이려면 웹 수정이 필요(내 범위 밖 · Chromium 확인 안 함).
- 웹 화면(Chromium)은 보지 않았다 — 서버 응답 모양은 테스트로 고정.

## 리드가 할 일
1. `node scripts/api-doc.mjs` (docs/API.md 재생성 — apiDoc2563).
2. 다른 그룹의 config.js(arch2579 ②-c)·pdu/sanswitch push(audit2583 #13) 실패는 해당 그룹 확인.
3. (선택) `central/bmUsageEdgePull.js` 에서 `TIMEOUT_MS` export → `routes/api/bmUsage.js EDGE_PULL_WORST_MS` 가 import.

수행 시간: 이 그룹 실제 작업 약 15분(22:03~22:18 KST). `task_start_g`(18:39 KST)는 리드가 먼저 적은 값으로 대기 시간 포함 216분.


## 수정 그룹 보고 (h)

# fix-h — G7 테스트 구조·문서 생성기 (v2.613)

기준 커밋 8ece9d9. 회귀 테스트 `server/test/audit2613h.test.js`(11건) + `server/test/edgeSweep2574.test.js`(9건, 재작성).
변이 검증은 worktree(`wt-h`, 기준 커밋 + node_modules 심링크 + 변경 파일 복사)에서 했고 worktree 는 제거했다.
수행 시간 약 17분(벽시계). 서버는 띄우지 않았다(`pgrep` 0).

⚠ 메인 트리에서는 다른 그룹의 진행 중 편집 때문에 두 테스트가 지금 깨진다 — **내 변경과 무관**(기준 worktree 에서는 통과):
· `secAudit2535.test.js` "SQLite 0600" — `horizon/sessionDb.js`·`metrics/db.js`·`storage/db.js` 가 WIP(sqliteOpen 이관?) 상태.
  기준 커밋의 원본 테스트 파일로 현재 src 를 돌려도 같은 실패(내 stripComments 교체와 무관함을 확인).
· `audit2605f.test.js` TIM2605-04 스윕 — 배치 실행 때 1회 실패, 이후 기준 테스트 파일로 재실행 시 통과(다른 그룹 src 편집이 지나가는 중).
· `src/storage/registry.js:42` 가 `makeLoadError is not defined`(다른 그룹 WIP) — 메인 트리에서 `pullStorageConfigNow` 테스트가 그 줄에서 죽는다. 기준 worktree 에서는 11/11 통과.

## ID 별

### TESTDOC2613-05 — pull 진입 함수 실제 호출 + IMP-06 확장
- `server/test/audit2613h.test.js:58-118` — 인프로세스 목 중앙(http.createServer)에 `config.agent.{centralUrl,centralToken,name}` 을 붙이고
  `pullCurUserConfigNow`·`pullPartFaultConfigNow`·`pullStorageConfigNow`·`pullUsersConfigNow` 를 **실제로 호출**: 적용 결과(applied·enabled·count·intervalsApplied) ·
  요청에 실린 `X-Central-Token`·`agent` 쿼리 · 두 번째 호출의 '변경 없음' · 중앙 403 이면 네 함수 모두 던지지 않고 `ok:false` + 사유에 403.
- `server/test/edgeSweep2574.test.js` IMP-06(:120-175): 정규식 `(push|pull|run)\w*(Now|Once)`(인자 없음 또는 `...args`), 대상 `agent/*.js` + 각 모듈 `push.js`·`perfPush.js`,
  기대 목록 30개를 **열거**해 `deepEqual`(`length >= 10` 제거) + 전부 실제 호출. 폴러의 `poll*Once|run*Now` 는 **부르지 않는다**(등록 장비에 실제 접속하는 수집 — 목 없이는 위험. 보고서에 명시).
  파일 상단에 `CONFIG_DIR` 임시 디렉터리를 잡았다 — 예전엔 없어서 진입 함수가 개발 트리의 `server/config`(실제 등록부 존재)를 읽었다.
- 변이: 테스트 자체가 수정물이라 제품 되돌림은 없음. `edgeSweep2574.test.js` 를 기준으로 되돌리면 audit2613h CONTRACT 테스트가 FAIL(M2).

### CONTRACT2613-06 + RUNTIME2613-02 — spec 누락 + 스윕 확장
- `server/src/edgelog/spec.js:126-137` — `collect.vcLogs`(logs/poller.js logStatus) · `collect.capacity`(capacity/sampler.js capacitySamplerStatus) · `collect.certs`(security/certMonitor.js certStatus) 등재.
  `logStatus()` 의 `dbPath` 는 다른 폴러 항목의 경로와 같은 취급(adminOnly+fullScopeOnly 화면 · redactDeep 은 비밀 키만) — 주석에 근거.
- `server/test/edgeSweep2574.test.js:44-96` — 대상 = **index.js 의 `start*` import 원천 모듈**(60개 이상 검증) ∪ 예전 파일명 패턴; 정규식 `export (?:async )?function \w*Status\w*\(\s*\)` + `export const … = () =>`;
  인자 있는 헬퍼(`statusFromPull(r)`·`certExpiryStatus(ts)`)는 제외. `EXCLUDED` 를 Map(사유)으로: relaycheck·mail/service·collector/state·partfault/poller(기존) +
  `central/svcmonSilence.js`(중앙 전용) · `alerts.js`(alertStatus().config 에 웹훅 URL — v2.604 봉인 필드, redactDeep 이 키 이름으로 못 가림) ·
  `backup/settings.js`·`security/loginMonitor.js`·`reports/dailyReport.js`(포탈 자체 서비스, 각자 설정 화면 있음 — 등재는 값 검토 뒤 별건). EXCLUDED 파일 실재 검사 추가.
- `audit2613h.test.js:157-186` CONTRACT 테스트 — 새 항목 셋이 실재·호출·redactDeep 통과 + 같은 스윕을 독립 구현(edgeSweep 소스의 정규식·대상 선정도 검사).
- 변이: spec.js 되돌림 → edgeSweep 스윕 FAIL + audit2613h CONTRACT FAIL(M1) · edgeSweep2574 되돌림 → CONTRACT FAIL(M2).

### TESTDOC2613-08 — 2줄 정규식 주석 제거기 → `_stripComments.js`
- 24 파일 교체(33곳): ssrfAgents2537 · bmUsage2551 · bmUsagePerf2550 · audit2604e · audit2611e · atRest2538 · hardening2577 · auditMiscall2569 · secAudit2535 · bmUsageEnterprise2554(2) ·
  tracerouteParse · license2576 · audit2612d · audit2611b(2) · authzGates2536(2) · commMap2584 · storageGrowth2531 · collectorSetPasswordGuard · hangClear2560 · sanPerfPush2566 ·
  partFaultSan2548 · bmUsage2550 · powermaxApiVer2532 · tokenCheck2560(4). 전부 `import { stripComments } from './_stripComments.js'` 로. 24파일 전부 실행해 통과
  (secAudit2535 의 1건 실패는 위 WIP 사유 — 기준 worktree 15/15).
- **건너뜀**: `audit2575.test.js`(25번째) — CSS 파일에서 블록 주석만 지우는 단일 정규식이고 2줄 판본이 아니다. CSS 에는 상태 기계가 맞지 않는다(`url(http://…)` 의 `//` 를 줄 주석으로 읽는다). 스윕 허용 목록에 사유와 함께.
  다른 그룹 파일(audit2590·audit2611c·arch2582·arch2579·audit2606g)은 애초에 2줄 판본을 쓰지 않았다(스윕 0건).
- 웹 vitest 8파일(`web/src/**/*.test.js`)도 2줄 판본을 쓰지만 서버 `test/_stripComments.js` 를 import 할 수 없다(번들 경계) — **미착수**(별건: 웹 쪽 사본 코어).
- `audit2613h.test.js:189-205` 테스트-of-테스트 스윕(허용 목록 = audit2575 만). 변이: secAudit2535 되돌림 → FAIL(M3).

### TESTDOC2613-02 — 200ms 이하 절대 시간 단언 → 1초 상한 + 입력 확대
- 10 파일 24곳: audit2591:339(입력 8,000→80,000자) · audit2599a:97·107·112·205(N 30k→60k) · audit2600d:169·173 · audit2601c:99(2만→4만) · audit2602a:180·188 ·
  audit2602d:28·31·43·48 · audit2602e:130 · audit2605f:361(30k→90k) · audit2607c:231·246·262·277(2만→6만) · audit2611b:296·300(4만→10만).
  판단: 비율형(`tOld > tNew*5`)은 옛 구현이 테스트 안에 있어야 성립하는데 이 24곳은 대부분 없다 → v2.603 기준 "회귀와 확실히 갈리는 값"(1초)으로 두고,
  옛 O(n²) 실측이 1초 미만이던 곳은 **입력을 키워** 회귀 시 수 초가 되게 했다(각 줄 메시지에 산수 기록). 캡(256자·500자)으로 고친 파서는 입력 크기와 무관하게 즉시 끝난다.
  10 파일 전부 실행해 통과(audit2605f 1건은 WIP 사유).
- `audit2613h.test.js:208-224` 스윕(≤200 절대 상한 0건, 다른 그룹 파일 5개 허용). 변이: audit2602a 되돌림 → FAIL(M4).

### TESTDOC2613-07 — 느린 테스트
- `sanSwitch.test.js:325-333` 재진입 가드 테스트 — 앞 테스트가 저장한 장비 파일(`sanswitch-devices.json`, 10.2.2.2)을 지우고 `_resetForTest()` → 실제 SSH 시도 없이 가드만 검증.
  `_busy` 는 첫 await 앞에서 동기로 세워지므로 장비 0대여도 두 번째 호출이 스킵된다 — 단언은 그대로. **60초 → 1초**. (정직: '장비가 수집 중일 때' 경합은 이제 실제로 만들지 않는다 — 가드 순서만 검증.)
- `securityH1RegisterCollector.test.js`·`securityAudit2026-09-13.test.js` — `SSRF_ALLOW_LOOPBACK=true` + 루프백 목 엣지(`/api/collector/ping` 에 `{ok:true, agent:'edgeA'}`) 를 띄우고
  `urlHint` 를 그 주소로. 검증 ping 이 **성공 경로까지** 실제로 지난다(예전엔 응답 없는 10.20.30.40 로 8초 시한 대기). **20초×2 → 1초·0초**.
- `audit2613h.test.js:227-238` 소스 검사. 변이: sanSwitch 되돌림 → FAIL(M5) · securityH1 되돌림 → FAIL(M5b).
- CLAUDE.md 의 예산 수치(115초·1,910건) 갱신은 문서 편집 금지 규칙으로 **하지 않았다** — 리드가 v2.613 전량 로그 수치로 갱신할 것.

### TESTDOC2613-11 — `scripts/arch-doc.mjs` + 생성 절 + CI
- `scripts/arch-doc.mjs`(신규): `moduleMap()`(server/src 최상위 63 디렉터리 — 파일·줄 수, 대표 파일 = index.js 또는 밖에서 가장 많이 import 되는 **머리말 있는** 파일, 머리말 첫 줄) ·
  `docList()`(docs/*.md 전부 + 첫 `#` 제목) · 마커 사이만 교체(`<!-- arch-doc:modules:start/end -->`, `<!-- arch-doc:docs:start/end -->`) · `--check`(exit 1). 머리말·제목을 못 읽으면 exit 1 · 미기록.
- `docs/ARCHITECTURE.md` §3-2 를 생성 절로 교체(손 목록·'나머지' 문단 제거) · `docs/INDEX.md` §9 '전체 문서 목록(생성)' 추가 · `.github/workflows/ci.yml` 에 `arch-doc --check` 스텝(continue-on-error, api-doc 과 같은 관례).
- ⚠ **리드**: 지금 문서는 메인 트리(다른 그룹 WIP 포함)에서 생성한 것이라 통합 뒤 `node scripts/arch-doc.mjs` 를 다시 돌릴 것(예: `agent/` 의 대표 파일이 WIP `envTimeout.js` 재수출 머리말로 잡혀 있다).
- `audit2613h.test.js:241-260` — 디렉터리 집합 deepEqual · 파일/줄/머리말 비어 있지 않음 · docs 집합 deepEqual · `--check` exit 0 · ci.yml 스텝. 변이: ARCHITECTURE.md 되돌림(마커 없음) → FAIL(M6) · ci.yml 되돌림 → FAIL(M6b) · 생성기 제거 → FAIL(M6c).

### TESTDOC2613-13 — onIntervalsChange
- `audit2613h.test.js:121-146` — 리스너 등록 → `applyCentralIntervals({pushMs})` 발화(실효값 수신) · 같은 값은 미발화 · 해제 뒤 미발화 · `pullStorageConfigNow` 경로(목 중앙 intervals)로도 발화.
- 변이: 제품(`storage/intervals.js`)은 기준에서 정상이라 되돌릴 대상 없음(테스트-only). N/A.

## 변이 매트릭스 (worktree wt-h, 기준 8ece9d9)
| 되돌린 파일 | 기대 FAIL 테스트 | 결과 |
|---|---|---|
| server/src/edgelog/spec.js | edgeSweep2574 '전부 표에 있다' / audit2613h CONTRACT | FAIL / FAIL |
| server/test/edgeSweep2574.test.js | audit2613h CONTRACT | FAIL |
| server/test/secAudit2535.test.js | audit2613h TESTDOC2613-08 | FAIL |
| server/test/audit2602a.test.js | audit2613h TESTDOC2613-02 | FAIL |
| server/test/sanSwitch.test.js | audit2613h TESTDOC2613-07 | FAIL |
| server/test/securityH1RegisterCollector.test.js | audit2613h TESTDOC2613-07 | FAIL |
| docs/ARCHITECTURE.md | audit2613h TESTDOC2613-11 | FAIL |
| .github/workflows/ci.yml | audit2613h TESTDOC2613-11 | FAIL |
| scripts/arch-doc.mjs 제거 | audit2613h TESTDOC2613-11 | FAIL |
복원 뒤 audit2613h 11/11 · edgeSweep2574 9/9 · audit2605f 13/13 · secAudit2535 15/15.

## 파일
server/src/edgelog/spec.js · server/test/edgeSweep2574.test.js · server/test/audit2613h.test.js(신규) · scripts/arch-doc.mjs(신규) · docs/ARCHITECTURE.md · docs/INDEX.md ·
.github/workflows/ci.yml · server/test/{sanSwitch,securityH1RegisterCollector,securityAudit2026-09-13}.test.js ·
stripComments 24: ssrfAgents2537 bmUsage2551 bmUsagePerf2550 audit2604e audit2611e atRest2538 hardening2577 auditMiscall2569 secAudit2535 bmUsageEnterprise2554 tracerouteParse license2576 audit2612d audit2611b authzGates2536 commMap2584 storageGrowth2531 collectorSetPasswordGuard hangClear2560 sanPerfPush2566 partFaultSan2548 bmUsage2550 powermaxApiVer2532 tokenCheck2560 ·
timing 10: audit2591 audit2599a audit2600d audit2601c audit2602a audit2602d audit2602e audit2605f audit2607c audit2611b.

## 남긴 것
- 웹 vitest 의 2줄 주석 제거기 8파일(번들 경계 — 서버 헬퍼 import 불가). 별건.
- README 특수 기능 절 생성(발견 보고의 확장안) — 지시 범위(모듈 지도·INDEX) 밖이라 안 함.
- CLAUDE.md 예산 수치(115초·1,910건 → 220초·3,941건) 갱신 — 문서 편집 금지.
- 폴러 `poll*Once|run*Now`(bmusage·partfault·storage·vmtrack …) 실제 호출 테스트 — 등록 장비 접속이라 목 없이 부르지 않았다(개별 모듈 테스트가 이미 다루는 것도 있음).
