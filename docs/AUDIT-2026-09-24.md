# 4차 점검 — 7축 병렬 감사 (v2.593, 2026-09-24)

요청: "아키텍처 점검, 개선, 버그 개선, 튜닝, 업그레이드 찾아서 진행, 문서화"
방식: 병렬 에이전트 7축이 찾고, 축마다 별도 에이전트가 **반증**(재현 시도 · 불확실하면 반증 쪽)을 했다.
검증 수준: 전체(전량 테스트 + 변이 검증 + Chromium 1440/400). 업그레이드: semver 범위 안 patch/minor 만.

## 결과 요약

| 축 | 발견 | 확정 | 가능성 | 반증 | 고침 |
|---|---:|---:|---:|---:|---:|
| 최근 변경 회귀(v2.587~2.592) | 4 | 4 | 0 | 0 | 4 |
| 인가·범위 | 3 | 1 | 1 | 1 | 2 |
| 엣지↔중앙 계약 | 4 | 2 | 2 | 0 | 3 |
| 데이터 정확성 | 5 | 4 | 1 | 0 | 4 |
| 웹 화면 | 3 | 3 | 0 | 0 | 3 |
| 성능·튜닝 | 4 | 2 | 0 | 2 | 1 |
| 의존성·아키텍처 | 7 | 6 | 1 | 0 | 6 |
| **합계** | **30** | **22** | **5** | **3** | **23** |

감사가 **결함 없음**으로 확인한 범위(다시 돌리기 전에 볼 것): 상태 변경 라우트 356개를 viewer 로 호출 — v2.590 D1 과 같은 잔여 0 ·
파라미터 없는 GET 342개를 범위 계정으로 호출해 다른 vCenter 이름 대조 — 누출 0 · 웹 90카드·43설정·7인사이트 패널 206로드 —
콘솔 오류·넘침·`[object Object]`·NaN 0 · MOCK_SCALE=3 11분 부하 — 폴링 라우트·SQL·동기 fs·힙 결함 없음 · 서버 날짜 경계(로컬 getter) 0.

## 고친 것

| id | 심각도 | 위치 | 내용 |
|---|---|---|---|
| R2593-01 | medium | `idrac/redfish.js` | 동시 캐시 미스가 각자 세션을 만들고 뒤 요청이 앞 세션을 DELETE → 거짓 401 → authGuard 가 주기 수집 정지. 세션 생성 single-flight + 교체 토큰 재시도. 재현 401 4건 → 0 |
| R2593-02 | medium | `upgrade/archive.js` | 하드링크 사본이 압축 해제 상한 우회(20KB → 1.22GB). 사본 생성 중 누적 크기·개수 검사 |
| R2593-03 | low | `util/collectRequestQueue.js` + 웹 `collectDropText.js` | 미인출 TTL 만료가 폐기 기록 없이 사라짐 → drops + 사유 3종 |
| R2593-04 | low | `central/ingestStats.js` | 상한 도달 시 검증된 행도 밀려남(pullStats 형제) |
| AUTHZ-01 | medium | `routes/api/relaytopo.js` + 웹 | tools 권한으로 전 사이트 IP·SSH 계정명 → 비-admin 가림 + `addressHidden` |
| AUTHZ-02 | low | `routes/api/hardwareGpu.js` | ip-ping 이 임의 IP 수용 → 그 vCenter VM IP 만 |
| EDGE-1 | medium | `agent/svcmonConfigPull.js` | ack 1회 실패로 배정 영구 pending → ack 성공 뒤에만 appliedSig |
| EDGE-2 | medium | `agent/scanner.js` | 결과 회신 거부를 삼킴 → 상태·콘솔 |
| EDGE-4 | low | `agent/idracScanWorker.js` | 회신 실패가 상태에 없음 → `postError` |
| DATA-01 | medium | REST 스토리지 수집기 5종 | 사용량 결측 → 0 → null |
| DATA-02 | medium | `gpu/poller.js`·`store.js` + 웹 | MIG N/A 가 0% 로 저장·평균 → null + `utilNA` |
| DATA-04 | low | `gpu/guestops.js` | nvidia-smi 빈 칸 → 0 → null |
| DATA-05 | low | 웹 `Overview.jsx` | 분모 0 사용률 0% → '—' |
| UI-01 | low | 웹 `SpecialTools.jsx`·`console/nav.js` | 준비 중 도구 빈 화면 → 안내 |
| UI-02 | low | 웹 `App.jsx` | 꺼진 업그레이드 탭 빈 화면 → 안내 |
| UI-03 | low | 웹 `styles.css` | 400px 상태바 라벨 세로 쪼개짐 → 2×2 |
| PERF-2 | low | `release-notes.js` | 1.9MB 재파싱 23~48ms → mtime 캐시 0ms |
| DEPS-01 | low | `util/ping.js` | 손 풀 concurrency 0 → 무측정 → `poolRun` |
| DEPS-02 | low | `upgrade/upgrade.js` | 튜플 비교기 이름 충돌 → `cmpVersionTuple` |
| DEPS-03 | low | `ipam/scanStore.js` | IPv4 파서 사본 → `util/ipv4` |
| DEPS-04 | low | web lockfile | dompurify 3.4.15 → 3.4.16(범위 안 patch) |
| DEPS-05 | info | `.github/workflows/ci.yml` | 루트 `npm audit` 추가 |
| DEPS-06 | info | `test/arch2579.test.js` | 재수출 edge 도 순환 검사(SCC 여전히 4) |

## 남긴 것 (판단 필요)

- **DATA-03(가능성)** — DISCONNECTED 호스트가 사용률 분모에만 남아 글로벌 CPU·MEM % 가 낮게 나올 수 있다(합성 스냅샷에서 50% → 33%).
  실 vCenter 가 끊긴 호스트의 `summary.hardware` 를 계속 주는지 확인하지 못했다. 고치면 KPI 의 '전체 용량' 표시도 바뀌므로 실데이터 확인 후.
- **PERF-1(확정)** — 30초마다 IPAM 원장 재구성·서명 50~85ms(내용이 같아도). 건너뛰면 외부 ipam.db 리더의 신선도 계약이 바뀐다 — v2.590 과 같이 사용자 결정.
- **EDGE-3(가능성)** — PDU·SAN push 가 위임 장비는 있는데 스냅샷 0건이면 보내지 않는다. v2.583 이 의도한 설계(빈 목록으로 덮지 않기).
- **DEPS-07(보고)** — 메이저: express 5 · vite 8 · vitest 5(dev critical 해소) · react 19 · recharts 3 · react-simple-maps 5 · undici 8 · concurrently 10.
- 손 풀 7곳·버전 비교 사본 3곳·/24 기준 파서 3곳은 입력이 고정·검증돼 잠재 상태다(CLAUDE.md v2.593 항목에 파일 목록).

## 반증된 것

- AUTHZ-03 svcmon 쓰기의 범위 계정 개방 — v2.506 S1 N-2 가 문서화한 설계(svcmon 에는 vCenter 축이 없다).
- PERF-3 1회성 조회 36~40ms — 폴링이 아니라 결함이 아니다(v2.577 memoJson 규약은 상시 폴링 라우트 대상).
- PERF-4 — 결함 없음 기준선 기록.

## 검증

서버 3,083 · 웹 1,605 · lint 0 · 빌드 · `test/audit2593.test.js` 17건(변이 18/18) · Chromium 1440/400 6화면(오류 0 · 넘침 0 · 스크린샷 판독).
⚠ 실장비 확인 없음 — iDRAC 동시 세션·Redfish DELETE·실 스토리지 결측 응답·MIG 출력은 전부 가짜 서버·합성 입력 재현이다.
