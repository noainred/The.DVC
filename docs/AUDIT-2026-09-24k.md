# 14차 점검 — 10축 병렬 감사 + 발견별 2인 반증 (v2.603, 2026-09-24)

요청: "자세하게 3번 더"(1회차). 방식은 v2.602(`AUDIT-2026-09-24j.md`)와 같고 첫 축은 **v2.602 회귀**(`git diff 9c1b4c1..8880749`)다.
'자세하게' 에 맞춰 수정 보고서는 ID 마다 재현 입력·수정 전/후 실측·위치·변이 결과·남은 한계를 빠짐없이 적게 했다
(scratchpad `fix2603_{a..f}.md`). 수정은 파일이 겹치지 않는 6그룹 병렬, 변이 검증 기준은 감사 기준 커밋 `c766e01`.

## 결과 요약

| 축 | 발견 | 확정 | SPLIT | 가능성 | 반증 | 고침 |
|---|---:|---:|---:|---:|---:|---:|
| v2.602 회귀 | 7 | 4 | 0 | 0 | 3 | 4 |
| 남은 후보·형제 | 3 | 2 | 1 | 0 | 0 | 3 |
| 인가·범위 | 3 | 3 | 0 | 0 | 0 | 3 |
| 중앙 수신 계약 | 6 | 6 | 0 | 0 | 0 | 6 |
| 수집기 정확성 | 8 | 4 | 1 | 1 | 2 | 6 |
| 웹 화면 | 2 | 1 | 1 | 0 | 0 | 2 |
| 타이머·자원 | 2 | 1 | 1 | 0 | 0 | 2 |
| SQLite | 5 | 3 | 2 | 0 | 0 | 3(2 반증) |
| 보안 | 6 | 5 | 0 | 1 | 0 | 6 |
| 엣지 계약 | 5 | 5 | 0 | 0 | 0 | 5 |
| **합계** | **47** | **34** | **6** | **2** | **5** | **40** |

감사 반증 5건: iDRAC 비전력 섀시 non-404 · XtremIO 8클러스터 상한(XMS 최대 8) · scrubberFor 같은 길이 제자리 변경(이론) ·
베어메탈 df 비마운트 경로 · PDU VA 단위. 수정 단계 반증 2건: **DB2603-04**(ping meta 는 대상 선택 시 1회 — 폴링 아님) ·
**DB2603-05**(로스터 조건 새 SQL 이 오히려 44.1 → 39.0ms). 후속 **17건**도 같은 릴리스에 고쳤다.

⚠ **이전 회차 반증을 뒤집은 2건**: EDGE2603-04(현재 사용자 — v2.601 EDGE2601-05·v2.602 EDGE2602-04 에서 반증) ·
EDGE2603-05(파트 장애 꺼짐 — v2.601 EDGE2601-07 에서 반증). 이전 판정의 **근거 자체는 맞았다**(옛 행은 조회 시 stale 로 재판정되어
인원을 부풀리지 않는다 / 화면 문구가 거짓은 아니다). 새로 확인한 것은 ① latest 표가 prune 대상이 아니라 옛 행이 **무기한** 남고
화면이 대상 아닌 서버를 '확인 불가' 로 계속 센다는 점, ② 중앙이 그 엣지에 꺼짐을 **스스로 내려보내** 원인을 아는데도 추측한다는 점이다.
**반증 기록은 '그 근거로는 결함이 아니다' 이지 '결함이 없다' 가 아니다** — 다음 감사가 다른 근거로 다시 볼 수 있게 근거를 함께 남길 것.

회귀 테스트: `server/test/audit2603{a..f}.test.js` 73건 + 웹 vitest(`nsxUnknown2603`·`ipamSheetScope2603`·`logAnalysisButton2603`·
`nsxLimit2603d`·`licenseScopeText`·`powerstoreVolumeText`·`dailyReportText`·`partFaultText`). 변이 검증: a 11/11 · b 8/8 · c 29/29 · d 21/21 · e 10/10 · f 10/10.

## v2.602 가 만든 결함(회귀)

| id | 심각도 | 재현 → 수정 |
|---|---|---|
| RECENT2603-01 | medium | v2.602 가 만든 NSX 매니저 'unknown' 이 V4·관제 콘솔 네트워크 타일에서 **critical(2)** — degraded(1)보다 나쁘게. 배지는 원문 'unknown' → `managersUnknown` + 타일 1 + `nsxManagerBadge`('상태 확인 불가' 회색) + 표 행 판정 `nsxManagerLevel` 하나(후속에서 degraded 행 2→1 불일치도 정리) |
| RECENT2603-03 | low | v2.602 의 vmtrack changes prune 이 삭제된 VM 의 전원 전이 행을 **무기한** 보존 → 현재 로스터 VM 으로 한정(59.9만 행에서 22,400 → 11,704) |
| RECENT2603-04 | low | 연결 테스트 `counts.alerts/nodes` 가 null 을 0 으로(v2.602 RECENT2602-02 의 형제) → null + 화면 '—' |
| RECENT2603-06 | info | SAN perfDb `pruneNow` 가 진행 중 정리를 공유해 새 보존일 미적용 → 끝난 뒤 새 경계로 한 번 더 |

## 주요 확정 결함

- **CEN2603-01 (high)** `gpu-guest-data` 의 byVm·byHost 에 상한이 없어 토큰 하나로 중앙 메모리를 수 GB(30,000 VM × 60회 push = 180만 항목, RSS +486MB) → 전체 5만·엣지별 2만 · 미등록 vCenter 버림 · `omitted`·`unregistered`.
- **WEB2603-01 (high)** IPAM '서브넷 대장' 버튼이 클릭 이벤트 객체를 vCenter id 로 넘겨(`?vcenterId=[object Object]`) **대장이 통째로 빔** → `sheetScopeArg` + 세대 가드 + '기본 인자 함수를 핸들러에 직접 넘기는 곳' 전수 스윕(0건). Chromium 실제 클릭으로 254개 확인.
- **CEN2603-03** IPAM 원장 `push(...applied)` 14만 행 RangeError → 반복 push. 후속에서 `util/pushAll.js` 로 **37곳**(store 20 · nsx/store 6 등) + 스윕 테스트.
- **DB2603-01** ipam.db 열 추가가 외부 리더 잠금(SQLITE_BUSY)을 '열 이미 있음' 으로 삼켜 **프로세스 끝까지 NDJSON 폴백** → table_info 로 없는 열만 · 'duplicate column name' 만 삼킴 · 잠금은 재시도(WAL 전환 없음). 후속에서 vmtrack·linkcheck 같은 패턴도.
- **CEN2603-05** `toString`/`valueOf` 를 가진 본문 객체로 **16개 경로 500(119건)** → `util/coercionTrap.js`(라우터 입구에서 자기 속성 제거) + `strOf`.
- **CEN2603-02·04·06** 스캔 결과·agentIdentity 무제한 · 미등록 vCenter ping-result 로 실제 결과 밀어내기.
- **AUTHZ-2603-01·02·03** 라이선스 만료가 범위 계정에 범위 밖 NSX 키·Horizon 호스트명 · SAN 월간 점검 detail 주소 · PDU db-stats 라우트 가림(도달 불가) → 범위 절단 + `omittedOutOfScope`(화면 한 줄) · `scrubStringsDeep` · 라우트 순서 + 경로는 admin 만.
- **LEFT2603-01·02** NSX group-members 가 매니저 id 의 콜론에서 잘라 엉뚱한 그룹 조회 · IPv6 호스트(`[fd00::1]:443`)를 ':' 로 잘라 '[fd00' → `splitNsxGroupId` · `util/hostPort.js splitHostPort`(인증서 점검·중계 점검·VM 콘솔·NSX 프록시) + HAProxy 설정 줄 `ipv6@`.
- **SEC2603-01·02·04·05** ReDoS: SAN sensorshow **3차**(공백 3천 자 8,869ms → 0.0ms) · `stripUemcliBanner` 빈 줄 10만 11,870ms → 16.9ms · 베어메탈 FC 속도 · Unity 버전 · Isilon 풀 셀 — 전부 옛 정규식과 결정적 난수 수천 건 **결과 동일성** 대조.
- **SEC2603-03 · 후속** svcmon 키워드 검사와 RMA URL 점검이 응답 본문 **전체**를 읽은 뒤 잘랐다(주석의 '256KB 상한' 은 사실이 아니었다) → `util/readPrefix.js` 스트림으로 256KB 에서 끊기(200MB 송신 시도에 실제 64MB 미만 · 연결 닫힘).
- **SEC2603-06** GPU 게스트 결과 파일 다운로드 8MB 상한.
- **COL-2603-01·04·05·07** PowerStore SSH 알람 못 읽음이 '0건 ok' · NSX 규칙·IDS·라이선스 조회 실패가 0 · PowerStore 볼륨 size 결측 0 합산 → null + 사유(`alertOutputUnread`·`rulesFailed`·`listsFailed`·`sizeUnknown`, 위협 분석·라이선스·스토리지 화면까지).
- **COL-2603-02** Isilon 노드 통계를 lnn 으로 조인(devid≠lnn 이면 다른 노드 값) → id(devid) 우선.
- **COL-2603-06** 물리 GPU Windows 보정이 다음 줄 재대입에 덮여 도달 불가.
- **DB2603-02** 청크 없는 보존 DELETE 6모듈 7곳(ping 100만 행 최대 정지 1,009ms → 50ms) → `util/chunkedPrune.js createPruneFlight`(진행 공유 한 벌) + **'시계열 한 방 DELETE 금지' 스윕**(허용 15항목 사유 포함).
- **DB2603-03** 음수 `VMTRACK_RETENTION_DAYS` 가 이력 **전체 삭제** → 정리 건너뜀 + 경고.
- **TIM2603-01·02** vCenter 로그 주기 로드 경로 1ms 루프 · 일일 보고 전 채널 실패 시 1분마다 재시도(12시간 720회 → 10~15회, 15→30→60분) + 화면 `failStreak`·`nextRetryAt`.
- **LEFT2603-03** vmStats CPU·메모리 분모 분리(잠재) · 후속 rightsizing 피크 결측 → 권고 보류(`held`, 예전엔 메모리 32GB → 1GB 권고).
- **EDGE2603-01·02·03** 파트 장애·SAN 사용량 즉시 push 가 주기 push 와 겹치면 조용히 버려짐 → `_again` · 'DB 비활성' 분기의 거짓 `statusSent:true`.
- **EDGE2603-04·05** 위 '뒤집은 2건'. 파트 장애는 새 kind `off`(+`offSource`) 와 화면 '꺼짐(중앙 설정)', 전부 꺼짐이면 '장애 없음이 아니라 점검 안 함'.
- **WEB2603-02** 로그 분석 주 실행 버튼이 맨 글자 → `login-btn`.

## 검증

- 서버 전량 3,549건(통합 중 실패 1건 — v2.602 성능 테스트 200ms 상한이 전량 병렬 부하에서 260ms → 회귀 5.6초와 확실히 갈리는 1초로) · 웹 vitest 127파일 1,759건 · eslint 0 · vite build · 문서 생성기 4종(새 env 10여 개 반영).
- Chromium 목 스택 29화면 × 1440/400: 가로 넘침 0 · 콘솔 오류 0. 그룹 e 는 IPAM 서브넷 대장 실제 클릭·NSX unknown 응답 가로채기까지 확인.

## 정직 기록 — 확인하지 못한 것

- HAProxy `ipv6@주소:포트` 설정 줄은 이 환경에 haproxy 가 없어 `haproxy -c` 로 확인하지 못했다(첫 IPv6 매핑 배포 때 확인 — 실패하면 배포가 멈추고 원본 유지). 이 컨테이너는 IPv6 소켓도 지원하지 않는다.
- 범위 계정 라이선스 문구·일일 보고 실패 상태·PowerStore 크기 미상·파트 장애 '꺼짐' 화면은 목 데이터로 만들 수 없어 브라우저로 보지 못했다.
- Isilon devid≠lnn·PowerStore size 결측·GPU 게스트 큰 결과 파일은 합성 재현이다.
- 엣지 env(`PARTFAULT_ENABLED=false`)로 끈 경우는 중앙이 알 수 없어 여전히 silent 다.
- `coercionTrap` 이 지운 뒤 일부 값은 '[object Object]' 문자열로 저장될 수 있다(500 은 아님 — 형식 검사는 각 라우트 몫).
- 공유 토큰은 배정 범위 없는 이름으로도 스캔 결과를 전체 상한 안에서 올릴 수 있다(기존 테스트가 고정한 동작).
- 스윕 허용 목록의 '후속 후보'(capacity samples·storage api_history 등 한 방 DELETE)는 규모를 재지 않았다.
