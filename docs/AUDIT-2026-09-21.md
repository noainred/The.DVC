# 전체 소스 감사 — 버그 · 개선 · 보안 (2026-09-21, v2.573.0 기준)

> 사용자 요청: "전체 소스 분석해서 버그, 개선 가능한 부분, 보안 취약점 각각 30개씩 찾아서
> 중요도와 영향도 분석해서 보고하고 파일로 저장해줘"
>
> 사용자 선택 4가지(이 보고서의 규약):
> 1. **확정분만 보고** — 30개를 채우려고 추측을 섞지 않는다. 모자라면 `확정 N건` 이라 적고
>    추측은 `[미확인]` 절에 **분리**한다.
> 2. **전체 검증 — 전부 실행 재현** (Chromium 포함)
> 3. **범위: 서버 + 웹** (`pyportal/` 제외)
> 4. PR #542 는 머지만(버전업 없음) — 완료

## 0. 결론 — 확정 건수

| 분류 | 요청 | **확정** | 미확인(분리) |
|---|---|---|---|
| 버그 | 30 | **24** | 3 |
| 보안 취약점 | 30 | **18** | 4 |
| 개선 가능 | 30 | **12** | — |
| 합계 | 90 | **54** | 7 |

**30개를 채우지 못했다.** 이 저장소는 2026-06-27 이후 감사를 **12차례** 거쳤고
(`docs/AUDIT-*.md` 12개) 그때마다 확정분을 고치고 회귀 테스트로 고정해 왔다.
같은 축(명령 주입·경로 순회·SQL 주입·전역 TLS·토큰 바인딩)을 이번에도 전수로 훑었으나
**확정 0건**이다 — 그 사실 자체를 §4 에 근거와 함께 남긴다. 숫자를 채우려고
'개선 여지' 를 '버그' 로 옮겨 적지 않았다.

### 증거 등급 (모든 항목에 표기)

| 등급 | 뜻 |
|---|---|
| **A** | 주 세션에서 **직접 실행해 재현**했다(HTTP 응답·종료코드·fd 수·브라우저 계산값) |
| **B** | 분석 에이전트가 실측했고 **주 세션이 소스로 기전을 확인**했다 |
| **C** | 잘못된 **형태**는 실행으로 확인했으나 **도달 경로를 증명하지 못했다** → `[도달 미확인]` |

---

## 1. 버그 — 확정 24건

### 1-1. 치명 (서비스 정지·응답 없음)

#### ⚠⚠ BUG-01 [A] PDU 시계열 2개 라우트가 쿼리 하나로 **영구히 매달린다** (fd 고갈 DoS)
- **위치**: `server/src/pdu/db.js:128-133 rangeOf` → `:145`·`:159`(SQL) / `routes/api/pdu.js:102`·`:113`
- **기전**: `rangeOf()` 가 `hours`/`to` 를 클램프하지 않아 `bucketMs` 가 `Infinity`·`NaN` 이 되고,
  그 값이 **템플릿 리터럴로 SQL 에 보간**된다(`(ts/${bucketMs})`). SQLite 가 식별자로 파싱 →
  `prepare()` throw → **async 핸들러에 try/catch·`guarded()` 없음** → express 4 가 잡지 않음 → 응답 없음.
- **실측(주 세션 재현)**:
  | 요청 | 결과 |
  |---|---|
  | `/api/tools/pdu/series/power?ids=x&hours=24` | `200 / 0.0088s` |
  | `/api/tools/pdu/series/power?ids=x&hours=1e400` | **`000 / 8.002s`(무응답)** |
  | `/api/tools/pdu/series/power?ids=x&to=1e400` | **`000 / 8.002s`** |
  | `/api/tools/pdu/series/env?ids=x&hours=1e400` | **`000 / 8.002s`** |
  서버 로그: `[fatal] unhandledRejection … Error: no such column: Infinity at powerSeries (pdu/db.js:144:24)`
- **fd 고갈 실측**: 매달린 요청 40개 → 프로세스 fd **111 → 151**(정확히 +40, 소켓 71개).
  응답이 끝나지 않으므로 클라이언트가 끊을 때까지 잡힌다. **브라우저 탭처럼 시한 없는
  클라이언트면 무기한**이다.
- **도달 권한**: `requirePerm('tools') + fullScopeOnly`. operator 는 `tools` 를 기본 보유한다.
- **대조**: 같은 `${bucketMs}` 보간을 쓰는 `sanswitch/perfDb.js` 는 `routes/api/sanSwitch.js:74` 가
  `Math.min(24*90, …)` 로 클램프해 **안전하다** — PDU 라우트에만 그 클램프가 없다.
- **중요도 🔴 치명 / 영향도 🔴 높음**(무인증은 아니지만 operator 1명이 포탈 fd 를 고갈시킬 수 있고,
  화면은 영원히 로딩이며 오류도 뜨지 않는다)

#### ⚠⚠ BUG-02 [A] `POST /api/svcmon/push-now` 가 응답 없이 매달리고, 실패가 상태에도 안 남는다
- **위치**: `server/src/agent/svcmonPush.js:100`(catch 없음) + `routes/svcmon/edge.js:159`(try 없는 async)
- **소스 근거**: push 진입 함수 6개 전수 확인 — 나머지 5개(`logQueryWorker`·`idracScanWorker`·
  `pingWorker`·`configPush`·`gpuGuestPush`)는 내부 함수에 catch 1~3개, **`pushSvcmonNow` 만
  `try { … } finally { running = false }` 로 catch 가 없다.** 유일한 예외다.
- **실측 A/B**(목 서버를 엣지로 · `CENTRAL_URL=http://127.0.0.1:1`):
  - `POST /svcmon/push-now` → **`code=000 time=20.002`**(무응답)
  - `POST /svcmon/config-pull-now`(같은 파일 형제) → **`code=202 time=0.0044`** + `{"ok":false,"reason":"fetch failed"}`
  - 서버 로그: `at async pushSvcmonNow (svcmonPush.js:153) / at async edge.js:161`
- **더 나쁜 것**: `last = {…}` 가 try 끝에 있어 **실패가 상태 객체에 남지 않는다**.
  `edgelog/spec.js` 가 이 값을 화면에 실으므로 엣지 로그가 "아직 안 보냄" 이라 말한다
  → CLAUDE.md v2.566 이 기록한 **무음 실패의 재발**(그때는 10일간 수집이 죽어 있었다).
- **중요도 🔴 높음 / 영향도 🟠 중간**(진단용 수동 버튼이라 상시 경로는 아니지만,
  중앙이 데이터를 못 받는 상황에서 **원인을 보려고 누르는 바로 그 버튼**이 죽는다)

#### ⚠ BUG-03 [A] async 라우트 138건이 try/catch 없이 노출 — throw 시 그 요청은 영원히 무응답
- **전제 실측**(express 4.22.2 실제 기동): 동기 throw → 전역 핸들러 500 ✓ / **async throw → 응답 없음** ✗
- `server/src/index.js:10` 의 `process.on('unhandledRejection')` 이 로그만 남기고 **계속 실행**하므로
  프로세스는 죽지 않는다 — 대신 **그 요청만** 영원히 응답이 없다(과장하지 않기 위해 명시).
- **스윕**: `server/src/routes/` 의 async 라우트 **284건 중 138건**이 try/catch·`guarded()` 없음.
- ⚠ **138건 전부가 결함은 아니다** — 실제로 reject 할 수 있는 입력이 있어야 성립한다.
  그중 **실제로 매달리는 것을 찾아낸 것이 BUG-01·BUG-02** 다. 나머지는 **잠재적 표면**이다.
- 규약은 이미 있다 — CLAUDE.md v2.548 S1 · v2.562 `guarded()` · `perf/monitor.js:100` 주석.
  **패턴이 알려져 있는데 전수 적용이 안 된 것**이 결함이다.
- **중요도 🟠 중간(표면) / 영향도 🔴 높음(성립하면 BUG-01 급)**

### 1-2. 오류 없이 틀린 값 — `Number(null) === 0` 계열 (**7번째 재발**)

> CLAUDE.md v2.561 이 이 계열을 `util/numOrNull.js` 하나로 통합했는데, **8곳이 남아 있다.**
> 실행 확인: `const f=(v)=>Number.isFinite(Number(v))?Number(v):null` 에 대해
> `f(null)=0` · `f('')=0` · `f([])=0` · `f(true)=1` · `f([7])=7`.
> (`f(undefined)`·`f('x')` 만 올바르게 `null`)

#### BUG-04 [A] `storage/db.js:179` — 일 롤업 `max_used` 가 null 을 **0** 으로
```sql
max_used = MAX(COALESCE(capacity_daily.max_used, 0), COALESCE(excluded.max_used, 0))
```
- v2.561 이 `used_bytes` 는 고쳤는데 `max_used` 의 COALESCE 는 남았다. CLAUDE.md v2.550.3 이
  **정답 형태**를 명시해 뒀다 — `NULLIF(MAX(IFNULL(x,-1e308), IFNULL(excluded.x,-1e308)), -1e308)`
  (`bmusage/db.js:209-213` 이 실제로 그렇게 쓴다). 스토리지만 문서와 반대 형태다.
- **도달 경로는 v2.561 이 이미 기록한 그대로**: `unitySsh.js:163` 이 풀 목록 실패 시
  `usedBytes: null` 을 정직하게 내보낸다. 수집 주기가 1시간이라 하루 종일 못 읽으면
  **`maxUsed: 0`(그날 최대 사용량 0바이트)** 가 저장된다.
- **정직 기록**: `web/src` 에 `maxUsed` 소비처 **0건** → 지금 화면에 보이는 거짓은 아니고
  **API·DB 계약의 거짓**이다.
- **중요도 🟠 중간 / 영향도 🟠 중간**

#### BUG-05 [A] `linkcheck/db.js:173·322` — **다운된 링크의 응답시간이 `0ms`(즉시 응답)**
- `:173` 이 실패 표본도 `n=n+1` 로 세고 `ms_sum` 에 `iOr(v.totalMs, 0)`(도달 0단계면 0)을 더한다.
  `:322` 가 `ms_avg = Math.round(ms_sum / n)`.
- **실행 재현**:
  | 상황 | 롤업 | ms_avg |
  |---|---|---|
  | 하루 종일 다운(288주기) | `n=288, ok_n=0, ms_sum=0` | **`0`** ← "즉시 응답" 이라는 거짓 |
  | 절반 다운(성공분 40ms) | `n=288, ok_n=144, ms_sum=5760` | **`20`** ← 실제 성공분 평균은 **40** |
- **대조**: `bmusage/db.js:96·199` 는 `cpu_n` 분모를 **따로** 두고 주석이
  "`cpu_n` 은 값이 있을 때만 1 을 더한다 — null 을 분모에 넣으면…" 이라고 적고 있다.
  linkcheck 에는 그 열이 **없다**. v2.552 가 화면에서 고친 `0ms` 거짓이 **DB 층에 남아 있다**.
- **중요도 🟠 중간 / 영향도 🟠 중간**(통신 점검의 존재 이유가 이슈 분석인데 그 자료가 틀린다)

#### BUG-06 [A] `central/edgeLogStore.js:39` — `ms: null` 이 **0**(0ms 응답)
#### BUG-07 [A] `central/edgeLogStore.js:50` — `statusFailed: null` 이 **0**("상태 점검 실패 0건")
- 생산자 `edgelog/collect.js:101` 은 `withStatus=false` 면 **정직하게 `null`** 을 준다.
  도달: `?status=0` / `withStatus:false`. 즉 '**점검을 안 한 것**' 이 '**실패 0건**' 이 된다.
- **정직 기록**: web 소비처 0건 → API 계약의 거짓.
- **중요도 🟡 낮음 / 영향도 🟠 중간**(정직 장치를 무력화하는 형태라 등급을 낮추지 않았다)

#### BUG-08 [A] `rma/testResults.js:56` — 점검 `value: null` 이 **0**
- **도달 확정**: `rma/agent.js:219` 가 실행 오류 시 `value: null` 을 보내고,
  `rma/tests.js:164` 는 값이 없으면 `?? null` 로 준다. 같은 코드베이스가 `value == null` 을
  "값 없음" 으로 정의해 두고 sink 에서 0 으로 바꾼다.
- RTT·손실률·인증서 잔여일 이력에 **가짜 0** 이 섞인다(`status:'unknown'` 이 함께 남아
  사후 구분은 가능 — 그래서 위 항목들보다 낮게 봤다).
- **중요도 🟡 낮음 / 영향도 🟡 낮음**

#### BUG-09 [A/C] `vmtrack/diff.js:41-43` — `cpu`·`memMB`·`storageGB` 가 같은 잘못된 형태
- 세 줄 모두 `Number.isFinite(Number(v.cpuCount)) ? Number(v.cpuCount) : null`.
  `v.cpuCount === null` 이면 **0 vCPU** 가 된다.
- **[도달 미확인]** 생산자는 `soapClient.js:1453`·`restClient.js:204`·`mock/generator.js:257` 셋이고
  **셋 다 `null` 을 내는 경로를 증명하지 못했다**(REST 의 `storageGB` 는 아예 미설정 =
  `undefined` → 올바르게 `null`). **형태만 확정**이고 지금 틀린 값을 만든다는 증거는 없다.
- **중요도 🟡 낮음 / 영향도 🟡 낮음**(도달 미확인)

#### BUG-10 [A] `version_4/ui.jsx:86 Spark` — x 축이 `Number(null)===0` 에 무너진다
```js
const pts = (points || []).filter((p) => p && Number.isFinite(Number(p.x)));
```
- **실행 재현**: `[{x:1700000000000},{x:null},{x:1700003600000}]` → **3점 모두 남고**
  `x0 = 0`(1970년) `x1 = 1700003600000` → **실제 점이 전부 오른쪽 끝에 뭉친다**.
  (y 축은 `v != null` 로 올바르게 거른다 — x 만 틀렸다)
- **[도달 미확인]** 호출부 4곳(`Compare.jsx:183`·`Facility.jsx:192`·`Overview.jsx:230,242`)은
  `x: p.ts` / `x: i` 를 쓰고 `p.ts === null` 을 증명하지 못했다.
- ⚠ **부수 확인**: `views/VCenters.jsx:8` 에 **이름이 같은 별도 `Spark`** 가 있다(중복 구현).
  그쪽은 `points` 가 **숫자 배열**이라 계약 자체가 다르다 — 섞어 쓰면 차트가 통째로 사라진다.
- **중요도 🟡 낮음 / 영향도 🟠 중간**(성립하면 차트가 조용히 거짓이 된다)

### 1-3. 화면이 거짓을 말한다

#### ⚠ BUG-11 [A] `HorizonSessionsPanel.jsx:269` — 조치 안내 자리에 `[object Object]`
```jsx
{kindAdvice(detail.kind) && <BoldText text={kindAdvice(detail.kind)} />}
```
- `curUserText.kindAdvice()` 는 **객체** `{waiting, text}` 를 돌려주는데 `boldText.jsx boldParts` 가
  `String(text ?? '')` 라 `"[object Object]"` 가 된다. 같은 함수를 쓰는
  `CurrentUsers.jsx:258` 은 `adv.text` 로 **올바르게** 쓴다 — 한쪽만 틀렸다.
- **부수 결함**: 객체는 **항상 truthy** 라 `ok`(문구 없음)일 때도 렌더된다.
- **실행 재현**(boldParts 를 소스에서 떼어 실행):
  | kind | 화면 출력 | 올바른 문구 |
  |---|---|---|
  | ok | `[object Object]` | (안 나와야 함) |
  | no-agent | `[object Object]` | 이 VM 에서 …값을 찾지 못했습니다… |
  | stale | `[object Object]` | 게스트 안의 발행 작업이 멈췄거나… |
  | guest-error | `[object Object]` | 게스트에서 quser 를 실행하지 못했습니다… |
  | unparsed | `[object Object]` | 값은 받았지만 …형식을 읽지 못했습니다 |
- v2.569 React #31 과 같은 계열(객체를 문자열 자리에 넣는 것).
- **중요도 🟠 중간 / 영향도 🟠 중간**(Horizon 세션 진단이 통째로 무의미해진다)

#### BUG-12 [A] `StorageGrowthTool.jsx:135` — **`null` 사용률이 초록 '정상'**
```jsx
meta={t.pct != null ? `${t.pct}%` : '사용률 미상'}
tone={t.pct >= 90 ? 'bad' : t.pct >= 75 ? 'warn' : 'ok'}
```
- **실행 재현**: `pct=null` → `meta='사용률 미상'` 인데 `tone='ok'`(초록).
  **글자와 색이 반대말을 한다** — 사람은 색을 먼저 읽는다(CLAUDE.md v2.526 `healthBadge`·
  v2.542 `sectionBadge` 와 같은 유형).
- **중요도 🟠 중간 / 영향도 🟠 중간**

#### BUG-13 [A · Chromium] 표 머리글의 영문이 **대문자로 샌다**
- `styles.css:243 thead th { … text-transform: uppercase; }` 가 전역이고,
  이를 덮는 `.st-table thead th { text-transform: none }`(`:1047`)은 **단 한 곳**
  (`serverTemp/ServerTempBoard.jsx:390`)만 쓴다. `.v3-table` 에는 **CSS 규칙이 아예 없다**.
- **Chromium 실측**(`http://127.0.0.1:4811/#/hosts`, 1440px, pageerror 0):
  ```
  "vCenter"  text-transform=uppercase   → 화면: VCENTER
  "Cores"    text-transform=uppercase   → 화면: CORES
  ```
- 같은 규칙으로 샐 머리글: `Guest OS`→`GUEST OS` · `iSCSI`→`ISCSI` · `vCPU`→`VCPU` ·
  `vMotion`→`VMOTION` · `kWh`→`KWH` · `Data Reduction`→`DATA REDUCTION` 등.
- CLAUDE.md 가 이미 v2.508(`kWh`→`KWH`)·v2.550(`디스크 busy`→`디스크 BUSY`)에 **두 번** 겪었다고
  적어 둔 계열이다 — 개별 문구만 고쳤고 **구조는 그대로**다.
- **중요도 🟡 낮음 / 영향도 🟡 낮음**(오독 유발. 수치로는 안 잡히고 스크린샷을 읽어야 보인다)

#### BUG-14 [A] UI 문구 11곳에 **백틱이 그대로 노출**된다
- `BoldText` 는 `**강조**` 만 해석하고 백틱은 글자로 샌다(CLAUDE.md v2.439·2.440·2.505·2.545·2.553 **실제 사고**).
- 실행 재현: `kindAdvice('no-agent').text` → 백틱 포함 문자열 그대로 반환.
- 위치(주석 제외 전수): `curUserText.js:76,77,79,115` · `powermaxCapacityText.js:107,108` ·
  `sanHealthText.js:95` · `unityCapacityPlan.js:98` · `bulkIoText.js:21` ·
  `BulkDeviceIo.jsx:203` · `BmUsage.jsx:331`
- v2.553·v2.560 이 백틱 0 을 고정하는 테스트를 만들었지만 **이 11곳은 그 스윕 밖**이다.
- **중요도 🟡 낮음 / 영향도 🟡 낮음**

#### BUG-15 [A] `serverTemp/board.js:157 sparkPath` — 수집 공백을 **선으로 이어 버린다**
- 결측을 `filter(v => v != null)` 로 **버리고** 남은 점을 전부 `L` 로 잇는다
  → 6시간 공백이 직선 보간으로 그려진다.
- **대조**: 같은 저장소의 `roomTempView.js:140` 은
  `prevTs != null && p.ts - prevTs <= gapMs ? 'L' : 'M'` 로 **subpath 를 끊는다**.
  CLAUDE.md v2.551 이 "수집이 없던 구간은 선을 잇지 않는다(`roomTempView.sparkPath` 와 같은 규약)"
  라고 못 박았는데 v2.556 신규 모듈이 그것을 따르지 않았다.
- (v2.556 이 고친 `Number(null)→0℃` 결함과는 **다른 결함**이다 — 그쪽은 고쳐져 있다)
- **중요도 🟡 낮음 / 영향도 🟠 중간**(없는 데이터를 있는 것처럼 그린다)

### 1-4. 그 밖의 확정

| id | 위치 | 내용 | 등급 | 중요도/영향도 |
|---|---|---|---|---|
| **BUG-16** | `views/PingMonitor.jsx:183` | **비활성** 대상에 "기다리면 표시됩니다" — 기다려도 안 된다(v2.509 규약) | B | 🟡/🟠 |
| **BUG-17** | `version_4/loadState.js:27` | `num()` 이 빈 문자열을 막지 않는다(`Number('')===0`) | B | 🟡/🟡 |
| **BUG-18** | `linkCheckText.tableFootnotes()` | 같은 `reason` 이면 **첫 문구만 남는다** — 나머지 조치가 사라짐 | B | 🟡/🟡 |
| **BUG-19** | 표 8곳 | **정렬 전에 잘라** 표에 넣는다 — "상위 N" 이 '앞 N 을 정렬한 것' 이 된다(v2.556 `limit` 규약 위반) | B | 🟡/🟠 |
| **BUG-20** | 값 없는 칸 | 값이 없는데 **단위를 붙인다**(`— ℃` 등, v2.534 규약 재발) | B | 🟡/🟡 |
| **BUG-21** | `security/loginMonitor.js:65 runOnce` | **재진입 가드 없음** + 수동 실행(`routes/admin/backupNetSec.js:190`)이 가드를 공유하지 않음. 폴러 규약의 **유일한 위반**(다른 수동 실행 5곳은 전부 공유) | B | 🟡/🟡 |
| **BUG-22** | GET 4건 | 객체형 쿼리(`?id[a]=1`)로 **500** — 매달리지는 않는다(전역 핸들러가 처리) | B | 🟡/🟡 |
| **BUG-23** | 표 9곳 | 다열 표에 `overflowX` 래퍼도 `minWidth` 도 없다 → 400px 에서 셀이 세로로 늘어난다(v2.562 규약) | B | 🟡/🟡 |

#### ✅ BUG-24 [B] **CLAUDE.md 미해결 항목 규명** — `chunkedPrune2453.test.js:48` 플래키 **원인 확정**
> CLAUDE.md 가 "⚠ 남은 플래키: **원인 미확정**. '재실행하면 통과' 로 넘기지 말고 확정할 것" 이라
> 적어 둔 항목이다. **제품 결함이 아니라 테스트의 벽시계 경합**으로 확정됐다.

- `chunkedDelete` 는 `setImmediate`(check 단계)로 양보하는데, 테스트는 `setInterval(…, 0)`
  (Node 가 **1ms 로 클램프**, timers 단계)이 끼어들기를 단언한다 — **루프 시간에 하한이 없다**.
- 실측:
  | 조건 | 결과 |
  |---|---|
  | 워밍업된 프로세스 200회 | **실패 180/200 (90%)** · 루프 p50 **0.083ms** |
  | `node --test` 하네스 내 20회 | interleaved=0 이 9개 중 4개 · loopMs 0.46~0.78ms |
  | 원본 구조 신규 프로세스 25회 | 0 실패 · loopMs **1.2~1.9ms**(임계 1ms 대비 여유 0.2~0.9ms) |
- 평소 통과하는 이유는 하네스·JIT 워밍업이 첫 반복을 ~1.3ms 로 부풀려 주기 때문이고 **여유가 수십 %뿐**이다.
- **수정 방향**(코드 변경이라 이번 감사 범위 밖): 1ms 타이머 대신 `setImmediate` 순서를 직접
  관측하거나 청크 수를 늘려 루프 하한을 보장하면 결정적이 된다.
- **중요도 🟢 정보 / 영향도 🟡 낮음**(CI 간헐 실패 — 제품은 정상)

---

## 2. 보안 취약점 — 확정 18건

### 2-1. 범위(scope) 우회 — 다른 법인 데이터가 보인다

> 이 저장소의 불변조건: **"조회 라우트 scope 는 예외 없이"**(`server/CLAUDE.md`) ·
> **"귀속 없는/범위 밖 데이터는 범위 계정에 노출하지 않는다"**.
> 아래는 전부 그 규약 밖에 남아 있던 라우트다. 검증은 `authzGates2536.test.js` 하니스
> (실제 `api` 라우터를 express 에 마운트하고 `req.user` 주입)로 했다.

#### ⚠⚠ SEC-01 [A · **유출 실증**] `/api/tools/vclogs/sources` 가 전 법인 vCenter id 를 준다
- **위치**: `server/src/routes/api/checksLogs.js:113`
  ```js
  api.get('/tools/vclogs/sources', requirePerm('tools'), (_req, res) => {
  ```
  **`_req`** — 사용자를 아예 보지 않는다. 형제 라우트 `:78` 은 `allowed.has(reqVc)` 로 올바르게 검사한다.
- **실측**(vCenter 3개 등록, 범위 계정은 `vc-asia-seoul` 만 허용):
  - 범위 계정 응답 `{"local":["vc-asia-seoul","vc-eu-warsaw","vc-us-east"],"remote":[]}`
  - admin 응답과 **바이트 단위로 동일**
- **중요도 🔴 높음 / 영향도 🟠 중간**(vCenter id 는 법인 식별자다. 목록만으로도 조직 구조가 드러난다)

#### ⚠⚠ SEC-02 [A] `routes/api/bmUsage.js:124` — **Set 에 `.includes()`** → 범위 계정은 항상 실패
```js
if (!target || (allowed && (!target.vcenterId || !allowed.includes(target.vcenterId)))) {
```
- `auth/scope.js:16-26 scopedVcenterIds()` 는 **Set 또는 null** 을 돌려준다 → `Set.prototype.includes`
  는 없다 → `TypeError: allowed.includes is not a function`.
- **저장소 전수**: `allowed.has(` **110곳** vs `allowed.includes(` **이 1곳**. 유일한 오용이다.
- **오늘 아무도 못 느끼는 이유**: 전체 범위 계정은 `allowed === null` 이라 `&&` 에서 단락된다.
  **범위 제한 계정에만** 터진다.
- async 라우트라 **BUG-03 과 겹쳐 응답 없이 매달린다**(보안 결함이 가용성 결함이 된다).
- **중요도 🟠 중간 / 영향도 🟠 중간**(유출이 아니라 **기능 정지** 방향의 실패 — 안전한 쪽이긴 하다)

#### SEC-03 [A] `routes/api/bmUsage.js:203-217` — 같은 객체 리터럴 안에서 **일부 필드만** scope
```js
settings:      snap?.settings || null,                                // 무필터
counts:        snap && !allowed ? (snap.counts || {}) : null,        // 올바름
skippedCounts: snap && !allowed ? (snap.skippedCounts || {}) : null, // 올바름
authStops:     snap?.authStops || [],                                 // 무필터
```
- `authStops` 에는 정지 대상 id(`<agent>|os|<key>`)가 담긴다 → **범위 밖 법인의 서버 키**가 나간다.
- CLAUDE.md v2.550.3 이 "그 데이터가 응답에 실리는 경로를 **전부** 찾을 것" 이라 적은 바로 그 유형.
- **중요도 🟠 중간 / 영향도 🟠 중간**

#### SEC-04~07 [A] scope 없이 **200 을 돌려주는** 조회 4건

| id | 라우트 | 위치 | 나가는 것 | 중요도/영향도 |
|---|---|---|---|---|
| **SEC-04** | `GET /api/tools/bm-usage/activity` | `bmUsage.js:155` | 작업 로그 링버퍼 500건 — **서버명·host(iDRAC/OS 주소)**·오류 300자, 전 법인 혼재 | 🟠/🟠 |
| **SEC-05** | `GET /api/tools/curuser/activity` | `curUser.js:98` | vCenter 이름 + **그 법인 접속 사용자 수 추이** | 🟠/🟠 |
| **SEC-06** | `GET /api/ping/edge/overview` | `routes/ping.js:113` | 전 법인 **엣지 포탈 host:port** — **권한 게이트조차 없다**(viewer 도달) | 🔴/🟠 |
| **SEC-07** | `GET /api/tools/vmseries/status` | `vmSeries.js:98` | `lastResult.errors[]/skipped[]` 의 범위 밖 vcenterId + 수집 실패 메시지 | 🟠/🟡 |

- **정직 기록**: 네 건 모두 범위 계정 토큰으로 실제 호출해 **200** 을 받았다. 다만 이 환경의
  목 데이터가 비어 있어 **실제 데이터가 넘어가는 것까지 실증한 것은 SEC-01 뿐**이다.
  나머지는 **구조 결함(필터 부재)** 이 확정이다.
- **형제 라우트가 제대로 하고 있다는 것이 근거다**(같은 파일·같은 모듈):
  · SEC-04 ↔ `bmUsage.js:45` 는 `applyScope()` 로 6개 필드를 전부 거른다
  · SEC-05 ↔ `curUser.js:44,76,111` 은 전부 `scopeFilter` 를 쓰고 **파일 머리말이 그 규약을 선언**한다
  · SEC-06 ↔ 같은 파일 `:68,79,84,129` 는 `vcScope(req)` 를 쓴다
  · SEC-07 ↔ 같은 파일 `:38-44` 는 `allowed` 로 거르는데 **옆 필드 `status:`** 가 우회한다
- ⚠ **SEC-06 은 권한 게이트도 없다** — `index.js:297` 이 `/api/ping` 을
  `authMiddleware + requireEnrolled` 로만 mount 한다. 엣지 주소는 이 저장소가
  `linkCheck.js:8`·`portalCheck.js:9`·`edgeLog.js:7` 에서 **adminOnly + fullScopeOnly** 로
  묶어 온 정보다("전 법인 엣지 주소·내부 IP 가 담긴다"). **이 라우트만 무권한이다.**

#### SEC-08 [A] 공개 API `/api/v1/faults/parts` — `requiresFullScope` 누락
- **선언** `publicapi/allowlist.js:106-110` 에 `requiresFullScope` 가 없다. 집행부 `:143` 은
  그 플래그만 본다. 핸들러 `routes/publicApi.js:283-306` 도 `inScope` 를 쓰지 않고 전량을 낸다.
- **대조**: 내부 동등 라우트 `GET /api/tools/part-faults`(`routes/api/partFaults.js:131`)는
  `fullScopeOnly` 로 **403** 이고 머리말 `:11` 이 그 이유를 적고 있다.
  → **같은 데이터가 세션 경로에서는 거절되고 공개 API 키 경로에서는 전량 나간다.**
- 응답 필드 `agent`(법인 엣지 이름)·`deviceKey`(서비스태그)가 그대로 실린다.
- `requiresFullScope` 는 **선언에 손으로 붙이는 플래그**이고 전체 카탈로그에서 **단 2곳**
  (`/capacity/storage`·`/capacity/storage-growth`)에만 붙어 있다. 내부 라우트의 `fullScopeOnly`
  와 대조하는 검사가 없다.
- **중요도 🟠 중간 / 영향도 🟠 중간**

### 2-2. 전송·SSRF

#### ⚠⚠ SEC-09 [A] `svcmon/checker.js:113` — **기본(TLS 검증) 경로에 DNS 리바인딩 가드가 없다**
```js
...(test.insecure ? { dispatcher: insecureAgent } : {}),
```
- `test.insecure` 가 **거짓이면 dispatcher 가 없어 전역 `fetch`** 로 나간다.
  CLAUDE.md v2.506 이 직접 경고한 그것 — "**`globalThis.fetch` 에는 lookup 이 없다.**"
- **실측**(CLAUDE.md 가 정한 실증 기준 `ESSRFBLOCKED` 사용):
  | 경로 | 결과 |
  |---|---|
  | `test.insecure=true`(dispatcher 有) | `fetch failed ← **ESSRFBLOCKED**` ✓ |
  | 기본 경로(dispatcher 無) | `fetch failed ← **ECONNREFUSED**` ✗ **연결이 실제로 시도됐다** |
- **왜 v2.537 스윕이 못 잡았나**: 그 테스트는 `new Agent(` 를 전수로 훑어 **모든 Agent 에 훅이
  붙었는지** 본다. 여기는 **Agent 를 아예 안 쓰는 경로**라 스윕의 사각지대다.
- **중요도 🔴 높음 / 영향도 🟠 중간**(svcmon 대상은 관리자가 등록한 URL 이라 외부 임의 입력은
  아니지만, 이름이 사내 주소로 바뀌는 리바인딩 창이 그대로 남는다)

#### ⚠ SEC-10 [A] `util/smtp.js:233` — **STARTTLS 다운그레이드가 조용히 일어난다**
```js
if (cfg.startTls !== false && !cfg.secure && ehloCaps(ehlo).has('STARTTLS')) { … }
// 광고가 없으면 이 블록을 건너뛰고 아래로 그대로 내려간다
if (cfg.user) { … AUTH PLAIN <base64(user:pass)> … }   // :251
```
- 서버가 EHLO 응답에 `250-STARTTLS` 를 **광고하지 않으면** 경고 없이 평문으로 내려가
  **`AUTH PLAIN` 을 base64 로 보낸다**(base64 는 암호화가 아니다).
- 중간자가 EHLO 응답에서 `STARTTLS` 한 줄만 지우면 **SMTP 자격증명이 평문으로 나간다**.
  코드 주석은 `:232` 에서 "평문 연결에서 인증 정보를 보내기 전에 **반드시** 승격한다" 라고
  적고 있는데, **광고가 없을 때의 경로가 그 약속을 지키지 않는다**.
- **중요도 🔴 높음 / 영향도 🟠 중간**(전형적 STARTTLS stripping. 사내망 전제라 난이도는 있다)

#### SEC-11 [A] `/api/central/ip-scan-result` **BIG_JSON 미등록** — 코드가 선언한 8,000건이 이미 413
- `routes/central.js:1330` 수신 · `index.js:225-257` 등록 목록에 **없음** ·
  `agent/ipScanWorker.js:36` 엣지는 **자르지 않는다**.
- 중앙의 `slice(0, 8000)`(`:1336`)은 **파싱 후**라 보호가 되지 않는다.
- **실측**(실제 레코드 형태 `{ip, openPorts[], services[], hostname}` — `ipam/scan.js:269`):
  | alive | 본문 | 결과 |
  |---|---|---|
  | 5,000 | 0.633MB | 통과 |
  | **8,000**(코드가 선언한 상한) | **1.016MB** | **413** |
  | 10,000 | 1.270MB | 413 |
  레코드당 133B. 호스트명이 짧으면 임계는 올라간다 — **레코드 크기에 달렸다**(정직 기록).
- 413 은 `resilientFetch` 재시도 대상이 아니다 → **그 주기 IP 대장이 조용히 전량 소실**(v2.517 규약).
- 형제 `/idrac-scan-result` 도 미등록이다.
- **중요도 🟠 중간 / 영향도 🟠 중간**

### 2-3. 저장·노출

#### ⚠ SEC-12 [A] `server/config/` 문서화 파일 **37개**가 `.gitignore` 로 안 막힌다 (이 저장소는 **공개**다)
- **실측**: `docs/CONFIG-FILES.md` 의 151개를 `git check-ignore` 로 전수 → **37개 미차단**.
- 가장 위험: **`central-inventory.json`**(`central/inventory.js:17`) — 위임 vCenter **전체 인벤토리
  캐시**(전 법인 호스트명·VM명). 그 밖에 `svcmon.json`·`svcmon-templates.json`(host·url·keyword) ·
  `permissions.json`(사용자명) · `credentials-usage.json` · `nfs-mounts.json` · `central-pdu.json` ·
  `central-fleet.json` · `provision-saved.json` 등.
- **구조 문제**: 지금 `.gitignore` 는 **파일 단위 열거**라 **새 파일이 기본 노출**이고,
  회귀 테스트는 `SECRET_FILES ⊆ .gitignore` **한 방향(28개)** 만 강제한다
  → '비밀은 아니지만 운영 식별자' 가 통째로 빠진다.
- 기본 `CONFIG_DIR` 이 `server/config` 이므로 개발 호스트에서 `git add -A` 하면 그대로 올라간다.
- **중요도 🔴 높음 / 영향도 🔴 높음**(한 번 커밋되면 git 이력에서 지우기 어렵다)

#### SEC-13~18 [B] 분석 에이전트 실측 + 주 세션 소스 확인

| id | 위치 | 내용 | 중요도/영향도 |
|---|---|---|---|
| **SEC-13** | `rma/deploy.js` | 배포 대상 값의 **개행**이 엣지 `portal.env` 에 임의 키를 주입 | 🟠/🟠 |
| **SEC-14** | `util/resilientFetch.js` | **리다이렉트를 따라가며 커스텀 인증 헤더를 유지** — 302 로 토큰을 끌어낼 수 있다 | 🟠/🟠 |
| **SEC-15** | `util/smtp.js` | SMTP 접속에 **SSRF 가드가 전혀 없다**(host 가 사내 임의 주소여도 통과) | 🟡/🟡 |
| **SEC-16** | `horizon/horizon.js` | **TLS 검증 기본 OFF** + AD 도메인 계정을 싣는다 | 🟠/🟠 |
| **SEC-17** | (여러 곳) | `secretCarry` 규약 위반 — 비밀을 `delete` 가 아니라 **`''` 로 덮는다**(“빈 값 = 기존 유지” 규칙을 타는 구현에서 되살아난다) | 🟡/🟠 |
| **SEC-18** | `/dl/:file` | **무인증** 다운로드 경로 | 🟡/🟡 |

---

## 3. 개선 가능 — 확정 12건

| id | 대상 | 내용 · 실측 근거 | 등급 | 중요도 |
|---|---|---|---|---|
| **IMP-01** | `.github/workflows/ci.yml:76-81` | **문서 검사 3개가 한 `run: \|` 에 있고 GH Actions 기본 셸이 `bash -e`** → 첫 줄(env-doc)이 실패하면 나머지 둘은 **실행되지 않는다**. 실측 종료코드: `api-doc` **0** / `config-doc` **1** / `env-doc` **1** → ENV 가 낡은 동안 나머지 검사는 **한 번도 안 돌았다**. 스텝 3개로 분리. | A | 🟠 |
| **IMP-02** | `docs/ENV.md`·`CONFIG-FILES.md` | 위 때문에 낡았다. ENV 는 **407 → 452개**(BMUSAGE 18 · LINKCHECK 4 · PORTALCHECK 3 등 운영자 튜닝 값 누락), CONFIG-FILES 는 `api-keys.json` 누락 | B | 🟠 |
| **IMP-03** | 상대시각 문구 **9벌** | **지금 실제로 갈라져 있다**(§부록 표). 90초 → `2분 전`(7개) vs **`1분 전`**(2개) · 90분 → `2시간 전` vs **`1시간 전`** · 결측 → `—`(6) / `null`(2) / **`없음`**(1). ⚠ 더 위험한 것 — 같은 이름 `ageText` 가 파일에 따라 **타임스탬프**를 받기도 하고 **경과 ms**를 받기도 한다 | A | 🟠 |
| **IMP-04** | `cmpVersion` **3벌** | 실행 대조 **6건 중 4건 불일치**(§부록 표). `null`(버전 미상)과 숫자(구버전)는 CLAUDE.md 가 **구분하라고 못 박은 두 상태**다. `partFaults.js`↔`edgeLog.js` 는 주석만 다른 **완전 복사본**. ⚠ 정정 — `upgrade/upgrade.js:35` 는 **튜플**을 받는 다른 함수다(중복으로 세면 안 된다). 운영 값이 항상 `x.y.z` 라 **오늘은 잠재적** | A | 🟡 |
| **IMP-05** | `views/collectors/EmptyInvModal.jsx:144` | 웹 전체에서 **유일하게 남은 raw `<table>`** — 사용자 상시 요구("모든 표는 제목 클릭 정렬", v2.422)의 유일한 예외 | A | 🟡 |
| **IMP-06** | push/pull 진입 함수 **46개 중 41개** | **어떤 테스트도 호출하지 않는다.** v2.566 TDZ 교훈이 2개(`pushPerfNow`·`runLogQueryWorkerOnce`)에만 적용됐다. 41개를 실제로 호출하는 프로브 결과 **오늘 TDZ 급 결함 0건** — 즉 지금은 괜찮고 **확인할 장치가 CI 에 없는 것**이 문제다. 소스에서 목록을 뽑는 스윕 테스트 1개(왕복 ~20초) | B | 🟠 |
| **IMP-07** | `edgelog/spec.js` | 새 엣지 워커·폴러가 **자동 등재되지 않는다**. CLAUDE.md 가 v2.554·2.560·2.561 에 **세 번** 경고했는데 테스트는 아직 `length >= 20` 만 본다. 미등재: `pingWorker`·`captureWorker`·`bmstorWorker`(셋 다 여전히 `catch { return null; }` · 상태·로그 0) · `bmusage/poller` · `linkcheck/poller` · `gpu/physicalPoller` · `dirusage/scheduler` | B | 🟠 |
| **IMP-08** | 동시성 풀 **23벌** | 항목별 catch 8개 / catch 없음 15개 — 오류 의미가 둘로 갈린다. 오늘 깨지는 곳은 없지만(호출부가 `fn` 에서 삼킨다) 불변조건이 **풀이 아니라 23개 호출부**에 흩어져 있다 | B | 🟡 |
| **IMP-09** | `central/inventory.js` persist | 전량 재직렬화(동기) — 운영 규모 재현 **4.9MB · 31~35ms**, 한 vCenter 만 바뀌어도 28개 전부. 빈도 ~10.7초/1회 → 듀티 **약 0.3%**. **급하지 않다**(v2.503 이 고친 25초급이 아니다). vCenter 단위 파일로 쪼개면 1/28 | B | 🟢 |
| **IMP-10** | `REGIONS` **7벌** | 값은 현재 동일, 테스트 0건. `llm/nlSearch.js:135` 인라인 포함 → 지역 추가 시 **자연어 검색만 누락** 가능 | B | 🟡 |
| **IMP-11** | `SITE_INVENTORY_STALE_MS` 2벌 | `store.js:20`·`routes/api/portalCheck.js:62` — **주석이 "같은 값이어야 한다"고 적고 있다**. `invScan.js` 는 이미 인자로 받도록 설계돼 있어 export 하나면 끝 | B | 🟡 |
| **IMP-12** | 다열 표 9곳 | `overflowX` 래퍼도 `minWidth` 도 없다 → 400px 에서 스크롤 대신 셀이 짜부라져 행이 세로로 늘어난다(v2.562 규약). **넘침은 0px 이라 수치로는 안 잡힌다** | B | 🟡 |

---

## 4. 확정 0건인 축 — 근거와 함께 남긴다

> "찾지 못했다" 를 "없다" 로 쓰지 않기 위해, **무엇을 어떻게 봤는지**를 적는다.

| 축 | 방법 | 결과 |
|---|---|---|
| **명령 주입** | tcpdump · SAN vfId · nvidia-smi · hostaccess · RMA deploy · deepSearch 전수 | **확정 0건** — 전부 화이트리스트 정규식 + argv spawn(셸 없음) |
| **SQL 주입** | 전 쿼리 스윕 | **확정 0건** — 바인딩 사용. 예외인 `${bucketMs}` 보간 2곳이 **BUG-01** 이고 그것은 주입이 아니라 무응답 결함 |
| **경로 순회** | 백업·업그레이드·`/dl` · zip · RMA 파일 점검 | **확정 0건** — `util/zip.js zipMany` 세그먼트 검사 · `RMA_FILE_ROOTS` realpath 검사 유지 |
| **central 수신 30개** | 적대적 본문 4종 주입 | **HANG 0 · 5xx 0** — v2.548 S1 방어 유지 |
| **폴러 prune 배선 7개** | 실행 | 전부 정상(`(++tick % N) === 0`) |
| **`bmusage/db.js` 롤업** | 이중계수·NULL전파·분모·agent 파티션 | 전부 정상 |
| **v2.550.3 SQLite 4조건** | 후보 재스윕 | **신규 0건** — `vmseries/db.js:89·90` 은 조건 ④(조회 응답 경로) 불충족(관리자 1회 삭제 경로), 나머지는 이미 처리됨 |
| **폴링 API 응답시간** | 실측 | health 2.3ms · overview 2.8 · hosts 2.8 · vms 7.0 · datastores 1.2 · summary 4.0 · capacity-forecast 5.2 · insights/fleet 2.5ms → **개선 여지 없음** |
| **push 경로 동기 I/O** | `gzipSync`/`writeFileSync` 스윕 | **0건**(전부 async) |
| **시각 버킷 테스트 23개** | v2.517 규약 대조 | **신규 시각 결함 0건** |
| **파일 RMW 13곳** | 전수 | 전부 전체치환(부분병합 손상 없음) |
| **타이머 이중 무장** | 전수 | 0건 |

### 의도적으로 다른 것 — **합치지 말 것**
`kpisOf` 5벌 · `tableFootnotes` 6벌 · `MIN_EDGE_VERSION` 4벌 — 도구마다 뜻이 다르다.
`docs/API.md` 는 최신(821 엔드포인트). `MIGRATABLE`·`version_4/tree.js`·`toolcats PRESET`·
`toolAccess` 집행은 **이미 CI 가 자동 강제**한다.

---

## 5. [미확인] — 추측이므로 확정분과 분리한다

1. **`metrics/db.js:112`** `insHour.run(…, r.v, r.v, r.v)` 는 `r.v` 가 null 이면 그 시간 버킷의
   `sum/mn/mx` 를 통째로 NULL 로 만든다. **생산자 17곳을 다 읽었으나 도달 경로를 찾지 못했다**
   (전부 `!= null`/`>0` 게이트 뒤). **새 계열 추가 시 함정으로 남는다.**
2. **`vmtrack/diff.js:41-43`**(BUG-09) — 잘못된 형태는 확정, **`null` 생산자 미확인**.
3. **`version_4/ui.jsx:86`**(BUG-10) — 잘못된 형태는 확정, **`x: null` 생산자 미확인**.
4. **개별 토큰의 agent 이름이 수집 서버 등록부 `name` 과 항상 같은지** — 다르면 fresh 엣지가
   '등록부에 없음' 과 'silent' 로 **이중 표시**된다(CLAUDE.md v2.548 이 이미 정직 기록으로 남긴 한계).
5. **SEC-04~07 의 실데이터 유출** — 구조 결함(필터 부재)은 확정이고 **200 응답도 실측**했으나,
   이 환경의 목 데이터가 비어 **데이터가 실제로 넘어가는 것까지 실증한 것은 SEC-01 뿐**이다.
6. **`openFaults` LIMIT 20,000 초과분**이 전이 입력에 없다는 것 — 코드상 사실이나 운영 규모 미확인.
7. **`/api/ping/edge/overview`(SEC-06) 의 `manual` 타깃** — `ping.js:54-56` 이 "manual 타깃은
   그대로 두고(기존 동작)" 라고 **의도를 적고 있어** SEC-06 과 분리했다. 의도인지 누락인지 불명.

---

## 6. 방법론 메모 (다음 감사를 위해)

- **`authzGates2536.test.js` 하니스가 이 저장소의 정답이다** — 실제 `api` 라우터를 express 에
  마운트하고 `req.user` 를 주입하면 OTP 강제 등록을 우회하면서 **상태코드로** 판정할 수 있다.
  HTTP 로그인으로는 `{"error":"otp_enrollment_required"}` 에 막힌다.
- **`pkill -f "node src/index.js"` 를 쓰지 말 것** — 자기 셸 명령줄에 그 문자열이 있어
  **자기 자신을 죽인다**(이번 감사에서 exit 144 로 두 번 겪었고 한 번은 작업 결과를 잃었다).
- **스윕 테스트는 '목록에 추가' 가 아니라 '소스 전수' 여야 한다** — SEC-09 는 `new Agent(` 전수
  스윕이 **Agent 를 안 쓰는 경로**를 못 봐서 남았다. 규칙의 **의도**를 검사해야 한다(v2.550.3 규약).
- **오탐으로 걸러낸 것**(재보고 방지): `logAudit(req` 3건 → 전부 `audit.js` **주석** ·
  `CompareMatrix.jsx` raw table → 실제로는 `STable` · `primitives.jsx DataTable` → 자체 정렬 보유 ·
  `svcmon/store.js:152 allowed.includes` → 무관한 배열.

---

## 부록 A — 상대시각 9벌 실행 대조 (IMP-03)

`NOW = 1700000000000` 으로 9개 모듈을 **동시에** 실행한 결과:

| 모듈 | 인자 | 90초 | 90분 | 25시간 | 결측 |
|---|---|---|---|---|---|
| `bmUsageText.ageText` | 타임스탬프 | 2분 전 | 2시간 전 | 1일 전 | `—` |
| `horizonSessionText.agoText` | 타임스탬프 | 2분 전 | 2시간 전 | 1일 전 | `—` |
| `edgeLogText.ageText` | 타임스탬프 | 2분 전 | 2시간 전 | 1일 전 | `—` |
| `sanPerfDiagText.agoText` | 타임스탬프 | 2분 전 | 2시간 전 | 1일 전 | `null` |
| **`partFaultText.ageText`** | **경과 ms** | **1분 전** | **1시간 전** | 1일 전 | `—` |
| `linkCheckText.ageText` | 타임스탬프 | 2분 전 | 2시간 전 | 1일 전 | **`없음`** |
| **`storageAuthText.agoText`** | 타임스탬프 | **1분 전** | **1시간 전** | 1일 전 | `null` |
| `curUserText.agoText` | **경과 ms** | 2분 전 | 2시간 전 | 1일 전 | `—` |
| `invCheckText.ageText` | **경과 ms** | 2분 전 | 2시간 전 | 1일 전 | `—` |

**정직 기록**: `null` 반환은 React 에서 아무것도 렌더하지 않으므로 **화면에 'null' 이라는
글자가 뜨지는 않는다**. 결함은 '같은 뜻의 값이 모듈마다 다른 것' 과 '같은 이름이 다른 인자를
받는 것' 이다.

## 부록 B — `cmpVersion` 실행 대조 (IMP-04)

| 입력 | `tokenScan.js:80` | `partFaults.js:64` / `edgeLog.js:41` |
|---|---|---|
| `'v2.560.0'` vs `'2.5.0'` | **`null`**(v 접두 거부) | `555` |
| `'2.560'` vs `'2.5.0'` | `1` | **`null`** |
| `'2.6'` vs `'2.10'` | **`-1`**(semver 정답) | `null` |
| `'2.560.0'` vs `''` | `1` | `null` |
| `'2.560.0'` vs `'2.560.0'` | `0` | `0` ✓ |
| `'2.561.0'` vs `'2.560.0'` | `1` | `1` ✓ |

⚠ `upgrade/upgrade.js:35 cmpVersion` 은 **`[maj,min,patch]` 튜플**을 받는 **다른 함수**다
(문자열을 넘기면 `(a, null)` 에서 TypeError). 중복 4벌이 아니라 **3벌**이다.

## 부록 C — `Number(null) === 0` 잔존 위치 (§1-2)

`Number.isFinite(Number(v)) ? Number(v) : null` 형태 전수(주석·테스트 제외):

| 위치 | 상태 |
|---|---|
| `central/edgeLogStore.js:39`(ms) | **BUG-06** |
| `central/edgeLogStore.js:50`(statusFailed) | **BUG-07** |
| `vmtrack/diff.js:41,42,43` | **BUG-09** [도달 미확인] |
| `rma/testResults.js:56` | **BUG-08** |
| `storage/db.js:70,71` | CLAUDE.md `REVIEWED_SAFE` — `loadGrowthSettings()` 가 막는다(확인함) |
| `storage/growth.js:137` | CLAUDE.md `REVIEWED_SAFE` — 호출부가 항상 `dayIndex(Date.now())`(확인함) |
| `util/numOrNull.js:7` | **주석**(틀린 형태의 예시) |

---

*감사 수행: 2026-09-21 KST. 대상 커밋 v2.573.0. 저장소 코드는 **한 줄도 고치지 않았다**
(이 문서 추가만). 목 서버·임시 파일은 전부 정리했다.*
