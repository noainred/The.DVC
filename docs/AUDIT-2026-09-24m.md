# 16차 점검 — 10축 병렬 감사 + 발견별 2인 반증 (v2.605, 2026-09-24)

요청: "자세하게 3번 더"(3회차). 방식은 v2.604(`AUDIT-2026-09-24l.md`)와 같고 첫 축은 **v2.604 회귀**(`git diff 5795a83..40f4508`)다.
감사 기준 커밋 `40f4508`(v2.604.0). 수정은 파일이 겹치지 않는 6그룹 병렬, 변이 검증은 기준 커밋 판본으로 되돌려 확인했다.
그룹 보고서는 ID 마다 재현 입력·수정 전/후 실측·위치·변이 결과·남은 한계를 적었다(scratchpad `fix2605_{a..f}.md`).

⚠ **실행 환경(정직 기록)**: 직전 세션의 컨테이너가 과부하(load 885)로 멈춰 이 회차는 **처음부터 다시** 돌렸다. 이번 컨테이너는
CPU 4개라 Workflow 도구의 동시 에이전트 상한이 2였다 — 그래서 Agent 도구로 감사 10축을 직접 병렬 실행했고, 감사·반증 에이전트에
'전량 테스트·빌드 금지 · 목 서버 1개·짧게 · 부하 실측 금지' 를 명시했다. 감사 중 load 는 2 안팎이었다.

## 결과 요약

| 축 | 발견 | 확정 | 가능성 | 반증 | 고침 |
|---|---:|---:|---:|---:|---:|
| v2.604 회귀 | 7 | 7 | 0 | 0 | 7 |
| 남은 후보·형제 | 8 | 7 | 1 | 0 | 7 |
| 인가·범위 | 4 | 4 | 0 | 0 | 4 |
| 중앙 수신 계약 | 5 | 5 | 0 | 0 | 5 |
| 수집기 정확성 | 5 | 4 | 1 | 0 | 5 |
| 웹 화면 | 8 | 8 | 0 | 0 | 8 |
| 타이머·자원 | 5 | 5 | 0 | 0 | 5 |
| SQLite | 4 | 4 | 0 | 0 | 4 |
| 보안 | 2 | 2 | 0 | 0 | 2 |
| 엣지 계약 | 5 | 4 | 1 | 0 | 4 |
| **합계** | **53** | **50** | **3** | **0** | **51** |

중복 3쌍(같은 결함을 두 축이 찾음): CEN2605-05 = TIM2605-03(RMA 하트비트) · LEFT2605-04 = SEC2605-02(업그레이드 번들 push) ·
WEB2605-01 = RECENT2605-06(폴더 사용량 보존). 고유 결함은 48건이다. 가능성 3건 중 COL2605-05 는 최소 수정(판독 0 + Unknown 만 건너뜀)을
했고, LEFT2605-08(장비 REST 응답 상한)·EDGE2605-05(중앙 등록부 손상 연쇄)는 고치지 않고 남겼다.
발견 수: v2.600 67 → 52 → 47 → 47 → 39 → **53**(이번 회차는 인가·웹·엣지 축이 형제 경로를 더 넓게 훑었다).

회귀 테스트: `server/test/audit2605{a..f}.test.js` **62건**(a 9 · b 9 · c 13 · d 6 · e 12 · f 13) + 웹 vitest(`vcdOverview`·`ipamScanForm`·
`sanHealthText`·`bmUsageText`·`storageGrowthText`). 변이 검증: a 9/9 · b 18/18 · c 13/13 · d 9/9 · e 13/13 · f 19/19 파일.

## v2.604 가 만든 결함(회귀)

| id | 심각도 | 재현 → 수정 |
|---|---|---|
| RECENT2605-01 | high | v2.604 EDGE2604-01 fleet push 보류에 **시한이 없었다** — vCenter 하나가 인증 정지·점검중으로 호스트 0 인 채 엣지가 재시작되면 매 주기 건너뛰고, 중앙 `central/fleet.js` TTL 30분이 그 엣지의 베어메탈 **전체**를 지웠다. v2.601 '보류에는 반드시 시한' 의 재발 → `FLEET_WITHHOLD_MAX_MS`(20분, 중앙 TTL 보다 짧게) 뒤에는 못 읽은 vCenter 귀속·귀속 없는 항목만 빼고 `partial`·`unreadVcenters` 와 함께 보낸다. bmusage `withholdUnreadBareMetal` 도 6시간 시한 + `expired` |
| RECENT2605-02 | medium | v2.604 CEN2604-04 가 미검증 자기등록을 '아는 엣지' 에서 빼면서 중앙이 닿지 못하는 공유 토큰 엣지의 **엣지 로그 폴백 회신이 403**(인출은 되는데 회신만 거부). 실제 라우터로 재현 → 등록부에 있는 이름(미검증 포함) + acked 면 받는다 |
| RECENT2605-03 | medium | Isilon `isi statistics`(best-effort 20초)가 주기마다 성패를 오가면 정확/반올림 값이 섞여 1일 증가량에 −41 TiB 계단, 최신이 정확 값이면 해상도 표지도 없음 → 반올림 주기를 본 장비는 `capacityApprox{mixed:true}` + 증가량 각주 |
| RECENT2605-04 | low | PowerMax 빈(0 TB) SRP 를 '못 읽은 SRP' 로 세어 문서화되지 않은 기준 어레이가 매 주기 partial-pools(미적재), 적재되는 어레이에도 '적재하지 않습니다' 문구 → `powermaxSrpIsEmpty` 로 구분, 문구는 실제로 막은 경우만 |
| RECENT2605-05 | low | 로그 분석 168시간 창이 prune 경계 때문에 오래 켜진 포탈에서도 늘 '일부' → prune 경계(`prunedThroughH`)를 기록해 창 시작 바로 앞이면 완전으로 본다 |
| RECENT2605-06 | low | 폴더 사용량 보존 0 입력이 1일로 저장돼 이력 삭제(= WEB2605-01) |
| RECENT2605-07 | low | v2.604 SAN 테스트 회신 정제가 섹션 실패 사유를 표식 없이 40자로 자름 → 2,000자 + '…(잘림)' |

## 주요 확정 결함

- **AUTHZ2605-01 (high)** 범위 제한 admin 이 설정 화면(자원 최적화 vmperf 대상 · 현재 사용자 · 20초 스파이크 · 베어메탈 사용률 법인)을
  **바꾸지 않고 저장**만 해도 GET 이 범위로 거른 목록이 PUT 에서 전체 목록으로 저장돼 **다른 법인 설정이 지워지고 vmperf DB 파일이 삭제**됐다
  (실제 api 라우터로 재현 — 파일 3 → 1). 새 `auth/scopeMerge.js` 가 범위 밖 키는 직전 값을 보존하고 본문의 범위 밖 키는 무시·개수를
  밝힌다(`ignoredOutOfScope`). 범위 밖 DB 는 삭제 대상에서 빠지고 PUT 응답도 GET 과 같은 필터를 거친다. 형제 `DELETE /tools/waste/settings/data` 도.
- **AUTHZ2605-02** 수동 실행 라우트(guest-disk/run · off-check/run · vm-track/snapshot · Horizon collect · Horizon settings PUT)가 범위 계정에
  전 법인 결과·전체 설정을 줬다 → 형제 bm-usage/collect 규약대로 403.
- **AUTHZ2605-03·04** SAN 사용량 빈 상태 진단(`diag.facts.error`)과 스토리지·SAN 스냅샷 `sections` 문구, 조닝·점검 이력의 IP 이름이 비-admin 에
  관리 주소를 줬다 → `scrubStringsDeep`·`maskSnapAddress` 가 sections 까지.
- **SEC2605-01 (high)** 자연어 검색 폴백 파서의 `/(\d+)\s*%/` 가 숫자열에서 O(n²) — 로그인한 viewer 가 `/api/search/nl` 한 번으로 이벤트 루프를
  수십 분 멈출 수 있었다(30k 자 1초, 120k 자 16.7초 실측). 정규식을 `/(?<!\d)(\d{1,6})\s*%/` 로, 질의는 500자 상한(라우트 400·chatOps·nlSearch
  자체). 난수 질의 2,000개에서 옛 정규식과 결과가 같다(7자리 이상 퍼센트만 의도적으로 제외).
- **CEN2605-01 (high)** 엣지 `/inventory` 의 수치 필드가 문자열이면 `hc.cores += h.cpuCores || 0` 가 **문자열 이어붙이기**가 되어 함대 전체 합계가
  `'1616'` 처럼 오염됐다(v2.599 CEN-2599 는 글자 필드만 좁혔다) → 수치 키 33개를 `numOrNull` 로, 버린 개수는 `dropped.coerced`.
- **CEN2605-02·04** 파트 장애·SAN 사용량 엣지 보고 저장소에 `admitAgent` 가 없어 공유 토큰 이름 수백 개로 실제 엣지 행이 밀리거나 메모리가 무한히
  컸다(형제 storage·pdu·sanswitch 는 v2.599 에 적용). 공유 토큰으로 같은 이름을 사칭하는 것은 **문서화된 한계**라 고치지 않았다.
- **CEN2605-03** 원격 전력 map 이 호스트명만 키로 써서 두 엣지의 같은 호스트명이 서로 덮었다(한 법인 전력 소실) → 저장 키 `collectorId\0host`,
  DB 키는 **충돌이 없으면 `rmt:<host>` 그대로**(이력 연속), 충돌한 호스트만 `rmt:<collectorId>:<host>` + `hostConflicts`.
- **CEN2605-05 = TIM2605-03** RMA 하트비트 Map 이 삭제 경로 없이 자람 → 정책 원소 200자 · 법인당 인스턴스 32(`RMA_MAX_INSTANCES_PER_AGENT`) · 7일 정리.
- **COL2605-01·02** 베어메탈 사용률: 가상 NIC(tun/tap 이 보고하는 10Mb/s)로 net_pct 가 100%, Windows `PercentDiskTime` 이 287% → 물리 NIC·bond 만
  사용률(처리량은 그대로), `100 − PercentIdleTime` + 범위 밖 null(`busyOutOfRange`).
- **COL2605-03** vGPU 사용률 표본 없음·−1 이 0%(유휴) → 값을 받은 호스트만 기록. ⚠ 주석의 '유휴=0' 의도를 '실제 0 보고' 로 좁힌 설계 변경이다.
- **COL2605-04** iDRAC Enterprise 대체 경로에서 `SystemBoardIOUsage` 가 sysPct 로 잡혀 I/O 가 영원히 null → 정확 일치 우선 `sensorFieldOf`.
- **TIM2605-02** XtremIO(·Isilon) REST 수집기가 장비 시한 signal 을 받지 않아 withDeadline 180초가 지켜지지 않았다(요청 수 × 요청 시한) → signal 전달 + 스윕 테스트.
- **LEFT2605-03** `cliSsh.parseJsonLoose` O(n²)(잘린 160KB JSON 26.7초) → 문자열 인식 괄호 깊이 스캔 1회. 옛 구현과 결정적 난수 6,000개 대조 차이 0.
- **LEFT2605-01** 중계 경로 점검이 **주기 폴러인데** 응답을 상한 없이 읽었다(v2.604 허용 목록의 '관리자 수동 실행' 사유가 사실이 아니었다) → `readJsonCapped`, 허용 목록 비움.
- **LEFT2605-04 = SEC2605-02** 업그레이드 번들 push 가 `{ok: res.ok, ...body}` 라 엣지 본문이 500 을 성공으로 덮고 결과는 버려졌다 → 아는 필드만, 결과를 `lastResult.edgePush`·콘솔에.
- **EDGE2605-01·02** 엣지 재시작 직후 PDU push 가 일부 장비만, GPU 게스트 push 가 첫 폴 전 빈 목록을 보내 중앙이 통째로 교체했다 → 첫 수집 전 보류(시한 15분·사유).
- **EDGE2605-04** 설정 push 진행 중 변경은 조용히 버려졌다 → `_again`(끝난 뒤 onlyIfChanged 로 한 번 더).
- **WEB2605-01·05 · LEFT2605-02·06** 숫자 칸 빈 값: 폴더 사용량 보존(1일)·알림 임계(화면 0 · 실제 90)·svcmon 로그(보관 1개·전체 무제한)·IPAM 스캔(보존 0=정리 안 함).
  v2.596 규약대로 서버는 빈 값을 미지정(이전 값 유지), 웹은 원문 문자열을 상태에 두고 `blankOr` 로 보낸다.
- **WEB2605-03** 끊긴 호스트의 0% 가 클러스터 평균을 끌어내렸다(같은 표의 vCenter 행은 v2.594 `usageReadable` 로 제외) → 웹 `hostUsageReadable`·`clusterAvgPct`.
- **WEB2605-02·04·07** SAN 점검: 조닝 상한(40) 밖 문제 포트에 '이상 목록에 없다' 는 거짓 문구 · 축약 카운터를 '카운터 없음' · 디렉터 슬롯/포트 표기 누락.
- **DB2605-01** ping baseline 조회가 OK 표본이 없는 대상(1년 52만 행)의 이력 전체를 15초 폴링마다 훑었다(대상당 약 90ms) → 최근 7일 하한 + 대상 사이 양보.
  ⚠ 동작 변화: 7일 안에 OK 표본이 없으면 자동 baseline 은 null 이다.
- **DB2605-02·03·04** 전력 정리·Capacity 보존·게스트 디스크 보존이 한 방 동기 DELETE(60만 행 1.6초 · 77만 행 1.5초 · 32만 행 1.3초 실측) → 청크 + 양보.
  게스트 디스크는 옛 DELETE 와 남는 행이 같음을 사본 DB 로 대조한다. audit2603c 허용 목록에서 4줄을 뺐다.
- **TIM2605-01** 데이터스토어 탐색 캐시가 만료 뒤에도 남고, 잘라 낸 파일 이름(SlicedString)이 원문 SOAP XML 전체를 붙잡았다(데이터스토어당 약 30MB) → 만료 청소 + 평탄화(31.4 → 0.3MB).
- **TIM2605-04** 시한·주기 env 여러 개가 `Number(x)||기본값` 이라 2^31 초과·음수면 1ms 루프 또는 즉시 타임아웃 → `clampIntervalMs`/`reqTimeoutMs` + 스윕 테스트(남은 95개는 사유와 함께 허용 목록).
- **TIM2605-05** 수집 토큰 거부 로그 스로틀 맵 무상한 → 상한 2,048.

## 통합 단계에서 고친 것

- 그룹 d 의 설정 push 래퍼(`pushConfigNow`)에 catch 가 없어 `asyncHang2574` 의 '무음 실패 금지' 스윕이 실패했다 → catch + 콘솔 경고.
- API·ENV 문서 재생성(라우트 줄 번호·새 env).

## 검증

- 서버 전량 **3,662건**(통합 수정 전 1건 실패 → 수정 후 해당 파일 통과) · 웹 vitest 129파일 1,785건 · eslint 0 · vite build · 문서 생성기 4종.
- Chromium 목 스택 14화면 × 1440/400: 가로 넘침 0 · 콘솔 오류 0 · 본문에 `[object Object]`·`undefined`·`NaN` 0.

## 정직 기록 — 확인하지 못한 것

- 실장비 출력(리눅스 `/sys/class/net/*/device`, Windows WMI, iDRAC Sensors 이름, ESXi 흡기 센서, Isilon `isi statistics`, PowerMax 빈 SRP 응답)은 보지 못했다 — 전부 합성 입력 재현이다.
- vGPU `queryHostGpuUtil` 은 수집 함수 안쪽 로직이라 행위 테스트 없이 소스 검사로 고정했다.
- 화면은 목 데이터로 만들 수 없는 상태(끊긴 호스트 막대 '—', 폴더 사용량 저장 메시지, 베어메탈 hostsUnread 배너, SAN 조닝 상한 문구)를 브라우저로 보지 못했다(단위 테스트로 고정).
- 운영 규모의 전력 정리 소요 시간은 재지 않았다. 원격 전력 호스트명 충돌이 이 현장에 실제로 있는지도 확인하지 못했다.
- 남긴 것: LEFT2605-08(장비 REST 응답 상한 — 정상 대용량 응답 최대치를 모른 채 상한을 두면 멀쩡한 수집이 실패할 위험) · EDGE2605-05(중앙 등록부 손상 시 빈 목록이 엣지로 전파 — 드물고 원본이 보존됨) ·
  범위 admin 이 전 법인 공통 스칼라 설정(보존일·enabled)은 여전히 바꿀 수 있다 · 재등록 시 일시 검증 실패로 기존 검증 항목이 강등되는 것(collector/registry.js) ·
  ping seriesOf 의 `meta()`(79ms) · env 허용 목록 잔여 후보(하한만 57 · 하한 없음 32).
