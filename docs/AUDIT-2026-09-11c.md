# 전체 소스 감사(3차) — 2026-09-11 (v2.479.0 기준 → v2.480.0 조치)

요청: "버그·보안·개선·튜닝 각 10개 이상"(같은 요청의 재실행). 2차(`AUDIT-2026-09-11b.md`)에서 **완독하지 못한 영역**에 집중했다:
`routes/admin/*` 전부 · `routes/api/*` 잔여 · `routes/{auth,upgrade,collector}.js` · `auth/`·`health/`·`inventory/`·`toolcats/`·`upgrade/`·
`util/`·`security/`·`backup/`·`mail/`·`llm/` · pyportal(버그/튜닝 — 2차는 보안만) · `packaging/`·CI·uagmon/horizon · 웹 잔여 뷰.
방법: 영역별 읽기 전용 에이전트 4개(기본 모델·결과 파일 저장, 각 7~10분) + 직접 sweep(admin/upgrade/remote/api 라우트 게이트 전수)
+ **높음 등급은 파일을 열어 직접 재검증**. 실행/부하 측정은 없고(에이전트가 pyportal 3건은 실측 재현), 수치는 코드 기반 추정.
상태 표기: ✅ v2.480.0 조치 · ⏳ 보류 · 확신도 C=CONFIRMED / P=PLAUSIBLE.

정직 기록:
- v2.478 S5→v2.479 S-4 로 닫았다고 한 `/tools` 조회 권한은 이번 sweep 에서 **잔여 0** 을 재확인했다. 반면 **연결 테스트 4종
  (vCenter·NSX·Horizon·iDRAC)과 SMTP 가 PDU S-1 과 같은 '저장 비밀번호 + 요청 host' 병합 패턴**이었다(S6·S8) — 2차에서 PDU·
  베어메탈만 고치고 같은 클래스를 전수 확인하지 않은 것은 2차의 누락이다.
- `server/CLAUDE.md` 의 "업그레이드 번들 sha256 은 자체·엣지 푸시 양쪽 모두 검증" 불변조건이 **실제로는 한쪽만**이었다(엣지 `/api/upgrade/bundle`·
  수집기 `/api/collector/upgrade` 는 sha 를 받지도 검증하지도 않았음) — 문서의 회귀 방지 규칙이 코드와 달랐다. 이번에 코드를 규칙에 맞췄다.
- 에이전트가 완독하지 못한 파일 목록은 각 에이전트 보고(`scratchpad/audit3-*.md`)에 있으며, 특히 `insights/`(2,802줄)·`routes/svcmon/*`·
  `packaging/windows/*`·horizon `.cs` 대부분은 이번에도 grep 수준이다.

---

## 1. 보안 취약점 (24건 — 높음 3 · 보통 12 · 낮음 9)

| # | 상태 | 심각도 | 위치 | 내용 | 확신 |
|---|---|---|---|---|---|
| S1 | ✅ | 높음 | `routes/admin/deployLlm.js:52-59` | `/agent-deploy/defaults` 가 `centralTokenInfo()`(**CENTRAL_TOKEN 평문**)을 adminOnly 로 반환 — 같은 값의 `/central-token` 은 소유자 전용. 비소유자 admin 이 토큰 취득(EDGE_MODE=all 이면 COLLECTOR_TOKEN 과 동일). `hasToken` 만 반환(웹 AgentDeploy 는 이미 소유자 경로 사용) | C |
| S2 | ✅ | 높음 | `routes/admin/collectorsDc.js:361-366,423-431` | `/collectors/test`·`force-token` 이 `{id, url}` 로 **저장된 엣지 토큰을 임의 외부 URL 헤더로 전송**(SSRF 가드는 내부만). 저장 토큰을 쓰면 URL 도 저장값 고정 | C |
| S3 | ✅ | 높음 | `routes/upgrade.js:187-199`, `routes/collector.js:192-213`, `upgrade/upgrade.js:423`, `collector/upgradePush.js:35` | 엣지/수집기 번들 수신이 **sha256 무검증**(CLAUDE.md 불변조건과 불일치) — 토큰 탈취·http 중간자가 임의 tar.gz 설치·재시작. 중앙 push 가 `X-Bundle-Sha256` 헤더를 보내고 수신측이 `bundleShaIssue` 로 검증(부재는 `UPGRADE_ALLOW_UNVERIFIED` 만 예외) | C |
| S4 | ✅ | 보통 | `vcenter/registry.js:166-171`, `nsx/registry.js:114-119`, `horizon/horizon.js:153-158`, `idrac/registry.js:340-345` | 4종 연결 테스트가 `{...saved, ...body, password: saved}` 병합 → `{id, host:'attacker'}` 로 **저장 비밀번호 외부 전송**(PDU S-1 클래스). 비밀번호 상속 시 host(vCenter 는 port 도) 저장값 고정 | C |
| S5 | ✅ | 보통 | `routes/admin/gpuGuest.js:304-321` | `/gpu-guest/test` 의 `it.ip` 가 SSH 대상이 되는데 creds 는 저장/공용 비밀번호 → `{vmId, useShared:true, ip:'attacker'}` 로 법인 공용 계정 비밀번호 외부 전송. 저장 자격증명 사용 시 대상 IP 를 그 VM 의 알려진 IP 로 제한 | C |
| S6 | ✅ | 보통 | `routes/admin/centralIpam.js:69,75` | 엣지 개별 central 토큰 **발급/회수**가 adminOnly — 공유 토큰 조회/생성은 소유자 전용(게이트 불일치). `requireSettingsOwner` 추가 | C |
| S7 | ✅ | 보통 | `routes/admin/backupNetSec.js:72-78`, `logs/settings.js:41` | `PUT /vclogs/settings` `storagePath` 무검증 → 임의 절대경로에 SQLite(+wal/shm) 생성. 절대경로·`..`·제어문자·시스템 디렉터리 거부 | C |
| S8 | ✅ | 낮음 | `mail/settings.js:87-93` | SMTP host 변경 + 비밀번호 미입력 시 이월(PDU S-1 규칙). host 변경 시 폐기 + 테스트 | P→C |
| S9 | ✅ | 보통 | `security/secretVault.js:197-209` | 복호 실패 → `''` 반환 → 이후 저장/정책 전환(`migrateSecretFiles`)이 `''` 를 재기록 → **암호문 영구 소거**(키 env 누락 상태에서 '평문 전환' 1회면 19개 레지스트리 비밀 유실). 복호 실패값은 '' 유지(봉인문을 비밀번호로 쓰면 매 폴링마다 잘못된 로그인 → 서비스 계정 잠금 위험 — 기존 테스트가 이 설계를 고정)하되, **정책 전환(migrateSecretFiles)이 복호 실패 파일을 재기록하지 않게**(실패 카운터로 판정, 오류 보고) + 테스트 | C |
| S10 | ✅ | 낮음 | `auth/auth.js:786` | OTP 등록 확정이 `minCounter` 없이 검증하고 `totpLastCounter` 미기록 → 확정 코드를 같은 30초 창에서 로그인에 재사용 가능. 카운터 기록 + 테스트 | C |
| S11 | ✅ | 보통 | `packaging/offline/build-package.sh:73-77` | Node 런타임 SHASUMS 다운로드 실패 시 **검증을 조용히 건너뛰고** 빌드 계속(`set -e` 는 `if` 조건 미적용) — 공급망. 실패 시 빌드 중단 | C |
| S12 | ✅ | 낮음 | `routes/remote.js:105` | `GET /remote/proxies` 가 권한 없이 중계 주소(`proxyHost`) 노출 → `requirePerm('remote.access')` | C |
| S13 | ⏳ | 보통 | `pyportal/hub/server.py:1076-1085` | 동시 처리 세마포어가 **이미 생성된 스레드 안**에서 대기 → 스레드 생성은 무제한(미인증 1,000 연결 = 스레드 1,000). `verify_request` 오버라이드로 accept 시점 거절 | C |
| S14 | ⏳ | 낮음 | `routes/admin/deployLlm.js:33-39,472-475` | `GET /packages?baseUrl=`·`POST /llm-test{url}` SSRF 가드 없음(`PUT /upgrade/settings` 는 `urlReason` 검사) — 내부 JSON 오라클 | C |
| S15 | ⏳ | 낮음 | `routes/admin/statusTools.js:126-129`, `insights/dbLocation.js:115-145` | `/portal-db/location/preflight` 임의 절대경로 mkdir 부작용 | P |
| S16 | ⏳ | 낮음 | `security/guestScanScheduler.js:21,42,51,69` | `guest-scans.json` 의 `guestPass` 가 `SECRET_FILES` 미등록 → 항상 평문, 로드 catch preserveCorrupt 없음 | C |
| S17 | ⏳ | 낮음 | `upgrade/settings.js:237`, `upgrade/packageSettings.js:302` | PAT·엣지 토큰 파일 손상 시 무음 `{}` → 다음 저장이 토큰·edges 를 덮어써 유실 | C |
| S18 | ⏳ | 낮음 | `security/secretVault.js deriveKey` | 암호문 헤더 `logN` 무검증으로 scrypt 전달(로컬 파일 쓰기 전제 DoS) | P |
| S19 | ⏳ | 낮음 | `pyportal/hub/server.py:418-421` | 로그아웃(DELETE)이 헤더 없으면 쿠키를 상태변경에 인정 — "쿠키는 GET/HEAD 만" 불변조건의 유일한 예외 | C |
| S20 | ⏳ | 낮음 | `pyportal/hub/auth.py:761-782` | `_purge_fails` 가 count==0 만 제거 → 1~7회 실패 후 멈춘 출발지 영구 잔존(실측) | C |
| S21 | ⏳ | 낮음 | `horizon-uag-monitor/MonitorService.cs:190-194` | 인증서 콜백 항상 `true`(TLS 검증 비활성) — 자격증명 미전송이라 유출은 없으나 판정 위조 가능 | C |
| S22 | ⏳ | 낮음 | `pyportal/hub/server.py:441-444` | `api_settings_state` 가 viewer 에게 `initialPasswordFile` 절대경로·`dataDir` 노출 | C |
| S23 | ⏳ | 낮음 | `backup/service.js:54` | 백업 아카이브(AUTH_SECRET·TOTP 사본) 비원자 쓰기; `secrets-key` 미포함이라 암호화 모드 백업을 타 호스트에 복원하면 비밀 전부 봉인문(S9 와 결합) | C |
| S24 | ⏳ | 낮음 | `security/credentialStore.js hostAllowed` | IPv4 CIDR 만 지원, `RE_HOST_ENTRY` 는 `:` 허용 → IPv6 항목이 저장은 되고 브로커 매칭은 항상 거부 | C |

## 2. 버그 (30건 — 아래 서버 17 · pyportal/CI 8 · 웹 5 대표)

| # | 상태 | 심각도 | 위치 | 내용 | 확신 |
|---|---|---|---|---|---|
| B1 | ⏳ | 보통 | `upgrade/archive.js parseTar`, `upgrade.js:139` | tar 모드·심볼릭링크를 버리고 0644 기록 → 업그레이드 후 실행비트 소실. 병행 관찰: `build-package.sh` 스테이징에 `otp-enroll.sh` 가 없어 `install.sh:225` 의 `vmware-portal-otp` 링크 미생성 | C |
| B2 | ⏳ | 보통 | `routes/admin/deployLlm.js:198-208` | `/agent-deploy/deploy-all` 재진입 가드 없음 + `bulk/run` 과 미조율 → 같은 호스트 이중 install | C |
| B3 | ⏳ | 보통 | `upgrade.js:424`, `manager.js:130` | `pushToEdges` 가 엣지마다 `readFileSync` + 무제한 `Promise.all` → 번들 × 엣지 수 메모리 동시 상주 | C |
| B4 | ⏳ | 보통 | `routes/admin/vcenters.js:58-63`, `idracCore.js:97-102` | 설정 저장이 `await store.refresh()`(전체 재수집)에 묶임 — 폴링 중이면 가드로 즉시 반환돼 '즉시 반영' 미보장, 프록시 타임아웃이면 저장됐는데 오류 표시 | P |
| B5 | ⏳ | 낮음 | `upgrade/manager.js:88` ↔ `config.js:76-77` | `checkpointConfigDbs(config.configDir)` — v2.379 `dbDir` 이전 후 no-op + 동기 `wal_checkpoint(TRUNCATE)` 블로킹 | C |
| B6 | ⏳ | 낮음 | `auth/ad.js:426-432,228-230` | AD 검색 실패를 삼키고 `defaultRole` 승격; `saveAdConfig` 가 `timeoutMs:'abc'`(→ `setTimeout(NaN)` 즉시 타임아웃)·`enabled:'false'`(truthy) 무검증 | C |
| B7 | ⏳ | 낮음 | `llm/config.js:672`, `ollama.js:692` | `timeoutMs` 문자열 저장 시 `AbortSignal.timeout(NaN)` TypeError → AI 검색/ChatOps 전면 실패 | C |
| B8 | ⏳ | 낮음 | `auth/auth.js:151` | `{users:[]}` 를 파일 부재로 취급해 임의 비번 admin 재시드 | C |
| B9 | ⏳ | 낮음 | `inventory/osScanner.js:449-455` | vCenter 직렬 스캔 + vCenter 단위 타임아웃 없음(루트 CLAUDE.md 규약 미적용) | C |
| B10 | ⏳ | 낮음 | `routes/admin/backupNetSec.js:181` + `security/loginMonitor.js:65-67` | `/security/login-fails/run` — 비활성이면 즉시 return 인데 200 `{ok:true}`, `running` 가드 없음 | C |
| B11 | ⏳ | 낮음 | `routes/admin/centralIpam.js` startScan | '이미 스캔 중'(`ok:false`)도 200 → 프론트 ok 판정·감사 2xx 기록 | C |
| B12 | ⏳ | 낮음 | `audit.js:29` vs `admin/users.js:20` | describe 규칙 `PUT:'사용자 수정'` 인데 라우트는 `PATCH` → 죽은 규칙; admin 대부분 인라인 logAudit + auditMiddleware **이중 기록** | C |
| B13 | ⏳ | 낮음 | `routes/admin/opsSettings.js:127-138` | `PUT /alerts` `rules` 무검증 → 잘못된 형태면 규칙이 조용히 꺼짐 | P |
| B14 | ✅ | 낮음 | `routes/admin/backupNetSec.js:134` | `/net/history?limit=` 음수·무상한 → clamp | C |
| B15 | ⏳ | 낮음 | `routes/admin/nfsMounts.js:169-173` | `umount` 만 try/catch 없음 → throw 시 응답 없이 매달림 | P |
| B16 | ⏳ | 낮음 | `util/compress.js:71-104` | bodyCache/ETag 가 객체 동일성 키 → memo 객체 제자리 수정 라우트가 생기면 영원히 304(회귀 함정) | P |
| B17 | ⏳ | 낮음 | `securitySettings.js:331`, `upgrade/settings.js:265` | `invalidateLoginPolicyCache` 가 `_dsAt` 미리셋 · `edges` 배열 무검증 저장 | C |
| P1 | ⏳ | 보통 | `pyportal/hub/store.py:142,335` ↔ `server.py:53` | JSON 가져오기가 파일의 `id` 를 무검증 저장 → 라우트 패턴 불일치 id 는 편집·삭제 불가 유령 항목(실측) | C |
| P2 | ⏳ | 보통 | `pyportal/hub/csvio.py:83`, `server.py:280-285` | 셀 131,072자 초과 시 `_csv.Error`(ValueError 아님) → 핸들러 크래시, 응답 없이 종료(실측) | C |
| P3 | ⏳ | 보통 | `pyportal/hub/backup.py:299-317` ↔ `jsonfile.py:72-84` | `restore()` 가 저장소 락 없이 `write_json` 직접 호출, tmp 이름이 프로세스당 하나 → 복원 중 동시 저장 시 반쯤 쓰인 tmp 승격(손상 JSON) | C |
| P4 | ✅ | 보통 | `.github/workflows/release.yml` | `concurrency` 없음 — 태그 push 와 dispatch 가 겹치면 두 러너가 prune·`--clobber` 로 서로 자산을 지움(자동 업그레이드 유실). 직렬화 + `timeout-minutes` | C |
| P5 | ⏳ | 보통 | `packaging/offline/install.sh:238-250,367,400-406` | `rm -rf runtime/node` 후 `cp -a` 실패 시 런타임 소실 + 서비스 비활성이어도 exit 0 | C |
| P6 | ⏳ | 보통 | `pyportal/hub/pinned.py:68` | `build_opener()` 가 환경변수 프록시를 자동 추가 → 프록시 환경에서 IP 핀 접속이 프록시 포트로 dial → 점검·웹훅 전부 unreachable | P |
| P7 | ⏳ | 낮음 | `pyportal/hub/server.py:512-513` ↔ `auth.py:629-632` | `enabled` 비-bool 입력에서 저장(`bool()`)과 세션 폐기(`is False`) 판정이 갈림 | C |
| P8 | ⏳ | 낮음 | `packaging/offline/uninstall.sh:437,456` | `--prefix` 무검증 `rm -rf` — `/opt` 오타로 전체 삭제 | C |
| W1 | ✅ | 보통 | `web/components/VmReconfig.jsx:63,83` | memMB 가 1024 배수가 아닌 VM(1.5GB·2.5GB) 은 초기값(반올림 GB)과 원본 MB 가 달라 **아무것도 안 바꿔도 memoryMB 전송**(반올림이 내려가면 감소 요청 → 서버 차단). 표시 GB 기준 변경 판정 | C |
| W2 | ✅ | 보통 | `web/views/svcmon/BulkTab.jsx:291` | 입력 편집 표가 정렬 활성 STable 안 → 헤더 정렬 후 타이핑마다 행 재정렬. `sortable={false}` | C |
| W3 | ⏳ | 보통 | `RemoteCommand.jsx:406`, `CredentialManager.jsx:97-101`, `SvcMonitor.jsx`(9곳), `svcmon/TestWizard.jsx:56`, `svcmon/BulkTab.jsx:157` | `sendJson` 400 비throw 계약인데 결과 버림 → 검증 실패가 성공 흐름(폼 닫힘)으로(2차 W-1 의 전수 판, 12곳) | C |
| W4 | ⏳ | 낮음 | `web/views/tools/GuestDiskDetailModal.jsx:256-258`, `RightsizeReport.jsx:271-289` | `data-sort=-1/-9999`(null 은 뒤 규칙 위반) · 실시간 배지 텍스트가 숫자 인식을 깨 정렬 뒤섞임 | C |
| W5 | ⏳ | 낮음 | `web/views/Collectors.jsx:56,170,199,252`, `IpamCore.jsx:75-79` | `delJson` ok 미검사·실패를 폴링 오류 배너로 오표시 · 자체 fetch 다운로드 `res.ok` 미검사 | C |

## 3. 개선 가능한 부분 (21건)

| # | 위치 | 내용 | 확신 |
|---|---|---|---|
| I1 | 연결 테스트 5중 복제(`vcenter/nsx/horizon/idrac/collectors`) | "비밀 상속 시 host/url 덮어쓰기 금지"를 공용 헬퍼 + 테스트로 고정(S2·S4·S8 근원) | C |
| I2 | `audit.js:18-49 describe()` | 라벨 미등록 상태변경 20여 라우트(`/admin/alerts`·`ipam/*`·`assignments*`·`backup/settings`·`net/monitors*`·`gpu-physical*`·`idrac` 등) — 라우터 측 `res.locals.auditLabel` | C |
| I3 | 수동 실행 라우트 | 재진입 가드 규약 불균일(있음 6/없음 5) — 2회 연속 호출 테스트로 고정 | C |
| I4 | 응답에 `token/password/privateKey` 를 싣는 admin 라우트 | 소유자 게이트 전수 테스트 부재(S1 이 그 사례) | C |
| I5 | `pyportal/tests` | scheduler·pinned·audit 회전·HubServer 상한·`_purge_fails`·CSV 대형 셀·가져오기 id·복원 동시 저장 테스트 0건 | C |
| I6 | `release.yml:164` vs `ci.yml` | 릴리스는 `npm run install:all`(=`npm install`), CI 는 `npm ci` — 재현성 불일치 | C |
| I7 | `release.yml:232-238` | prune 단계 `\|\| true` 로 전부 삼킴 → 1000개 상한은 업로드 422 에서야 발견 | C |
| I8 | `ci.yml:60-64` | 문서 `--check` 가 `continue-on-error` 라 드리프트가 게이트가 안 됨(09-11 실제 불일치) | C |
| I9 | `pyportal/systemd/dc-service-hub.service` | 본체 유닛의 `ProtectControlGroups`·`ProtectKernelTunables`·`RestrictSUIDSGID`·`TimeoutStopSec` 부재 | C |
| I10 | `backup/service.js:54`, `inventory/osStore.js:24` | 백업 아카이브·OS 인벤토리 비원자 쓰기(5,850 VM 결과 소실→전량 재스캔) | C |
| I11 | `upgrade.js:377`, `fetchPackage.js:152` | 전량 수신 후 크기 검사, `downloadPackage` 상한 없음 | C |
| I12 | `web/components/STable.jsx:42-48` | th 에 `tabIndex`/`onKeyDown` 없음 → 키보드로 181개 표 정렬 불가 | C |
| I13 | `web reportExport.test.js` | 벡터 PDF(`drawKvRow/drawTable/drawLineChart/ensureSpace`) 테스트 0건 — `drawKvRow` 7개 이상 시 페이지 넘침 잠복 | C |
| I14 | 웹 14파일 `toLocaleString()` 55곳 | 로케일 없는 날짜 문자열 정렬 — `data-sort={ts}` 관행화 | P |
| I15 | `RightsizeReport.jsx:33-40,69` | `memCell` 2벌·저장 로직 복붙 → `useReportExport` 훅 | C |
| I16 | `reportExport.js:150-157` | 폰트 서브셋(KS X 1001) 밖 한글/한자/이모지 VM 이름은 빈칸 — `?` 치환 | C |
| I17 | `web/components/HostPowerPanel.jsx:41` | 403 도 "불러올 수 없습니다"(AccessDenied 분기 없음) | C |
| I18 | `routes/admin/gpuGuest.js:256,313` | `revealCreds` 주석("평문 표시")과 코드(`maskPw`) 불일치 — 명칭 정정 | C |
| I19 | `nsxImport.js:320,368-370`, `upgrade.js:237-239`, dbLocation | 하드코딩 경로 중복 → config 상수 | C |
| I20 | `pyportal/hub/health.py:483`, `notify.py:160` | http 점검·웹훅이 `addresses[0]` 하나만 핀(듀얼스택 첫 AAAA 불통 시 오판) | C |
| I21 | `packaging/offline/install.sh:400-406` | 종료 코드로 성공/실패 구분 + `app.bak.*` 무한 누적 정리 | C |

## 4. 튜닝 (19건)

| # | 효과 | 위치 | 내용 | 확신 |
|---|---|---|---|---|
| T1 | 높음 | `collectorsDc.js:347` + `collector/upgradePush.js:57-66`, `upgrade.js pushToEdges` | 번들 push 무제한 `Promise.all`(28 엣지 × 수십 MB, 고RTT) + 재진입 없음 → `eachLimited(4)` + 가드(B3 동일) | C |
| T2 | 보통 | `routes/admin/gpuGuest.js:254` | `items` 무상한(워커 ≤8 이지만 5,850 VM × timeout = 수 시간 요청) → 상한 + deadline | C |
| T3 | 보통 | `routes/admin/opsSettings.js:121` + `audit.js:102-111` | 감사 조회가 매 요청 파일 전체 read+parse+reverse → mtime 캐시 | C |
| T4 | 보통 | `routes/upgrade.js /bundle` | 256MB gzip 동기 파싱(`upgradeFromBundleBytes`)으로 엣지 루프 정지 — 스트리밍/워커 | C |
| T5 | 보통 | `upgrade/manager.js:88` | `dbCheckpoint` 동기 `wal_checkpoint(TRUNCATE)` × DB 수 × busy 5s | C |
| T6 | 보통 | `inventory/osScanner.js:449` | vCenter 직렬 스캔(28 × RTT) | C |
| T7 | 낮음 | `routes/admin/idracCore.js:64-74` | `GET /idrac` 매 폴링 `hostVcByTag`(658 호스트)+dedup 재계산 → memo | P |
| T8 | 보통 | `routes/admin/backupNetSec.js:123-131` | pcap base64 JSON(×1.33 + stringify) → 바이너리 스트리밍 | C |
| T9 | 낮음 | `routes/admin/deployLlm.js:33-39` | `GET /packages` 페이지 로드마다 원격 fetch(20s×2 재시도) → 60초 캐시 | C |
| T10 | 낮음 | `security/loginRateLimit.prune` | 5,000 초과 시 매 실패마다 전체 정렬(4,000 까지만 줄여 반복) | C |
| T11 | 낮음 | `logs/db.js initJson` | NDJSON 폴백 prune 이 전량 동기 `rewrite()` | C |
| T12 | 보통 | `pyportal/hub/auth.py:435-447,562-576` | `resolve()` 가 `token_version()`+`role_of()` → 인증 요청당 stat 2회·RLock 2회·선형 탐색 2회 | C |
| T13 | 보통 | `pyportal/hub/history.py:216-233` | `latest()` 가 폴링마다 `checks` 전체 GROUP BY MAX + self-join → `record()` 시 메모리 갱신(idrac `withLatestCache` 패턴) | P |
| T14 | 낮음 | `pyportal/hub/server.py:296-301,967-980` | `/api/meta` 가 개수만 필요한데 전체 dict 복사 · 정적 파일 요청마다 `read_bytes()`(no-cache) | C |
| T15 | 낮음 | `pyportal/hub/settings.py:493-499`, `backup.py:336-360` | `section()` 4섹션 deep-copy · `due()` 60초마다 glob+stat | C |
| T16 | 보통 | `web/views/tools/RightsizeReport.jsx:131-133`, `GuestDiskDetailModal.jsx:170-173` | `merge()`·`vmRows`·`toRows` 매 렌더(2016점×4계열 재병합 + recharts 3개 재렌더) → `useMemo` | C |
| T17 | 보통 | `web/components/EntityDetail.jsx:140,160-174` | `filtered` 매 렌더 새 배열 → useMemo deps 무효(5,000행 키 입력마다 sort) | C |
| T18 | 보통 | `web/views/IdracAdmin.jsx:51-75` | 스캔 중 30s+5s+5s 3중 폴링, `document.hidden` 무시 | C |
| T19 | 낮음 | `web/views/tools/reportExport.js:75-86,103-105` | JPG `scale:2` × 전체 높이 → 2240×12000 ≈ 27M 픽셀(≈107MB) + base64 복사본 | P |

## 5. 버그 아님(역기록)

- `routes/admin/toolCategories.js:17` 무게이트 GET — 특수 기능 화면이 모든 로그인 사용자에게 필요(의도·문서화). `/remote/targets` 는 scope 적용됨.
- `auth/ad.js` `\x00` 정규식(v2.479 치환) 정상 — RFC 4515 치환값 `\5c \2a \28 \29 \00`.
- `verifyToken` exp·`authenticateLocal` OTP 잠금(로그인 잠금이 감쌈)·`secureCompare`·`sessions` sid·`smtp` 헤더 인젝션·`nfsMounts` 인자·`brokerFetch`(설계)·`atomicWrite` tmp — 문제 없음.
- `collectorsDc.js:276,299` requirePerm + `vmScopeDenied` 존재 · `gpu-physical/test` 저장 host 고정 · `/auth/ad-test` AD bind 비번 저장 없음 · `GET /upgrade/settings` redact · relay-test 내부 SSRF 차단 · `POST /tool-usage` validKey.
- pyportal: `notify.py` 플래핑 스킵(설계) · `audit.py` 회전 순서 정확 · `ssrf.py` 포트 처리 · `VERSIONS_KEEP=15` 경계 정확 · Python 3.9 호환(grep) · uagmon IP 핀·host 변경 비번 폐기 유지.
- 웹: `sortableText.js:114` ISO `Z` 무시는 열 전체 동일 오프셋 · `key={i}` 는 sortChildren 키 보존(BulkTab 만 문제) · Topology3D 세대 가드 정상 · `Collectors:95`·`AgentScans:66`·`PduTool:69` 등 `error && !data` 준수.

## 6. v2.480.0 조치 요약 / 다음 후보

조치 17건: S1·S2·S3·S4·S5·S6·S7·S8·S9·S10·S11·S12 / B14·P4 / W1·W2 + 회귀 테스트 4건(`test/audit2480.test.js`: OTP 재사용 거부·SMTP host
변경·secretVault fail-safe·bundleShaIssue) + `ci.yml`/`release.yml` `timeout-minutes`.
**운영 영향 주의**: S3 로 엣지·수집기가 `X-Bundle-Sha256` 헤더 없는 push 를 거부한다 — **중앙을 먼저 v2.480 으로 올린 뒤** 엣지에 push 할 것
(엣지가 GitHub 자동 업그레이드로 먼저 올라가고 중앙이 구버전이면 push 가 400 — 그때만 엣지 `UPGRADE_ALLOW_UNVERIFIED=true` 임시 완화).
다음 후보(영향 순): S13(pyportal 스레드 상한 accept 시점) · B1(tar 모드/실행비트 + otp-enroll.sh 스테이징) · T1/B3(번들 push 동시성) ·
B2(deploy-all 가드) · P1~P3(pyportal 가져오기 id·CSV 셀·복원 락) · W3(sendJson 400 12곳 — 계약 변경 I-3) · S13~S24 잔여.
