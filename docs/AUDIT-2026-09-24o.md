# 18차 점검 — 10축 병렬 감사 + 발견별 2인 반증 (v2.607, 2026-09-24)

요청: "자세하게 두번 더"(2회차). 방식은 v2.606(`AUDIT-2026-09-24n.md`)과 같고 첫 축은 **v2.606 회귀**(v2.606 감사 기준 ~ v2.606 릴리스 커밋 diff)다.
감사 기준 커밋 `0173c65`(v2.606.0 소스). 수정은 파일이 겹치지 않는 **7그룹**(a~g) 병렬이고, 통합 단계에서 그룹 보고의 '배정 밖' 잔여 2건(h)을 직접 고쳤다.
변이 검증은 기준 커밋 판본(`git show 0173c65:<경로>`)으로 되돌려 확인했다. 그룹 보고서는 scratchpad `fix2607_{a..g}.md`.

⚠ 진행 기록: 수정 도중 세션 사용량 한도로 7그룹과 recent 검증자 2개가 모두 중단됐다. 한도가 풀린 뒤 반쯤 적용된 수정(capacity 3파일·roomTempSeries)을 되돌리고
변이 백업 디렉터리를 지운 다음 전 그룹을 처음부터 다시 실행했다.

## 결과 요약

| 축 | 발견 | 확정 | SPLIT | 가능성 | 반증 | 고침 |
|---|---:|---:|---:|---:|---:|---:|
| v2.606 회귀 | 8 | 8 | 0 | 0 | 0 | 8 |
| 남은 후보·형제 | 10 | 10 | 0 | 0 | 0 | 10 |
| 인가·범위 | 7 | 7 | 0 | 0 | 0 | 7 |
| 중앙 수신 계약 | 6 | 6 | 0 | 0 | 0 | 6 |
| 수집기 정확성 | 8 | 5 | 0 | 2 | 1 | 5 |
| 웹 화면 | 9 | 7 | 2 | 0 | 0 | 9 |
| 타이머·자원 | 3 | 3 | 0 | 0 | 0 | 3 |
| SQLite | 4 | 4 | 0 | 0 | 0 | 4 |
| 보안 | 7 | 7 | 0 | 0 | 0 | 7 |
| 엣지 계약 | 3 | 3 | 0 | 0 | 0 | 3 |
| **합계** | **65** | **60** | **2** | **2** | **1** | **62** |

- 같은 결함을 두 축이 찾은 것 8쌍은 한 번만 고쳤다: LEFT04↔WEB06 · LEFT05↔TIM01 · LEFT06↔SEC07 · LEFT07↔WEB03 · LEFT08↔TIM02 · LEFT10↔SEC03 · RECENT06=WEB02 · RECENT07=COL04.
  고유 수정은 **54건** + 통합 잔여 2건.
- 반증: COL2607-02.
- 가능성(고치지 않음): COL2607-06 · COL2607-08.
- SPLIT(고침): WEB2607-06(= LEFT2607-04, REST 폴백 vCenter 의 알람 칸) · WEB2607-07(백업 없음 리포트 `patternsOmitted` 안내). 의도 검증자는 '설계' 로 봤지만
  재현 검증자가 화면에 거짓 값(0건·전부 사용)을 확인해 고쳤다.
- **v2.606 '남긴 것' 중 이번에 다시 확정된 것**: AUTHZ2607-05(범위 admin 의 전 법인 공통 스칼라 설정) · TIM2607-02 일부(`sshExec withDeadline` 시한 상한) ·
  LEFT2607-02(fleet partial 화면 표시) · LEFT2607-03(SAN 점검 `failed` 표시) · LEFT2607-08 · LEFT2607-09(curuser 청크 0 교체).

회귀 테스트: `server/test/audit2607{a..h}.test.js` **66건** + 웹 vitest(`consoleData2607` · `collectorDiag2607` · `emptyInv2607` · `sanHealth2607` · `vcLogPaging` 등).
변이 검증: 그룹별로 고친 파일을 하나씩 기준판으로 되돌려 해당 테스트 실패를 확인했다(c 15/15 · d 10/10 · e 7/7 등, 상세는 그룹 보고서).

## v2.606 이 만든 결함(회귀)

| id | 재현 → 수정 |
|---|---|
| RECENT2607-01 | SAN 엣지 수신의 `_lastRec` 정리가 청크마다 돌아 다중 청크 push 에서 작업 로그 중복 제거가 깨졌다 → 마지막(또는 단일) 청크에서만 정리 |
| RECENT2607-02 | curuser push 의 `slimRecord` 가 v2.606 에 생긴 `truncated` 를 버려 중앙에서 '최소 N명' 이 다시 전체 인원이 됐다 → 필드 보존 |
| RECENT2607-03 | 원격 프록시 `POST /proxies` 를 범위 admin 이 저장하면 범위 밖 vCenter 배정이 지워졌다(v2.605 범위 병합의 형제 누락) → `mergeScopedIds`, 범위 밖 배정 프록시의 삭제·주소 변경 403 |
| RECENT2607-04 | 수집 서버 hostConflicts 경고가 영속된 과거 충돌까지 경고로 띄웠다 → 경고는 현재 충돌만, 영속분은 키 선택에만 |
| RECENT2607-05 | RMA 인스턴스 상한 거절(403 refused)을 엣지가 오류로 보고 짧은 주기로 재시도했다 → '연결됨·작업 없음' + 백오프 |
| RECENT2607-06 | (= WEB2607-02) 범위 계정 저장의 `unapplied`·`ignoredOutOfScope` 를 화면이 말하지 않았다 |
| RECENT2607-07 | (= COL2607-04) Capacity sys 모드 used 0 이 '측정 0' 으로 적재됐다 |
| RECENT2607-08 | `storeStatus.collectMode` 에 REST 폴백 여부가 섞여 빈 인벤토리 판정이 흔들렸다 → collectMode 는 site/direct 만, REST 는 `collectSource` 별도 |

## 주요 확정 결함

- **AUTHZ2607-01 (범위 admin 이 자기 범위를 넓힌다)** 사용자 관리 PATCH/POST 가 범위 제한 admin 의 요청을 막지 않아 자기·타인 scope 를 전체로 바꾸거나
  전체 범위 계정의 비밀번호·OTP 를 대리 변경할 수 있었다 → 새 scope 는 요청자 범위의 부분집합, 전체 범위 계정 대상 조작은 거부.
- **AUTHZ2607-02~07** deep-search probe 범위 교집합 · vCenter 등록 관리(범위 밖 404, 추가·데이터 소스 전환 403 — `fleetWideOnly`) · 중앙 IPAM vc-ranges ·
  전 법인 공통 스칼라 설정(`ignoredGlobal` 로 밝힘) · 게스트 조사·로그인 실패·vclogs 수집 · 감사 로그·중앙 인벤토리(`fleetOnly`).
- **CEN2607-01·02·05** SAN 스냅샷 정제가 health·extra 하위 목록·표시 문자열을 좁히지 않아 원소 하나가 시리얼 조회 전체를 죽이거나 객체가 화면에 새었다 → 아는 필드·객체 배열·scalarize.
- **CEN2607-03** 스토리지·SAN·PDU push 가 중앙 직접 수집 장비·미등록 id 를 받았다 → 등록부 agent 가 빈 장비는 거절하고 `notOwned` 로 밝힘(미등록 id 는 공유 토큰 흐름 때문에 유지 — 한계).
- **CEN2607-04·06** svcmon 메타 상한 · `/inventory` 가 중앙 직접 수집 vCenter 에 쓰는 것(엣지 vCenter 쓰기 거부 + ingestReject OWNER).
- **TIM2607-01 (= LEFT2607-05)** v2.606 이 계측에만 적용한 `capStr` 을 상주 레코드 전부로 — central/* 20여 파일 · rma/jobs. 자식 프로세스 `--expose-gc` 로 2MB × 6회 잔존 < 20MB 를 단언하고,
  소스 스윕이 허용 목록 밖 `.slice` 를 막는다.
- **TIM2607-02 (= LEFT2607-08)** `withDeadline` 이 3e9·NaN·음수를 1ms 시한으로 만들어 수집·대량 테스트·업그레이드 push 가 즉시 실패했다 → `util/deadline.js deadlineMs`(기본 60초, [1초, 2시간]) 단일 관문.
- **TIM2607-03** 알림 엔진 intervalSec 정규화.
- **LEFT2607-01** 통신 점검 엣지 보고는 중앙이 그 agent 에 내려준 링크 집합에 있을 때만 받고, `link_latest` 정리에 현재 id 집합을 넘긴다.
- **LEFT2607-04 (= WEB2607-06)** REST 폴백 vCenter 는 알람을 모르는데(`alarmsUnknown`) 0건으로 보였고 알림 엔진이 경보 '해소' 를 냈다 → 알람 칸 '—' · 'REST 폴백' 배지 · 합계 제외 · 해소 판정 보류.
- **COL2607-01·03·04·05·07** Capacity 처리량 상태를 샘플러·push 가 공유해 서로의 델타를 먹었다 → 소비자별 prev · 전력 센서 amp 제외 · sys used 0 → null · PowerStore 목록 절단 표시 · PDU 에너지 부분 합 표시.
- **SEC2607-01~06** 중계 점검 SSH 배너 무상한 버퍼 · VPLEX 버전 정규식 · 라이선스·텔레메트리·lsan 정규식(옛 결과와 동일성 대조) · SSH stderr 상한 · WS SSH hostname 채널 상한·시한 ·
  업그레이드 다운로드 바이트 상한 스트림(`util/readBytesCapped.js`).
- **SEC2607-07 (= LEFT2607-06)** 스토리지·SAN·PDU·bmstor 등록부의 비밀 승계 조건을 `accessMoved(host·port·username)` 로(agent 변경은 승계 유지 — 위임 수집에 필요) + `droppedSecrets` 응답.
- **DB2607-01~04** 전산실 온도 collectedSince 를 `metaKey` 로 · vCenter 로그 필터 COUNT 상한(1만+1) + `totalCapped` · LIKE 이스케이프 · 파트 장애 이벤트 `pageArgs`.
- **EDGE2607-01~03** 엣지 로그 폴백 회신에 agent 이름 · IP 스캔·파트 장애 push 가 중앙 거절 요약을 읽음 · GPU 게스트 unread 문구.
- **WEB2607-01·03·04·05·08·09** 세션 보안 빈 칸 · 비밀 폐기 안내 공용 문구(`droppedSecretText`) · 알림 억제 창 빈 칸 · 전력 측정 0대 vCenter powerKw null · 릴리스 노트 BoldText · 수집 서버 serversDropped 배지.
- **LEFT2607-02·03** fleet partial 배너 · SAN 전체 점검 `failed` 요약·PDF note.

## 통합 단계에서 직접 고친 것(h)

| id | 내용 |
|---|---|
| INT2607-01 | `collector/registry.js updateCollector` 가 `normalize` 의 `droppedSecrets` 를 버려 수집 서버만 '다시 입력' 안내가 안 떴다(그룹 g 보고) |
| INT2607-02 | SAN push 재전송의 청크 0 이 실패하면 '직전 push 그대로' 라 말했지만 첫 시도가 이미 목록을 교체했다(그룹 e 가 curuser 에서 찾은 구멍의 형제) → 첫 시도의 수신 수를 이어받아 '부분 상태' 로 말함 |
| 문서 | 새 게이트 `fleetOnly`·`fleetWideOnly` 를 `api-doc.mjs` GUARD_NOTE 에 등록(없으면 apiDoc2563 이 '뜻 모르는 게이트' 로 실패) |

## 검증

- 서버 `npm test` 전량: 통합 전 1건 실패(apiDoc2563 — 위 GUARD_NOTE) → 고친 뒤 해당 파일 13/13, audit2607* 66/66. 웹 vitest 145파일 1,847건 · eslint 0 · vite build OK.
- 생성기 `--check`: api-doc · config-doc · env-doc · third-party-notices 최신.
- Chromium(목 스택): 개요 · vCenter · 설정(세션 보안·vCenter 관리·수집 서버·릴리스 노트) · SAN 스위치 · 베어메탈 사용률 · 통합 서버 인벤토리 · vCenter 로그 · 스토리지 · PDU · 중계 토폴로지 · 관제 콘솔
  14화면 × 1440/400 — 가로 넘침 0 · 콘솔 오류 0 · `[object Object]`·`undefined`·`NaN` 0.

## 확인하지 못한 것·남긴 것(정직 기록)

- CEN2607-03: **미등록 id** 는 여전히 받는다(공유 토큰으로 먼저 보내고 나중에 등록하는 흐름). CEN2607-06: 미등록 vCenter id 는 막지 않는다.
- TIM01 스윕은 줄 단위라 여러 줄로 나뉜 `.slice` 는 못 본다. `sftpReadFile` 에는 크기 상한이 없다. VPLEX 버전 줄이 64KB 뒤에 있으면 못 찾는다.
- COL2607-03: 실장비 전류 센서의 `sensorType` 값은 확인하지 못했다. PowerStore 목록 절단은 실장비로 보지 못했다.
- 폴러의 `*_DEVICE_TIMEOUT_MS` env 는 하한만 있다(관문이 자른다). IPAM import dryRun 이 범위 밖 vCenter 의 존재를 드러낸다.
- 손대지 않은 라우트: IPAM 스캔 설정 GET · vclogs/status · 네트워크 캡처 · 프록시 health/test.
- 업그레이드 다운로드 상한 1.5GB 는 추정(실제 최대 자산 74.7MB).
- 목 데이터로 만들 수 없는 상태(REST 폴백 vCenter · 폐기된 비밀 · SAN 점검 실패 장비)의 안내 문구는 브라우저로 보지 못했다 — 문구 모듈 vitest 로만 고정했다.
