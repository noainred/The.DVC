# 15차 점검 — 10축 병렬 감사 + 발견별 2인 반증 (v2.604, 2026-09-24)

요청: "자세하게 3번 더"(2회차). 방식은 v2.603(`AUDIT-2026-09-24k.md`)과 같고 첫 축은 **v2.603 회귀**(`git diff c766e01..cd416fe`)다.
수정은 파일이 겹치지 않는 6그룹 병렬, 변이 검증 기준은 감사 기준 커밋 `5795a83`. 그룹 보고서는 ID 마다 재현 입력·수정 전/후 실측·위치·
변이 결과·남은 한계를 적었다(scratchpad `fix2604_{a..f}.md`).

## 결과 요약

| 축 | 발견 | 확정 | SPLIT | 반증 | 고침 |
|---|---:|---:|---:|---:|---:|
| v2.603 회귀 | 4 | 4 | 0 | 0 | 4 |
| 남은 후보·형제 | 2 | 2 | 0 | 0 | 2 |
| 인가·범위 | 7 | 6 | 0 | 1 | 6 |
| 중앙 수신 계약 | 5 | 5 | 0 | 0 | 5 |
| 수집기 정확성 | 5 | 4 | 1 | 0 | 5 |
| 웹 화면 | 3 | 3 | 0 | 0 | 3 |
| 타이머·자원 | 6 | 5 | 0 | 1 | 5 |
| SQLite | 1 | 1 | 0 | 0 | 1 |
| 보안 | 3 | 3 | 0 | 0 | 3 |
| 엣지 계약 | 3 | 2 | 1 | 0 | 3 |
| **합계** | **39** | **35** | **2** | **2** | **37** |

감사 반증 2건: 범위 제한 admin 의 iDRAC 전량 조회(문서화된 정책) · 10분 16만 요청 장시간 실측(누수 없음). 수정 단계 반증 0.
후속 **9건**도 같은 릴리스에 고쳤다. 발견 수가 회차마다 줄고 있다(v2.600 67 → 52 → 47 → 47 → **39**).

⚠ **작업 사고(정직 기록)**: 두 수정 에이전트가 변이 검증 백업에 같은 파일 이름(`scratchpad/mut.bak`)을 써서 `linkcheck/checks.js`·
`agent/inventoryPush.js`·`agent/captureWorker.js` 가 잠시 서로 덮였다. 각 에이전트가 기준본에 자기 수정을 다시 얹어 복구했고,
오케스트레이터가 ① 변경 파일 전체의 머리말이 기준본과 같은지 ② 기준본 대비 30% 이상 지워진 파일이 없는지 ③ 해당 테스트 통과
④ `captureWorker` 를 기준본으로 되돌리면 테스트가 실패하는지(변이 유효)를 직접 확인했다. 이후 백업 이름은 그룹 접두(`grpX_*`)·전용 디렉터리.
**병렬 변이 검증의 백업 경로는 반드시 에이전트별로 나눌 것.**

회귀 테스트: `server/test/audit2604{a..f}.test.js` 51건 + 웹 vitest(`rightsizeText`·`commMapText`·`storageGrowthText`·`sanSwitchTestText` 등).
변이 검증: a 16/16 · b 9/9 · c 11/11 · d 6/6 · e 13/13 · f 4/4.

## v2.603 이 만든 결함(회귀)

| id | 심각도 | 재현 → 수정 |
|---|---|---|
| RECENT2604-01 | medium | v2.603 이 추가한 ip-scan-result 409(unassigned)가 배정 조회(쿼리 이름)와 결과 조회(토큰 이름)의 **대소문자 차이**로 갈려, 엣지는 배정을 받아 스캔하고 **결과는 전부 409 로 버려졌다**(IPAM 에 한 건도 반영 안 됨, 문구는 '범위를 배정하라' 는 틀린 조치). 실제 라우터로 재현 → `agentValueOf`/`agentKeyOf` 로 대소문자 무시 + 배정 라우트도 토큰 이름 |
| RECENT2604-02 | low | v2.603 의 백그라운드 ping 대상 삭제가 같은 id 로 다시 만든 대상의 새 표본까지 지움 → `dropTarget(target, untilTs)` 삭제 시각 경계 |
| RECENT2604-03 | low | v2.603 일일 보고 백오프가 `run()` **예외**에는 적용되지 않아 여전히 1분마다 → 예외도 `noteResult` |
| RECENT2604-04 | low | v2.603 라이트사이징 `held` 가 화면에 없어 현재 사양이 초록 '권고' 로 보임 → '권고 보류(피크 미확인)' 배지 + 각주 |

## 주요 확정 결함

- **CEN2604-01 (high)** 자기등록 URL 검증(`verifyDerivedCollectorUrl`)이 엣지 응답을 상한 없이 해제·파싱하고 비-JSON 을 `{}` 로 삼켜 통과시켰다 — 공유 토큰 1명이 중앙 RSS 를 수 GB 로 → `readJsonCapped`(64KB), 비-JSON·ok 아님·agent 없음은 검증 실패. 후속으로 형제 4곳(upgradePush·idracScanPush·bmstor poller·edgeReport)도 상한 + 소스 스윕.
- **CEN2604-04** 공유 토큰 자기등록 개수 무제한 + 그 이름이 `edgeNameKnown` 의 '아는 엣지' 가 되어 **v2.601 미검증 제한을 우회** → 전체 256·미검증 16(429), 검증 실패 표식, '아는 엣지' 는 managed·검증 통과·pull 성공만.
- **CEN2604-02 (통신 점검)** 응답 본문 전체를 읽고 slice(gzip 299KB 로 RSS 375 → 1,003MB) → `readBodyPrefix` + `.text()…slice(` 금지 스윕(ollama 도 옮겨 예외 0).
- **CEN2604-03·05 · TIM2604-04** 엣지 bm-usage 보관분·SAN 테스트 회신 원문 저장(화면 React #31) · idracScan agentPolls 상한.
- **SEC2604-01** `portal.env` 의 `*_PASS`(PROXY_SSH_PASS·HAPROXY_DATAPLANE_PASS)가 **백업 번들·엣지→중앙 설정 사본에 평문** → `SECRET_KEY_RE` 에 `_PASS|_PWD|_CREDS|_CREDENTIALS`(밑줄 접두일 때만 — `pass`·`bypass` 같은 식별자는 제외). ⚠ 중앙에 이미 저장된 옛 사본은 엣지 다음 push(30분)에 덮여 정리된다.
- **SEC2604-03** 웹훅 URL(비밀 포함)이 암호화 모드에서도 평문 → `alerts.json` 을 SECRET_FILES 에, 이 파일의 `url` 만 봉인(`FILE_EXTRA_SECRET_FIELDS` — 전역 `SECRET_FIELDS` 에 넣으면 다른 파일의 접속 주소까지 봉인된다). 옛 평문 로드·plain↔encrypted 전환·무변경 재저장 바이트 동일 확인.
- **SEC2604-02** 엣지 로그 가림이 홑따옴표 다단어·`SECRETS_KEY=`·`*_PASS=` 를 놓침.
- **AUTHZ-2604-01·02·06** 공개 API 가 내부 비-admin 화면에서 가리는 **스토리지 이름(=관리 IP)** 과 **파트 장애 키의 iDRAC IP** 를 그대로 줬다 → 내부와 **같은 함수**(`maskPartRow`·`maskedNameLabel`)로. `/faults/parts` 의 `openedAt`·`reason` 이 필드명 불일치로 **항상 null** 이었다 → `firstSeenAt`·`holdReason`. 문서(`API-PUBLIC.md`)도 갱신.
- **AUTHZ-2604-03·04·05** 스토리지·SAN 목록의 `poller.inFlight` 주소(실제 수집을 매달아 재현) · 범위 제한 admin 의 `/api/admin/alerts` 가 전 vCenter 알림 → 범위 절단 + `omittedOutOfScope`.
- **LEFT2604-01·02** 폴더 사용량 보존 빈 칸이 1일(서버)/365일(웹) → 스캔 이력 삭제 · 중계 점검 빈 칸·0 이 기본값 → 이전 값.
- **COL-2604-01** Isilon SSH 용량이 `isi status` 반올림(`5.0P`)이라 증가량이 **0.1 PiB(102.4 TiB) 계단** → 같은 세션에서 `isi statistics` 정확 바이트를 시도, 못 읽으면 `capacityApprox.resolutionBytes`. **결정**: 적재는 막지 않고(정확 값이 없는 장비의 이력이 사라지는 것이 더 나쁘다) 증가량 화면·공개 API 가 해상도를 밝힌다('약(±102.4 TB)', 해상도 반 칸 미만은 '±… 미만' — 0 으로 단정하지 않음).
- **COL-2604-02·03** PowerMax 일부 SRP 실패가 **어레이 전체 용량으로 둔갑** → partial-pools · 어레이 8개 절단이 원격(SRDF) 필터보다 앞이라 로컬 누락 → 필터 먼저(상세 최대 32회 — RTT 800ms 에서 약 26초).
- **COL-2604-04·05** PSU `0W` 가 '미확인' · 공유 FS 디렉터리가 '미발견'(사유 표시).
- **TIM2604-01** in-app 업그레이드 재시작이 gracefulExit(RMA long poll 해제·CSV finish·응답 유예)를 건너뜀 → `setShutdownHandler` 로 같은 종료 경로(순환 import 없음). ⚠ 실서버 재시작 로그는 실측 안 함.
- **TIM2604-02** VM 복제 `done(true)` 가 스냅샷 병합(최대 2시간) 전에 `_running` 을 내려 **같은 잡이 끝없이 다시 돌 수 있었다**(감사 보고보다 심각) → finally 에서만. 후속: 존재하지 않는 `config.mode` 판정 때문에 **목 모드에서도 실제 vCenter 경로**를 타고 live+설정 없음이 가짜 '성공' 이던 것 → `getDataSource()==='mock'`.
- **TIM2604-03** 백업 `every` 로드 경로 클램프.
- **DB2604-01** ping 개요 30일/365일이 원시 GROUP BY 로 **10.6초 정지** → 시간당 롤업 `samples_hourly`(insert 트랜잭션 upsert · prune·dropTarget 동기 정리 · 구버전 DB 청크 시드). 1,470만 행 실측 365일 10,574ms → 약 205ms, 30일 898ms → 약 62ms, 원시와 결과 diff 0.
- **EDGE2604-01·02·03** fleet push 가 호스트를 못 읽은 부팅 직후 ESXi 백킹 iDRAC 을 베어메탈로 보냄 → 그 주기 보류(후속: bmusage 대상 계산도 같은 가드) · 0 vCenter 조기 반환도 상태 갱신 · 워커 '수집 완료' 로그에 회신 실패 표기.
- **WEB2604-01·02·03** 통신 지도 pull 시각 null 이 0(epoch, '약 56년') · 로그 분석 추적 시작을 정시로 말하고 부분 창을 완전하다고 표시 · 상세 문구.

## 검증

- 서버 전량 **3,600건 0 실패** · 웹 vitest 128파일 1,773건 · eslint 0 · vite build · 문서 생성기 4종(새 env 반영) · `API-PUBLIC.md` 갱신.
- Chromium 목 스택 33화면 × 1440/400: 가로 넘침 0 · 콘솔 오류 0. 그룹 f 는 held 행·실패 엣지를 응답 가로채기/실제 등록으로 확인.

## 정직 기록 — 확인하지 못한 것

- Isilon `isi statistics ... --format json` 출력, FOS PSU 0W 표기, PowerMax 부분 SRP 응답은 실장비로 보지 못했다(합성 재현).
- in-app 업그레이드의 실제 재시작 로그·CSV finish 대기는 실측하지 않았다(단위 테스트·소스 검증).
- 중계 점검 화면 빈 칸·SAN 테스트 모달·베어메탈 `hostsUnread`·Isilon 해상도 표지는 브라우저로 보지 못했다(`hostsUnread` 는 화면 표시도 아직 없다).
- 소스 스윕이 못 보는 무상한 `.json()` 1곳(경로를 변수로 조립하는 upgrade.js 엣지 push 응답)과 관리자 수동 실행 3곳은 다음 후보다.
- 범위 제한 admin 의 알림 응답 `config`(전역 웹훅·임계)는 그대로 준다 — 저장이 전역이라 나눌 축이 없다(정책 판단).
