# 12차 점검 — 10축 병렬 감사 + 발견별 2인 반증 (v2.601, 2026-09-24)

요청: "세번더"(2회차) + "자세하게 3번 더". 방식은 v2.600(`AUDIT-2026-09-24h.md`)과 같고, 첫 축은 **v2.600 회귀**
(`git diff 72aa866..3778733`, 서버 90여 파일)다. 수정은 파일이 겹치지 않는 6그룹 병렬, 그룹마다 회귀 테스트 +
변이 검증(감사 기준 커밋 `79bde27` 판본으로 되돌리면 실패).

## 결과 요약

| 축 | 발견 | 확정 | SPLIT | 가능성 | 반증 | 고침 |
|---|---:|---:|---:|---:|---:|---:|
| v2.600 회귀 | 5 | 4 | 1 | 0 | 0 | 5 |
| 남은 후보·형제 | 7 | 6 | 0 | 0 | 1 | 6 |
| 인가·범위 | 4 | 3 | 0 | 1 | 0 | 4 |
| 중앙 수신 계약 | 5 | 5 | 0 | 0 | 0 | 5 |
| 수집기 정확성 | 6 | 3 | 0 | 2 | 1 | 5 |
| 웹 화면 | 7 | 7 | 0 | 0 | 0 | 7 |
| 타이머·자원 | 4 | 4 | 0 | 0 | 0 | 4 |
| SQLite | 4 | 4 | 0 | 0 | 0 | 4 |
| 보안 | 3 | 2 | 1 | 0 | 0 | 3 |
| 엣지 계약 | 7 | 5 | 1 | 1 | 0 | 5(2 반증) |
| **합계** | **52** | **43** | **3** | **4** | **2** | **48** |

감사 단계 반증 2건: LO2601-07(롤업 templates 개수 — 설명 문구의 과장일 뿐 결함 아님) · COL-2601-04(Brocade REST media-rdp 키 — 실제 키 형식과 일치).
수정 단계 반증 2건: **EDGE2601-05**(현재 사용자 0건이면 push 생략 — 중앙 직접 경로도 같고 조회 시 `refreshKinds` 가 `at` 기준으로 낡음 판정) ·
**EDGE2601-07**(파트 장애 env 꺼짐 무보고 — v2.548 F3 설계이고 화면 silent 문구가 이미 '꺼짐 가능' 을 말한다. 바꾸려면 중앙 `classifyEdges` 와 함께).
그룹이 배정 밖으로 보고한 후속 **13건**도 같은 릴리스에 고쳤다(아래).

회귀 테스트: `server/test/audit2601{a..f}.test.js` 65건 + 웹 vitest(`trackPct2601`·`sanPowerText`·`dsUsageCalc`·`forecastRowText`·`apiKeyText`·
`consoleData`·`dataFlow`·`deviceFlow`·`tokenCheckText`). 변이 검증: a 12/12 · b 12/12 · c 9/9 · d 9/9+웹 · e 17/17 · f 11/11+웹 5.

## v2.600 이 만든 결함(회귀) — 가장 중요

| id | 심각도 | 재현 → 수정 |
|---|---|---|
| RECENT2601-01 | **high** | v2.600 이 여유 미보고 파티션의 `usedGB` 를 null 로 바꾸자 **중앙 직접 수집 경로가 그 값을 `used_gb REAL NOT NULL` 에 넣어 vCenter 전체 커밋이 매 주기 롤백**(정상 VM 도 미저장, 보고서가 옛 값에 고정). 실제 `commitCollection` 으로 재현(`NOT NULL constraint failed`). → 직접 경로도 중앙 수신과 같은 `sanitizeGuestDiskVms` · `commitCollection` 2차 방어(숫자가 아닌 파티션은 적재하지 않고 개수·경고, 전부 걸러지면 vm_latest 보존) |
| RECENT2601-02 | medium | 엣지의 '불통·빈 인벤토리 보류' 에 시간 제한이 없어, 6시간 뒤 lastGood 가 만료된 vCenter 는 **영원히 보내지 않았고** 중앙은 그 호스트·VM 을 함대 합계에 계속 셌다(중앙 HOLD 규칙이 집행되지 않음). → `withholdDecision`(보류 시작 시각) + `WITHHOLD_MAX_MS`(= LASTGOOD_HOLD) 뒤엔 빈 슬라이스를 보내 중앙 규칙을 적용 |
| RECENT2601-03 | low | NSX 목록 실패 시 도메인 타일이 '세그먼트 null · 엣지 null' + 초록 → `nsxCount`·`nsxFailedShort`·주의 등급 |
| RECENT2601-04 | low | GPU 가 전부 오류면 파서가 null 을 돌려 `gpuErrors` 가 사라지고 원인이 '파싱 실패' → `gpuLostError`('GPU 응답 없음 + 원문') |
| RECENT2601-05 | low | `keepIfBlank` 입력 칸을 비울 수 없고 입력 중 하한이 걸려 '6' 이 10 → 원문을 들고 있다가 저장 때만 변환(`applyDrafts`, Chromium 실입력 확인) |

**교훈**: v2.600 은 '결측을 null 로' 를 수집기에서 했는데 **그 값이 들어가는 DB 열이 NOT NULL** 이었다. 결측을 새로 만들 때는 소비처(화면)만이 아니라
**저장 스키마까지** 따라가야 한다(v2.599 '소비처 전수' 교훈의 연장).

## 주요 확정 결함

- **CEN2601-01 (high)** 엣지 IP 스캔 결과의 원소 하나(`services` 가 문자열 아님 등)로 **함대 IPAM 원장 저장이 매 주기 실패**하고 `/tools/ipam/insights` 500 → `cleanAliveHost`(포트 1~65535 정수 · 서비스 문자열 64자 · 호스트명 제어문자 제거) — 병합과 디스크 로드 둘 다.
- **CEN2601-02·03** `/fleet`: 엣지 베어메탈을 **남의 법인 vCenter 로 귀속** 가능 → 그 엣지가 소유한 vCenter 에만(나머지는 귀속만 비우고 `vcenterBlanked`) · 공유 토큰의 임의 이름 × 5만 대로 195MB 파일·1.8초 정지 → 에이전트당 5,000 · 전체 20,000 · 미검증 이름 20개 상한(`omitted`).
- **CEN2601-04·05** `/result` 유령 이름 영속 → 배정 없으면 409 · 상한 500 / rma-poll 정책 필드가 문자열이면 매 폴링 500 → 배열일 때만(`policyInvalid`).
- **AUTHZ-2601-01~04** 식별자·이름 **자체가 IP 인** iDRAC 행(bm-usage·파트 장애·인사이트 finops/전력/fleet·서버 온도)을 비-admin 에 그대로 줬다 → 프로세스별 키 HMAC 토큰 `masked-<12자>`(같은 원문 = 같은 토큰이라 표 매칭·상세 조회 유지, 조회 시 원래 키로 되찾음). ⚠ 재시작하면 토큰이 바뀐다. ESXi 호스트 이름과 같은 이름은 가리지 않았다(인벤토리에서 이미 보이는 값).
- **WEB2601-04** 삭제된 vCenter 로 범위를 둔 연동 키가 전부 0 을 `ok:true` 로 받았다 → 전부 없으면 403 `scope-empty`, 일부면 `meta.scopeUnknownVcenters` + 설정 화면 경고.
- **EDGE2601-01** 중앙에서 지운 GPU 게스트 VM 비밀번호·고정 IP 가 **엣지에 영원히 남았다** → `applyPulledGpuGuestSettings`(중앙 사본에 없는 키 삭제, `removed` 기록).
- **EDGE2601-02** vmseries 수집 범위표를 청크 0 에 실어, 뒤 청크가 실패하면 잃은 스파이크가 '측정됨·스파이크 없음' → 마지막 청크에.
- **EDGE2601-03** 캐시 없는 점검중(maintenance) vCenter 의 빈 슬라이스가 중앙 직전 목록을 덮었다 → UNREAD 에 maintenance.
- **EDGE2601-04·06** storage·PDU push 가 중앙 거부 개수를 무시(PDU 500대 조용한 상한) · '지금 수집' 직후 push 가 `_busy` 로 조용히 거절 → 기록 + `_again` 재전송.
- **LO2601-01** 에이전트 배포 envPairs 개행 → 원격 portal.env 임의 키 주입 → `envPairsIssue`(실행 전 + 저장 시 400 + LLM 대량 배포 행 오류).
- **LO2601-02** AD 타임아웃 칸을 비우면 **모든 AD 로그인 실패** → [1초, 60초] 정규화, 빈 칸은 이전 값, 저장된 `''` 복구.
- **LO2601-03** iDRAC 스캔·원격 접속마다 change 백업 → MIXED_STATE_FILES.
- **DB2601-01·02** guestdisk·vmtrack prune 이 조회 창 시작의 이월 행을 지워 증가 추이 오판 → 키별 마지막 행 보존.
- **DB2601-03** vCenter 로그 prune 단일 DELETE(183만 행 4.8초) → 청크 + 양보(`pruneAsync`), 용량 루프도 청크마다 양보. ⚠ VACUUM 은 여전히 동기.
- **DB2601-04** 현재 사용자 상태의 COUNT(*) 3회를 60초 캐시(`countsAt`).
- **SEC2601-01** zip 중앙 디렉터리 항목이 같은 로컬 데이터를 재사용하는 **겹침 폭탄** → 같은 localOffset 거부 + 누적 크기·개수.
- **SEC2601-02** `parseOs` 정규식 2만 자 1.9초 → 256자 절단 + `trimEnd`(200ms 미만).
- **SEC2601-03** 엣지 로그 가림이 JSON 인용 키·camelCase 비밀 필드를 놓침 → 키 끝이 비밀 이름일 때만(`deviceKey`·`partKey`·`tokenFp` 는 유지 — v2.549).
- **COL-2601-01** 'No devices were found' 가 GPU 모델로 자동 등록 → `--query-gpu=name,uuid` + `GPU-<uuid>` 줄만.
- **COL-2601-02·03·05·06** VPLEX 반복 머리글 · switchshow `Disabled (Persistent)` 주석 열 · PowerMax 경보 이중 계수·확인분 포함 · chassisshow 전력 부분 합 표시(`powerPartial`).
- **LO2601-04·05·06** DS 사용량 결측이 0% 초록 막대(합계도 부풀었다) · VM/스토리지 추이 사용률 0% · PowerStore 파일시스템 `size_used` 결측 0.
- **TIM2601-01~04** RMA 로그 점검 fd 누수 · `getMs()` 예외로 타이머 영구 정지 · 설정 파일 JSON `null` TypeError · 게스트 디스크가 재시작마다 주기 무시 전량 수집.
- **WEB2601-01·02·03·05·06·07** 경로 접두가 있는 수집 서버 URL '기록 없음' · 같은 origin 두 엣지 기록 합침(`withOutboundTag` — AsyncLocalStorage) · 받은 적 없는 보고 'N초 전' · '1분마다' 하드코딩 · 첫 실패가 ok:true · 가득 찬 DS '안정'.

## 후속(그룹이 배정 밖으로 보고 → 같은 릴리스에 고침)

vmtrack service·화면 사용률 null · GPU 게스트 설정 pull 연결(목 중앙 실경로 테스트) · SAN `powerPartial` 전달 · logs poller `pruneAsync`·용량 루프 양보 ·
배포 대상 저장 시 envPairs 검사 · LLM 대량 배포 행 검사 · secretScan fd · iDRAC 온도 응답 `intervalMs` · 지도 '주소 공유' 표시 ·
중앙 → 엣지 호출 6곳 태그 · 토큰 점검 `reportAt` · 공개 API 테스트의 없는 vCenter(`vc-kr-seoul` 로 빈 결과를 공허하게 통과하던 것) · API-PUBLIC.md `scope-empty`.

## 검증

- 서버 전량 3,425건(통합 중 실패 1건 — v2.600 테스트가 옛 `/result` 200 을 기대 → 409 로 정정) · 웹 vitest 118파일 1,715건 · eslint 0 · vite build.
- 문서 생성기 4종(API·CONFIG·ENV·고지) 최신.
- Chromium 목 스택 21화면 × 1440/400(DS 사용량·VM/스토리지 추이·파트 장애·인사이트·AD·연동 키 + v2.600 목록): 가로 넘침 0 · 콘솔 오류 0 · `undefined`/`NaN`/`[object Object]` 0.
- ⚠ 감사 단계 보안 축이 띄운 목 서버(포트 4865)가 워크플로 종료 뒤에도 남아 있었다 — 통합 중 발견해 종료했다.

## 정직 기록 — 확인하지 못한 것

- 비-admin 가림 토큰·`scope-empty` 경고·DS/VM 추이 '—'·용량 예측 '포화'·법인별 온도 주기 문구는 목 데이터로 해당 상태를 만들 수 없어 **브라우저로 보지 못했다**(순수 함수·실제 라우터 테스트).
- 실장비: PowerMax 경보 필터(`acknowledged=false`)·summary 필드명, VPLEX 머리글 반복, nvidia-smi 오류 줄 형식은 합성 재현이다.
- `/fleet` 상한 적용 후 RSS·저장 시간은 재측정하지 않았다. 공유 토큰으로 **이미 알려진 이름을 사칭**하면 그 이름의 vCenter 로 귀속할 수 있다(공유 토큰 전면 신뢰 정책의 한계).
- 엣지의 보류 시작 시각은 메모리 값이라 엣지 재시작 시 다시 센다.
- 중앙 → 엣지 호출 중 linkcheck·relaycheck·배포 후 검증·자기등록 URL 검증은 아직 태그가 없다(머리말이 개수를 밝힌다).
