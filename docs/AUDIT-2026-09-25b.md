# 20차 점검 (v2.612) — 2026-09-25

- 기준 커밋: `4e0f296` (v2.611.0)
- 방법: 10축 병렬 발견(RECENT·LEFT·AUTHZ·CEN·COL·WEB·PERF/TIM·DB·SEC·EDGE) → 축별 반증 검증(의도·재현 두 렌즈, 5개 검증 에이전트) → 파일이 겹치지 않는 수정 그룹 6개(A 범위 관리자 권한 · B CVP · C Redfish/OME 리다이렉트·수집기 단위 · D 엣지↔중앙 계약 · E iDRAC 스캔 관리·GPU 배포·ping·파트 장애 키 · F 웹) → 그룹별 변이 검증(**별도 git worktree** 에서 기준 커밋 판본으로 파일을 하나씩 되돌려 해당 ID 테스트가 실패하는지 확인 — 메인 트리는 건드리지 않았다) → 전량 테스트·빌드·Chromium 1440/400.
- 회귀: `server/test/audit2612{a,b,c,d,e}.test.js` · 웹 `views/audit2612f.test.js`·`queryKeyText.test.js`·`tools/hzListText.test.js`·`droppedSecretText.test.js`·`idrac/scanRunText.test.js`·`tools/cvpText.test.js`·`tools/partFaultText.test.js`.
- 기존 테스트 조정(결함 동작을 고정하던 단언): `cvp2608a`(BGP `prefixesUnknown`) · `audit2611c`(파트 경로 전부 404 → `partsDueUnread` 없음) · `cvp2608b`(위임 0건 엣지는 상태를 보관하지 않음 — CEN2612-01).

## 반증 결과 요약

- 확정·부분 확정: 약 45건(high 5 — AUTHZ2612-01·03·04, PERF2612-01, 그리고 RECENT/검증에서 medium→유지된 SEC 2건은 medium)
- 반증: RECENT2612-02(재진입 가드 규약대로) · LEFT2612-03(배너는 BGP 피어를 가린다고 말하지 않음 — 정책 판단으로 남김) · LEFT2612-06(자가 회복) · EDGE2612-04(설계) · SEC2612-04(설계 한계) · 위임 등록 잡 iLO(도달 불가) · DB2612-04(기록)
- 수정하지 않고 남긴 것: PERF2612-03(백업 생성 동기 gzip — 빈도 낮음) · DB2612-03(vmperf/vmseries LRU 가 prune 핸들을 닫음 — vCenter 48개 초과에서만) · CEN2612-01 관련 `_adopted` Set(호출이 위임 있을 때로 좁혀졌다) · `setAssignedGpuGuest` 원문 이름 저장(대소문자 변형이 둘째 항목을 만든다 — 기존) · 엣지 `agent/bmstorWorker.js` 가 `/bmstor-collect` 잠금을 공유하지 않음 · `corruptOnlyReason` 이 등록부 4곳에 복제 · CVP BGP 피어 IP 가림(정책)


## recent 검증 원문

| ID | 판정 | 검증 후 심각도 | 도입 | 근거 한 줄 | 권장 최소 수정 |
|---|---|---|---|---|---|
| RECENT2612-01 | CONFIRMED | medium | v2.611 TIM2611-01 (`cvp/poller.js:115-116`) | 재실행: 파트 경로 전부 404 인 목 CVP 8대에서 주기 1·2·3 마다 파트 GET 25/24/24회, partsRead=false. `client.js:245` 가 전부 실패면 `dev.parts=null` 이라 `_partsAt` 이 영원히 안 올라가 30분 주기가 매 주기가 된다. 배너(`cvpText.js:205`)는 원인을 '시간 예산·시한' 으로 단정하고 '이전 값' 이라 말하지만 읽은 적이 없다(v2.493 규약 위반) | client 는 실패해도 `dev.partsAt=now()` 를 이미 찍는다 → `partsReadNow` 대신 `attempted = r.devices.some(d => d.partsAt != null)` 로 `_partsAt` 을 올리고, `partsDueUnread` 는 시도조차 못 한 장비(예산·시한)가 있을 때만; 전부 실패면 `missing[kind]` 사유를 배너에 쓴다 |
| RECENT2612-02 | REFUTED | — (정보) | v2.611 TIM2611-04 (의도) | 재실행으로 동작은 재현(`periodic tick during adhoc: { ok:false, '이미 스캔 중입니다.' }`)되나, CLAUDE.md 성능 규약 '폴러 재진입 가드: 진행 중이면 이번 틱을 건너뛴다 … 같은 작업의 수동 실행 API도 가드를 공유할 것' 을 그대로 집행한 것이다. '지금 스캔' 에서도 이미 같은 건너뜀이 있었다(기존 동작) | (선택) 긴 주기에서 건너뛴 틱을 lastRun 에 `skipped:'busy'` 로 기록하거나 해제 후 짧은 지연 재시도 — 결함 수정이 아니라 개선 |
| RECENT2612-03 | CONFIRMED | low | v2.611 TIM2611-04 (`routes/admin/idracScan.js:253`) | 원 스크립트는 CONFIG_DIR 미설정으로 파일 읽기에서 죽었다(하니스 결함) — CONFIG_DIR 를 주고 재실행: `stop entries persisted: 50 ['scan|adhoc|10.9.0.1',…]`, 주기 대역 정책의 `skip('10.9.0.1')` = null. 주기 스캔은 `scan|<rangeId>|ip` 만 보므로 'adhoc' 기록은 아무도 읽지 않고, `util/authGuard.js` 에 크기 상한·만료가 없어 임시 스캔마다 최대 2,048건 누적(주석 '주기 스캔이 본다' 는 사실이 아님) | 임시 스캔에는 기록하지 않는 정책(`noteAuthFailed`/`noteOk` no-op, 필요 시 `skip` 만)을 넘기고 주석 정정. 이미 쌓인 `scan|adhoc|*` 는 로드 시 1회 정리 |
| RECENT2612-04 | CONFIRMED | low | v2.611 (`cvp/db.js adoptAgentVariants` 신규) | 재실행: 같은 날 Edge-A 2표본(max 300) + edge-a 1표본(max 900) → 모은 뒤 daily `samples:2, inBpsMax:300` (원시는 3행). `UPDATE OR IGNORE port_daily` 충돌 행이 이어진 `DELETE` 로 사라진다 — 원시 7일 보존 뒤 유일한 기록인 일 롤업에서 피크·표본 소실. 발생 조건은 한 엣지가 하루 안에 이름 대소문자 변형으로 push 한 경우뿐(드묾). 같은 방식으로 latest 표도 변형 쪽이 더 새 값이어도 옛 행이 남는다(다음 push 가 덮으므로 일시적) | `port_daily` 충돌은 `upDaily` 와 같은 식으로 합산(`*_sum`·`*_n`·samples 더하기, 최대는 MAX)한 뒤 변형 행 삭제; latest 표는 ts 가 더 큰 쪽을 남긴다 |
| RECENT2612-05 | CONFIRMED | low | v2.611 LEFT2611-03 (`gpu/settings.js carriedPw` 도입, `central/agentGpuGuestConfig.js:49` 가 `dropped` 를 안 넘김) | 재실행: 배포 설정에서 username 만 바꾸면 password "" · hasPassword:false 로 저장, `PUT /gpu-guest/deploy/:agent` 응답에 `droppedSecrets` 없음. 폐기 자체는 규약(v2.503 S-2)대로지만 v2.607 INT2607-01 '폐기 사실은 droppedSecrets 로 응답' 을 이 경로만 어김 — 로컬 PUT 은 보고한다(형제 비대칭) | `setAssignedGpuGuest(agent, partial, dropped=[])` 로 배열을 `mergeGpuGuestSettings` 에 넘기고 deploy PUT 이 `droppedSecrets` 를 싣는다(웹 `droppedSecretNote` 가 이미 그린다) |
| RECENT2612-06 | SPLIT | low | v2.611 WEB2611-07·08 | 코드 읽기로 동작은 확정: `GuestOsTools.jsx RealOs` 와 `VmProvision.jsx SavedJobs` 는 scope/mm/vc 를 바꾼 뒤 조회가 실패하면 이전 필터의 행을 그대로 두고, 배너는 '직전에 받은 결과' 라고만 말한다(다른 범위·필터라는 말은 없음). 다만 배너가 '직전' 임을 밝히고, 권한 누출은 아니다(이미 받은 데이터). 날카로운 부분은 RealOs 의 CSV 내보내기가 새 선택 아래에서 옛 행을 내보낼 수 있다는 점. 브라우저로는 확인하지 않음 | 조회 키(scope·mm·vc)를 행과 함께 저장하고 키가 바뀐 채 실패하면 행을 비우거나 배너에 '이전 선택(…)의 결과' 를 명시, 그 동안 CSV 버튼 비활성 |

## authz-left 검증 원문

| ID | verdict | severity after | one-line evidence | recommended fix |
|---|---|---|---|---|
| AUTHZ2612-01 | CONFIRMED | HIGH | Re-run: sadm GET /tools/storage 403 but export.csv/.txt 200 (host 10.88.0.7), DELETE storage/pdu/SAN device of other corp 200 → gone for full admin; SAN export 403 in same file (sibling miss) | **403 (fullScopeOnly)** on every storage/PDU/SAN device export/import/write/test/collect route. No UI break: the screens' main GETs are already 403 for scoped, so these buttons are unreachable for scoped admins anyway. Add sweep test "adminOnly ⇒ fullScopeOnly" in the 3 files. |
| AUTHZ2612-02 | CONFIRMED (known leftover) | MEDIUM | Re-run Y1–Y8 all 200; sanswitch perf retentionDays 7 persisted for full admin (lowering prunes every corp's history). Same class as v2.607 AUTHZ2607-05; CLAUDE.md v2.605 lists it as "남긴 것" | These are fleet scalars with no per-corp axis → **403 (fullScopeOnly)** is simplest; keepScopedFields(...,[])+ignoredGlobal only if a scoped admin screen needs to read them (none do — storage/PDU/SAN/perf screens are already 403 for scoped). Overlaps -05 (perf/settings) and -06 (bm-storage/settings). |
| AUTHZ2612-03 | CONFIRMED (SPLIT on GET intent) | HIGH (write/apply) · MEDIUM (read) | Re-run repro2: sadm GET relaytopo/export/render unmasked IPs+SSH user 200; PUT {sites:[]} 200 wipes fleet topology. GET exclusion `!admin &&` was deliberate in v2.600 ("admin 은 설정 소유라 제외") but predates v2.607/2.611 scoped-admin rule | Change gate to `scopedVcenterIds(...)` for any role (403) and fullScopeOnly on PUT/import/apply/fetch/test-ssh + relaycheck settings/run. Web RelayTopoTool falls to AccessDenied via ErrorBox — acceptable (site topology has no vCenter axis). |
| AUTHZ2612-04 | CONFIRMED | HIGH | Re-run: sadm PUT rma agents/edge-eu/access allowedIps 0.0.0.0/0 → 200; POST rma/run {cmd:'uptime'} → 200 reqId queued (repro1's D4 400 was only an arg mistake; repro2 fixed it) | fullScopeOnlyWith(...) on all rma routes (403). Scoped admins lose the RMA screen — consistent with edge-log/link-check 403 rule; RMA is edge-unit remote command execution. |
| AUTHZ2612-05 | CONFIRMED | MEDIUM | Re-run A1–A4: sadm reads other admin's IP/path in hang log, clears it; file header itself says "범위 제한 계정에 주지 않는다"; sibling /admin/audit 403 | 403 on /admin/perf* (hang rows carry other users' identities; filtering would be complex). |
| AUTHZ2612-06 | SPLIT (bm-storage CONFIRMED · credentials PLAUSIBLE) | MEDIUM | bm-storage: B1–B4 re-run 200 (list/export/delete other corp server). credentials: GET 200 empty store only; listCredentials→publicView returns name/username/hosts/agents (no secrets), writes require reauth — not seeded | fullScopeOnly (403) on bmstor.js and credentials.js. Vault is fleet-wide/agent-bound; UI loss for scoped admin acceptable. |
| AUTHZ2612-07 | CONFIRMED (status) / PLAUSIBLE (edge stores — empty in harness) | LOW | login-fails/status 200 vs sibling /login-fails 403; lastSummary = counts only (security/loginFails.js:74-77); ingest-stats/agent-tokens/net-monitors returned empty | fleetOnly (403) on ingest-stats, agent-tokens, net/agents, net/monitors*, net/capture; status: 403 or strip lastSummary. LoginFails.jsx already 403s on the main call, so no screen breaks. |
| AUTHZ2612-08 | PLAUSIBLE | LOW | statusTools.js:47 emergency-stop has adminOnly only while same file uses fleetOnly on codex-check/portal-db; needs 2 OTP admins — not run | Require approvers with scopedVcenterIds==null (or fleetOnly on POST). |
| LEFT2612-01 | CONFIRMED (documented leftover EDGE2605-05) | MEDIUM | storage/registry.js:37-45 catch→preserveCorrupt→{devices:[]}; storage-config returns ok:true devices:[]; storageConfigPull applies (sig changes) → applyPulledDevices replaces own list + dropSnapshot(removed). Re-run shows corrupt renamed, [] returned. Same shape SAN/PDU/CVP/agentUsers | Record _loadError on corrupt load; config routes return 503 registryUnreadable; edge pull already skips non-2xx (throws on !res.ok) so 503 keeps previous list. |
| LEFT2612-02 | CONFIRMED (message) — drop itself intended | LOW | ilo2.mjs: same ranges → no drop; range change → drops ['password','iloPassword'] (v2.606 LEFT2606 rule, intended). Route idracScan.js:382 replaces accurate r.skipped reasons with fixed text ("스캔 보류"), CSV has no iLO columns so iLO can't be re-entered via CSV | Put r.skipped reasons into passwordDropped; note "iLO 는 화면에서 입력"; optionally ilo_username column. |
| LEFT2612-03 | REFUTED (as honesty bug) | INFO | addressHidden note (addressHiddenText.js:17) explicitly scopes itself to "관리 IP·접속 계정"; BGP peers are not claimed hidden. Already documented as policy leftover v2.611 AUTHZ2611-06 | Policy decision only; if desired mask peers via maskedIdToken. |
| LEFT2612-04 | CONFIRMED (documented in v2.611) | LOW | deviceKeyOf prefers registry serviceTag (partfault/extract/idrac.js:214); correctHpeServiceTag changes it (poller.js:212) → new key, old key missing-hold. CLAUDE.md v2.611 already states "열린 장애가 한 번 새로 열릴 수 있다 — 남은 일" | One-time part_state key migration or close old rows with reason key-migrated + badge. |
| LEFT2612-05 | CONFIRMED (documented v2.605 leftover) | LOW | Re-run: 3-aggregate 40.7ms vs MIN/MAX alone 0.00ms on 525k rows; ping/service.js:113 returns meta, web PingMonitor.jsx never reads it (grep 0); called on selection only | Drop meta from response or split into MIN/MAX single queries, remove COUNT. |
| LEFT2612-06 | REFUTED | INFO | Only hinted entries get stored unverified (non-hinted rejected); central.js:1028 still counts an entry as known if `st[c.id]?.ok === true` (successful pull), so a transient verify failure on a working edge does not remove it from known set; self-heals next cycle | None required (optionally don't downgrade when URL unchanged). |
| LEFT2612-07 | PLAUSIBLE (documented v2.607 leftover) | LOW | proxy/sshExec.js sftpReadFile reads whole file; targets are admin-chosen deploy hosts; not reproduced | stat + size cap (4MB) before readFile. |

## ced 검증 원문

| ID | 판정 | 검증 후 심각도 | 근거(한 줄) | 권장 최소 수정 |
|---|---|---|---|---|
| CEN2612-01 | SPLIT | 낮음 | 재실행: 유령 8개 200 → 위임 엣지 429 재현. 다만 전제가 공유 `CENTRAL_TOKEN` 보유자다. 이는 CLAUDE.md v2.601·v2.607 이 '공유 토큰 사칭 한계' 로 남긴 계열이다. 형제 storage/SAN/PDU 도 공유 토큰이면 같은 `admitAgent` 로 막힌다(storage 는 장비 push 가 소유 검사조차 없다). 참고로 스크립트의 storage 200 은 status-only 레코드가 `at:0` 이라 밀려난 결과이고, 장비 push 로 채우면 막힌다. cvp 에만 해당하는 점은 '위임 CVP 0개여도 상태 슬롯을 점유한다' 는 것 하나다. | `/cvp-data` 에서 `ownedServers.length === 0` 이면 `saveEdgeCvpStatus` 를 부르지 않고 `{ok:true,noDelegation:true}` 로 답한다. 기존 키가 있으면 삭제한다. 이것만으로 소유 없는 이름의 점유가 사라진다. `admitAgent` 의 verified 우선은 형제 전체의 별건이다. |
| CEN2612-01(관련 `adoptAgentVariants` `_adopted` 무한) | PLAUSIBLE | 정보 | `_adopted` Set 에 상한이 없고 새 이름마다 작은 latest 표를 DISTINCT 스캔한다(코드 확인). 공유 토큰 보유자만 가능하고, 표도 작다. | 위와 같은 가드 안(`ownedServers.length>0`)으로 호출을 옮긴다. |
| EDGE2612-01 | CONFIRMED | 중간(bmstor) / 낮음~중간(idrac-scan) | 재실행: 타임아웃 뒤 같은 POST 가 다시 도착했다(arrivals 2 · 동시 2). 504 도 재전송한다. `resilientFetchInner` 는 메서드를 보지 않는다. 엣지 라우트 `/idrac-scan`·`/bmstor-collect` 는 **동기**다: `await runLocalIdracScan`/`collectMany` 후 응답하고 잠금·req close 확인이 없다. 그래서 타임아웃 재시도가 실제로 동시 이중 실행이 된다. 현실 도달 경로는 셋이다. ① bmstor: 동시 4 · 서버 200 · 불통 서버마다 SSH readyTimeout 15초이고, 불통 약 50대면 180초 기본 시한을 넘긴다. 이 경우 SSH 세션·로그인 시도가 두 배가 된다(가장 현실적). ② idrac-scan: 2048 IP·동시 32·호스트 3초에 등록까지 하면 10분대이고, 저장소가 생성하는 HAProxy 중계 `timeout server 10m` 이 15분 클라이언트 시한보다 먼저 504 를 낸다. ③ 스캔이 던지면 500 이 되고 **순차** 재스캔이 한 번 더 돈다. 다른 POST 도 점검했다. retries>0 인 호출은 대부분 엣지→중앙 데이터 upsert(멱등)다. 중앙→엣지 동작 POST 는 이 둘과 `set-password`(retries 1, 같은 값이라 멱등)뿐이다. 업그레이드 push 는 전역 fetch 다. | ① `idracScanPush.js:107`·`bmstor/poller.js:123` 를 `retries: 0` 으로 바꾼다(비멱등 — bulkRun '자동 재시도 금지'). ② 엣지 `/idrac-scan` 과 `idracScanWorker`, `/bmstor-collect` 에 single-flight 를 두고 진행 중이면 409 `busy` 로 답한다. |
| EDGE2612-02 | CONFIRMED | 낮음 | `pushPduNow`→`pushPduOnce` 는 오류를 `{ok:false}` 로 **반환**만 한다(pdu/push.js:119 catch). 그래서 `pduConfigPull.js:79` 의 catch 는 도달 불가이고 `pushError` 는 늘 비어 있다. v2.602 EDGE2602-05 가 의도한 표시가 죽어 있다. 실패는 pdu-push 상태·콘솔에 남으므로 무음은 아니다. storage 도 반환값을 무시한다. | `const pr = await pushPduNow(); if (pr && !pr.ok && !pr.withhold) pushError = pr.reason \|\| 'push 실패';` storage 도 같은 방식으로 `_last.pushError` 에 싣는다. |
| EDGE2612-03 | PLAUSIBLE | 낮음(설정 의존) | 코드 확인: 413 이면서 `chunkRows>CHUNK_MIN` 일 때 errors 없이 break 한다. 8회째에도 shrink 면 루프가 빠져나가 `ok:true` 가 된다. 기본 2000 은 4회째에 CHUNK_MIN(250)에 닿아 errors 에 들어가므로 해당 없다. `SVCMON_PUSH_CHUNK` 가 약 32,000 을 넘을 때만 성립한다(envNum 에 상한 없음). | 루프가 `shrink` 상태로 끝나면 `errors.push('413 축소 상한')` 을 넣는다. 또는 `CHUNK_START` 를 `CHUNK_MIN×2^7` 이하로 클램프한다. |
| EDGE2612-04 | REFUTED(결함 아님 — 설계) | 정보 | 실패 주기에는 행이 없다('표본 없음'). 값을 지어내는 것이 아니다. 기준선을 되돌리면 두 창이 수신 시각 한 행으로 합쳐지고, 그쪽이 오히려 추이를 왜곡한다. CLAUDE.md 의 'null 은 null' 규약과도 맞는다. | 수정하지 않는다(필요하면 주석에 '실패 구간은 표본 없음' 만 적는다). |
| EDGE 정보(위임 등록 잡 iLO 미탑재) | REFUTED(현재 도달 불가 · 문서화된 공백) | 정보 | v2.610 기록 "수동 임시 스캔 창은 iLO 를 아직 다루지 않는다" 와 일치한다. 임시 스캔 found 에 HPE 가 없으므로 경로가 없다. | 임시 스캔에 iLO 를 붙이는 릴리스에서 `ilo`·`skippedNoCreds` 회신을 함께 넣는다. |
| DB2612-01 | PLAUSIBLE | 낮음 | 재실행: 옛 ts 레코드를 넣으면 `Gone99` 가 생겨 포트 49개가 된다. 더 새 레코드가 오면 1개로 복구된다. INSERT 분기에 ts 가드가 없는 메커니즘은 확정이다. 그러나 정상 흐름에서 옛 ts 레코드가 뒤에 올 경로를 찾지 못했다. 엣지 push 는 await 직렬이다. 재시도는 같은 ts 를 보낸다. 엣지는 자기 DB 의 최신 레코드만 보낸다. | saveDevices 포트 루프 전에 기존 `device_latest.ts` 를 조회하고, 레코드 ts 가 그 값 이하이면 upPort·delPortsOld 를 건너뛴다. |
| DB2612-02 | PLAUSIBLE | 정보 | rowid 재사용 메커니즘은 맞다. `samples` 에 AUTOINCREMENT 가 없고, dropTarget 이 최상위 rowid 를 지우면 새 행이 rowid ≤ upto 를 받는다. 하지만 구버전 DB 첫 open 뒤 시드가 도는 짧은 창(50k행 청크) 안에 '가장 최근 표본을 가진 대상의 삭제' 와 새 적재가 겹쳐야 한다. 시계열 prune 은 오래된 행만 지우므로 해당하지 않는다. | 시드 중(rollupReady=false) dropTarget 이 `rollup_v1_upto` 를 `min(upto, MAX(rowid))` 로 줄인다(1줄). |
| DB2612-03 | PLAUSIBLE | 정보 | `evictIfNeeded` 는 usedAt 만 보고 prune 중인 핸들도 닫을 수 있다(코드 확인). 상한 48에 현장은 28~33 vCenter 라 현재 도달하지 않는다. 실패도 warn 으로 정리 중단뿐이다. | 청크마다 `usedAt` 을 갱신하거나 `inUse` 카운터로 evict 에서 제외한다. |
| DB2612-04 | PLAUSIBLE(기록) | 정보 | power_hourly 가 비고 원시가 있으면 동기 GROUP BY 백필이 한 번 돈다(idrac/db.js:56-66). 롤업 도입 이전 버전에서 올라올 때 1회만 도는 경로라 현재 도달성이 낮다. 코드 주석이 이미 '기동 시 1회' 로 밝히고 있다. | 수정하지 않는다(롤업이 비는 새 경로가 생기면 ping 처럼 rowid 청크 + 양보로 바꾼다). |

## cs 검증 원문

| ID | verdict | severity after | one-line evidence | recommended minimal fix |
|---|---|---|---|---|
| COL2612-01 | CONFIRMED | medium | 재실행: `parseWinPerf('NIC=eth2\|10\|1e9\|150e6\|90e6').nics[0].pct === 100`, `linkPct(200e6,1e9) === 100`. 문서화된 의도적 클램프 없음 — 같은 파일 :84-89 가 v2.605 COL2605-02 로 '100 클램프 = 조용한 보정(v2.578 D3)' 이라 적고 디스크 busy 는 null 처리(형제 비대칭). `net_pct`·`hba_pct` 가 `alertRules.ALERT_METRICS` 에 있어 거짓 초과 알림 가능(알림은 기본 꺼짐). 기존 테스트는 ≤100 값만 고정(bmUsage2550:50) → 수정해도 깨지지 않음 | `winPerf.js:117`·`rates.js linkPct`: `p > 100 ? null : p` + `pctOutOfRange` 개수(선택: 지터 여유 100~105 만 허용). `cpuPctFromJiffies`·`busyPct` 클램프는 별건(지터 흡수, 판단 필요) |
| COL2612-02 | CONFIRMED(코드) / 실영향 PLAUSIBLE | low | `linkWord('disconnected') === 'up'`(down 정규식에 disconnect 없음, up 정규식 `connected` 부분일치). EOS 가 실제로 'disconnected' 를 내는지 미확인(v2.608 경로·필드 전부 추정) — 방향은 장애 은닉. v2.586 healthWord 경계 규칙과 같은 계열 | down 목록에 `disconnect` 추가(부정 먼저), up 은 `\bconnected\b` |
| COL2612-03 | CONFIRMED(코드) / 실영향 PLAUSIBLE | low | `partState({hwStatus:'hwStatusOk'}) === 'fault'`, `{status:'fanOk'}` 는 ok — 접두 제거 목록에 hwStatus 없음. healthWord 는 '모르면 bad'(스토리지 노드 규칙)라 추정 필드의 모르는 enum 이 fault 가 됨. 값 형태 미확인 | 접두 제거에 `hwstatus` 추가 + CVP 에서는 healthWord 가 모르는 단어면 `unknown`(fault 아님) |
| COL2612-04 | CONFIRMED | low | `bgpSummary([{Established,10},{Established,null}]).prefixes === 10`, 부분 표식 없음 — '부분은 하한으로 말한다'(v2.606) 위반 | `prefixesUnknown: peers.length - pKnown` 추가, 화면 '최소' 표기 |
| COL2612-05 | CONFIRMED(코드) / 실영향 PLAUSIBLE | low | `speedBps('100Gbps'\|'1000Mbps'\|'10 Gbps') === null`; `speed:'speedUnknown', bandwidth:1e10` → speedBps null(pick 이 첫 비어있지 않은 값만). 거짓 값 아님(사용률이 빌 뿐) | 단위 뒤 `(?:bps\|b\/s)?\b` 허용, speed 후보마다 speedBps 적용해 첫 non-null |
| COL2612-06 | CONFIRMED(코드) / 도달성 낮음 | low (info) | 재실행: `toBytes('3 tiers')`=3.3e12, `'7 entries'`=8.07e18, `'1.2e+12'`=1.38e18. 호출부는 xtremio/powerstore 용량 셀(`Physical-Space` 등)뿐이라 이런 문자열이 올 경로는 확인 못함 — 방어성 | 단위 뒤 `(?![A-Za-z])`, 지수 표기는 `Number()` 선처리 |
| COL2612-07 | CONFIRMED(코드) / 도달성 PLAUSIBLE | low | `toWatts(1980,'VA') === 1980000`(kVA 는 우연히 정상). APC `devReading appower` 는 kVA 로 알려져 현재 도달 가능성 낮음 | `/^kva?$\|^kw/`→×1000, `/^va?$\|^w/`→×1, 모르는 단위는 null |
| COL2612-08 | CONFIRMED(코드) / 실영향 PLAUSIBLE | low | `redfish.js:1029-1055` 팬 루프는 `Status.State` 를 보지 않음(온도는 :1023 에서 Absent 건너뜀), `partfault/extract/idrac.js:161` 은 health 만 → 빈 슬롯이 unknown(absent 아님), HPE 는 `pct:0`. unknown 은 열지도 닫지도 않아 거짓 장애는 아님 — 칸 오분류·0% 표시. HPE Absent 팬 표기는 미확인 | 팬도 `State==='Absent'` 면 `state:'absent'` 를 실어 extract 가 absent 로, 센서 시계열에서는 건너뜀 |
| SEC2612-01 | CONFIRMED | medium | 작은 입력 재측정(vcs/cvpmerge_small.mjs): 2k=567ms · 4k=2,573ms · 8k=11,668ms(초선형, 2배당 ~4.5배). 8MB 상한이면 ~18만 항목 → 외삽 수천~수만 초. 파싱은 메인 스레드 동기라 `withDeadline` 도 못 끊음. 전제: 등록 CVP 침해 또는 MITM(verifyTls 기본 false — registry.js:144) | `put` 을 제자리 병합(`Object.assign(prev, fields)`) + 개체당 필드 상한(예 400, 초과 개수 기록). 회귀는 상대 비율 |
| SEC2612-02 | CONFIRMED | medium | 재실행: 192.0.2.2 의 307 → 127.0.0.1 로 `{"userId":"svc","password":"S3cret!"}` POST 재전송 확인. undici 기본 follow + IP 리터럴은 `withSsrfLookup` 을 타지 않음(ssrfLookup.js '한계' 명시). v2.583 resilientFetch 규약(교차 출처 307 본문 거부·hop 마다 ssrfBlockReason) 밖. ⚠ resilientFetch 그대로 재사용은 부적합: `fetchFollowing` 이 호출자 `signal` 을 자체 `AbortSignal.timeout` 으로 덮어 v2.417 세션 절단이 깨짐 | `redirect:'manual'`(3xx = 실패, CVP API 는 리다이렉트 불필요) — 또는 resilientFetch 의 `fetchFollowing` 을 signal 을 받는 형태로 export 해 재사용 |
| SEC2612-03 | CONFIRMED (redfish 재현) · ome 는 동일 패턴(미재현) | low-medium | 신규 재현(vcs/rfredir.mjs): `fetchPower` 가 Basic 401 → 세션 POST 에서 307 → 127.0.0.1 로 `{"UserName":"root","Password":"S3cret!"}` 재전송. 스캔 경로(`probeIdrac`)는 대역 안 **미등록** 호스트도 대상이라 비신뢰 호스트가 루프백/내부로 블라인드 POST SSRF 가능(비밀번호는 그 호스트에 이미 감). 커스텀 `X-Auth-Token` 은 undici 가 교차 출처에서 떼지 않음(GET 경로) | `redirect:'manual'` 이 가장 작지만 iLO/iDRAC 의 같은 출처 트레일링 슬래시 301/308 을 깨뜨릴 수 있음 → 같은 출처만 따르는 수동 루프(`trustedRedirect`+`ssrfBlockReason`, 교차 출처 307/308 본문 거부) 권장. ome.js login/#req 도 같이 |
| SEC2612-04 | REFUTED as defect (정보·설계 한계) | info | v2.610 CLAUDE.md 가 '서비스 루트는 무인증이라 로그인 전에 벤더를 안다' 를 설계로 명시, v2.611 이 unknown→Dell 만으로 좁힘. 스푸핑 가능성은 스캔 TLS 무검증과 같은 기존 한계 — 신규 결함 아님 | 문서화만(선택) |

## web 검증 원문

| ID | verdict | severity after | one-line evidence | recommended minimal fix |
|---|---|---|---|---|
| WEB2612-01 | CONFIRMED | low | IpamCore.jsx:96 `.catch(() => ({subnets:[]}))` and :118 `.catch(() => setSubnets([]))` → KPI `서브넷(/24) 대역 0`(:203) and `서브넷 0`(:202) mean "none" when it is "could not read"; :84 `.catch(() => null)` blanks sheet silently. Violates "don't swallow errors into none / missing ≠ 0". | `subnetsErr` state; on failure keep previous list, KPI `—` + banner "서브넷 목록을 읽지 못했습니다 — 0개라는 뜻이 아닙니다"; `sheetErr` shown in sheet area. |
| WEB2612-02 | CONFIRMED | low | StorageGrowthTool.jsx:67-73 `load(p)` no generation/active guard; preset buttons :167 not disabled while busy; `useEffect([periods])` has no cleanup → late older response `setD` overrides, and earlier `finally` clears `busy`. Violates v2.596 stale-response rule. | `const gen=useRef(0)`; `const g=++gen.current` and check `g===gen.current` in then/catch/finally (or `active` flag via effect cleanup). |
| WEB2612-03 | CONFIRMED | medium | BmUsage.jsx:166-181 `loadDetail` sets `setDetail(await fetch…)` unconditionally; panel renders `detail` under `selRow`(B) with no key check → late A history drawn as B (wrong server, no error). On failure `detail` stays null → :695 "추이를 불러오는 중…" forever (error only in top msg). | `detailGen` ref incremented per call; apply setDetail/setDetailLoading only when current; set `detailErr` and render it in the panel instead of the loading line. |
| WEB2612-04 | CONFIRMED | low | LicenseTools.jsx:140-142 non-403 failure of `/admin/horizon` ignored → `hz` null → :248 `(0대 등록)` + `<details open>`; same text flashes during initial load. | `hzErr` state: failure → "등록 목록을 읽지 못했습니다: 사유", `hz==null` → "(불러오는 중)"; open only when `hz` is a real empty array. |
| WEB2612-05 | CONFIRMED | low | Server `idrac/scanRanges.js:258` pushes `'iloPassword'` into `droppedSecrets`; web `droppedSecretText.js:15-18` KEY_LABEL lacks it → note shows raw `iloPassword`. Sentence hardcodes "접속처(주소·포트·계정)가 바뀌어" while server reason (`:265-266`) is "스캔 대역·수행 엣지·(iLO) 계정이 바뀌어"; header comment "수집 엣지(agent)만 바꾸면 승계된다" is false for scan ranges (agent is in `accessMoved` fields :249,:255). Server already sends exact per-field reasons in `out.skipped[].reason`. | Add `iloPassword: 'iLO 비밀번호'`; in `droppedSecretNote` prefer `r.skipped[].reason` when present (else generic "접속처(주소·계정·대역·수행 엣지 등)"); fix header comment; add test case. |
| WEB2612-06 | CONFIRMED (feature gap) | low | Server `central/idracScanJobs.js` stores `hpeCountsOf(data)`, `noCreds`, `unsupportedCount`, `dellFound` in `j.result`; IdracScanJobs.jsx:126 renders only `발견·등록·스캔`, ScanJobLogModal only error/authFailed → v2.610 "HPE N대" not shown for delegated/manual job rows. | Reuse `describeScanRun`/hpe info helper in job result cell and log modal (`{...j.result, found: j.result.foundCount}`). |
| WEB2612-07 | CONFIRMED | low | CvpTool.jsx:189 `장비 {countText(shown.length)}대` rendered when `devices==null` (loading) or `devErr && !devices` (:199 ErrorBox) → "장비 0대" contradicts Loading/ErrorBox. | `devices ? \`장비 ${…}대…\` : '장비 —'`. |
| WEB2612-08 | CONFIRMED | low | ToolsReports.jsx:53 `/admin/report/daily` and :361 `/admin/alerts` `.catch(() => {})`; sections gated by `isAdmin && sched`(:79) / `isAdmin && cfg`(:387) vanish silently (incl. 403/timeout). | `schedErr`/`cfgErr` state → one line "일일 보고 설정을 읽지 못했습니다: 사유" (403 → AccessDenied text via ErrorBox). |
| WEB2612-09 | PARTIALLY CONFIRMED (needs browser) | low | Global `tbody td{white-space:nowrap}` (styles.css:290) + `th` nowrap → all-nowrap tables cannot squash; they overflow and the existing `overflowX:auto` wrapper scrolls (fine). Squash risk only where cells set `whiteSpace:'normal'`: **LinkCheck.jsx:296 (11 col, wrap ✓)**, **:361 (8 col, ✓)**, **PortalCheck.jsx:200 (10 col, ✓)**, **SanHealthCheck.jsx:135 (7 col, ✓)**, **:448 (6 col, ✓)**, **PartFaults.jsx:231 (7 col, ✓)**, **:290 (6 col, card overflowX ✓)** — likely squash at 400px. Low risk (≤5 col or nowrap): BmUsage 308(5)/380(3)/408(6 nowrap)/462(6 nowrap)/711(9 nowrap, only :687 normal outside table?), PartFaults 185(7 nowrap), EdgeLog 182(6 nowrap)/270(2), LinkCheck 280(4), PortalCheck 176(4)/275(3), SanHealth 60(5)/262(5)/288(8 nowrap), StorageGrowth 235(7 mapped, nowrap by design, wrapper :229), IdracScanJobs 112(7 nowrap, .table-wrap), ScanJobLogModal 85(2). Separate issue: **IdracDetailModal.jsx:315 (PSU 5 col) / :347 (3) / :457 (GPU 5)** have NO scroll wrapper and Modal body is `overflowX:'hidden'` (Modal.jsx:28) → at narrow widths right columns are clipped (not squashed); :331/:418 sit in `overflow:auto` boxes. | `<STable minWidth={N}>` (wrap={false} where already wrapped) for the 7 whiteSpace-normal tables; for IdracDetailModal 315/347/457 add `minWidth` (creates wrapper) so the modal body can scroll horizontally. Confirm with 400px screenshots. |

## PERF 축(발견 보고 — 수정 그룹 B 가 재현·변이 검증)

### PERF2612-01 — CVP `adoptAgentVariants` 가 원시 표본 전체를 한 문장으로 옮겨 이벤트 루프를 수십 초~분 단위로 막는다 (high, CONFIRMED — 측정)
- 위치: `server/src/cvp/db.js:318-327` (v2.611 CEN2611-02 에서 추가). 호출: `routes/central.js:1721` — `/api/central/cvp-data` **요청 처리 중**.
- 동작: 대소문자만 다른 agent 이름 변형이 `device_latest`/`port_latest` 에 있으면, 테이블마다
  `UPDATE OR IGNORE port_sample SET agent=? WHERE agent=?` + `DELETE … WHERE agent=?` 를 **한 번에** 실행한다.
  `agent` 가 UNIQUE 인덱스 `ux_ps_key` 의 선행열이라 행마다 인덱스 재배치가 일어난다. `setImmediate` 양보는 **테이블 사이에만** 있다.
- 측정(`scratchpad/audit2612/perf/adopt.mjs`, 실제 스키마·WAL): `port_sample` **100만 행 이동 = 12,975ms**(단일 동기 구간).
  docs/CVP.md 의 규모 계산으로 200대 스위치는 원시 **221만 행/일**, 7일 1,548만 행 → 선형 외삽 시 **수 분** 동안 포탈 전체 무응답
  (CPU 경합을 절반으로 봐도 1분 이상). 그 동안 모든 API·폴러가 멈춘다.
- 발생 조건: 중앙이 CVP 표본을 저장해 둔 agent 표기와 `canonicalAgent()` 가 고르는 표기(등록부 `srv.agent`)가 대소문자만 다를 때 —
  v2.611 이전에는 저장 키가 인증 이름 그대로였으므로 **2.608~2.610 을 쓰던 현장이 2.611 로 올린 뒤 첫 push** 가 전형적인 트리거다
  (프로세스당 키마다 1회 — `_adopted`). 엣지 이름 대소문자 변경 시에도 같다. 실제 현장에 변형이 있는지는 확인하지 못했다(조건 자체는 코드로 확인).
- 재현: 위 스크립트(`node adopt.mjs 1000000`), 또는 cvp.db 에 `agent='Edge-A'` 표본 수백만 행 + 등록부 `agent:'edge-a'` 로 cvp-data push.
- 최소 수정: 행 청크로 옮긴다 — `UPDATE OR IGNORE port_sample SET agent=? WHERE rowid IN (SELECT rowid FROM port_sample WHERE agent=? LIMIT 5000)`
  반복(청크마다 BEGIN/COMMIT, **COMMIT 뒤** `setImmediate` — v2.611 DB2611-04 규칙), 그 뒤 남은 충돌 행 DELETE 도 같은 방식.
  `port_daily` 도 같은 루프. 대안: 요청 경로에서 떼어 백그라운드로 돌리고 끝날 때까지 그 agent 조회에 두 이름을 합쳐 읽는다.

### PERF2612-02 — CVP `saveDevices` 가 CVP 한 대분(장비 전량 × 포트)을 단일 동기 트랜잭션으로 쓴다 (medium, CONFIRMED — 측정)
- 위치: `server/src/cvp/db.js:166-203`. 호출: 폴러 `cvp/poller.js:148`(매 주기, CVP 마다) · 중앙 수신 `routes/central.js:1733`.
- v2.611 DB2611-04 가 `importSamples` 는 "7,600행 170ms 실측" 을 근거로 2,000행 청크 + COMMIT 뒤 양보로 고쳤는데,
  **같은 표에 같은 양을 쓰는 `saveDevices` 는 그대로**다(형제 누락). 한 트랜잭션에 `upPort` 12,800 + `insSample` 7,800 + `upDaily` 7,800 + 장비별 DELETE.
- 측정(`scratchpad/audit2612/perf/cvpsave.mjs`, 실제 모듈 `cvp/db.js`, 64포트·60% up):
  - 200대: 1주기 **121ms → 10주기 뒤 165ms**(표가 자라며 증가)
  - 1,000대(docs/CVP.md 의 두 번째 규모): **628~640ms** 동기 정지 / 주기(5분)
- 재현: `node cvpsave.mjs 200 10`, `node cvpsave.mjs 1000 3`.
- 최소 수정: 장비 N대(예: 30대 ≈ 2,000 포트) 단위로 BEGIN/COMMIT 하고 COMMIT 뒤 `setImmediate` 양보(`importSamples` 와 같은 모양).
  장비 단위로 끊으면 한 장비의 포트·표본·`delPortsOld` 는 같은 트랜잭션에 남으므로 재전송 이중 계수 방지 성질은 유지된다.

### PERF2612-03 — 백업 생성이 설정 디렉터리 전체를 동기 읽기 + `JSON.stringify` + `gzipSync` 한다 (low, PLAUSIBLE)
- 위치: `server/src/backup/service.js:155-173` (`collectConfigDir` readFileSync 루프 → `zlib.gzipSync(Buffer.from(JSON.stringify(archive)))`).
- 측정: 14.3MB JSON 텍스트 기준 stringify 71ms + **gzipSync 203ms**(경합 환경). 파일당 상한 8MB 라 설정 파일이 여럿 크면(v2.590 에 svcmon.json 16.4MB 실측 기록)
  아카이브 원문 수십 MB → 수백 ms~1초 정지. 빈도는 정기·변경·기동 백업이라 낮다. 실제 현장 설정 디렉터리 크기는 확인하지 못했다.
- 최소 수정: `zlib.gzip`(비동기, promisify) + 파일 읽기 `fs.promises`. 반환 모양이 동기라 호출부(`await` 여부) 확인 필요.

