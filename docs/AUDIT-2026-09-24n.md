# 17차 점검 — 10축 병렬 감사 + 발견별 2인 반증 (v2.606, 2026-09-24)

요청: "자세하게 두번 더"(1회차). 방식은 v2.605(`AUDIT-2026-09-24m.md`)와 같고 첫 축은 **v2.605 회귀**(`git diff 40f4508..f11fa8d`)다.
감사 기준 커밋 `f11fa8d`(v2.605.0 소스). 수정은 파일이 겹치지 않는 **7그룹** 병렬(보안 축이 늦게 끝나 g 를 따로 두었다),
변이 검증은 기준 커밋 판본으로 되돌려 확인했다. 그룹 보고서는 scratchpad `fix2606_{a..g}.md`.

## 결과 요약

| 축 | 발견 | 확정 | SPLIT | 가능성 | 반증 | 고침 |
|---|---:|---:|---:|---:|---:|---:|
| v2.605 회귀 | 6 | 6 | 0 | 0 | 0 | 6 |
| 남은 후보·형제 | 5 | 4 | 0 | 1 | 0 | 5 |
| 인가·범위 | 7 | 7 | 0 | 0 | 0 | 7 |
| 중앙 수신 계약 | 4 | 4 | 0 | 0 | 0 | 4 |
| 수집기 정확성 | 7 | 6 | 1 | 0 | 0 | 6 |
| 웹 화면 | 10 | 10 | 0 | 0 | 0 | 10 |
| 타이머·자원 | 5 | 5 | 0 | 0 | 0 | 5 |
| SQLite | 6 | 6 | 0 | 0 | 0 | 6 |
| 보안 | 8 | 6 | 0 | 1 | 1 | 6 |
| 엣지 계약 | 5 | 4 | 1 | 0 | 0 | 5 |
| **합계** | **63** | **58** | **2** | **2** | **1** | **60** |

- 반증: SEC2606-08(`svcDiag.parseTitledBlocks` O(n³) — 호출부 0, v2.542 가 의도적으로 뗐다. 다시 연결할 때 볼 메모로 남긴다).
- SPLIT: COL2606-07(quser 세션 ID 3자리 파싱 — 세션명·ID 가 화면·DB 로 나가지 않아 **고치지 않음**) ·
  EDGE2606-05(fleet 부분 전송의 귀속 없는 항목 제외 — 문서화된 트레이드오프라 엣지 로직은 그대로, **중앙이 partial 을 저장·표시**하는 부분만 고침).
- 가능성: LEFT2606-03(= AUTHZ2606-07 과 같은 대상, 함께 고침) · SEC2606-07(로그 분석 카탈로그 정규식 — 줄 2,000자 상한과 25ms 양보가 방어선, 기록만).
- ⚠ **보안 축 재현 검증자는 결과를 쓰지 못하고 끝났다**(응답이 안전 분류기에 막혔다고 보고). 보안 6건은 **의도 검증자의 판정**과 감사자의 재현 근거로 확정했고,
  수정 그룹 g 가 각 건을 기준 커밋 판본에서 다시 재현한 뒤 고쳤다(변이 검증 8/8).

회귀 테스트: `server/test/audit2606{a..g}.test.js` **70건**(a 12 · b 9 · c 11 · d 11 · e 5 · f 9 · g 13) + 웹 vitest(`audit2606e`·`collect2606c`·`latestGate` 등).
변이 검증: a 19 · b 10 · c 12(+웹 3) · d 11 · e 19 · f 14 · g 8 파일.

## v2.605 가 만든 결함(회귀)

| id | 심각도 | 재현 → 수정 |
|---|---|---|
| RECENT2606-01 | medium | Isilon 'mixed' 표지가 한 번 붙으면 재시작 전까지 남아 **양끝이 정확 값인 증가량까지** '±해상도 미만' 으로 가렸다 → mixed 는 칸을 가리지 않고 표지만(각주·툴팁), 표지 시한(마지막 반올림 + 366일) |
| RECENT2606-02 | medium | v2.605 원격 전력 충돌 판정이 신선도·활성 여부를 보지 않아 죽은 엣지 잔재 때문에 새 엣지 키가 `rmt:<cid>:<host>` 로 바뀌고, 재시작 직후에는 pull 순서에 따라 옛 계열에 두 서버 값이 섞였다 → 신선한(`POWER_CURRENT_STALE_MS`) 활성 수집기끼리만 충돌, 결정은 DB 계열 존재로 영속 |
| RECENT2606-03 | low | fleet 보류 시한을 첫 보류 판정부터 재 push 주기 10분이면 중앙 TTL(30분) 뒤에 도착, 중앙은 partial 정보를 버렸다 → 시한은 마지막 온전한 push 기준 min(20분, TTL − 2×주기) · 중앙이 partial·unreadVcenters·withheldItems 저장 |
| RECENT2606-04 | low | 범위 계정 저장이 '전체 대상([])' 을 고정 목록으로 바꿔 이후 추가 vCenter 가 추적에서 빠졌다 → 전체 모드는 범위 계정이 바꾸지 못함 + `unapplied`·사유를 응답에 |
| RECENT2606-05 | low | SAN 조닝을 못 읽은 장비에서도 41번째 이후 포트에 '조회 상한 때문' 이라 말함 → 조닝 미수집 사유 먼저 |
| RECENT2606-06 | low | RMA 인스턴스 상한 거절을 라우트가 버려 거절된 인스턴스가 작업만 받음 → 403 + refused·사유, 목록에 거절 수 |

## 주요 확정 결함

- **AUTHZ2606-01·02 (범위 admin 이 범위 밖을 바꾼다)** 통합 서버 인벤토리 귀속(`/insights/fleet/*` 4라우트)을 범위 admin 이 바꿔 **읽기 범위를 넓힐 수** 있었고,
  Ping 대상은 PUT 으로 `vcenterId` 를 바꿔 범위 밖 대상을 끌어온 뒤 주소·이력을 읽고 삭제할 수 있었다 → fleet 변경은 `fleetFullScopeOnly`(403),
  Ping 쓰기는 기존·새 값 모두 범위 판정(밖이면 404), 전 vCenter 를 건드리는 seed·vcport·poll-now 는 범위 계정 403.
- **AUTHZ2606-03** 원격 매핑: quick-connect 는 범위 밖 403 인데 `POST /mappings` 는 200 이었다 → **범위 제한 admin 에만** 대상 범위를 건다(전체 범위 admin 면제는 문서화된 설계라 유지).
- **AUTHZ2606-04·05·06·07** vmseries 데이터 삭제 범위 가드 · vmseries/run·curuser/collect `denyScopedRun` · os-scan PUT 응답을 GET 과 같은 함수로 · anomaly perVcenter GET 필터 + PUT `mergeScopedMap`.
- **CEN2606-01 (high)** 통신 점검 엣지 보고가 `link.id` 를 대조하지 않아 **엣지 하나가 다른 엣지의 push 링크를 '정상' 으로 덮어** 장애를 숨길 수 있었고 임의 id 가 `link_latest` 에 영구히 쌓였다 → `linkIdOf(kind, from, to)` 재계산 대조(대소문자 무시) · 길이 상한 · `link_latest` 오래된 행 청크 정리.
- **CEN2606-02·03·04** PDU `units`·SAN `ports.list`·스토리지 `appliances` 원소를 좁히지 않아 문자열 전력이 `'01200800'` 으로 이어붙고 null 원소 하나가 **PDU 목록·SAN 전체 점검을 500** 으로 만들었다 → 객체 배열 + `numOrNull`, 전체 점검은 장비별 try/catch(`failed[]`).
- **TIM2606-02** `.slice(0,N)` 절단이 원문(SlicedString)을 붙잡아 v2.583·v2.591 의 메모리 상한이 실제로는 무효였다(5MB agent 30개 → 143MB) → `util/capStr.js`(자르고 평탄화) 를 수신 계측 전부에.
- **SEC2606-01~06** iDRAC 스캔 대상 응답(서비스 루트·401 본문) 무상한 gzip 해제(프로브당 RSS 640~730MB) → 64KB/16KB 상한 · Digest 챌린지 파서 O(n²)(15KB 헤더 858ms → 0.3ms) · racadm 퍼센트 정규식(v2.605 SEC2605-01 의 형제) ·
  GPU 실패 본문 태그 제거 O(n²) · 백업 없음 리포트 패턴 개수 무상한(5,300개 4.4초 → 43.8ms) · 감사 로그 target 대형(`logAudit` 가 1,000자로 자름).
- **LEFT2606-01·02 (저장 비밀 승계)** GPU 게스트 고정 IP 와 iDRAC 스캔 대역을 바꾸면 **저장 비밀번호가 새 주소로 전송**됐다(v2.503 S-2 규약 누락) → 고정 IP 는 VM 의 알려진 IP 만(폴러·테스트·저장이 같은 헬퍼), 스캔 대역·agent·username 이 바뀌고 새 비밀번호가 없으면 폐기.
- **COL2606-01 (high)** 현재 사용자 발행기가 원문을 7,200자에서 자르면(`omitted`) 포탈이 **부분 인원을 전체로** 세고 잘린 줄을 가짜 세션으로 셌다 → 잘린 줄 버림 + `truncated`·'최소 N명'(엣지 위임 경로 포함).
- **COL2606-02·03·04·05·06** NSX 그룹 멤버 cursor 무시(1,000 절단)·한쪽 실패 0 → 전 페이지 + null · Capacity 처리량 bond·VLAN 중복 합산(3배)·리셋 0 → 물리 NIC 만·null · 디스크 df 정의 ·
  Horizon 페이지 절단·상태 미확인을 확정값으로 추이 적재 → 하한 표지·부분 주기 NULL · vCenter REST 폴백 지어낸 필드 → null + `collectSource:'rest'`.
- **EDGE2606-01~04** GPU 게스트 push 가 **인벤토리 미수집 상태의 빈 폴**로 v2.605 보류를 통과해 중앙 사용률을 지웠다 → 못 읽은 vCenter 를 `unreadVcenters` 로, 전부 읽은 폴만 push ·
  svcmon 재시작 직후 빈 완결 두 번이 중앙 행·메타를 GC → 예열(`warmingUp`) 중 GC 보류 · push 5종이 중앙 거절 요약을 버림 → `agent/centralReply.js` · SAN 청크 실패 시 1회 전체 재전송 + '중앙 목록 부분 상태'.
- **WEB2606-01~10** 메일 진단 빈 칸이 포트 1·타임아웃 1초로 저장(화면은 20000) · 끊긴 호스트 0% 가 호스트 목록·N+1(80% '여유' vs 실제 160% '위험')·용량·비교 매트릭스에 남음 ·
  이상동작·프록시·중계 토폴로지(빈 포트 행 조용히 삭제)·통신 점검 빈 칸 · PDU 부분 합·vCenter 전력 결측 0 W · 늦은 응답 2곳 · iDRAC 부분 전력 문구 · Horizon 상태 미확인.
- **TIM2606-01** LLM 타임아웃이 문자열로 저장돼 **AI 검색·ChatOps·연결 테스트가 전부 실패**('delay must be number') → `normTimeoutMs` + `resilientFetch` 2차 관문.
- **TIM2606-03·04·05 · LEFT2606-04** SSH exec 시한 단일 관문(`execTimeoutMs` 1초~30분) · SAN/스토리지 CLI·HTTP 시한 env · 웹 SSH 게이트웨이 readyTimeout · `DIRUSAGE_TICK_MS`·`GUESTDISK_TIMEOUT_MS` · 중복 제거 맵 정리.
- **DB2606-01~06** vCenter 로그 상태가 30초 폴링마다 events 전체를 두 번 훑음(캐시 TTL = 폴링 간격) → MIN/MAX 단독 + 건수 캐시 5분 · 스토리지 이력 prune 청크 ·
  RMA·SAN 점검 이력 DB 첫 open 잠금 래치 → `util/sqliteOpen.js` · 스파이크 순위 t0 하한 · RMA NOCASE 식 인덱스 · 통신 점검 이벤트 limit 정수화.

## 통합 단계에서 고친 것

- 그룹 사이에 걸친 한 줄 3건: `toolsCapacity` PUT 응답에 `unapplied`(b 요청) · `sanitizeCurUserRecords` 가 `truncated` 를 싣기(c 요청) · GPU push 보류 문구가 못 읽은 vCenter 를 말하기(c 요청).
- `scripts/api-doc.mjs` GUARD_NOTE 에 새 게이트 `fleetFullScopeOnly` 설명(apiDoc2563 '뜻 모르는 게이트 0' 스윕이 잡았다) · API·ENV·CONFIG 문서 재생성.

## 검증

- 서버 전량 **3,732건**(통합 전 1건 실패 → 수정 후 해당 파일 통과) · 웹 vitest 1,811건 · eslint 0 · vite build · 문서 생성기 4종.
- Chromium 목 스택 16화면 × 1440/400: 가로 넘침 0 · 콘솔 오류 0 · 본문 `[object Object]`·`undefined`·`NaN` 0.

## 정직 기록 — 확인하지 못한 것

- 실장비 출력(quser 3자리 ID 정렬, NSX `result_count`, iDRAC Sensors·Digest 헤더, Horizon 상태 필드 일부 결측, vCenter REST Summary 필드, 끊긴 호스트의 hardware 요약)은 보지 못했다 — 합성 재현이다.
- 새 응답 필드 중 화면이 아직 그리지 않는 것: SAN 전체 점검 `failed`, RMA 거절 필드, fleet partial(중앙은 저장·응답에 싣는다), `hostsUsageExcluded`(InsightsThreats·비교 매트릭스·CapacityTools), 보고서 `patternsOmitted`, 스캔 대역 `droppedSecrets`, REST 폴백 표지.
- 늦은 응답 경합은 브라우저로 재현하지 않았다(헬퍼·소스 검사로 고정).
- 남긴 것: curuser '청크 0 교체' 부분 목록(EDGE2606-04 와 같은 유형) · `sshExec withDeadline`·`execCapture` 시한 상한 · `linkcheck/poller.js` 가 `currentIds` 를 넘기지 않아 link_latest 는 시간 기준 정리만 · SEC2606-07 · SEC2606-08(재연결 시) · COL2606-07.
