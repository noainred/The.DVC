# 13차 점검 — 10축 병렬 감사 + 발견별 2인 반증 (v2.602, 2026-09-24)

요청: "세번더"(3회차, 마지막) — 이어서 "자세하게 3번 더"(v2.603~v2.605)가 예정돼 있다. 방식은 v2.601(`AUDIT-2026-09-24i.md`)과 같고,
첫 축은 **v2.601 회귀**(`git diff 79bde27..0a2434e`, 117파일)다. 수정은 파일이 겹치지 않는 6그룹 병렬, 그룹마다 회귀 테스트 +
변이 검증(감사 기준 커밋 `9c1b4c1` 판본으로 되돌리면 실패).

## 결과 요약

| 축 | 발견 | 확정 | 가능성 | 반증 | 고침 |
|---|---:|---:|---:|---:|---:|
| v2.601 회귀 | 3 | 2 | 0 | 1 | 2 |
| 남은 후보·형제 | 6 | 5 | 0 | 1 | 5 |
| 인가·범위 | 3 | 1 | 0 | 2 | 1 |
| 중앙 수신 계약 | 6 | 6 | 0 | 0 | 6 |
| 수집기 정확성 | 6 | 3 | 1 | 2 | 4 |
| 웹 화면 | 4 | 3 | 0 | 1 | 3 |
| 타이머·자원 | 5 | 4 | 1 | 0 | 5 |
| SQLite | 3 | 3 | 0 | 0 | 3 |
| 보안 | 4 | 4 | 0 | 0 | 4 |
| 엣지 계약 | 7 | 4 | 1 | 2 | 4(1 반증) |
| **합계** | **47** | **35** | **3** | **9** | **37** |

감사 단계 반증이 이번 회차에 가장 많았다(9건, 19%) — 검증자가 '설계대로' 또는 '도달 불가' 로 판정한 것이다
(redact 불리언 가림은 실제 로그에 없음 · ping 수동 대상 가시성은 형제 기준과 일치 · ui-settings 는 전역 표시 설정 · PowerStore
어플라이언스 공간 · Horizon 페이징 · 템플릿 '꺼짐' 집계 · IPAM topTab 이동 · vmseries 상태 · `/result` BIG_JSON).
수정 단계 반증 1건: **EDGE2602-04**(현재 사용자 0건 주기 — 대상 VM 마다 레코드가 생겨 성공한 0건 주기는 생기지 않는다).
후속 **10건**도 같은 릴리스에 고쳤다.

회귀 테스트: `server/test/audit2602{a..f}.test.js` 51건 + 웹 vitest(`downloadFailText`·`sanPerfDbText`·`storageUnits alertTotals`·`nsxLimitText`·`invCheckText`).
변이 검증: a 8/8 · b 6/6 · c 7/7 · d 10/10 + 웹 · e 11/11 · f 5/5 + 웹 8/8.

## v2.601 이 만든 결함(회귀)

| id | 심각도 | 재현 → 수정 |
|---|---|---|
| RECENT2602-01 | medium | v2.601 의 비-admin 주소 가림이 **행마다** 호스트 변형·Set·정렬을 다시 만들었다 — 등록 호스트 1,200개에서 파트 장애 2,000행 **5.6초**, 2만 행 55초, bm-usage 작업 로그 500건 0.8초 동안 이벤트 루프 정지(실측). → `makeScrubber`·`scrubberFor`(주소 목록당 1회 · 글자 트라이) · `extendMatcher`. 2,000행 × 1,200 호스트 **55~80ms**. 수정 전 구현을 테스트에 옮겨 **결과 동일성**을 대조한다 |
| RECENT2602-02 | low | PowerMax 경보 개수를 못 찾으면 남긴 '미수집' 사유가 `'오류'` 만 옮기는 필터에 버려져 섹션 'skip' · 미해결 0 → `raw.alertsSection` 으로 전달, 미해결 null(조회 오류 경로도) |

**교훈**: 가림·정제처럼 **응답마다 도는 코드**를 새로 만들면 '행 수 × 대상 수' 를 운영 규모로 먼저 계산할 것(이 현장 등록 서버 1,135대).

## 주요 확정 결함

- **CEN2602-01 (high)** 엣지 export(pull) 원소를 정제 없이 보관 — `serviceTag` 가 `{toString:1}` 인 원소 하나로 **함대 전체 물리 서버 집계·3단 지도·전산실 온도가 TypeError**, 수집 서버 화면 React #31 → `remoteInventory` 정제(서버 원소 2만 상한, 사유별 버린 개수 `serversDropped`, 온도는 숫자만, version·agent·hostname 64자). 가짜 엣지 HTTP 로 실제 `pullNow` 확인.
- **AUTHZ-2602-01** 범위 제한 operator 가 **전역 svcmon 트리·엣지 배정·템플릿·정렬**을 바꿀 수 있었다 → 변경 라우트 32개에 `fullScopeOnly`(조회 GET·저장하지 않는 변환은 그대로 — server/CLAUDE.md N-2).
- **CEN2602-02** 엣지 로그 보관분 크기 무제한 + 공유 토큰이 본문 agent 로 남의 보관분을 덮음 → 줄 2,000자·항목 200·합 512KB, 모르는 이름 403, 요청하지 않은 회신은 저장하지 않음.
- **CEN2602-03·04** RMA 명령 결과 원문 보관(객체 reason → React #31, 객체 exitCode → 영속 이력 적재가 조용히 0행) · 점검 결과 name 객체 → **재시작 전까지 500** → `sanitizeRmaResult`, 영속 실패 경고.
- **CEN2602-05·DB2602-01·02** SAN 사용량 메타 무검증·`port_meta` 무정리 · prune 단일 DELETE(276만 행 5.4초) · `perfDbStats` 가 **20초 폴링 경로**에서 풀스캔 → 포트 0~4095·128자 · 청크 prune(100만 행 최대 정지 83ms) · MIN/MAX 단독 + COUNT 60초 캐시(`countsAt`, 화면 표시).
  ⚠ **정정**: CLAUDE.md v2.550.3 이 `perfDbStats` 를 '폴링하지 않는 진단 경로' 로 적은 것은 **틀렸다** — 설정 화면이 20초마다 부른다.
- **SEC2602-01·04 + normPath** `/\/+$/` O(n²): register-collector `urlHint`(엣지 토큰 하나로 수십 초) · toolAccess `stripPath`(인증 요청당 0.19초) · central `normPath` → 길이 상한 + 선형 루프. **SEC2602-02·03** PDU `parseCode`(빈 줄 8만 5.8초) · bmstor `sanitizeMounts`(길이 검사 전 정규식).
- **COL-2602-01** NSX cluster/status 실패가 'connected' + 노드 1 → 'unknown' + null(회색 '상태 확인 불가').
- **COL-2602-02** SAN 포트 처리량 분모가 스냅샷 조립 시각 → 카운터를 받은 시각(REST·SSH 둘 다).
- **COL-2602-04** XtremIO — 32개 초과 클러스터와, **수정 중 발견한** 9번째 이후 미조회 클러스터가 합계에서 조용히 빠짐 → `clustersNotQueried`·`poolsUnreadable`.
- **LEFT2602-04** iDRAC 섀시 하나의 Power 실패를 삼켜 부분 합을 전체 전력으로 → `partial`·`failedChassis`, 폴러는 부분 합을 **적재하지 않는다**(`powerNotStored`).
- **LEFT2602-01·02·03·05 / TIM2602-01·02·04** 숫자 설정: 배포 대상 상태 쓰기마다 change 백업 · 통신 점검·전원 꺼짐 빈 칸이 하한·기본값 · `VMPERF_RETENTION_DAYS=0` 이 90일 · 보존일 음수가 0(무제한)·상한 없음 · 로그인 모니터 주기 로드 경로 1ms 루프. 수정 중 `VMSERIES_RETENTION_DAYS=''` 무제한도 발견.
- **TIM2602-03·05** SSH 유휴 시한 env 1ms · dirusage DB 첫 open 잠금 영구 래치(`sqliteOpen` 규약).
- **DB2602-03** vmtrack changes prune 이 VM 의 유일한 전원 전이 행을 지워 '꺼진 지 N일' 과대 → 키별 마지막 행 보존.
- **EDGE2602-01** 설정 push `fs.watch` 가 런타임 상태 파일 쓰기마다 반응해 **30분 주기 push 가 약 20초마다** → 백업과 같은 `isRuntimeStateFile` 기준 + 설정 지문 무변경 생략.
- **EDGE2602-02** 엣지 워커 4곳이 404 'central disabled' 를 구버전 중앙으로 읽어 상태에 직전 성공 유지 → `classifyCentral404Body`.
- **EDGE2602-03** 상태 전용 storage push 가 v2.580 이하 중앙에서 엣지 목록을 비움 → health-probe 로 중앙 버전 확인 뒤 2.581.0 이상에만.
- **EDGE2602-05** PDU '지금 수집' 실패도 즉시 push.
- **WEB2602-01** 다운로드 8곳 + 공용 헬퍼가 `res.ok` 를 보지 않아 **409 export_busy·403 오류 JSON 을 .xlsx/.csv 로 저장** → `downloadFile` + 사유 표시(Chromium: 실패 시 download 이벤트 0회).
- **WEB2602-02** 인벤토리 점검이 '중앙 수신 꺼짐'·'없는 경로' 거부를 토큰 인증 실패로 보고 → `unauthRejectsByKind`.

## 후속(같은 릴리스)

iDRAC 폴러 부분 합 미적재 · 스토리지 경보 합계 `alertTotals`(미확인 N대 제외) · PowerMax 경보 오류 경로 null · central `normPath` ·
`/sanswitch-perf` `metaRejected`·엣지 기록 · SSH 처리량 분모 · 베어메탈 DB 청크 prune · SAN 사용량 화면 `countsAt` · 공용 `saveResponseAsFile`·GPU 내보내기 ·
기존 테스트 3건 정정(`audit2599d` 목 중앙 health-probe, `vmperfSettings` 음수 단언, `reauditFixes20260809` 고정 길이 창).

## 검증

- 서버 전량 3,476건(통합 중 실패 1건 — 소스 검사의 고정 2,400자 창이 길어진 핸들러를 못 담음 → 다음 라우트까지로) · 웹 vitest 120파일 1,731건 · eslint 0(통합 중 1건 — 테스트의 `__dirname` → `import.meta.url`) · vite build.
- 문서 생성기 4종 최신.
- Chromium 목 스택 25화면 × 1440/400(IPAM·RMA·SAN 사용량 설정·수집 서버·svcmon + 이전 목록): 가로 넘침 0 · 콘솔 오류 0 · `undefined`/`NaN`/`[object Object]` 0.

## 정직 기록 — 확인하지 못한 것

- 스토리지 '경보 미확인 N대'·NSX '상태 확인 불가'·인벤토리 점검 거부 종류 문구는 목 데이터로 해당 상태를 만들 수 없어 **브라우저로 보지 못했다**.
- 디렉터 portperfshow slot 접두(COL-2602-03)는 실장비 확인 없이 방어만 했다(형식을 모르는 블록은 저장하지 않고 센다).
- 두 개 이상 섀시가 PowerControl 을 내는 iDRAC 구성, 엣지 storage push 의 실제 구버전 중앙 상대 동작은 목으로만 확인했다.
- 가림 치환 결과 안에서 새로 생기는 주소는 치환하지 않는다(실제 표기로는 생기지 않음).
- svcmon 에서 '등록부에 없는 agent 이름 거부' 는 공유 토큰 미리 배정 흐름을 막을 수 있어 하지 않았다(판단 필요).
- 지표 보존일 상한을 3650일로 잡았다(감사 제안은 1830일) · SSH 유휴 시한 하한 1분 신설.
