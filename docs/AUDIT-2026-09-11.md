# 전체 소스 감사 — 2026-09-11 (v2.477.0 기준 → v2.478.0 조치)

대상: `server/src`(73,813줄·464파일) · `web/src`(45,211줄·218파일) · `pyportal`(9,156줄·38파일).
방법: **직전 감사(`AUDIT-2026-09-09.md`, v2.446)의 미적용 37건을 파일:라인 근거로 재판정** + 의존성·CI·문서 생성기는
실측(`npm audit --omit=dev`, `scripts/env-doc.mjs --check`, node:test 1609 · vitest 295 · 웹 빌드). 병렬 분석 에이전트
6개(보안·서버코어·서버도메인·웹·pyportal/CI·문서)는 10시간 동안 완료되지 않아 **결과 없음** — 이 문서의 판정은 전부 직접 확인한
것이며, 에이전트가 맡았던 "v2.446 이후 신규 코드의 신규 취약점 탐색"은 **이번에 수행되지 않았다**(다음 감사 후보).

## 1. 직전 감사 미적용 항목 재판정 (버그 11 · 보안 11)

| # | 항목 | v2.477 상태 | 근거 / v2.478 조치 |
|---|---|---|---|
| B2 | osScanner 수동 스캔 재진입 | **열림 → 수정** | 틱만 `running` 가드, `POST /os-scan/run` 은 무가드였음. `runOsScanNow` 안으로 가드 이동(`inventory/osScanner.js`) |
| B5 | 설정 4종 비원자 쓰기 | **열림 → 수정** | `metrics/settings.js`·`metrics/vmperfSettings.js`·`idrac/scanPoller.js`·`ui-settings.js` → `atomicWriteFileSync` + 로드 `preserveCorrupt` |
| B7 | net/monitor `useSudo` 토글 복귀 | **열림 → 수정** | `saveMonitor` 가 미전송 시 기존 값 유지, `redact` 가 `useSudo` 노출 |
| B8 | toolsCapacity `catch { points = [] }` | **열림 → 수정(부분)** | 4곳에 `console.warn` 추가(응답 형태는 유지 — 프론트 호환) |
| B9 | storage 단건 수집 가드 | 수정됨 | `storage/poller.js:39-46 _inFlight` set/finally |
| B10 | 삭제 수집기 유령 | 수정됨 | `collector/puller.js:34 skipped:'removed-or-disabled'` + `clearStaleRemote` |
| B11 | ping 대상 id 소문자 | **열림(보류)** | `ping/store.js:16 idOf`. id 체계 변경은 저장된 대상·시계열 키를 바꿔 업그레이드 시 중복 생성 위험 — 마이그레이션 설계 후 별도 릴리스 |
| B12 | bmstor 정리 기준 | **열림 → 수정** | 정리 기준을 '등록 전체'로(`bmstor/poller.js`) — 비활성 서버의 마지막 결과 유지 |
| B13 | `hostMhz` 키 이름만 | **열림 → 수정** | `${vcenterId}|${name}` 키(`routes/api/toolsCapacity.js`) |
| B14 | snapCache undefined | 수정됨 | `util/snapCache.js:52 has 플래그`(v2.447) |
| B15 | 폴링 뷰 ErrorBox 교체 | **열림 → 수정** | `IpamNet.jsx` 2곳 `error && !data` + 배너. `HardwareTools` 4곳은 `useTool`(상태 교체형)이라 해당 없음 |
| S5 | `/tools/*` 조회 requirePerm 부재 | **열림 → 수정** | `toolsCapacity`(12)·`checksLogs`(9)·`hardwareGpu`(9)·`toolsGuestDisk`(4) 라우트에 `requirePerm('tools')` |
| S6 | federate reqId IDOR | **열림 → 수정** | `logQueries.js` 가 큐잉 사용자를 기록, GET 은 본인/admin + vCenter scope 검사(위반 404) |
| S7 | 소유자 없는 매핑 노출 | **열림 → 수정** | `listMappingsForUser`: 비-admin 은 자기 소유만. `GET /remote/mappings` 에 `requirePerm('remote.access')` |
| S8 | `GET /remote/rdp/:id` 무검사 | **열림 → 수정** | `requirePerm` + 소유자/scope 검사(위반 404) |
| S9 | relaycheck 엣지 IP 노출 | **열림 → 수정** | 비-admin 응답에서 `host`/`port` 마스킹 |
| S11 | relayProbe DNS 리바인딩 | **열림(보류)** | `vcenter/relayProbe.js:17,32,48` 호스트명으로 접속. adminOnly 라 영향 제한 — uagmon 핀 패턴 이식은 TLS servername 분리 설계 필요 |
| S12 | auth/ad.js 비원자 쓰기 | **열림 → 수정** | `atomicWriteFileSync` + `preserveCorrupt` |
| S13 | ad.js NUL 바이트 | ~~수정됨~~ **오판 → v2.479 에서 수정** | `grep -cP` 는 0 을 돌려줬지만 `tr -cd '\000' \| wc -c` 로 1바이트 잔존 확인(`file` 이 'data' 로 분류한 이유). v2.479 에서 이스케이프로 치환 |
| S14 | 의존성 취약점 + CI audit | **부분 → 수정** | ci.yml 에 단계는 있었으나 `\|\| true` 비차단 → **critical 차단**. 아래 §2 |
| S15 | `/api` 감사로그 부재 | **열림 → 수정(표적)** | 알람 음소거 POST/DELETE·`/vms/upgrade-tools` 에 `auditMiddleware` + `audit.js describe` 라벨(전 `/api` 적용은 읽기성 POST 다수라 소음 — 표적 적용) |
| S16 | installer 경로 오라클 | 수정됨 | `agent/deploy.js resolveInstaller` 가 규격 파일명 + 허용 디렉터리 realpath 검사(v2.447 S1 조치에 포함) |
| S17 | `/dl/versions.json` sha256 부재 | **열림 → 수정** | 번들별 sha256(스트리밍 해시, size+mtime 캐시, 동시 요청 1회) — 엣지 fail-closed 가 통과되어 `UPGRADE_ALLOW_UNVERIFIED` 우회 유인 제거 |

§3 개선 12·§4 튜닝 17 은 이번에 재판정하지 않았다(다음 후보).

## 2. 의존성 실측 (npm audit --omit=dev)

| | v2.477 | v2.478 | 비고 |
|---|---|---|---|
| 웹 | critical 1 · high 1 · moderate 1 | **critical 0 · high 1 · moderate 0** | `jspdf` 2.5.2→4.2.1(사용 API 는 text/line/rect/addFont/splitTextToSize 등 기본뿐 — 한글 폰트 임베드·줄바꿈 헤드리스 재검증). 잔여 `d3-color` ReDoS: 3d-force-graph·react-simple-maps 경유, 색 문자열이 상수라 외부 입력 도달 불가 → 수용 |
| 서버 | high 1 · moderate 6 | **high 0 · moderate 2** | brace-expansion 1.1.18·undici 6.28.1·body-parser 1.20.8·`qs` 6.16.0(overrides — express 4.22.2 가 ~6.15.1 핀). 잔여 `exceljs→uuid`: exceljs 는 `uuid.v4` 만 호출(취약 경로 v3/v5/v6 buf 아님), 수정판이 exceljs 3.4 다운그레이드뿐 → 수용 |

## 3. 문서

- `docs/ENV.md` 가 코드와 불일치(`env-doc.mjs --check` 실패) → 재생성(329키, guestdisk push env 3개 반영). `CONFIG-FILES.md` 는 최신(130개).
- `docs/INDEX.md` 수치 갱신(ENV 284→329, CONFIG 118→130).
- `README.md`: `/api/tools/*` 루트 14개·central 라우트 16개·collector 1개 누락 보강, `/tools/*` 조회 권한 규칙(v2.478) 반영, 최근 추가 도구(게스트 디스크 회수·자원 축소 근거·벡터 PDF·접속확인) 절 추가.

## 4. 다음 감사 후보 (이번에 못 한 것 — 정직 기록)

1. v2.446 이후 신규 코드(guestdisk·rightsize·toolcats·relaytopo·credentialStore·sanswitch·pdu·dirusage·pyportal 변경분)의 **신규 취약점 탐색** — 에이전트 실패로 미수행.
2. B11(ping id 대소문자) 마이그레이션 설계, S11(relayProbe IP 핀).
3. `AUDIT-2026-09-09.md` §3 개선 12·§4 튜닝 17 재판정.
4. 웹 전수 규칙 점검(훅 위치·STable·`.input` min-width vs 인라인 width 패턴 — v2.477 ToolCategories 와 같은 잠재 겹침).
