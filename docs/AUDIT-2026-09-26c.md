# 감사 2026-09-26(c) — v2.621 버그·코드 개선(2회차)

사용자 요청: "한번 더"(= v2.620 "버그 및 코드 개선" 과 같은 방식) · 선택: 병렬 감사 + 반증 · 전체 검증.

6개 축(직전 릴리스 회귀 · 데이터 정확성 · 엣지↔중앙 · 보안 · 수명주기 · 웹)을 병렬로 감사하고, 발견마다 재현(코드 실행) 또는
코드 확인으로 판정했다. **발견 36건 = 확정 32 · 가능성 3 · 반증 1.** 확정분은 파일이 겹치지 않는 6개 그룹(A~F)과 리드 통합분으로
나눠 고쳤고, 각 그룹이 **변이 검증**(고친 파일을 감사 기준 커밋 `a0c2a5b` 판본으로 되돌리면 새 테스트가 실패하는지)을 했다 —
A 7/7 · B 11/11(idracScanJobs 는 export 심으로 행위 검증) · C 6/6 · D 4/4 · E·F 전부 · 리드 3/3.

회귀 테스트: `server/test/audit2621{a,b,c,d,f,lead}.test.js` · 웹 `web/src/views/audit2621e.test.js` ·
`web/src/views/tools/audit2621f.test.js` · `web/src/views/tools/serverTemp/board.test.js`(절단 안내).

## 직전 릴리스 회귀(v2.620)

| ID | 심각도 | 내용 | 조치 |
|---|---|---|---|
| RECENT-01 | 중 | v2.620 WEB2620-09 가 ErrorBox 에 Error 객체를 넘겨 4xx 하나로 트래픽 분석 탭 전체가 ErrorBoundary 로 떨어졌다(React #31). | WEB-01 과 같은 수정(ErrorBox 입구 정규화). |
| RECENT-02 | 중 | v2.620 SRV2620-02 의 step 채움으로 빈 버킷도 점이 되어, 점 상한 5000 이 '1주·분' 조회를 **3.47일**로 조용히 잘랐다(재현). | 1시간 미만 버킷은 기간을 덮는 점 수로 상한을 올리고(상한 1주×분) 그래도 넘치면 `truncated`·`coveredSince`·`limit` 을 응답에 싣는다 · 온도 추이 모달이 그 사실을 말한다(`board.js histCutNote`). |
| RECENT-03 | 중 | v2.620 SRV2620-03 의 낡은 vCenter 제외가 점검중(maintenance) vCenter 의 얼린 캐시를 빼지 않았다. | `unreadVcenterReasons`(maintenance·unreachable·stale) · `lastRun.staleSkipped.byReason`. 첫 수집 중(pending)은 일부러 제외(코드 주석에 사유). |
| RECENT-04 | 저 | v2.620 이 바꾼 pull 실패 문구 '(2 · 재시도 없이 1회)' 를 로그 분석 규칙이 못 읽어 2회차부터 '미분류'. | 정규식이 괄호 안 부가 문구를 받는다(선형) · 테스트가 `puller.js` 의 실제 템플릿으로 문장을 만든다. |
| RECENT-05 | 저 | `POST /tools/part-faults/scan` 이 비-admin 에게 `notify.results[].partKey` 를 원문으로 줬다(SEC2620-02 형제). | `lastView(r, isAdmin)`. |
| RECENT-06 | 저 | 큰 본문 세션 풀 판정이 svcmon 경로 전부를 범위 계정에 막아 hostmap/parse 가 1MB 파서로 떨어졌다. | 그 경로의 실제 게이트(역할·svcmon 권한)만 본다. |
| RECENT-07 | 저 | CVP 상태의 `missing`·`authStopped.reason` 에 리다이렉트 출처 주소가 남았다. | `maskStatusText`(목록·장비 상세 둘 다). |
| RECENT-08 | 저 | scan-jobs 가 fleetOnly 가 된 뒤 화면이 403 을 삼켜 범위 관리자에게 '진행 중인 스캔이 없습니다' 라고 말했다. | `scanJobsView` — 권한 없음 / 조회 실패 / 로딩 / 정상을 나눈다 · 403 이면 폴링 중단. |
| RECENT-09 | 저 | (= EDGE-02) 설정 push 413 재전송이 뺀 파일 표시 없이 부분 목록을 보내 중앙 사본의 큰 파일이 사라졌다. | 엣지가 `partial`·`retainFiles` 를 싣고 중앙이 그 이름의 직전 사본을 유지(`retained`). 구버전 중앙은 모르므로 엣지가 `centralRetainUnsupported` 로 밝힌다. |
| RECENT-10 | 저 | 직전 실패 엣지 재시도 0 이 '두 주기 실패 = 끊김' 가정을 약하게 한다. | **보류(가능성)** — 의도한 절충이다. |

## 데이터 정확성

| ID | 심각도 | 내용 | 조치 |
|---|---|---|---|
| DATA-01 | 고 | 게스트 로그인 실패 조사가 journalctl 의 `+0900` 을 `++09:00` 으로 만들어 시각 파싱이 항상 실패 → 전부 '지금' 으로 적재됐다. 저널과 secure 를 둘 다 읽어 같은 사건이 두 번 들어갔다. | 오프셋 변환을 `loganalysis/parse.js` 와 같게 · 첫 줄에 게스트 오프셋 · 저널이 있으면 저널만. 두 번 조사해도 새 행 0(테스트). |
| DATA-02 | 중 | 일일 헬스체크가 경보 미조회·첫 수집 중·낡은 위임·점검중 vCenter·사용량 미상 DS 를 '정상(✅)' 으로 보고했다. | 섹션 상태 `unknown`('확인 불가: …') · 전체 순서 crit > warn > unknown > ok · 화면 KPI '❔ 확인 불가' + 섹션 설명. |
| DATA-03 | 중 | 스토리지(팹 A/B 스위치 합산) 사용량이 짧은 버킷에서 한 스위치만 담긴 부분 합이 되어 절반값·톱니로 그려졌다. | 포트별 직전 표본 이월(한계 = 주기×2) · 넘으면 그 버킷을 그리지 않고 `partial`·`partialBuckets` 로 밝힌다. |
| DATA-04 | 중(가능성) | HPE iLO 의 PSU 흡기 센서가 전산실 흡기로 분류될 수 있다. | 전원공급장치 센서를 흡기·배기 판정보다 먼저 '기타' 로. Dell 센서 분류 불변을 테스트로 고정. 실제 iLO 응답은 확인하지 못했다. |
| DATA-05 | 저(가능성) | FC HBA 사용률 분모가 명목 속도라 포화해도 약 80%. | **보류** — 인코딩 오버헤드 보정은 실장비 확인 뒤. |

## 엣지 ↔ 중앙

| ID | 심각도 | 내용 | 조치 |
|---|---|---|---|
| EDGE-01 | 중 | 위임 iDRAC/iLO 스캔 잡을 전량 인출했는데 엣지는 순차 처리 — 뒤 잡이 거짓 재대기되고 10분 뒤 gc 가 지워 결과가 200 과 함께 버려졌다. | 폴당 1건(`TAKE_MAX`) · gc 는 진행 보고·폴 시각을 활동으로 본다 · 모르는 reqId 는 410 `unknown-job`(엣지가 상태에 남긴다). |
| EDGE-02 | 중 | = RECENT-09. | 위 참조. |
| EDGE-03 | 중 | 스토리지·SAN·PDU 엣지 push 의 '등록부를 못 읽음' catch 가 죽은 코드라 손상 등록부가 '위임 0대' 로 중앙 목록을 비웠다. | `registryLoadError()` 선판정 → 상태 전용 보고(`registry-unreadable`). |
| EDGE-04 | 중 | part-faults 수신이 중앙 스토리지·SAN 등록부 손상을 보지 않아 그 엣지 보고를 전부 '미소유' 로 버리고 200. | 503 `registryUnreadable`(그 장비군이 있는 보고만 — iDRAC 전용 보고는 받는다). |
| EDGE-05 | — | 엣지 export 가 vendor 를 싣지 않는다. | **반증** — 같은 내용을 WEB-04 가 화면 축에서 확정해 고쳤다(중복). |
| EDGE-06 | 저 | 위임 '지금 수집' 큐의 seen 맵이 지워지지 않았다. | 상한 4096 + LRU(대기·진행 중 기준선은 보존) · `seenStats().evicted`. |
| EDGE-07 | 저 | iDRAC 스캔 결과 재수신이 로그·이벤트를 두 번 남겼다. | 정제본 서명으로 멱등(`applyIdracScanResult` → duplicate 면 ack 만). |

## 보안

| ID | 심각도 | 내용 | 조치 |
|---|---|---|---|
| SEC-01 | 중 | 범위 관리자가 범위 밖 중계 프록시에 health·test·배포를 실행했다(형제 라우트 누락). | `proxyHiddenFor` — 단건 404, 전체 배포는 숨긴 프록시를 건너뛰고 `omittedOutOfScope`. |
| SEC-02 | 중 | `POST /admin/gpu-guest/test-ssh` 가 범위·SSRF 검사 없이 본문 IP 로 SSH 접속했다. 수집기의 `usableIp` 는 `000.0.0.0` 이 통과해 포탈 자신의 루프백에 닿았다(재현). | 라우트: 전체 범위 전용 + 정규형 IPv4 + 차단 대역 · 수집기(리드): `strictIpv4Num` + `ipBlockReason` + 루프백 상시 제외. |
| SEC-03 | 저 | 폴더 사용량 조회 4종이 범위 관리자에게 RMA 엣지 IP·호스트명·fileRoots 를 줬다. | 조회도 전체 범위 전용(v2.611 '조회는 연다' 결정을 되돌림 — `audit2611b` 단언 갱신) · 화면은 403 이면 권한 안내 + 폴링 중단. |

## 수명주기

| ID | 심각도 | 내용 | 조치 |
|---|---|---|---|
| LIFE-01 | 중 | 엣지 레거시 할당 스캐너가 iDRAC 스캔 공용 잠금을 공유하지 않았다. | `tryAcquireScan('assign')` · 못 잡으면 `skippedBusy`. |
| LIFE-02 | 중 | Enterprise 대체 경로의 Redfish 센서 조회가 시한 뒤에도 계속 돌았다(재현: 반환 뒤 BMC 요청 2건). | 단계 시한 AbortController + 폴러 signal 을 모든 GET 에 · 공유 세션 생성에는 걸지 않는다(거짓 401 방지) · 섀시 목록 진척 캐시. |
| LIFE-03 | 저 | 작업 로그가 기록 1건마다 전량 직렬화 + 동기 fsync. | 같은 틱의 기록을 묶어 쓴다(N=60: 83ms → 4ms) · 조회·종료 시 대기분 flush. |

## 웹

| ID | 심각도 | 내용 | 조치 |
|---|---|---|---|
| WEB-01 | 고 | ErrorBox 에 Error 객체를 넘기는 12곳이 React #31 로 패널을 죽였다. | `accessDeniedText.errorBoxInput` — 문자열·Error·HttpError 를 한 곳에서 정규화(403 → 권한 안내, 5xx → 서비스 안내). |
| WEB-02 | 중 | IPMS 설정 › vCenter별 스캔 대역이 조회 실패를 빈 칸으로 보여 저장하면 기존 대역을 통째로 교체했다. | `vcRangesGate` — 미로드·실패면 저장·스캔 잠금 + 사유 + 다시 불러오기. |
| WEB-03 | 중 | 개요 스토리지 합계가 사용량 미상 DS 를 조용히 뺐다. | `storageUsageUnknownNote` — classic·V4·V5·관제 콘솔. |
| WEB-04 | 중 | HPE(iLO) 서버가 목록·CSV 에서 'iDRAC' 으로 보였고 위임 서버는 vendor 가 없었다. | 엣지 export·중앙 수신에 vendor(hpe·dell) · 유형 배지 'HPE iLO'/'iDRAC'/'벤더 미상' · CSV vendor 열 · 벤더 필터 · 상세 모달 제목(리드). |
| WEB-05 | 저 | 미지원 서버 '인증' 칸이 계정이 없어 시도하지 않은 장비를 초록 '통과' 로 보였다. | `noCreds` → 회색 '시도 안 함(계정 없음)' · 위임 결과 정제도 noCreds 를 보존(리드). |
| WEB-06 | 저 | 400px 넘침 2곳(법인별 온도 필터 행 36px · 패키지 저장소 URL 98px). | `flexWrap` · `overflowWrap:'anywhere'`. |
| WEB-07 | 저 | 패키지 설정 폼이 유효값으로 채워져 dir 만 바꿔 저장해도 URL 이 웹 override 로 고정됐다. | 지정값만 채우고 기본값은 placeholder · 빈 칸 = 지정 해제. |
| WEB-08 | 저 | V4 '물리 서버' KPI 가 iDRAC 0·미상이면 ESXi 호스트 수로 대체됐다. | `physicalServersKpi` — '—' + 'iDRAC 등록 없음'. |

## 리드 통합분(그룹 밖)

- 온도 추이 모달의 절단 안내(`histCutNote`) · 일일 헬스체크 화면의 '확인 불가' KPI·섹션 설명 · iDRAC 상세 모달 제목(`bmcLabel`).
- 403 폴링 중단 3화면(긴급중단 · 폴더 사용량 설정·리포트) — 403 은 다시 물어도 결과가 같다.
- 위임 iDRAC 스캔 결과 정제가 미지원 행의 `noCreds` 를 보존.
- **게스트 스크립트 출력 상한**(그룹 A 관찰 → 리드 확인): `runGuestScript` 가 stdout 을 앞 2000자로 잘라 로그인 실패 조사(tail 80줄)의
  **최신 실패**를 조용히 버렸다 → `outMax` 옵션(기본 2000 — 다른 호출부 불변) · 로그인 조사는 32,000자 · `stdoutTruncated`.
- 새 환경변수 `AGENT_CONFIG_PUSH_TARGET_BYTES` 의 기본값 표기(문서에 `15` 로 찍혔다 → 15728640).

## 남긴 것(다음 점검 후보)

- 보류: RECENT-10 · DATA-05 · 첫 수집 중(pending) vCenter 를 샘플러 제외에 넣을지(넣으면 push 하지 않는 site 가 전체 합계를 영원히 막는다) ·
  점검중 vCenter 를 일일 헬스체크에서 '확인 불가' 로 셀지 '정보' 로 둘지.
- 확인하지 못한 후보(그룹 D): `redfish.js rawGet` 세션 폴백에서 토큰 GET 이 자체 시한(15초)으로 실패하면 원래 Basic 401 이 돌아와
  **멀쩡한 서버의 주기 수집이 인증 정지**될 수 있다 · `fetchUsageSensors` 가 섀시 Sensors GET 의 일시 오류를 'absent' 로 6시간 캐시한다.
- `central/captureJobs.js` 는 모르는 reqId 결과에 여전히 200(데이터 손실은 없다).
- 새 상태 필드(`centralRetained`·`registryError`·`skippedBusy` 등)는 엣지 로그 화면이 객체로만 보여 준다(전용 문구 없음).
- `reports/vmStats.js updateVmStats` 는 낡은·점검중 vCenter VM 을 계속 누적한다(v2.620 부터 기록).
- 구버전 중앙(< 2.621)은 `retainFiles` 를 몰라 413 재전송이 여전히 사본을 교체한다(엣지가 밝힌다).

## 정직 기록

- 실장비(HPE iLO 응답·Redfish 세션 DELETE·게스트 journalctl 실제 출력·팹 스위치 실제 표본 간격)로는 확인하지 못했다 — 전부 가짜 서버·합성 입력 재현이다.
- Chromium 검증은 목 서버(인증 끔) + 응답 주입으로 했다 — 범위 관리자·operator 의 실제 로그인 화면은 보지 않았다(403 은 주입).
