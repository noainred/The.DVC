# 8차 점검 — 7축 병렬 감사 (v2.597, 2026-09-24)

요청: "한번 더" — v2.594~v2.596 과 같은 방식(병렬 에이전트 7축 · 축별 반증 · 전체 검증 · 범위 안 patch/minor 만).
이번 축은 직전 감사가 '남긴 것(가능성)' 판정과 수집기 정확성·프로세스 수명·패키징을 새로 넣었다.

## 결과 요약

| 축 | 확정 | 가능성 | 반증 | 고침 |
|---|---:|---:|---:|---:|
| v2.596 회귀 | 3 | 0 | 0 | 3 |
| 남긴 것 판정 | 5 | 0 | 3 | 5 |
| 인가·범위 | 0 | 2 | 0 | 2 |
| 수집기 정확성 | 5 | 3 | 0 | 8 |
| 프로세스 수명 | 2 | 0 | 0 | 2 |
| 웹 화면 | 2 | 1 | 0 | 3 |
| 의존성·패키징·CI | 4 | 3 | 1 | 5 |
| **합계** | **21** | **9** | **4** | **28** |

고침 28건에는 가능성 판정 7건(AUTHZ-2597-01·02, C2597-05·07·08, WEBUI-2597-03, DEPS2597-05 — 코드상 성립·실장비 재현 없음)이 포함된다.

## 고친 것

| id | 위치 | 내용 |
|---|---|---|
| RECENT-01 | `backup/service.js` · `security/secretVault.js openSecretIfCached` | 암호화 모드 백업 지문이 값마다 scrypt — 100값 10~22초 루프 정지(v2.596 회귀) |
| RECENT-02 | `util/staticGzip.js acceptsGzip` | `gzip;q=0` 을 gzip 으로 응답 |
| RECENT-03 | `routes/svcmon/edge.js` | 비-admin probe 응답의 포트 문구 |
| L2597-01 | `vmtrack/db.js`·`service.js` · 웹 `VmTrackTool.jsx` | 연결 실패 vCenter 를 뺀 슬롯 합계에 표식(`skipped`) |
| L2597-02 | `storage/db.js` · `metrics/db.js` | 첫 open 잠금이 영구 비활성·NDJSON 폴백으로 래치 |
| L2597-03 | `util/agentKey.js` + 3 저장소 | 엣지 이름 대소문자로 설정만 빠짐 |
| L2597-04·05 | `pdu/push.js` · `pdu/registry.js` · `pdu/poller.js` · `agent/pduConfigPull.js` | push 재진입 · pull 로 빠진 장비 스냅샷 잔존 |
| C2597-01 | `pdu/collectors/apcSsh.js` | 데이지체인 유닛 2+ 뱅크·상 = 유닛 1 값 복제 |
| C2597-02~05 | `storage/collectors/unity.js`·`isilon.js`·`powerstore.js` | REST 결측 → 0 |
| C2597-06 | `gpu/guestops.js` | GPU 메모리 사용률 부분 결측 과소 |
| C2597-07 | `sanswitch/rates.js` | 디렉터 포트 키 충돌 |
| C2597-08 | `vcenter/restClient.js` | REST 폴백 DS free_space 결측 → 사용률 100% |
| AUTHZ-2597-01 | `routes/ping.js` | 대상 vcenterId 를 보지 않는 범위 판정 |
| AUTHZ-2597-02 | `routes/api/checksLogs.js` | 범위 계정에 함대 수치 문구 |
| LC2597-01 | `alerts.js` | 재시작마다 발생 중 알림 전부 재발송 |
| LC2597-02 | `gpu/physicalPoller.js` · `routes/admin/gpuGuest.js` · `agent/gpuGuestConfigPull.js` | 주기 변경 미반영 |
| WEBUI-2597-01·03 | `web/src/styles.css` + `cssVars.test.js` | 미정의 `--muted`(104곳)·`--hover`·`--blue` |
| WEBUI-2597-02 | 웹 BmUsage·EdgeLog·SecuritySelfCheck·AgentDeploy + `.btn-sm` | 흰 기본 버튼 |
| DEPS2597-01 | `packaging/offline/build-package.sh` | config 정리식이 Windows 보다 좁음 |
| DEPS2597-02 | `.gitignore` · `config/vm-track.db*` | 루트 config DB 추적 |
| DEPS2597-03 | `packaging/windows/build-collector-win.sh` | '네이티브 애드온 없음' 주석 정정 |
| DEPS2597-05·06 | `.github/workflows/*.yml` | persist-credentials · Node 버전 통일 · 릴리스 npm ci |

## 반증

- L2597-06 SAN rates 빈 목록 주기 — 값이 틀리지 않는다(한 주기 null 뒤 정상).
- L2597-07 porterrshow 디렉터 오귀속 — 'slot/port:' 행은 빈 결과가 된다(엉뚱한 포트에 붙지 않는다).
- L2597-08 VM quickStats 결측 → 0 — 도달 불가(v2.594 판정 유지).
- DEPS2597-08 — 결함이 아닌 요약 항목.

## 남긴 것

- DEPS2597-04 Windows 서비스(SYSTEM) · CONFIG_DIR ACL — Windows 환경이 없어 확인하지 못했다.
- DEPS2597-07 자산 prune 이 업로드보다 먼저 — 1000개 상한과 얽혀 순서를 바꾸면 업로드가 422 로 실패할 수 있다. 별건.
- vCenter REST 폴백 DS 사용량 결측 시 스토리지 합계는 부분 합(합계 헬퍼가 null 을 0 으로 더한다 — SOAP 실패 폴백 경로 한정).

## 검증

- 서버 `npm test` 3,150건 통과 · 웹 vitest 1,611건 · eslint · vite build 통과.
- 변이 검증 19/19 — 고친 서버 파일을 하나씩 HEAD 로 되돌리면 `audit2597.test.js` 가 실패한다.
- Chromium 1440/400: 베어메탈 사용률·엣지 로그·보안 자가진단·통신 점검·VM 수량 추이·SAN 스위치 — 페이지 오류 0 · 가로 넘침 0.
- ⚠ 정직 기록: 실장비(APC 데이지체인 CLI·Isilon/Unity 결측 응답·다중 프로세스 SQLite 잠금·Windows)로는 확인하지 못했다.
