# 5차 점검 — 7축 병렬 감사 (v2.594, 2026-09-24)

요청: "세번 더 수행"(v2.593 과 같은 "아키텍처 점검, 개선, 버그 개선, 튜닝, 업그레이드 찾아서 진행, 문서화")의 1회차.
방식: 7축 병렬 에이전트가 찾고 축마다 반증 에이전트가 재현을 시도했다. 검증: 전량 테스트 + 변이 검증 + Chromium 1440/400.

## 결과 요약

| 축 | 확정 | 가능성 | 반증 | 고침 |
|---|---:|---:|---:|---:|
| 직전 '남긴 것' 판정 | 1 | 1 | 2 | 2 |
| 최근 변경 회귀(v2.592~2.593) | 7 | 0 | 0 | 7 |
| 입력 처리 보안 | 4 | 0 | 0 | 4 |
| 엣지↔중앙 계약 | 3 | 1 | 0 | 4 |
| 데이터 정확성 | 1 | 2 | 2 | 2 |
| 설정·탭·모달 화면 | 2 | 0 | 0 | 2 |
| 성능 | 1 | 0 | 1 | 1 |
| **합계** | **19** | **4** | **5** | **22** |

## 고친 것

| id | 심각도 | 위치 | 내용 |
|---|---|---|---|
| R2594-01 | medium | `storage/collectors/powermax.js` | 정규화가 사용량 null 을 0 으로(v2.593 수정 무효) → null 유지 · 한 어레이라도 결측이면 합계 null |
| R2594-02 | medium | 웹 `StorageMonTool.jsx`·`storageUnits.js` | KPI·법인 요약·DC 보기가 결측을 0 으로 더해 사용률 과소 → `capacityTotals` · `formatBytes(null)='—'` |
| R2594-03 | medium | `storage/collectors/isilon.js` | 사용량 null 에서 HDD 사용량 0 → null(노드 풀 포함) |
| R2594-04 | medium | `relaytopo/ops.js` | 비-admin 결과 경로로 IP·오류·점검 문구 → 주소 비움 · 개수만 |
| R2594-05 | low | `storage/db.js` | 통합 추이 SUM 이 결측 장비를 분자에서만 뺌 → 그 버킷 사용량 NULL + `used_unknown` |
| R2594-06 | low | `routes/api/relaytopo.js` + 웹 | 가린 뒤 'IRS 있는 사이트' 항상 0 → `present` |
| R2594-07 | low | `gpu/poller.js` + 웹 | MIG 진단 'util 0%' → N/A(MIG) |
| SEC-2594-01 | medium | `routes/api/checksLogs.js`·`logs/db.js` | vclogs `limit=-1` → SQLite 무제한 → `pageArgs` + `clampPage` |
| SEC-2594-02 | low | `routes/api/reports.js` | 변경 이력 `limit=-1` → 거의 전량 |
| SEC-2594-03 | low | 같은 곳 | 실수·거대 offset → 500 |
| SEC-2594-04 | low | `routes/api/checksLogs.js` | 다운로드 파일명에 요청값 → 안전 문자만 |
| EDGE2-01 | medium | `central/svcmonEdge.js` | poller·caps·log 무상한 상주 → 16KB 상한 |
| EDGE2-02 | low | `central/sanSwitchPerfEdge.js` | 퇴출 순서가 삽입 순 → 갱신 시 뒤로 |
| EDGE2-03 | low | `routes/central.js` | ip-scan 병합 수 부풀림 |
| EDGE2-04 | low | `agent/idracScanWorker.js` | 진행 보고 무음 실패 → 상태·콘솔 |
| LO-1 | medium | `store.js` + 웹 개요 | 끊긴 호스트가 사용률 분모에만 남음 → 사용률 계산에서 제외 |
| LO-2 | low | `ipam/overrides.js`·`annotations.js` | 선행 0 표기 키 → 정규형 |
| DATA2594-03 | medium | `vmtrack/poller.js`·`service.js` | 첫 수집 중 vCenter 가 빠진 부분 합 → 최대 30분 대기 + `skippedVcenters` |
| DATA2594-05 | low | `pdu/collectors/apcSsh.js`·`types.js` | E1xx 외 실패로 뒤 유닛 무음 누락 → `unitsIncomplete` + 노트 |
| UI-2594-01 | medium | 웹 `EntityDetail.jsx` | 상세 모달 400px 잘림 → auto-fit 그리드 |
| UI-2594-02 | low | `svcmon/poller.js` | 시작 로그 `undefined` |
| PERF-2594-01 | low | `ipam/db.js`·`writeWorker.js` | 워커에 행 객체 전체 복제 → 레코드 배열 |

## 판단을 남긴 것 · 반증

- 반증: 손 풀 7곳·버전 비교 사본 3곳(입력 고정·검증 — 결함 아님) · VM 사용률 결측 2건(현 수집기로 도달 불가) · 캐시 없는 1회성 조회 3종(폴링 아님).
- 가능성(고치지 않음): SOAP 수집기가 VM quickStats 결측을 0 으로 싣는 것(도달 시점 미확인).
- 남은 판단: PERF-1(IPAM 원장 30초 재구성 — 사용자 결정) · EDGE-3(v2.583 설계) · 수집 실패 vCenter 가 빠진 VM 수량 슬롯의 부분 합 표식(스키마 변경 필요).

## 검증

서버 3,099 · 웹 1,606 · lint 0 · 빌드 · audit2594 16건(변이 21/21) · Chromium 1440/400(개요·스토리지·중계 토폴로지·VM 상세 모달 — 오류 0 · 넘침 0).
⚠ 비-admin 가림 화면과 사용량 결측 장비가 있는 스토리지 표는 화면으로 보지 못했다.
