# 19차 점검 (v2.611) — 2026-09-25

- 기준 커밋: `3c1f691` (v2.610.0)
- 방법: 10축 병렬 발견(RECENT·LEFT·AUTHZ·CEN·COL·WEB·TIM/PERF·DB·SEC·EDGE) → 축마다 반증 검증(의도·재현 두 렌즈) → 파일이 겹치지 않는 수정 그룹 5개(A iLO 체인 · B 권한·범위 · C CVP·DB · D 웹 · E 타이머·스캔 가드·튜닝) → 그룹별 변이 검증(기준 커밋 판본으로 되돌려 회귀 테스트 실패 확인) → 전량 테스트·빌드·Chromium 1440/400.
- 회귀: `server/test/audit2611a.test.js`(17) · `audit2611b.test.js`(10) · `audit2611c.test.js`(20) · `audit2611e.test.js`(7) · 웹 `virtRatioText.test.js`·`audit2611d.test.js`·`idrac/scanRunText.test.js`·`tools/cvpText.test.js`.
- 전량: 서버 3,893건(수정 전 실패 4 → 테스트 3건 정정 후 통과, 아래) · 웹 1,915건 통과 · vite build 통과 · Chromium 146경로 × 1440/400 페이지 오류 0.

아래는 각 축 검증 보고서의 판정 요약이다(원문은 작업 스크래치패드에 남겼다).

## RECENT 축 검증

| ID | 판정 | 심각도(검증 후) | 중복 | 한 줄 근거 |
|---|---|---|---|---|
| RECENT2611-01 | CONFIRMED | high | — | 중앙 직접 경로: iLO 전용 replace-datacenter 스캔에서 Dell 은 `noCreds` 로 found 밖, partial 식에 noCreds 없음 → 법인 전체 교체로 Dell 등록 삭제(재실행 확인) |
| RECENT2611-02 | CONFIRMED | medium | 01 의 진입 경로 | 폐기 안내 "스캔은 보류" 인데 `scanCredsOf.any`(iLO)로 `scanEntryReady`·enabled 가 참 |
| RECENT2611-03 | CONFIRMED | medium | 중복(EDGE2611-01) | 폴링 잡에 `username:''`+`ilo`, 구버전 엣지 `runLocalIdracScan` 은 ilo 를 모름, `rawGet` 은 2c18e14↔3c1f691 무변경 → 빈 계정 Basic + 세션 POST |
| RECENT2611-04 | CONFIRMED | low | 부분 중복(CEN2611-01 — 위임 회신 화이트리스트 몫) | 중앙 직접 lastRun(`recordScanRangeRun` 186-189)·스캔 로그(196-201)에 noCreds 없음, 웹 scanRunText 에도 없음 |
| RECENT2611-05 | CONFIRMED | low | 중복(SEC2611-01) | `credsFor` unknown/lenovo → `dellCred \|\| iloCred`; 로그인 후 vend2=hpe 면 credSet 'idrac' 이라 found 조건 불충족 → notIdrac 로 조용히 빠짐 |
| RECENT2611-06 | SPLIT | low | — | vendor 유실(CSV·JSON replace)과 bmusage 영향은 확정. 'CSV export 쪽' 은 **등록부 CSV 내보내기가 존재하지 않는다**(수정 범위는 parseCsv 만). 주석 "폴러가 건너뛴다" 는 사실 아님(주석 결함) |
| RECENT2611-07 | PLAUSIBLE | low | — | 메커니즘 확인(maybePrune 은 enabled 판정 뒤, `/cvp-data` 는 CVP enabled 를 보지 않음). 전제가 `CVP_SETTINGS_LOCAL=1` 또는 꺼진 상태의 수동 요청 수집 — 드문 구성 |

## CEN 축 검증

| ID | 판정 | 심각도(재평가) |
|---|---|---|
| CEN2611-01 | CONFIRMED | 중상(유지) |
| CEN2611-02 | CONFIRMED (CVP) / 형제 storage·SAN 은 표시 영향 없음, PDU 는 이미 정규화 | 중 → 중하 |
| CEN2611-03 | CONFIRMED (cvp-data 재현 + sanswitch-perf 재현) | 중(유지) |
| 가능성(saveEdgeCvpStatus at 비교 없음) | PLAUSIBLE(낮음) | 하 |

## COL 축 검증

| 발견 | 판정 |
|---|---|
| COL2611-01 HPE SKU → serviceTag | **CONFIRMED** (영향이 보고보다 넓다) |
| COL2611-02 HPE 팬 Reading(%) → RPM | **CONFIRMED** (HPE 문서 근거) |
| COL2611-03 Absent 온도 센서 0℃ | PLAUSIBLE |
| COL2611-04 CVP 트랜시버 present → fault | **CONFIRMED** |
| COL2611-05 CVP 빈 200 → 0대 확정·전량 prune | **SPLIT** |
| COL2611-06 카운터 경로 끝 조각 병합 | PLAUSIBLE |
| COL2611-07 열거형 비대칭(묶음) | 분할: speed2p5Gbps CONFIRMED(low) · INTF_OPER_UP / LINK_STATUS_UP PLAUSIBLE · bgp notEstablished REFUTED(실제 값 아님) · unknownHwStatus→fault PLAUSIBLE · speedBps 주석 불일치 CONFIRMED(주석만) |
| COL2611-08 CVP 간격 한계 | **CONFIRMED** (조건부 — 기본값에서는 발생 안 함) |

## EDGE 축 검증

| ID | 판정 | 중복 | 한 줄 |
|---|---|---|---|
| EDGE2611-01 | CONFIRMED (중간) | — | v2.609 엣지가 iLO 전용 대역 poll 잡을 받으면 빈 계정(`Basic Og==` + 세션 POST)으로 Dell·HPE 전 호스트 로그인 |
| EDGE2611-02 | CONFIRMED (중간) | CEN2611-01 과 인접(별건) | 구버전 엣지 회신 → '계정이 없어 등록하지 않음' 틀린 원인 |
| EDGE2611-03 | CONFIRMED (낮음~중간, 코드) | EDGE-01 의 push 형제 | iLO 전용 대역 push → v2.609 엣지 400, 중앙은 'HTTP 400' 만 |
| EDGE2611-04 | CONFIRMED (중간) | **중복(CEN2611-03)** + 엣지측 추가분 | 중앙 DB 불가 200 ok → 엣지 커서 전진·`_sent` 기록 |
| EDGE2611-05 | CONFIRMED (기제) / 영향은 규모 의존 | — | CVP push 가 상한 1회분만 보냄(형제 sanswitch perfPush 는 `more` 루프) · 백로그 미표시 · prune 이 커서 무시 |
| EDGE2611-06 | CONFIRMED (낮음) | — | CVP push 비-2xx 사유 폐기(같은 도메인 cvpConfigPull 은 `classifyCentral404` 사용) |
| EDGE2611-07 | REFUTED (결함 아님 — 정보) | — | 전 키 배포·`CVP_SETTINGS_LOCAL=1` 탈출구는 sanswitch perf·curuser 와 같은 관례. v2.409 '지정 키만' 은 portal.env 기반 storage/pdu intervals 전용 계약 |

## SEC 축 검증

| ID | 판정 | 등급 | 요지 |
|---|---|---|---|
| SEC2611-01 | CONFIRMED | medium | iLO 계정만 둔 대역에서 unknown·lenovo 등 비-HPE BMC 에 iLO 계정으로 로그인. 폴백은 이득이 0(성공해도 등록 안 됨) |
| SEC2611-02 | SPLIT | low | '판정과 접속이 어긋나 차단 우회' 는 REFUTED(ipBlockReason 도 8진 해석). '단건 토큰만 8진 해석 → 범위·CIDR 과 다른 주소' 는 CONFIRMED |

## AUTHZ 축 검증

| ID | 판정 | 핵심 근거 | 최소 수정 |
|---|---|---|---|
| 01 | SPLIT(쓰기 CONFIRMED / 조회 REFUTED=v2.604 정책) | assign-vcenter all 200 updated:2 → delete 200 removed:2 (idracScan.js:459·472) | idracScan/idracCore 쓰기·스캔 실행에 fullScopeOnlyWith; GET 유지 |
| 02 | CONFIRMED(신규 — 등록부, v2.605 스칼라 남긴 것과 다름) + 추가: `/vcenters/import` 가 v2.607 fleetWideOnly 우회(replace 200) | collectors 201/DELETE 200, horizon DELETE 200 vs sessions 403, nsxImport.js:63·80 | 파일별 표 — 최우선 nsxImport `/vcenters/import*` |
| 03 | CONFIRMED | /logs 200 vs log-analysis 403; /status vcenters:11 무필터 | statusTools.js:67 fullScopeOnly, :175 범위 필터 |
| 04 | CONFIRMED(조건: settingsOwner) | 범위 owner 발급 키 → v1 11개 전량 | apiKeys 라우트 fullScopeOnly(또는 부분집합 강제) |
| 05 | CONFIRMED | 엣지 region APAC → APAC 범위 계정에 vc-pl | store.js:319 region 은 등록부 우선 |
| 06 | PLAUSIBLE | cvp.js:124~138 peer 원문 | 정책 판단 |
| 07 | REFUTED(IDOR) — 범위 부분은 v2.607 남긴 것 재보고 | /net/history 가 이미 전 admin 공용 | — |
| 08 | REFUTED(v2.601 남긴 것 재보고) · 정보 | central.js:1048 | — |

## LEFT 축 검증

| id | 판정 | 의도 렌즈 | 재현 렌즈 | 중복 |
|---|---|---|---|---|
| LEFT2611-01 | **CONFIRMED**(중간) | v2.610 규약 "그 벤더 계정으로만 로그인" 위반 | `ilo.mjs` 재실행: 401만 주는 HTTPS 에 `Basic iloadmin:IloSecret!` 도착 · `vendor:unknown · credSet:'idrac' · authFailed:true` | = SEC2611-01 · RECENT2611-05 |
| LEFT2611-02 | **CONFIRMED**(중간) | v2.605 범위 병합 · v2.607 AUTHZ2607-05(전 법인 공통 스칼라)의 형제 누락 | `ipamset.mjs` 재실행: 범위 admin GET 에 `vc-B-secret` 노출 · PUT 200 뒤 파일에서 범위 밖 키 삭제 + `publicRanges:['0.0.0.0/0']` | 겹침: AUTHZ2611-02 와 **주제만 같다** — AUTHZ-02 본문은 `centralIpam.js` 를 "이미 fleetOnly 를 받은 형제" 로 적어 이 라우트는 포함하지 않았다. 독립 확정(수정은 같은 계열로 묶으면 된다) |
| LEFT2611-03 | **CONFIRMED**(중간) — 두 부분 | ① 범위: AUTHZ-02 가 `gpuGuest.js` 를 명시 ② 비밀 승계: v2.607 SEC2607-07 `accessMoved(host·port·username)` 의 형제 누락(범위와 무관, 전체 범위 admin 도 해당) | `gpuset.mjs` 재실행: GET 에 `poland-svc`·`hasPassword` 노출 · PUT 200 뒤 전역 `enabled:false` · `username:'x'` 인데 `hasPassword:true` | ① **중복(AUTHZ-02 일부)** ② 독립 |
| LEFT2611-04 | **CONFIRMED**(낮음) | v2.607 '남긴 것' 에 적힌 항목("IPAM import dryRun 이 범위 밖 vCenter 의 존재를 드러낸다"). 형제 PUT/DELETE 는 404 로 존재를 숨긴다 | `ipamset.mjs` dryRun: `Poland-VC` → `vcId:'vc-B-secret', action:'add'`, 없는 이름 → 'error'. 응답이 `outOfScope:1` 도 따로 센다 | 없음 |
| LEFT2611-05 | **CONFIRMED**(낮음) | v2.583·v2.596 "숫자 칸 빈 값은 미지정" 형제 누락. 코드 주석이 "0 은 보존" 을 의도했지만 `Number('')===0` 을 놓쳤다 | `finops.js:44` `Number(body.co2KgPerKwh)` + 웹 `web/src/views/Insights.jsx:111` 이 입력값(`e.target.value`, 빈 칸이면 `''`)을 그대로 `setCfg` 한 뒤 `:60` `putJson` 으로 보낸다 → 0 저장. 반대로 `:41` 요금은 `\|\| cur` 라 0 을 저장할 수 없다(세 필드 규칙이 제각각) | 없음 |
| LEFT2611-06 | **CONFIRMED**(낮음) | v2.602 SEC2602 가 같은 정규식을 central.js·toolAccess.js 에서만 선형화했다. 형제 누락 | `reg2.mjs`: `collectorInputIssue` 1만 43ms · 2만 154ms · 4만 620ms(2배마다 4배, O(n²)) · 결과 `null`(검증 통과 → 저장). 관리자 라우트(1MB 기본 본문) 한정 | 없음 |
| LEFT2611-07 | **CONFIRMED**(낮음) | v2.607 '손대지 않은 라우트: IPAM 스캔 설정 GET' 그대로. 같은 파일 PUT·run·results 는 `fleetOnly` | 코드 확인 `centralIpam.js:108`·`:137` — 게이트가 `adminOnly` 뿐, 응답에 `loadScanSettings(agent)`(임의 `?agent=`)·`getAgentReports()`·`getScanRuns(50)` | AUTHZ-02 본문에 없음(centralIpam 은 '고친 형제' 로 분류) → 독립. 같은 수정 묶음 권장 |
| LEFT2611-08 | **CONFIRMED**(낮음) | v2.601 '남긴 것' 목록(4종)보다 실제 누락이 많다 | 코드 확인: `withOutboundTag` 없는 `resilientFetch` — `collectorsDc.js:215`·`:400`·`:465`, `deployLlm.js:350`·`:462`. 단 **`collectorsDc.js:400`(연결 테스트)은 `/export` 가 의도적이다**(성공 경로가 `hosts`·`agent`·`version` 을 읽는다, `:423`). `/ping` 교체·본문 취소 제안은 `deployLlm.js:350`(성공 시 본문을 읽지도 취소하지도 않음 × 대상 50 × 2회)·`:462`·`collectorsDc.js:465` 에만 해당 | 없음 |
| LEFT2611-09 | **REFUTED**(결함으로는) — 정보 정리만 | v2.550.3 판정 기준은 네 조건의 논리곱(① WHERE 없이 전체 ② aggregate 2+ ③ 큰 테이블 ④ 조회 응답 경로 — 화면 로드마다). 이 쿼리는 `WHERE target=?`(인덱스 선행열)이고 폴링이 아니다(대상·기간 바꿀 때 1회) → ①④ 불충족. 규약상 "하나라도 빠지면 고치지 않는다" | 재실행: 52.6만 행 대상 `MIN,MAX,COUNT` 29.8~30.5ms · MIN/MAX 단독 0.03~0.05ms. 웹 `PingMonitor.jsx` 는 `meta` 를 읽지 않는다(`grep meta` 0건, `/ping/series` 소비처는 이 파일 하나) | 없음 |
| LEFT2611-10 | **CONFIRMED**(정보, 문서) | '고친 뒤에는 그 줄을 고칠 것' 규약(v2.586) | `server/CLAUDE.md:727-728` 의 "CONFIG-FILES.md 에 없다(생성기 결함, 별건)" — 소스의 정지 파일 15종(`*-auth-stops.json`)이 **전부** `docs/CONFIG-FILES.md` 에 있고 15종 전부 `git check-ignore` 통과(스크립트 확인) | 없음 |

## DB 축 검증

| ID | 중복 | 의도 | 재현 | 판정 | 심각도(검증 후) |
|---|---|---|---|---|---|
| DB2611-01 | — | 확인 | 재현(직접) | **CONFIRMED** | medium |
| DB2611-02 | — | 확인 | 재현(30만 행, EXPLAIN) | **CONFIRMED** | medium(규모 선형) |
| DB2611-03 | CEN2611-03 · EDGE2611-04 | 확인 | 모듈 재현 + 코드(HTTP 왕복은 미실행) | **CONFIRMED** | medium |
| DB2611-04 | — | 확인 | 재현(30만 행 170ms/7,600행) | **CONFIRMED** | low~medium |
| DB2611-05 | RECENT2611-07 | 확인 | 코드(실행 안 함) | **CONFIRMED** | low |
| DB2611-06 | — | 확인 | 소비 화면 없음 | **SPLIT** | low(잠재 — API 필드만) |
| DB2611-07 | EDGE2611-05 | 확인 | 산수(실측 없음) | **PLAUSIBLE** | low |

## WEB 축 검증

| ID | 판정 | 심각도 | 근거(기준 커밋 파일:줄) | 수정 범위 |
|---|---|---|---|---|
| WEB2611-01 | CONFIRMED | 중간 | `web/src/views/IdracAdmin.jsx:88` srEdit `username: e.username \|\| 'root'` → 저장 body 에 username 항상 실림(:104) → 서버 `idrac/scanRanges.js:210` `body.username !== undefined` 이면 그대로 저장. 비밀번호 없는 Dell 계정은 `scanCredsOf`(:125-129)가 dell=false 라 로그인 시도는 없음(보안 영향 없음 — 보고서와 동일). `IdracScanRanges.jsx:212` 라벨 `*` 는 hasPassword 만 봄, `IdracAdmin.jsx:97,114` noPw 경고도 iLO 무시. | `web/src/views/IdracAdmin.jsx`, `web/src/views/idrac/IdracScanRanges.jsx`(+ 폼 변환 순수 함수 테스트) |
| WEB2611-01 부수(DC1) | PLAUSIBLE(정보·낮음, 이번 회차 신규 아님) | 낮음 | PUT `/admin/idrac/scan-ranges`(`routes/admin/idracScan.js:297-304`)→`saveScanRanges`(`scanRanges.js:198-200`)는 비어 있지 않음·길이·제어문자만 검사, DataCenter 목록 대조 없음. 웹 폼은 `<select>`(`IdracScanRanges.jsx:187`), CSV import 는 `listDatacenters()` 로 해석(`idracScan.js` import 경로 :54-65) → **API 직접 호출에서만** 도달. 20d13b7 부터의 기존 동작. v2.609 `cvp/formChoices.pickDatacenter` 형제로 막을 수 있으나 기존 저장값 통과 규칙이 필요. | (택하면) `server/src/idrac/scanRanges.js` 또는 라우트에 `pickDatacenter` 형 검증 |
| WEB2611-02 | CONFIRMED | 중간 | 서버 `routes/api/cvp.js:83,87` 이 `partsUnread` 를 셈. 웹 `cvpText.js:74-91 kpiItems` 는 partsUnread 미사용. `bgpDown`/`portsDown`(cvp.js:88-89)은 `d.bgpPeers`/`d.ports` 가 null 인 장비를 조용히 건너뛰고 미확인 수를 세지 않음. | `web/src/views/tools/cvpText.js`(+test), `server/src/routes/api/cvp.js`(portsUnread·bgpUnread) |
| WEB2611-03 | CONFIRMED | 중간 | `CvpTool.jsx` 에 `collectDrops`·`dbUnavailable`·`pendingRequest`·`orphanRows` 참조 0건(grep). 서버는 `cvp.js:94-100` 에서 싣는다. 형제 `PduTool.jsx`·`SanSwitchTool.jsx`·`StorageMonTool.jsx` 는 `collectDropText` 사용 → 형제 비대칭(v2.591). dbUnavailable 시 '장비 0대' 거짓 = v2.509 계열. | `web/src/views/tools/CvpTool.jsx`, (문구) `cvpText.js` |
| WEB2611-04 | CONFIRMED | 낮음~중간 | `cvpText.js:158` ITEM_LABEL 키 {inventory,version,parts,interfaces,counters,bgp} vs 서버 `cvp/client.js:42-52` CANDIDATES 키 {inventory,cvpVersion,interfaces,counters,bgp,power,cooling,temperature,xcvr} + `missing.budget`(:244). `version`·`parts` 는 서버에 없는 키, 6개 키가 영문 노출. | `web/src/views/tools/cvpText.js`(+ 서버 키 대조 테스트) |
| WEB2611-05 | SPLIT(핵심 CONFIRMED) | 중간 | **웹**: `CvpTool.jsx` 에 `partsMissingKinds` 참조 0건(상세 응답 `cvp.js:134` 는 싣는다). **서버(재현)**: `cvp/client.js:226-235` 는 partsDue 주기에만 `partsMissingKinds` 를 세팅, 비-due 주기는 `parts: undefined`. `cvp/db.js:159` 가 그때 `extra=null` 을 만들고 upsert(`db.js:84`)가 `extra_json=excluded.extra_json` **무조건 덮어씀** — parts_json/parts_at 은 CASE 로 보존(:82-83)하는데 extra 만 비대칭. node 재현: cycle1 `{partsMissingKinds:[cooling,temperature]}` · 파트 1 → cycle2(parts undefined) `{}` · 파트 1 유지 ⇒ **옛 파트 목록은 남고 '못 읽은 종류' 표식만 사라진다**. 반증된 부분: not-streaming 장비는 행의 '스트리밍' 열(`CvpTool.jsx:208 streamingText`)이 원인을 보여 준다 — '행에서 원인 미표시' 는 약함(상세 탭 문구만 해당). 서버 status `missing` 에서 cooling 이 빠지는 것은 주기 단위 상태라 결함 아님(그 주기엔 시도 안 함). | `server/src/cvp/db.js`(upsert 에서 parts_at NULL 이면 extra_json 보존 또는 extra 병합), `web/src/views/tools/CvpTool.jsx`·`cvpText.js`(탭 상단 '읽지 못한 종류') + 서버 테스트(2주기 저장 후 partsMissingKinds 유지) |
| WEB2611-06 | CONFIRMED | 중간 | `Summary.jsx:38-40` ratioOf 분모 0 → 0, :71 배지 `ratio > HI ? 높음 : 정상`(green). `routes/api/inventory.js:113` byVcenter 는 **전 vCenter** 를 포함하고 호스트 없는 vCenter 는 bucket 0(첫 수집 중·연결 실패) → `0 : 1 · 정상`. 셀 `(vc[key]\|\|0)` 도 결측을 0 으로. | `web/src/views/Summary.jsx`(+ 순수 함수로 떼어 테스트) |
| WEB2611-07 | CONFIRMED | 중간 | `Audit.jsx:14-20` 가드 없음 + `SearchBox`(`primitives.jsx:214-230`)는 IME 조합만 보류하고 **디바운스 없음** → 영문 입력은 글자마다 fetch, 늦은 응답이 덮음. `LicenseTools.jsx:131-135`, `GuestOsTools.jsx:189-191`(loadResults), `NicTools.jsx:21-25,138-142`, `DavinciChecks.jsx:96-97`(vc 변경 시 실패하면 preview 가 이전 vCenter 로 남음) 모두 세대 가드 없음. v2.596 WS 규약 누락분. | 위 5파일(+ Audit 디바운스) · 웹 소스 스윕 테스트 |
| WEB2611-08 | CONFIRMED | 중간 | `PhysicalGpuManager.jsx:65` `.catch(() => {})` → d=null → :166 '등록된 물리 GPU 서버가 없습니다'(admin 전용 조회 실패를 '없음' 으로 — v2.590 W2/W3 계열), :80·:86 무음. `VmProvision.jsx:423` 실패 시 `{total:0,items:[]}`, :435 삭제 무음. `IdracAdmin.jsx:44-45` 스캔 대역·잡 실패 무음 → `IdracScanRanges.jsx:292` '저장된 스캔 대역이 없습니다'. `RemoteAccess.jsx:47,61`·`ProxySettings.jsx:68`·`AgentDeploy.jsx:144` delJson 무음. | 위 6파일 · `delJson(...).catch(() => {})` 스윕 테스트 |
| WEB2611-09 | SPLIT | 낮음 | mem: 서버 `gpu/poller.js:224` 가 `mem: r.memUsedPct ?? null` → 웹 `GpuGuestDiag.jsx:119` `{r.mem ?? '-'}%` = `-%`(v2.595 규약 누락) **CONFIRMED**. `undefinedGPU`: 같은 줄에서 `gpus: r.count` 로 성공 시 채워져 기준 코드 경로로는 도달 확인 못 함 → PLAUSIBLE. | `web/src/views/GpuGuestDiag.jsx` |
| WEB2611-10 | PLAUSIBLE(환경 의존) | 낮음 | `SanSwitchPerf.jsx:161` `<code>{db.file}</code>` 에 줄바꿈 지점 없음, 전역 `code` 규칙 없음(`styles.css` 는 `.login-hint code` 만) — 성질은 사실. 그러나 재현은 크롤 CONFIG_DIR 가 scratchpad 절대 경로(~95자)였기 때문이고 넘침은 19px·4px. 운영 경로(수십 자)에서는 400px 에 들어갈 가능성이 높아 실사용 발생은 확인 못 함. | (택하면) `web/src/styles.css` `code { overflow-wrap:anywhere }` 또는 해당 2화면 |
| WEB2611-11 | CONFIRMED | 낮음 | 서버 `cvp.js:155` `/tools/cvp/collect` 는 `writer`(admin·operator). 웹 `CvpTool.jsx:96-99` 버튼은 역할 무관 표시, :66 catch 가 `수집 요청 실패: …` 빨간 배너(`postJson` 은 403 을 `httpFail` 로 throw — `api.js:270`). 403 을 오류로 표시하지 않는 v2.398 규약과 어긋남. 역할 로그인 화면 확인은 못 함(코드 추적). | `web/src/views/tools/CvpTool.jsx` |
| WEB2611-12 | CONFIRMED | 낮음 | `cvpText.js:62-65 pctText` — n<10 은 0.1 단위 반올림이라 0 < n < 0.05 가 `0%`('트래픽 없음' 으로 읽힘). 예: 10Gbps 에 6.7Kbps = 6.7e-5% → `0%`. | `web/src/views/tools/cvpText.js`(+test: `<0.1%`) |

## TIM 축 검증

| ID | 의도 렌즈 | 재현 렌즈 | 판정 | 심각도 |
|---|---|---|---|---|
| TIM2611-01 | 규약 위반(v2.528 '시작해 놓고 잘림' · 조용한 절단) | 기준판 재현 성공 | **CONFIRMED** | 중 |
| TIM2611-02 | 규약 위반(v2.591 R-Q1 '장비 시한' 을 env 로 읽음) | 코드 확인, 증상 범위는 좁음 | **SPLIT** | 하 |
| TIM2611-03 | 규약 누락(v2.599 T2599-02 · v2.607 TIM2607-02 관문) | Node 오버플로 1ms 실측 | **CONFIRMED** | 하 |
| TIM2611-04 | 규약 위반(재진입 가드 · 연타가 로그인을 곱하지 않게) | 코드 확인 + authPolicy 도 없음(추가) | **CONFIRMED** | 하~중 |
| TIM2611-05 | v2.550.3 인메모리 맵 정리 규약 | 코드로 기제 확정. 값 오류는 없음 | **CONFIRMED(정보)** | 정보 |
| PERF2611-01 | 선택 튜닝 | 결과 동일성 보장 가능 | **CONFIRMED(선택)** | 하 |

## 전량 테스트 정정 3건(제품 결함 아님)

- `audit2606c` LEFT2606-01 · `secOutbound2583` ② — 소스 문자열 검사가 새 게이트(`fleetOnly`)·범위로 좁힌 `body` 를 몰랐다. 규칙의 의도(고정 IP 검사·설정 소유자 게이트)는 그대로 검사한다.
- `audit2606g` SEC2606-04·05 — '옛 정규식 > 150ms' 절대 기준이 이 컨테이너에서 110ms 로 떨어져 실패했다. `tOld > 40 && tOld > tNew × 5` 로 바꿨다.

## 남긴 것

- COL2611-01 교정으로 파트 장애·bmusage 키가 바뀌는 사실을 화면이 밝히는 배지.
- AUTHZ2611-06 CVP BGP 피어 IP 를 관리 주소로 볼지(정책 판단).
- pdu 등 다른 표본 커서 push 의 'DB 불가 200' 같은 모양 전수.
- `/idrac/:id/gpu-probe` GET 이 저장 자격증명으로 로그인하는 것.
- 실장비(iLO·CVP·v2.609 엣지)로는 확인하지 못했다 — 전부 가짜 Redfish/CVP 서버와 합성 입력 재현이다.
