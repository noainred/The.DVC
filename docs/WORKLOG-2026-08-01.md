# 작업 기록 — 권한 세분화 · 로그인 테마 · 계정 정책 (2026-08-01)

> 이 문서는 2026-08-01 세션에서 진행한 작업을 **초보자도 이해할 수 있게** 정리한 기록입니다.
> 이전 기록: [WORKLOG-2026-07-31.md](WORKLOG-2026-07-31.md) (v2.182 ~ v2.195)
> 이번 범위: **v2.196.0 ~ v2.204.0** · 기준일: 2026-08-01

---

## 0. 이 문서를 읽는 법

- 각 작업은 **버전 번호(vX.Y.Z)** 로 묶여 있습니다. 하나의 요청 = 하나의 버전 = 하나의 PR입니다.
- **"왜"** 는 무엇이 필요했는지, **"무엇을"** 은 어떻게 바꿨는지를 뜻합니다.
- 모든 작업은 동일한 표준 절차를 거쳤습니다:
  코드 수정 → `npm run verify`(테스트+빌드) → 버전 3곳 + 릴리스 노트 → 커밋·푸시 → PR 병합 →
  GitHub Actions 릴리스 → `versions.json`의 `latest` 갱신 확인(= 운영 포탈 자동 업그레이드 가능).

---

## 1. 한눈에 보기

| 버전 | 분류 | 한 줄 요약 |
|---|---|---|
| v2.196.0 | 권한 | 기능별 권한 매트릭스(역할 × 기능) + 사용자별 데이터 범위(vCenter/리전) |
| v2.197.0 | 권한 | VM 상세 3버튼 개별 권한 + 특수기능 40여 도구 도구별 접근 제어 |
| v2.198.0 | UI | Summary 상단 '전체 통합 합계' 카드를 화면 폭에 맞춰 1줄 고정 |
| v2.199.0 | UI | 로그인 화면 10종 랜덤 테마 |
| v2.200.0 | UI | (요청) 🎲 수동 변경 버튼 제거 — 자동 랜덤만 |
| v2.201.0 | UI | 로그인 테마 **총 20종**으로 확장(디자인 세션 10종 포팅) + 🎲 복원 |
| v2.202.0 | 계정 | 데모 전용 계정 `thedvcdemp` — 비밀번호 설정 시에만 로그인 |
| v2.203.0 | 버그·권한 | 사용자 관리 화면 크래시(React #310) 긴급 수정 + `noainred` 수퍼관리자 |
| **v2.204.0** | **보안** | **admin·operator OTP 전용 로그인**(데모/viewer는 비밀번호 허용) |

> 추가 산출물(코드 아님): 본 작업 기록 + 문서 갱신(README/INSTALL/SECURITY-AUDIT/CLAUDE).

---

## 2. v2.196.0 — 기능별 권한 매트릭스 + 데이터 범위

### 왜
기존 권한은 `admin`/`operator`/`viewer` 3역할뿐이라 "쓰기는 operator 이상" 수준의 **거친 통제**만
가능했습니다. "이 사람은 특수기능은 되지만 원격접속은 막고 싶다" 같은 요구를 표현할 수 없었고,
모든 계정이 전 세계 28개 vCenter를 다 볼 수 있었습니다.

### 무엇을
- **기능 권한 키 17종**을 정의했습니다(대시보드, 인벤토리 6종, 특수기능, 인사이트, 원격접속,
  원격콘솔, VM 사양변경/프로비저닝, 게스트계정 배포, 설정, 업그레이드, 사용자관리).
- **설정 › 사용자 관리**에 역할 × 기능 **체크박스 표**를 추가해 관리자가 직접 켜고 끕니다.
- **서버가 진실의 원천**: `requirePerm(key)` 미들웨어가 라우트를 막고, 브라우저 SSH/RDP
  게이트웨이도 같은 권한으로 검사합니다. 화면에서 메뉴를 숨겨도 API 직접 호출은 차단됩니다.
- **사용자별 데이터 범위(scope)**: 계정마다 볼 수 있는 vCenter/리전을 지정합니다
  (예: 폴란드 법인 계정은 유럽만). 호스트·VM·스토리지·네트워크·알람 목록과 vCenter 필터가
  서버에서 제한됩니다.
- **admin은 항상 전체 권한**이라 매트릭스로 낮출 수 없습니다(관리자 잠김 방지).

### 안전장치
기본 매트릭스를 **기존 role 동작과 똑같이** 맞춰서, 이 기능을 도입하는 것만으로는 아무 권한도
바뀌지 않습니다(무회귀). 매트릭스는 `permissions.json`에 원자적으로 저장되고 손상 시 보존됩니다.

---

## 3. v2.197.0 — VM 3버튼 · 특수기능 도구별 권한

### 왜
VM 상세 화면의 **[사양 변경] · [원격 콘솔] · [원격 접속]** 버튼과, 특수 기능 안의 40여 개
도구를 개별적으로 통제하고 싶다는 요청이 있었습니다.

### 무엇을
| 버튼 | 권한 키 | 기본값 |
|---|---|---|
| ⚙ 사양 변경 | `vm.reconfig` | admin만 |
| 🖥️ 원격 콘솔 | `vm.console`(신설) | operator·viewer 모두(기존 동작 유지) |
| 🔗 원격 접속 | `remote.access` | admin·operator |

- 특수 기능은 **거부목록(deny-list)** 방식입니다: '특수 기능' 권한이 있으면 전 도구 접근하되,
  역할별로 특정 도구만 콕 집어 차단합니다. '전체허용/전체차단' 일괄 버튼도 있습니다.
- 도구 목록을 `specialToolsList.js` 한 곳으로 모아 **화면과 권한 편집기가 같은 소스**를 봅니다
  (도구가 늘어도 권한 화면에 자동 반영).
- 권한 없는 도구는 URL 딥링크(`#/tools/<키>`)로 열어도 차단 안내가 표시됩니다.

---

## 4. v2.198.0 — Summary 상단 카드 1줄 고정

### 왜
Summary 화면 맨 위의 '전체 통합 합계' 카드 9개가 화면이 좁으면 **2줄로 줄바꿈**되어 한눈에
안 들어왔습니다.

### 무엇을
`.kpis-1row` 전용 스타일을 만들어 타일 개수와 무관하게 **항상 한 줄**에 균등 배치하고, 폭이
부족하면 줄바꿈 대신 **숫자 크기를 자동 축소**(clamp)합니다. Summary 상단에만 적용해 다른
화면(Overview·호스트·VM 등)의 카드 레이아웃은 그대로입니다.

---

## 5. v2.199.0 ~ v2.201.0 — 로그인 화면 20종 랜덤

### 왜
로그인 화면이 하나뿐이라 심심하다는 요청("10개를 만들어 랜덤으로 보여달라")에서 시작했습니다.

### 무엇을
- **v2.199.0**: 공통 인증 폼(`AuthFields`) + CSS 변수 팔레트 구조를 만들고 테마 10종을 제작
  (Davinci Map · Aurora Glass · Retro Terminal · Blueprint · Minimal Light · Neon City ·
  Orbital · Matrix Rain · Sunset · Brutalist). 접속할 때마다 랜덤 표시.
- **v2.200.0**: "수동 변경 말고 자동 랜덤만" 요청으로 🎲 버튼 제거.
- **v2.201.0**: 별도 디자인 세션에서 만든 컨셉 10종을 실동작 폼으로 포팅해 **총 20종**으로 확장
  (Terminal Boot · Split Panel · Radar Ops · Light Console · Region Tiles · Ultra Minimal ·
  NOC Preview · Left Rail · Amber Watch · Data Wall). 🎲 수동 변경 버튼도 복원.

### 품질
20종 전부 **실브라우저(Chromium) 스크린샷으로 렌더링을 검증**했고, 인증 기능(OTP·세션 유지·
3회 실패 경고)은 모든 테마가 동일한 공통 폼을 공유합니다.

---

## 6. v2.202.0 — 데모 전용 계정 `thedvcdemp`

### 왜
외부 시연용 계정이 필요하되, **쓰지 않을 때는 완전히 잠겨 있어야** 한다는 요구였습니다.

### 무엇을
| 상태 | 로그인 |
|---|---|
| 기본(비밀번호 미설정) | ❌ 불가 |
| 관리자가 [비번 설정] | ✅ 가능(viewer 권한) |
| 관리자가 [로그인 차단] | ❌ 비번/OTP 제거 + **활성 세션 즉시 종료** |

- viewer 고정이라 자격증명이 유출돼도 조회 이상은 할 수 없고, 삭제도 막혀 있습니다.
- [비번 설정]/[로그인 차단] 버튼은 데모뿐 아니라 **모든 로컬 계정**에 쓸 수 있어 일반 비밀번호
  리셋 UI로도 활용됩니다.

---

## 7. v2.203.0 — 사용자 관리 크래시 긴급 수정 + 수퍼관리자

### 왜 (버그)
v2.202 배포 후 **설정 › 사용자 관리에 들어가면 화면 전체가 "이 영역을 표시하는 중 오류"
(React #310)** 로 죽는다는 제보를 받았습니다.

### 원인과 수정
비밀번호 모달의 상태 훅(`useState`)을 **로딩 조기 return 뒤에** 선언한 것이 원인이었습니다.
데이터 로드 전후로 훅 개수가 달라져 React가 렌더를 중단합니다. 훅을 컴포넌트 최상단으로
옮겨 해결했고, 같은 실수를 막기 위해 `CLAUDE.md`에 회귀 방지 규칙을 추가했습니다.

### 수퍼관리자 `noainred`
- 항상 **admin 역할 보장** — 파일이 외부에서 강등된 상태로 저장돼 있어도 기동 시 자동 복구.
- **강등·삭제·로그인 차단 모두 거부**, 설정 소유자(settingsOwners)에 **항상 자동 포함**
  (최고 권한 계정이 설정 접근을 잃는 잠금 사고 방지).
- 사용자 관리에 '최고 관리자' 배지 표시 + 관련 버튼 잠금.

---

## 8. v2.204.0 — 고권한 계정 OTP 전용 로그인

### 왜
"admin·operator·최고 관리자는 OTP만 쓰고, 데모 계정은 비밀번호를 쓰게 해달라"는 요청입니다.
비밀번호는 유출·재사용 위험이 크고, 고권한 계정일수록 2단계 인증이 필수입니다.

### 무엇을
| 계정 | 비밀번호 로그인 | OTP 로그인 |
|---|---|---|
| admin / 최고 관리자 | ❌ 차단 | ✅ |
| operator | ❌ 차단 | ✅ |
| viewer / 데모 | ✅ 허용 | ✅ |
| AD 계정 | AD 인증 체계(대상 아님) | — |

비밀번호가 **맞아도** 고권한 계정은 서버가 403 + "OTP 등록 필요" 안내로 거부합니다
(무차별 대입 잠금 카운터에는 넣지 않고, 감사 로그에는 남깁니다).

### ★ 가장 중요한 설계 — 초기 구축 잠금 방지
비밀번호 로그인을 무조건 막으면 **첫 설치에서 아무도 로그인할 수 없어 OTP를 등록할 방법 자체가
사라집니다**(전체 잠김). 그래서:

1. **아무 admin도 OTP를 등록하지 않은 동안**은 admin 비밀번호 로그인을 허용(유예).
2. **admin 한 명이라도 OTP를 등록하면 유예 자동 종료** → 전 고권한 계정에 정책 적용.
3. operator는 유예가 없으므로 admin이 QR을 발급해 전달.
4. 마지막 admin이 OTP를 해제하면 유예가 자동으로 되살아나 복구 가능(잠김 없음).
5. 긴급 시 `OTP_ROLE_ENFORCE=false`로 정책 자체를 끌 수 있습니다.

---

## 9. 검증 요약

| 항목 | 결과 |
|---|---|
| 단위 테스트 | **466 pass / 0 fail** (신규: permissions 7 · scope 4 · demoUser 5 · otpPolicy 4) |
| 웹 빌드 | 성공 |
| 로그인 테마 | 20종 실브라우저 스크린샷 렌더링 확인 |
| 릴리스 | v2.196.0 ~ v2.204.0 전부 `versions.json`의 `latest` 갱신 확인 |

---

## 10. 운영자가 지금 해야 할 일

1. **admin·operator 계정의 OTP 등록** — v2.204 정책은 첫 admin이 OTP를 등록해야 전면 적용됩니다.
   설정 → User Control → 메인포탈 사용자 관리 → [OTP 등록] → QR 스캔 → 6자리 확정.
2. (선택) **기능 권한 매트릭스 조정** — 같은 화면 하단에서 operator/viewer의 기능·도구 접근을
   조직 정책에 맞게 조정.
3. (선택) **계정별 데이터 범위 지정** — 외주·감사·데모 계정에는 볼 수 있는 vCenter/리전을 제한.
4. (선택) **데모 계정 활성화** — `thedvcdemp`에 [비번 설정], 시연 종료 후 [로그인 차단].
