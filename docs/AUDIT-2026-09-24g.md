# 10차 점검 — 10축 병렬 감사 + 발견별 2인 반증 (v2.599, 2026-09-24)

요청: "더 자세하게, 두번 더"(2회차). 선택: **감사 깊게 + 보고 자세히** · **전체 검증**. 방식은 v2.598(`AUDIT-2026-09-24f.md`)과 같고,
이번 축은 **v2.598 회귀를 가장 먼저** 보고(124파일 diff), 수집기 정확성·보안·엣지 계약을 새로 넣었다.
수정은 파일이 겹치지 않는 6그룹 병렬, 그룹마다 회귀 테스트 + 변이 검증(감사 기준 커밋 `6e3613a` 판본으로 되돌리면 실패).

## 결과 요약

| 축 | 발견 | 확정 | SPLIT | 가능성 | 반증 | 고침 |
|---|---:|---:|---:|---:|---:|---:|
| v2.598 회귀 | 4 | 4 | 0 | 0 | 0 | 3(1 보류) |
| 남은 후보·형제 | 3 | 3 | 0 | 0 | 0 | 3 |
| 인가·범위 | 6 | 5 | 1 | 0 | 0 | 6 |
| 중앙 수신 계약 | 5 | 5 | 0 | 0 | 0 | 5 |
| 수집기 정확성 | 10 | 10 | 0 | 0 | 0 | 10 |
| 웹 화면 | 6 | 5 | 1 | 0 | 0 | 6 |
| 타이머·자원 | 3 | 3 | 0 | 0 | 0 | 3 |
| SQLite | 2 | 2 | 0 | 0 | 0 | 2 |
| 보안 | 5 | 3 | 1 | 1 | 0 | 5 |
| 엣지 계약 | 5 | 4 | 0 | 0 | 1 | 4 |
| **합계** | **49** | **44** | **3** | **1** | **1** | **47** |

같은 결함이 세 축에서 독립적으로 나왔다: secretVault 재사용 문맥 충돌(RECENT2599-02 = LO2599-02 = SEC2599-01) — 서로 다른 ID 45건을 고쳤다.
보류 1건: RECENT2599-04(`openSecretIfCached` 호출부 없음 — scrypt 없이 여는 유일한 안전 경로라 남기고 JSDoc 에 이유를 적었다).

회귀 테스트: `server/test/audit2599{a..f}.test.js` 57건 + 웹 vitest 신규 6파일. 변이 검증: a 8/8+11/11 · b 7/7+4 · c 13/13+3/3 · d 10/10+2 · e 12/12+22/22 · f 7/7.

## v2.598 이 만든 결함(회귀) — 가장 중요

| id | 심각도 | 재현 → 수정 |
|---|---|---|
| RECENT2599-01 | high | v2.598 IDRAC-2598-04 가 지속 조건을 '관측 간격 ≤ 주기×1.5' 로 바꿨는데 적응 타이머는 **수집이 끝난 뒤** 재무장하므로 간격 = 주기 + 수집 시간. 200대(수집 ~160~200초)면 460초 간격 → **알림 0회**. 한도를 `max(60초, 주기×1.5 + runMs)` 로(poller 가 실제 수집 시간을 넘긴다) — 15분 지속에서 울린다. 1시간 공백을 사이에 둔 두 초과는 여전히 지속으로 세지 않는다 |
| RECENT2599-02 · LO2599-02 · SEC2599-01 | medium | v2.598 암호문 재사용 문맥이 부모 객체의 식별 필드만 봐서 **맵 키로만 구분되는 항목**(gpu-guest vcenters/vms, byAgent)과 id 없는 배열 원소가 같은 문맥 → 같은 비밀번호가 같은 암호문. 문맥에 경로(객체 키 · 배열은 강한 식별자, 없으면 위치)를 넣고, 한 번의 봉인 안에서는 같은 암호문을 두 번 내지 않는다. 1대 수정 저장 **1.4ms**(v2.598 이득 유지) |
| RECENT2599-03 | medium | v2.598 이 SOAP 경로에서도 DS 사용량 null 을 내게 되자 형제 소비처 4곳이 null 을 **100% 사용**(diskTrend·compareMatrix) · **전량 여유**(용량 예측) · **부분 합**(롤업)으로 읽었다. 미상 DS 1 + 정상 1: 55% → 10%. 둘 다에서 빼고 개수 표시, 예측은 소진일 null |

## 발견별 상세(그 밖)

| id | 심각도 | 재현 → 수정 | 위치 |
|---|---|---|---|
| CEN-2599-01 | high | `/inventory` 원소 null 하나가 저장돼 **매 주기 store.refresh 실패 → 함대 전체 스냅샷 정지** → 평범한 객체 원소만 받음(400) + 병합부도 같은 검사(디스크에 남은 null 도 무시) | `routes/central.js`·`store.js` |
| CEN-2599-02 | medium | 원소 vcenterId 를 본문에 묶지 않아 타 법인 vCenter 에 호스트·VM 주입 → 다르면 버리고 `dropped.otherVcenter` | `routes/central.js` |
| CEN-2599-03·05 | medium·low | 엣지 객체 값 필드로 VM·스토리지·SAN·PDU 화면 React #31 · 비객체 원소 유령 행 → 공용 `central/edgeRecord.sanitizeEdgeDevices`(id 형식·예약어 거부·표시 필드 객체→null·수치 numOrNull) | `central/edgeRecord.js` |
| CEN-2599-04 | medium | storage-data 등 엣지 수·장비 크기 무제한 + push 마다 동기 전체 쓰기 → 장비 1MB·엣지 16MB·엣지 128(24시간 조용한 엣지만 퇴출, 모두 최근이면 429) · 디바운스 비동기 원자 쓰기(종료 flush 동기) | `central/*Edge.js` |
| EDGE2599-03 | medium | 인벤토리 TOFU 소유권이 만료·재지정 불가 — 담당 엣지 교체 시 영구 403 → **관리자 해제/지정 API** `POST /api/admin/central/inventory/owner`(adminOnly·감사) + 자동 인계 `CENTRAL_INVENTORY_OWNER_HANDOVER_HOURS`(**기본 0 = 꺼짐**, 현장 opt-in) | `central/inventory.js`·`routes/admin/centralIpam.js` |
| WEB2599-01 | medium | 무토큰 거부 POST 의 주장 이름이 데이터 흐름·3단 지도에 가짜 엣지(5개) → `(인증 실패)` 한 칸(1개) | `routes/central.js` |
| WEB2599-04 | low | 없는 central 경로 404 가 '수신 꺼짐(disabled)' → `unknown-route` + 화면 문구 | `ingestReject.js`·`invCheckText.js` |
| AUTHZ-2599-01 | medium | 범위 admin 이 범위 밖 VM 복제 잡을 덮어씀 201 → 403 · 기존 잡의 vCenter 변경 400 | `routes/api/vmClone.js` |
| AUTHZ-2599-02 | medium | `/remote/probe`·`quick-connect` 범위 밖 vcenterId 로 프록시 선택 200 → 404 | `routes/remote.js` |
| AUTHZ-2599-03 | medium | 스토리지·SAN·PDU 목록이 operator 에 관리 IP·SSH 계정 → 비-admin 가림 + 🔒 배너. **형제 경로**(작업 로그·SAN 점검 이력·포트 상세·PDU 상세)도 — Chromium 에서 하단 작업 표에 IP 가 남은 것을 보고 찾았다 | `auth/addressMask.js` |
| AUTHZ-2599-04 | low | 공개 API collection 이 범위 키에 전 함대 vCenter 수(11) → 범위 기준(1) + `meta.scopedToVcenters` | `routes/publicApi.js` |
| AUTHZ-2599-05 | low | 범위 admin 에 vm-clone·os-scan 범위 밖 항목 → 교집합 + `omittedOutOfScope` · os-scan 실행 범위 밖 404 · **범위 계정이 vCenter 를 비우면 400**(전 vCenter 게스트에 로그인하므로) · 실행 중 잡 id 가림 | `vmClone.js`·os-scan |
| AUTHZ-2599-06 | low(SPLIT) | 다른 vCenter 의 vmId 저장 → 400(B 의 moref 가 A 연결에 적용되는 정합성 결함) | `vmclone/store.js` |
| EDGE2599-02 | low | 공개 API `/capacity/storage` 로컬+엣지 같은 장비 2행 → 최신 1행(`storage/latestSnapshots.js` 공용) | |
| C2599-01 | medium | Isilon REST 노드 처리량 B/s 를 bps 칸에 — **8배 과소**(3.4M → 27.2M bps) | `isilon.js` |
| C2599-02·03 | medium | PowerStore SSH 공간 지표가 배열 끝 점(10 → 최신 80) · 알람 폴백이 CLEARED 포함(4 → 2) | `powerstoreSsh.js`·`powerstoreCore.js` |
| C2599-04 | medium | APC 뱅크·상·센서 탐지가 E1xx 아닌 오류에서 말없이 멈춤 → `*Incomplete` + 노트 | `apcSsh.js` |
| C2599-05 | medium | NSX cursor 무시(첫 페이지만) + 정책 60개 조용한 절단 → cursor 페이징(상한 20, `truncated`) · `policiesOmitted` · 화면 배너 | `nsx/client.js`·`nsxLimitText.js` |
| C2599-06~08 | low | PowerStore 'Empty' 슬롯을 이상으로(4 → 1) · Unity 알람 100개 절단 → entryCount · PowerStore 알람 500 상한 표시 | |
| C2599-09 | low | bmstor 사용률 used/total → df 정의 used/(used+avail)(95 → 100%) · 분모 0 null | `bmstor/collect.js`·`agg.js` |
| C2599-10 | low | GPU MIG 혼합 평균의 부분 표시(`utilPartial`) | `gpu/guestops.js` |
| LO2599-03 | low | XtremIO 한 클러스터 용량 결측 → 사용률 130% → 40% + `poolsUnreadable` | `xtremio.js` |
| LO2599-01 | medium | SAN 사용량·현재 사용자·Horizon 설정 빈 칸이 **기본값**으로(보존 3650 → 90일) → 이전 값 유지 + 웹 blankOr | |
| T2599-01 | medium | svcmon 로그 다운로드 중단마다 fd 1개 누수(8회 → 8개) → `stream.pipeline`(0개) | `routes/svcmon/logs.js` |
| T2599-02 | medium | 주기 env 가 2^31 초과·0 이하면 1ms 루프 → `clampIntervalMs`/`offOrIntervalMs` — config.js 11곳 + 워커·폴러 11곳('0=끔' 계약 유지) | `config.js` 외 |
| T2599-03 | low | svcmon 로그 분석 20MB 한 줄: RSS +709MB·2.3초 → +43MB·92ms + 동시 1건 가드 | `svcmon/loganalyze.js` |
| DB2599-01 | low | 재시작 뒤 엣지 전력 1표본 이중 계수(cnt 3 → 2) | `idrac/db.js` |
| DB2599-02 | low | 첫 생성 때 잠금이면 영구 래치 → 공용 `util/sqliteOpen.js` — **폴링 경로 DB 15종**(NDJSON·메모리 폴백 4종은 기다렸다 SQLite, 'unavailable' 래치 8종은 30초 뒤 재시도) | |
| SEC2599-02 | medium | 장비 출력 `replace(/\s+$/)` O(n²) — 형제 9곳 + 다른 정규식 3곳(최대 1.9초) → 전부 선형. 파서 11개 공백 3만 자 150ms 이내 타이밍 테스트 · 무작위 10만 건 결과 대조 0건 차이 | |
| SEC2599-03 | low | 게스트 스크립트 중간 실패 시 평문 비밀번호 파일 잔존 → finally 삭제(게스트 계정 거부 때는 삭제 안 함 — 잠금 방지) | `gpu/guestops.js` |
| SEC2599-04 | low(SPLIT) | `CENTRAL_URL` 의 `user:pw@` 가 엣지 워커 기동 로그 → 엣지 로그 화면. Basic/Digest·URL 비밀번호 가림 | `edgelog/redact.js` |
| SEC2599-05 | low(가능성) | 배포 토큰이 원격 셸 인자 → `ps` 노출(로컬 재현) → SFTP 0600 임시 파일 + `cat >>` (agent·rma 배포 둘 다) | `agent/deploy.js`·`rma/deploy.js` |
| EDGE2599-01 | medium | 스토리지 위임 0대일 때 중앙 목록을 비우지 않음 → 빈 목록 교체 후 상태 보고(재기동 직후·등록부 불가는 비우지 않음 — v2.581 BUG-D 유지) | `storage/push.js` |
| EDGE2599-04 | low | 설정 push 8MB 초과 조용한 제외 · `__skipped__` 를 파일로 셈 → 본문·상태·콘솔에 밝힘 | `agent/configPush.js` |
| WEB2599-02·03 | medium | 통신 점검 로그: 늦게 온 이전 기간 응답이 덮음 · 엣지 로그: 연달아 가져오면 진행 표시 해제·덮음 → 세대 가드·엣지별 진행 | `LinkCheck.jsx`·`EdgeLog.jsx` |
| WEB2599-05 | low(SPLIT) | 평문 자격증명 점검 오탐 9건(전수 1,110파일) → 0건 | `security/secretScan.js` |
| WEB2599-06 | low | 3단 지도 엣지 0곳 가운데 열이 빔 → 안내 상자 · 메인 카드 범례 마지막 줄 잘림(기존 결함, 스크린샷에서 발견) | `DeviceFlow.jsx` |

반증: EDGE2599-05(백오프 분기는 상태에 남는다).

## 검증

- 서버 `npm test` **3,288/3,288** · 웹 vitest 1,660/1,660 · eslint 0 · vite build · 문서 생성기 4종 최신(엔드포인트 830 · env 493).
- 통합 뒤 전량에서 3건이 깨졌고 고쳤다: `nsxLimitText` 가 값 없을 때 `—개`(단위를 붙인 결측 — v2.575 BUG-20 스윕이 잡았다) · 소스 계약 테스트 2건(가림·인계 반영).
- Chromium(목 서버, 1440·400px, 16화면 32회): 넘침 0 · 콘솔 오류 0. 그룹 c·f 는 operator 흉내·지연 주입으로 가림·경쟁을 직접 봤다.
- ⚠ 확인하지 못한 것: 실장비 응답(NSX cursor·Unity entryCount·PowerStore 'Empty'·APC 오류 문구·pstcli 폴백 형식) · 실제 SSH 배포 대상의 임시 파일 경로 · 자동 인계를 켠 분기(env 가 모듈 로드 때 고정) · 새 배너(NSX 절단·하드웨어 빈 슬롯·PDU 미완료)는 목 데이터에 없어 화면으로 못 봤다.
- 남긴 것: `cliSsh.parseJsonLoose` 구조적 O(n²)(다음 후보) · SQLite 헬퍼 미적용 7모듈(점검 시에만 열림·외부 공유 파일 등 — 근거는 그룹 e 보고) · 스토리지·SAN·PDU 조회 자체의 adminOnly 승격(사용자 결정).
