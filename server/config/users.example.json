{
  "_comment": "Copy to users.json and replace passwordHash values. Generate a hash with:  node -e \"import('./src/auth/auth.js').then(m=>console.log(m.hashPassword(process.argv[1])))\" 'YourPassword'  (run from the server/ directory). role is one of: admin | operator | viewer.",
  "users": [
    {
      "username": "admin",
      "name": "Administrator",
      "role": "admin",
      "passwordHash": "scrypt$REPLACE_WITH_SALT$REPLACE_WITH_HASH"
    },
    {
      "username": "noc",
      "name": "NOC Operator",
      "role": "viewer",
      "passwordHash": "scrypt$REPLACE_WITH_SALT$REPLACE_WITH_HASH"
    }
  ]
}
