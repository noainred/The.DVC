# Codex 프로그램 보안·완성도 점검 보고서

- 점검일: 2026-08-08 (Asia/Seoul)
- 버전: 2.246.0
- 범위: Node.js 서버, React/Vite 웹, Python Hub, C# 모니터, 테스트·배포 구성
- 성격: 정적 코드 점검 및 기존 테스트·빌드 결과 요약. 침투테스트 결과가 아님.

## 종합 판정

보안은 운영 전제조건을 명시적으로 강화해야 하는 **주의** 상태이며, 완성도는 핵심 기능이 넓지만 테스트와 운영 검증이 남아 있는 **부분 완료** 상태입니다.

## 핵심 보안 이슈

1. TLS 인증서 검증·레거시 TLS 설정을 운영 환경에서 명시적으로 강제해야 합니다.
2. SSRF DNS 재검증 후 실제 연결 시 hostname을 다시 해석하는 경로를 제거해야 합니다.
3. RDP 자격증명이 query parameter로 fallback되지 않도록 해야 합니다.
4. Python Hub의 `HUB_TOKEN` 미설정 공개 동작과 외부 바인드 여부를 점검해야 합니다.
5. CSP를 환경변수 opt-in이 아닌 운영 기본 정책으로 활성화해야 합니다.
6. 256MB 업그레이드 raw body를 스트리밍 처리하고 압축 해제 후 용량을 검증해야 합니다.
7. JSON/.env 백업 파일을 암호화하고 키를 별도 보관해야 합니다.
8. SSH/RDP 오류·배너의 브라우저 노출을 최소화해야 합니다.
9. 공개 `/dl` 다운로드 정책과 내부 미러 접근통제를 분리해야 합니다.
10. 예외 발생 후 프로세스를 계속 유지하는 정책에 워커 재기동·graceful restart를 추가해야 합니다.

## 완성도 핵심 이슈

1. 최근 전체 테스트 결과는 543 pass, 4 fail, 3 cancelled로 배포 기준상 미완료입니다.
2. `svcmon` 네트워크 테스트가 로컬 리스너 권한에 의존합니다.
3. `DatabaseSync`를 Worker Thread 또는 writer queue로 분리할 필요가 있습니다.
4. SQL p95/p99, `SQLITE_BUSY`, WAL 크기, event-loop lag 계측이 필요합니다.
5. 환경변수·JSON·UI 설정 검증을 단일 스키마로 통합해야 합니다.
6. 설정 중심 백업과 데이터 복구 범위를 구분하고 복구 리허설을 해야 합니다.
7. C# 클라이언트 빌드·패키징을 루트 검증 절차에 포함해야 합니다.
8. 대형 웹 청크와 3D 그래프 로딩을 추가 최적화해야 합니다.
9. `VmRemote.jsx`의 중복 `title` 속성을 제거해야 합니다.
10. Node/Python 인증·SSRF 정책을 공통 문서와 회귀 테스트로 통합해야 합니다.

## 권장 우선순위

1. TLS·RDP 자격증명·SSRF 문제 보완
2. 실패·취소 테스트의 CI 재현성 확보
3. `DatabaseSync` 이벤트 루프 분리 및 SQL 지표 추가
4. 백업 암호화와 복구 리허설
5. 운영 규모가 커질 때 PostgreSQL, 시계열 중심이면 TimescaleDB로 이전

## 검증 상태

- 웹 빌드: 성공(대형 청크 및 JSX 중복 속성 경고 존재)
- Node 테스트: 543 pass / 4 fail / 3 cancelled 기준으로 재검증 필요
- 본 문서는 정적 점검 결과이며 실제 침투테스트·부하테스트를 대체하지 않습니다.
