node_modules/
dist/
build/
.env
.env.local
*.log
npm-debug.log*
.DS_Store
coverage/
server/config/vcenters.json
server/config/nsx.json
server/config/users.json
server/config/upgrade.json
server/config/idrac.json
server/config/idrac-power.db
server/config/idrac-power.ndjson
server/config/idrac-inventory.json
server/config/collectors.json
server/config/datacenters.json
server/config/idrac-scan-ranges.json
# iDRAC 스캔 진단 로그(런타임 생성 — 발견물/IP 목록을 담아 커밋 금지, v2.289 #9)
server/config/idrac-scan-log.json
server/config/agent-assignments.json
server/config/agent-results.json
server/.upgrade-cache/
*.bak.[0-9]*
*.new.[0-9]*
dist-offline/
# 릴리스 바이너리는 GitHub Releases(롤링 'downloads' 태그)로 발행 — git에 커밋하지 않는다.
# (.git 비대화 방지). versions.json/README 는 추적 유지.
download/*.tar.gz
download/*.zip
download/*.sha256
server/config/alarm-mutes.json
server/config/tool-usage.json
server/config/fleet-tags.json
server/config/fleet-assign.json
server/config/auth.json
# JWT 서명 비밀(v2.289 #3) — AUTH_SECRET 미설정 시 CONFIG_DIR 에 영속(0600). 유출 시 임의
# 계정 토큰 위조가 가능하므로 절대 커밋 금지. 확장자 없는 단일 파일이라 개별 차단이 필수.
server/config/auth-secret
# 스토리지 모니터링(v2.302) — 장비 자격증명·수집 스냅샷(계정 목록 포함) 커밋 금지
server/config/storage-devices.json
server/config/storage-latest.json
server/config/storage-intervals.json
server/config/sanswitch-devices.json
server/config/sanswitch-latest.json
# 수집 작업 로그(링버퍼 · 재생성 가능한 런타임 캐시) — v2.516.
# storage-activity.json 은 v2.315 부터 있었는데 여기 빠져 있었다(런타임 파일 커밋 위험).
server/config/storage-activity.json
server/config/sanswitch-activity.json
server/config/sanswitch-perf-activity.json
server/config/sanswitch-err-baseline.json
server/config/rma-agents.json
server/config/credentials.json
server/config/rma-settings.json
server/config/rma-history.db*
server/config/rma-schedules.json
server/config/rma-tests.db*
server/config/rma-schedule-*.json
server/config/rma-outbox-*.json
server/config/sanswitch-perf-settings.json
server/config/sanswitch-perf.db
server/config/san-health.db
# '현재 사용자'(v2.520) 런타임 파일 — 비밀은 없지만(게스트 계정을 쓰지 않는 설계) 현장 설정·캐시라 커밋 대상이 아니다.
server/config/curuser-settings.json
server/config/curuser-activity.json
server/config/storage-history.db
server/config/storage-history.db-wal
server/config/storage-history.db-shm
# 자격증명 암호화 키·정책(v2.296) — 키 유출 = 전 계정 비번 복호 가능. 절대 커밋 금지.
server/config/secrets-key
server/config/secrets-policy.json
# 최초 기동 시 임의 생성되는 관리자 '평문' 비밀번호 파일(0600). CONFIG_DIR 미설정이면
# 기본값이 server/config 이라 개발/최초 기동 시 저장소 안에 떨어져 커밋될 수 있었다.
# *.txt 와일드카드도 함께 차단 — 이 경로에 추적 중인 .txt 는 없다(git ls-files 로 확인).
server/config/initial-admin-password.txt
server/config/*.txt
server/config/runtime.json
server/config/ipam.db
server/config/ipam.ndjson
server/config/remote-access.json
server/config/release-notes.json
server/config/gpu-guest.json
server/config/gpu-physical.json
server/config/central-agent-gpu-guest.json
server/config/central-agent-users.json
# 위임(중앙) 에이전트 런타임 파일은 토큰/계정을 담는다 — 앞으로 추가되는 것까지 와일드카드로
# 선차단(개별 항목 누락으로 시크릿이 커밋되는 사고 방지). 추적 중인 central-agent-* 는 없다.
server/config/central-agent-*.json
server/config/vmware-portal-release
server/config/agent-deploy-targets.json
server/config/llm.json
server/config/packages/
server/config/packages.json
server/config/ipam-settings.json
server/config/ipam-annotations.json
server/config/ipam-overrides.json
server/config/ipam-range-policies.json
server/config/ipam-vcenter-ranges.json
server/config/ipam-scan-agents.json
server/config/ipam-scan-history.json
server/config/ipam-scan-results.json
server/config/ipam-scan-runs.json
server/config/audit.ndjson
server/config/alerts.json
server/config/host-temp.db
server/config/host-temp.ndjson
server/config/ping-monitor.db
server/config/ping-monitor.ndjson
server/config/ping-targets.json

# 포탈 백업(런타임)
server/config/backups/
server/config/backup.json
server/config/central-agent-config.json

# vCenter 로그 보관(런타임)
server/config/vcenter-logs.db
server/config/vcenter-logs.ndjson
server/config/vcenter-logs.json

# 네트워크 캡처(런타임)
server/config/capture-history.json
server/config/capture-monitors.json

# 로그인 실패 분석(런타임)
server/config/login-fails.ndjson
server/config/login-monitor.json

# 게스트 조사 스케줄러 · 네트워크 이슈(런타임)
server/config/guest-scans.json
server/config/net-issues.ndjson
server/config/net-issues-state.json

# 세션 보안(유휴 로그아웃) 설정(런타임)
server/config/security-session.json

# 실제 OS 인벤토리(런타임)
server/config/os-inventory.json
server/config/os-scan.json

# SQLite WAL 부산물(런타임 생성 — 커밋 금지)
server/config/*.db-wal
server/config/*.db-shm
server/config/horizon.json
sbp-shortcuts.json

# 보안 감사 2026-09-12(L-8/L-9) — 누락돼 있던 시크릿/런타임 파일 차단.
#  - SECRET_FILES(secretVault 봉인 대상)인데 .gitignore 에 없던 3종: 커밋 시 비밀 평문 유출.
#  - 런타임 SQLite DB 는 개별 나열 대신 와일드카드로 선차단(capacity/guest-disk/vm-track 등 신규 포함).
#  - portal.env 는 AUTH_SECRET·CENTRAL_TOKEN 을 담는다(`.env` 정확일치 패턴만으론 안 걸림).
server/config/pdu-devices.json
server/config/relay-topology.json
server/config/mail.json
server/config/active-sessions.json
server/config/host-access.json
server/config/ipam-scan.json
server/config/portal.env
server/config/*.db
server/config/vmperf/

# 보안 감사 2026-09-13(M-2) — v2.49x 신규 런타임 파일이 차단 목록에 없었다.
#  - perf-hangs.ndjson: hang 기록에 사용자명·클라이언트 IP·User-Agent·요청 경로가 남는다.
#  - perf-monitor.json: 성능 측정 설정(임계값).
#  - central-unsupported-servers.json: 엣지가 보고한 BMC 관리 IP·hostname.
# 기본 CONFIG_DIR 이 server/config 라 개발 호스트에서 `git add -A` 시 그대로 올라간다.
server/config/perf-hangs.ndjson
server/config/perf-monitor.json
server/config/central-unsupported-servers.json
server/config/bm-storage.json
server/config/*.ndjson
sbp-shortcuts.csv
sbp-proxy-shortcuts.json
design-preview.html
design-preview-monitor.html
design-preview-svcmon2.html

# Claude Code worktree 격리(여러 창 동시작업 시 `claude --worktree <이름>` 이 만드는
# 임시 작업트리) — 커밋 대상이 아니므로 무시. `.claude/agents`·`.claude/skills` 등
# 공유 설정은 체크인해야 하므로 `.claude/` 전체가 아니라 worktrees 만 무시한다.
.claude/worktrees/

# 목(mock) 서버 검증용 CONFIG_DIR — v2.505.
# 문서·개발 절차가 `cd server && CONFIG_DIR=tmp DATA_SOURCE=mock node src/index.js` 로 로컬
# 검증을 안내하는데, 이 디렉터리가 차단 목록에 없어 `git add -A` 때마다 런타임 산출물
# (vmperf/*.db·-wal·-shm · auth-secret · initial-admin-password.txt · users.json)이 스테이징에
# 올라왔다. 실제로 v2.503 커밋에 섞여 들어가 amend 로 걷어낸 적이 있다(해당 파일들에는
# AUTH_SECRET·초기 admin 비밀번호가 들어 있어 커밋되면 그대로 유출이다).
# 매번 `rm -rf server/tmp` 를 기억하는 것에 의존하지 않는다.
server/tmp/

# Claude Design 캔버스 씨딩 결과물(에디터 런타임 2.6MB 포함) — 소스는 *.dc.html + canvas.json
Design/uiredesign/vmware-portal-ui-redesign.html

# v2.535 자격증명 감사 — 런타임 상태 파일(평문 비밀은 없지만 내부 host·자격증명 지문이 들어간다).
# 기본 CONFIG_DIR 이 server/config 라 개발 호스트에서 `git add -A` 하면 그대로 올라간다
# (v2.500 C/M4 와 같은 유형). 형제 디렉터리 server/config/vmperf/ 는 이미 등록돼 있는데
# server/config/vmseries/ 만 빠져 있었다.
server/config/horizon-sessions.json
server/config/horizon-session-activity.json
server/config/horizon-auth-stops.json
server/config/storage-auth-stops.json
server/config/storage-growth-settings.json
server/config/vmseries.json
server/config/vmseries/

# v2.547 파트 장애 — 엣지 보고 캐시(법인별 장비명·부품명·수집 주체가 들어간다). DB(part-faults.db)는
# 위 server/config/*.db 와일드카드가 이미 덮는다. 새 상태 파일을 만들면 그 커밋에서 함께 등록할 것.
server/config/partfault-edge.json
